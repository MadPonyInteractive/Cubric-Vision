# docs/workflows/ — workflow baselines

`baselines.json` — recorded workflow baseline data (currently empty). Workflow authoring
knowledge lives in [workflow-authoring/README.md](../workflow-authoring/README.md); the
workflow JSON files themselves in `comfy_workflows/`.
