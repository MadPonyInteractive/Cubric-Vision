{
  "schema": "mpi-kanban/handoff/v1",
  "id": "9a19f9e2-fcb4-40f8-879c-3f3a0ebcb47c",
  "generated_at": "2026-08-20T05:30:00Z",
  "status": "open",
  "session": {
    "name": "MPI-504 - filled the Masking group in flow_character_sheet.json; Fabio live-tested it and it works",
    "branch": "master"
  },
  "goal": {
    "original": "Fill the empty `Masking` group in comfy_workflows/raw/flow_character_sheet.json.",
    "status": "in_progress",
    "summary": "DONE and live-proven. The Masking group holds 14 authored nodes (745-758); Fabio ran the graph, got 3-4 successful outputs and called it ready to implement. THE GRAPH IS FINISHED. What remains is app work: the enhancer op (blocked on Fabio adding Input_Seed), then /mpi-add-flow end to end - which now also owes an Input_Recipe DROPDOWN in the FlowDef."
  },
  "plan": {
    "file": ".agents/mpi-kanban/tasks/MPI-504/plan.md",
    "completed": [
      "MASKING GROUP SHIPPED - 14 nodes (745-758) in raw/flow_character_sheet.json. face_yolov8n -> BboxDetectorSEGS -> ImpactSEGSOrderedFilter(area ASCENDING, take 1 = the front body's head, never the big portrait). The face box is GEOMETRY ONLY: it sizes a scale-free crop via MpiMaskSquareBbox x2 + one MpiMath 'a * 4 // 5' (2.6x the face box at ANY Input_Width/Height). SAM3_Detect on 'hair, face, hat' with individual_masks FALSE is the union - a hatless character just contributes no hat, so the degradation needs zero extra nodes. SolidMask(0,W,H) + MaskComposite(add) pastes it back into 733 Set_inpaint mask. No grow baked in; 690/721/718 already shape it.",
      "FABIO RAN IT LIVE - 3-4 outputs, all succeeded, 'ready to be implemented'.",
      "THE ONE REAL BUG WAS NOT THE GATE. With Input_Remove_Head OFF the branch ran anyway. MpiIfElse laziness was correct and irrelevant: an output node is an EXECUTION ROOT, and 688 PreviewImage sat UNMUTED with an 84-node upstream closure covering all 14 Masking nodes AND the whole inpaint chain. Fabio's fix is both halves - 759 MpiBlocker at the SOURCE (driven by a new 760 Get_is remove head, feeding 746 + 758) and muting 688, plus three debug previews added muted (765/766/767). Generalised into docs/workflow-authoring/mpi-nodes.md.",
      "CORE ImageCrop IS DEPRECATED - caught only by LOADING the graph in a browser; conversion and every offline check passed it silently. ImageCropV2 cannot replace it (its crop_region is a socketless UI bounding-box widget, undriveable from INT sockets). The crop is 752 MpiBox + 758 MpiBoxCrop. Documented.",
      "VERIFIED against Fabio's re-export, not just my own version: my 14 nodes came back type/widgets/titles IDENTICAL, offline checks PASS (146 nodes / 180 links), workflow-to-api.mjs converts clean vs live /object_info on :48188, validate-injection-rules clean, npm test 631/631."
    ],
    "pending": [
      "THE ENHANCER OP - raw/qwen3vl_4b_prompt_enhancer.json needs `Input_Seed` (MpiInt) added by FABIO (raw/ is user-owned), then convert + register in the 4 files with outputKind: 'text'. Until it exists the Enhance button warns and no-ops by design.",
      "/mpi-add-flow END TO END - convert both graphs, register the ops, write the FlowDef, live run. NEW: Input_Recipe wants a DROPDOWN (Fabio, 2026-08-20) - `type: 'select'` already exists at js/utils/declaredFields.js:154, mounts MpiDropdown, and emits the option's ORIGINAL `v` so the 1-indexed int reaches MpiAnySwitch intact. Model on Input_Tier (flowsRegistry.js:268) but select not radio; four options from prompts.md, default 1 = Photoreal.",
      "Remaining GPU questions, NOT blockers (it works): does the 2.6x crop ever reach a neighbouring panel on an unusual layout, and does the SAM3 union cover a tall hat.",
      "Klein removal A/B (baked removal prompt vs the no-prompt remove op); all four styles keeping medium + catch-light; the 10/10 stress test."
    ]
  },
  "task_card": {
    "id": "MPI-504",
    "title": "Character Sheet flow - a description in, a video-reference sheet out (front body HEADLESS)",
    "column": "doing",
    "workspace": ".agents/mpi-kanban/tasks/MPI-504/",
    "links": {
      "plan": ".agents/mpi-kanban/tasks/MPI-504/plan.md",
      "checklist": ".agents/mpi-kanban/tasks/MPI-504/checklist.md",
      "validation": ".agents/mpi-kanban/tasks/MPI-504/validation.md"
    }
  },
  "next_action": {
    "description": "The GRAPH IS FINISHED - do not re-open it. Next is the ENHANCER OP: it is blocked on FABIO adding an `Input_Seed` (MpiInt) node to comfy_workflows/raw/qwen3vl_4b_prompt_enhancer.json, so ASK HIM FIRST and do not edit raw/ yourself. Once it exists, convert the graph and register the op in the 4 files with outputKind: 'text'. Then /mpi-add-flow end to end for the sheet itself, and remember the new ask: Input_Recipe must be a DROPDOWN in the FlowDef (`type: 'select'`, four style options from prompts.md, 1-indexed, default 1 = Photoreal) - the field type already exists at js/utils/declaredFields.js:154 and emits the option's original `v`, so the int reaches MpiAnySwitch intact; model it on Input_Tier at flowsRegistry.js:268 but select rather than radio.",
    "command": "mpi-continue"
  },
  "context": {
    "key_decisions": [
      "THE GRAPH IS DONE AND LIVE-PROVEN. 146 nodes / 180 links, Fabio's re-export, 3-4 successful outputs. Do not re-verify or re-author it - read plan.md's top block instead.",
      "A LAZY GATE IS NOT ENOUGH ON ITS OWN. An unmuted PreviewImage/MaskPreview/PreviewAny/SaveImage is an EXECUTION ROOT and drags a gated branch into every run. Symptom: 'the branch's models load every run and the gate looks correctly wired' - do NOT re-check the gate, grep for unmuted output nodes. Fix is MpiBlocker at the branch SOURCE plus muting the previews. docs/workflow-authoring/mpi-nodes.md carries it.",
      "Converting and passing offline checks does NOT prove a graph opens. Core ImageCrop's deprecation was invisible to workflow-to-api.mjs, validate-injection-rules.mjs and every structural check; only loading it in a real ComfyUI editor showed the [DEPR] badge. Load-test any authored graph in a browser before handing it over.",
      "The enhanced phrase goes to `Input_Positive`, NOT `Input_Character` - that name only ever existed in the scratch fixture and is wrong against this graph.",
      "`--accent-frost` is a colour to move AWAY from (Fabio) - do not reach for it as a signal colour."
    ],
    "constraints": [
      "comfy_workflows/raw/ IS USER-OWNED. The Masking group was the one place the agent could author, and it is finished. Anything else there - Input_Seed on the enhancer, moving a gate - is Fabio's edit to make; ask, do not do.",
      "A PEER SESSION IS LIVE IN THIS TREE. js/data/modelConstants/models.js is currently modified by it and js/data/flowsRegistry.js is co-owned. Commit by explicit pathspec only, never `git add -A`.",
      "THE CHARACTER-SHEET FlowDef IS NOT IN THE REPO, deliberately. The scratch fixture is recorded VERBATIM in validation.md; paste it into js/data/flowsRegistry.js to drive the UI and DELETE IT before committing. A PROJECT MUST BE OPEN before Events.emit('flow:open', ...) - on Landing the overlay mounts but is not visible, which reads as a broken flow and is not one.",
      "Load-testing a graph in a browser: serve the raw JSON off a temp CORS server and drive `window.app.loadGraphData(d)` via playwright-cli against the BENCH on :8188 in its own session - never :3000 (the user's app) and never his own editor tab. The recipe is in validation.md.",
      "NEVER take the user's app on :3000. Spin your own with `npm run app:isolated`; do not pipe it through head/tail (the buffer swallows the READY line and the orphan then holds the profile).",
      "709 Get_steps has NO SetNode anywhere in the graph - the file's one unreachable node, pre-existing, in Fabio's Generation half. Not a bug to fix."
    ],
    "files_to_read_first": [
      ".agents/mpi-kanban/tasks/MPI-504/plan.md",
      ".agents/mpi-kanban/tasks/MPI-504/validation.md",
      "docs/workflow-authoring/mpi-nodes.md",
      "comfy_workflows/raw/flow_character_sheet.json"
    ]
  },
  "resume_prompt": "Resuming MPI-504, the Character Sheet flow in Cubric Vision (c:/AI/Mpi/Cubric-Vision, branch master). Read .agents/mpi-kanban/state/handoffs/9a19f9e2-fcb4-40f8-879c-3f3a0ebcb47c.json first, then .agents/mpi-kanban/tasks/MPI-504/plan.md - its `## Current State` top block is authoritative and was written while the details were fresh, so do not re-derive it. The card is in doing. THE COMFYUI GRAPH IS FINISHED: last session filled the Masking group in comfy_workflows/raw/flow_character_sheet.json (14 nodes, 745-758 - YOLO selects the front body's head by ascending face area, SAM3 shapes a hair+face+hat union, and it pastes back into 733 Set_inpaint mask), and Fabio then RAN it live: 3-4 outputs, all succeeded, ready to implement. He fixed one real bug himself - with Input_Remove_Head off the branch ran anyway, because an unmuted PreviewImage is an execution ROOT that defeats the lazy MpiIfElse - by adding 759 MpiBlocker at the branch source and muting 688. Do NOT re-open or re-verify the graph. Your job is the app half: the enhancer op, which is BLOCKED on Fabio adding an Input_Seed node to raw/qwen3vl_4b_prompt_enhancer.json (raw/ is user-owned - ask him, never edit it yourself), and then /mpi-add-flow end to end, which now also owes an Input_Recipe dropdown in the FlowDef. Standing rules learned the hard way: raw/ is USER-OWNED, a peer session is live in this tree so commit by explicit pathspec only, converting a graph does not prove it opens (load it in a browser), and never take the user's app on :3000."
}
