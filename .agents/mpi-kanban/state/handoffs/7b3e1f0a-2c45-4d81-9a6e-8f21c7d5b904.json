{
 "schema": "mpi-kanban/handoff/v1",
 "id": "7b3e1f0a-2c45-4d81-9a6e-8f21c7d5b904",
 "generated_at": "2026-07-30T01:00:00Z",
 "from_session": "84248f4f-ea74-4249-bc58-f380d16ff446",
 "to_role": "implementer",
 "status": "open",
 "session": {
  "name": "MPI-395 closed on live evidence; MPI-396 found + fixed at the store; three Model Library follow-ups carded",
  "branch": "master"
 },
 "goal": {
  "original": "Run the three MPI-395 residuals on the CPU Pod, then close MPI-395.",
  "status": "in_progress",
  "summary": "MPI-395 is CLOSED - all three residuals verified live and user-approved. Closing it surfaced three separate Model Library defects. MPI-396 (the real bug) is FIXED with 5 guard tests and a negative control, but its LIVE proof is still owed and the one attempt was vacuous. MPI-397 and MPI-398 are carded, diagnosed, and deliberately untouched. NOTHING IS COMMITTED."
 },
 "plan": {
  "file": null,
  "completed": [
   "MPI-395 CLOSED (done/complete). All three residuals ran live on CPU Pod omi9588i0gymlu. The load-bearing proof for cancel-while-queued is TIMING, because the queue is client-side and invisible to /comfy/downloads/status: after the cancel, the next Install POST reached the server within seconds (job table 20:36:04) instead of parking for 30 minutes. The 400 volume-full rejection that followed is the correct pre-flight, NOT a defect - a rejected POST is a POST that fired. engine:assets stayed suppressed on the unfocused reconnect; the OS toast that did appear was the legitimate 'Pod connected'.",
   "MPI-396 FIXED (uncommitted). Root: uninstall never settled the model's job in the SOT store. THREE compounding defects, all in the card: (1) the REMOTE uninstall leg returns res.json() at downloadManager.js:2410 and never reaches the LOCAL leg's reconciler.reconcileOnce() at :2547 - an engine-split half-wire; (2) pruneTerminal's DONE branch drops on confirmedInstalled.has(id) || age >= DONE_TTL_MS and an uninstalled model is in neither; (3) reconciler.start() gates its tick on store.hasActiveJobs(), so post-uninstall the poll returns early forever and the 120s belt never runs. The stale job was therefore PERMANENT, not transient - which is why Ctrl+R did not clear it (the store is main-process). Fix: store.dropModel(modelId) + broadcastSnapshot() at BOTH legs.",
   "MPI-397 and MPI-398 carded with full diagnosis and explicitly NOT fixed - the user asked for one fix this session, not three.",
   "docs/model-library.md gained the durable rule (incl. 'do NOT fix this in _tileState' and why). docs/releases/UNRELEASED.md gained a user-facing fixes entry."
  ],
  "pending": [
   "MPI-396 LIVE PROOF - the only outstanding work on the fix, and it does NOT need a Pod. Full recipe in tasks/MPI-396/validation.md. The critical step everyone will skip: after installing, POLL /comfy/downloads/status until the job reads complete AND IS STILL LISTED, and only THEN uninstall. The first attempt failed to prove anything because the store had already pruned the job 14s after install via the confirmedInstalled fast-exit, so dropModel found nothing to drop.",
   "COMMIT. Nothing from this session is committed. Source: routes/install/installStore.js, routes/downloadManager.js, tests/uninstall-store-settle.test.cjs, docs/model-library.md, docs/releases/UNRELEASED.md. Kanban: board.json, events.jsonl, tasks/MPI-395/, tasks/MPI-396/, tasks/MPI-397/, tasks/MPI-398/. Commit by explicit pathspec - the tree is shared and OTHER cards are dirty (see constraints).",
   "MPI-398 FIRST - run the cheap discriminator before any tile bisect: open the Model Library cold while DISCONNECTED from the Pod. Still slow means renderer/tile; fast means remote latency and the card merges into MPI-397. The user was starting exactly this comparison (models modal vs apps modal) when the session ended - ASK FOR THEIR RESULT before doing your own.",
   "MEMORY.md compaction, still deferred from the previous handoff. 20.4KB against a 24.4KB read limit. The MPI-392 block is stale (it shipped) and several MPI-380 lines are superseded. Propose per-entry, never bulk-delete."
  ]
 },
 "task_card": {
  "id": "MPI-396",
  "title": "Uninstall leaves a terminal DONE job in the install store, so the tile draws a 100% bar instead of Install",
  "column": "doing",
  "workspace": ".agents/mpi-kanban/tasks/MPI-396/",
  "links": {
   "plan": null,
   "checklist": null,
   "validation": "validation.md",
   "handoffs": null,
   "brief": "brief.md"
  }
 },
 "kanban_entry": null,
 "allowed_actions": ["read", "continue", "verify", "implement", "commit"],
 "knowledge_preservation": {
  "completed": [
   "tasks/MPI-395/validation.md - the three residual results, the transition log, and why the client-side queue is invisible server-side.",
   "tasks/MPI-396/brief.md + validation.md - all three root defects, the blast radius, and the live-repro recipe with the step that makes or breaks it.",
   "tasks/MPI-397/task.json - why an optimistic install-state flip is a product decision, not a bug fix.",
   "tasks/MPI-398/task.json - four hypotheses, the two-commit bisect range, and the disconnect discriminator.",
   "docs/model-library.md - the uninstall-must-settle-the-store rule.",
   "docs/releases/UNRELEASED.md - user-facing entry."
  ],
  "pending": ["MEMORY.md compaction."]
 },
 "next_action": {
  "description": "Prove MPI-396 locally (install, poll until the job is STILL LISTED as complete, then uninstall), then commit everything by explicit pathspec. Then ask the user for their models-vs-apps timing result and run the MPI-398 disconnect discriminator before any tile bisect.",
  "command": "mpi-continue"
 },
 "context": {
  "key_decisions": [
   "Fixed MPI-396 at the STORE, not at the tile. Suppressing the bar in _tileState when the model is not installed would have left the store reporting a finished job for a model that no longer exists, to the status endpoint and every other snapshot consumer. The user explicitly asked for no more symptom patches on this subsystem - two refactors have already been spent here.",
   "dropModel refuses a NON-terminal job. A model uninstalled mid-download is the uninstall route's in-flight dep guard's business; dropping a live job would strand the transport layer that still owns its downloaders.",
   "Did NOT touch pruneTerminal's TTL policy or the reconciler's hasActiveJobs gate. Both are correct for what they were designed for; the missing piece was that uninstall had no way to speak to the store at all.",
   "Carded MPI-397 and MPI-398 instead of fixing them. MPI-397 needs a product decision (it relaxes the install-state-IS-files-on-disk invariant); MPI-398 needs measurement first.",
   "The negative control keeps one test passing in BOTH states on purpose - it pins that MPI-276's DONE_TTL belt still prunes a job nobody uninstalled."
  ],
  "constraints": [
   "NOTHING IS COMMITTED. Commit by explicit pathspec.",
   "Another agent is active in this tree and its work appeared mid-session. Do NOT sweep into a commit: tasks/MPI-249, MPI-353, MPI-355, MPI-358, MPI-385/handoffs, MPI-392/handoffs, .claude/rules/comfy_injection.md, .claude/skills/mpi-end/SKILL.md, docs/DEVELOPMENT.md, docs/models/krea2/README.md, docs/models/qwen-edit/README.md, docs/project-integrity.md, docs/runpod-remote-engine.md.",
   "board.json is 1-space indent, LF, trailing newline; task.json is 2-space. Every write this session used a serialise-and-compare round-trip guard BEFORE writing - keep doing that.",
   "routes/ is MAIN-PROCESS: verifying the MPI-396 fix needs a FULL app restart, not Ctrl+R.",
   "Suite baseline is now 298/298 (was 293 + 5 new). Runner: node --test tests/*.test.cjs.",
   "CPU Pod omi9588i0gymlu was being torn down by the user as the session ended. Nothing outstanding needs it.",
   "Clock was verified clean against GitHub's Date header (no VPN skew) - timestamps in this session are trustworthy."
  ],
  "files_modified": [
   "routes/install/installStore.js",
   "routes/downloadManager.js",
   "tests/uninstall-store-settle.test.cjs",
   "docs/model-library.md",
   "docs/releases/UNRELEASED.md",
   ".agents/mpi-kanban/board.json",
   ".agents/mpi-kanban/events.jsonl",
   ".agents/mpi-kanban/tasks/MPI-395/",
   ".agents/mpi-kanban/tasks/MPI-396/",
   ".agents/mpi-kanban/tasks/MPI-397/",
   ".agents/mpi-kanban/tasks/MPI-398/"
  ],
  "files_to_read_first": [
   ".agents/mpi-kanban/tasks/MPI-396/validation.md",
   ".agents/mpi-kanban/tasks/MPI-396/brief.md",
   ".agents/mpi-kanban/tasks/MPI-398/task.json"
  ]
 },
 "project_knowledge": {
  "profile": ".agents/mpi-kanban/project-profile.md",
  "knowledge_index": ".agents/mpi-kanban/project-knowledge-index.md",
  "mode": "file",
  "relevant_topics": ["downloads", "model-library", "runpod-remote-engine"]
 },
 "rules_active": [
  ".claude/rules/downloads.md",
  ".claude/rules/kanban.md",
  ".claude/rules/git.md",
  ".claude/rules/components.md",
  ".claude/rules/dos_and_donts.md"
 ],
 "recent_events": [{"at": "2026-07-30T01:00:00Z", "event": "handoff_created"}],
 "resume_prompt": "You are continuing Cubric Vision 1.3 release prep on master. Read .agents/mpi-kanban/state/handoffs/7b3e1f0a-2c45-4d81-9a6e-8f21c7d5b904.json first. MPI-395 is CLOSED - do not reopen it. The live card is MPI-396: uninstalling a model left its terminal DONE job in the install store forever, so the Model Library tile drew a 100% progress bar instead of an Install chip and a reload did not clear it (the store is main-process). Three defects compounded - the remote uninstall leg never called reconcileOnce, pruneTerminal cannot express 'confirmed uninstalled', and the reconciler poll self-idles when no job is active so the 120s belt never runs. Fixed with store.dropModel(modelId) + broadcastSnapshot() at BOTH engine legs, 5 guard tests, negative control 4-fail-unfixed to 5/5, suite 298/298, eslint clean. NOTHING IS COMMITTED - commit by explicit pathspec, and never sweep the other agent's dirty cards (list in the handoff constraints). MPI-396's live proof is still owed and needs NO Pod: install a model locally, poll /comfy/downloads/status until its job reads complete AND IS STILL LISTED, and only then uninstall - the first attempt proved nothing because the store had pruned the job 14s after install. Then MPI-398 (Model Library blank for 10-15s on a cold renderer): run the cheap discriminator FIRST - open the library cold while DISCONNECTED from the Pod. Slow means renderer/tile and the bisect range is exactly f76791ff (MPI-356 MpiTileSheet extraction, the user's own lead) then d76b60c8 (MPI-394); fast means remote latency and it merges into MPI-397. The user reported Settings slow too, and Settings is NOT a tile consumer, so ask for their models-vs-apps timing before assuming a tile cause. MPI-397 (card move lags the toast in BOTH directions) is a parked product decision - it would relax the install-state-IS-files-on-disk invariant. Also still deferred: MEMORY.md compaction, 20.4KB against a 24.4KB limit, propose per-entry."
}
