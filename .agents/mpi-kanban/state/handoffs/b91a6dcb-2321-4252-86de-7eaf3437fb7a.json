{
  "schema": "mpi-kanban/handoff/v1",
  "id": "b91a6dcb-2321-4252-86de-7eaf3437fb7a",
  "generated_at": "2026-08-07T03:40:00Z",
  "from_session": null,
  "to_role": "implementer",
  "status": "open",
  "session": {
    "name": "MPI-466 — LTX re-wired to the new export (int8 balanced, bf16 high, one graph per tier); code done and committed, nothing run through the app",
    "branch": "master"
  },
  "goal": {
    "original": "Continue MPI-452, but resolve LTX first — then set up the engine-bump/smoke cards properly so a separate session can be dispatched on them.",
    "status": "in_progress",
    "summary": "LTX is fully re-wired and committed: 12 runtime files became 2, the arch axis and the _stage2 twins are gone, int8 replaced fp8/mxfp8, and the dead input-staging path was deleted. 478/478 pass. NOTHING has run through the app — the app was closed at the end of the session precisely so its engine drift-repairs KJNodes on the next boot, which is the prerequisite for any LTX run at all (MPI-465)."
  },
  "plan": {
    "file": null,
    "completed": [
      "MPI-468 created as a dispatch umbrella over MPI-457 (mechanic: in-place /engine/upgrade + the bump skill) and MPI-467 (guard: docs/playbooks/bump-engine/ + smoke runner + release gate) — they had both claimed the same playbook. A separate session has ALREADY picked it up and moved 467/468 to doing",
      "MPI-466 rewritten to the user's decision: distill-LoRA turbo toggle and dev non-distilled model DROPPED; High stays bf16, Balanced becomes distilled-1.1 int8_convrot",
      "MPI-456 narrowed to WAN — the LTX twins died inside this card instead",
      "raw export -> workflow-to-api.mjs (bench 8188) -> generate_ltx.py -> ltx_i2v_t2v.json + ltx_i2v_t2v_int8.json. 215 LiteGraph nodes -> 123 API nodes; both pass validate-injection-rules.mjs",
      "generate_ltx.py rewritten: stamps the transformer per tier, bakes the stage flags to a stage-1 run, hard-fails if a retired gate (Input_Is_Continue / Input_Preview_Only / Input_Text_to_video) reappears in a future re-export",
      "The 12 old runtime files deleted; ltx23-transformer-int8 added (sha256 30fe2173...b614, 21505993424 B); models.js: variants.arch deleted, singleFileStages on both cards, both ops point at one file per tier",
      "Input-staging path DELETED — WORKFLOW_INPUT_DEFAULTS, POST /comfy/prepare-workflow-inputs, the commandExecutor half, and the three dummy .latent files. No shipped graph carries a LoadLatent any more",
      "BUG CAUGHT AND FIXED IN-SESSION: deleting ltx23-transformer-fp8/-mxfp8 from modelDeps.js stranded 24-25GB on existing users' disks, because _orphanedDepIds iterates DEPS to decide what to sweep. Both restored as deprecated-but-present",
      "docs: model-set.md arch paragraph, tiers.md node ids (+ the /64 law RE-VERIFIED against the new graph), media-inputs.md rewritten (it previously FORBADE removing latent staging)",
      "Committed: 92420fad (LTX re-wire), 5ab43e4e (staging deletion + un-stranding), 0a0d2341 + d39483b4 + 7dcd74a8 (cards)"
    ],
    "pending": [
      "START THE APP. Its engine still holds KJNodes 7f43f2c; the boot drift check repairs it to 35e5956. Until that happens LTX dies at the stage-1 sampler no matter what this card did",
      "Run LTX t2v on the BALANCED (int8) card through the app, and read the DISPATCHED graph off /queue — a run that merely finishes is not evidence",
      "Verify the four media-derived routes by their OUTPUT: t2v, start-only, end-only, start+end. There is no op boolean any more",
      "Preview -> Continue, which drives MpiStageLatents through the app's own staging",
      "Re-measure PROGRESS_STAGES for ltx_i2v_t2v.json — the 3/2/1 in the file is the OLD graph's count and is flagged as unverified",
      "Finish the R2 upload (28% at session end) and HEAD the public URL for content-length 21505993424",
      "REMOVE THE DEAD variants AXIS — see constraints; it is mapped but deliberately not started",
      "MPI-465's hotfix decision (1.3.2 vs fold into 1.4.0) and the Pod image rebuild for remote LTX",
      "MPI-452 (MiniMax H3) was the session's original card and is still `validating` — untouched today"
    ]
  },
  "task_card": {
    "id": "MPI-466",
    "title": "Re-wire LTX to the new export: int8 balanced, bf16 high, media-derived routing",
    "column": "doing",
    "workspace": ".agents/mpi-kanban/tasks/MPI-466/",
    "links": {
      "plan": null,
      "checklist": ".agents/mpi-kanban/tasks/MPI-466/checklist.md",
      "validation": ".agents/mpi-kanban/tasks/MPI-466/validation.md",
      "handoffs": ".agents/mpi-kanban/tasks/MPI-466/handoffs/"
    }
  },
  "kanban_entry": null,
  "allowed_actions": ["read", "continue", "verify", "implement"],
  "knowledge_preservation": {
    "completed": [
      "tasks/MPI-466/validation.md — the proved-vs-unproved split, plus the _orphanedDepIds trap written up so no future card repeats it",
      "tasks/MPI-466/checklist.md — the runbook, marked through the code work",
      "tasks/MPI-468/brief.md — dispatch notes for the engine-bump session (ownership split, build order, one-GPU constraint)",
      "docs/models/ltx/tiers.md — the /64 law re-verified against the new graph rather than assumed",
      "docs/workflow-authoring/media-inputs.md — rewritten to explain WHY the do-not-remove-staging warning reversed"
    ],
    "pending": [
      "UNRELEASED.md not written. LTX's re-wire is a user-visible change (the balanced weight changes, so existing users re-download 20GB) and belongs in whatIsNew once it validates",
      ".claude/rules/comfy_injection.md, component-comfy.md and comfy_injection_multistage.md still describe WORKFLOW_INPUT_DEFAULTS / prepare-workflow-inputs. Those are ARCHITECTURAL RULE FILES — CLAUDE.md forbids editing them without explicit user permission. Ask, then fix",
      "docs/project-integrity.md also mentions the staged latents"
    ]
  },
  "next_action": {
    "description": "Start the app and confirm the boot drift check repairs KJNodes to 35e5956 (check engine/ComfyUI_windows_portable/ComfyUI/custom_nodes/comfyui-kjnodes/.mpi_node_commit and grep nodes/ltxv_nodes.py for _unwrap_upscale_model). Then run an LTX t2v on the BALANCED tier and read the dispatched graph off /queue to confirm all 22 Input_/Output_ titles landed — injection skips an unmatched title silently and LTX now routes on media presence, so a wrong-but-plausible video is the failure mode. Then the four routes, then preview -> Continue. Read tasks/MPI-466/validation.md first; its NOT-proved section IS the work list.",
    "command": "mpi-continue MPI-466"
  },
  "context": {
    "key_decisions": [
      "The distill LoRA toggle and the dev non-distilled model were DROPPED by the user. The gate that would have decided the old plan — does a bf16 rank-111 LoRA patch onto int8 weights — is dead. Do not re-run that experiment.",
      "int8_convrot replaced the fp8/mxfp8 PAIR, so variants.arch and its _fp8/_mxfp8 workflow suffixes were deleted. Proven on the user's Ada card; Blackwell is INFERRED from other int8 models, not measured for this weight — the dep comment says so deliberately and must not be upgraded to 'verified' without a run.",
      "Two files, not one: the tiers differ only in the baked unet_name, and models.js names each file outright rather than resolving a suffix.",
      "The app side needed NO work. WAN and H3 had already migrated the app onto MpiStageLatents — commandExecutor.js:1711 recognises it in saveLatentNodeIds and _buildParams already emits Video_Latent.is_continue/.is_preview. LTX was simply the last model on the old eight-node shape.",
      "The muted/bypassed nodes are pruned by the CONVERTER (workflow-to-api.mjs drops mode 2/4 exactly as the ComfyUI frontend does), not by the generator. 215 -> 123 nodes.",
      "MPI-465's app proof rides on THIS card rather than preceding it: fp8/mxfp8 were deleted from the drive and the 41GB bf16 was never on it, so no LTX weight could run until int8 was wired."
    ],
    "constraints": [
      "A DEP DELETED FROM modelDeps.js CAN NEVER BE UNINSTALLED. _orphanedDepIds (routes/downloadManager.js) iterates the DEPS map and trashes what no model protects — remove the entry and its file strands on every existing user's disk, untracked, forever. Retire a weight by keeping a deprecated entry (see the ltx23-transformer-fp8/-mxfp8 and wan-22-t2v-* blocks), never by deleting it.",
      "THE variants AXIS IS NOW DEAD CODE AND WAS LEFT IN PLACE, deliberately. LTX was its last consumer, so variantAxisTokens / archVariantOptions / detectOtherArchInstall / variantDepsOf, the workflowSuffix branch in resolveWorkflowFile, and the model-manager's arch toggle row have no caller. Consumers to sweep: js/data/modelConstants/resolveModelDeps.js, models.js, modelRegistry.js, footprint.js, js/components/Compounds/LandingPages/MpiModelManager/MpiModelManager.js, js/services/commandExecutor.js, routes/platformEngine.js, tests/resolve-model-deps.test.cjs, plus docs/workflow-authoring/variant-injection.md and the add-model playbook. NOTE: scripts/smoke-workflows.mjs (the parallel session's new, still-untracked file) passes variantTokens to resolveWorkflowFile — harmless as an ignored option, but coordinate before changing that signature.",
      "A PARALLEL SESSION IS ACTIVE in this same tree on MPI-470 (deprecating WAN text-to-video) and MPI-467/468 (engine-bump tooling). It owns js/data/modelConstants/models.js edits for WAN, scripts/smoke-workflows.mjs, and its own card files. Re-read before every write and commit by explicit pathspec. One of its changes briefly broke tests/uninstall-guards.test.cjs (no op-grouped model with common deps left once wan-22 lost t2v_ms) and then fixed it.",
      "Injection SILENTLY SKIPS an Input_* title matching no node. On LTX this is now the dominant risk because routing derives from media presence rather than an op int — the failure mode is a plausible WRONG video, not an error.",
      "NEVER hand-edit a runtime workflow JSON. Re-export from ComfyUI into comfy_workflows/raw/, then scripts/sync-raw-workflows.mjs (or workflow-to-api.mjs + orchestrate.py) — the generator is the sanctioned normaliser and it now hard-fails on a stale export.",
      "The conversion needs a LIVE /object_info. The bench on 8188 is the user's authoring install and has the right nodes; the app engine on 48188 was still on old KJNodes at session end. COMFY_URL overrides the default.",
      "The 41GB bf16 High-tier weight is NOT on this machine and there is no room for it. Both tiers share one generated graph, so verifying balanced verifies the graph; only that weight is unexercised.",
      "R2 uploads are capped at 3 MB/s and serialized by the capability rule (c:/AI/Mpi/MadPony-Identity/capabilities/cloudflare-r2/README.md). rclone copy is idempotent — re-running resumes. Never pipe rclone to tail; $? becomes tail's.",
      "PROGRESS_STAGES for ltx_i2v_t2v.json is the OLD graph's measurement and is flagged UNVERIFIED in the file. The user expects it to be unchanged and will report if not."
    ],
    "files_modified": [
      "comfy_workflows/ltx_i2v_t2v.json (new), ltx_i2v_t2v_int8.json (new), and the 12 old ltx_*.json (deleted)",
      "comfy_workflows/scripts/workflow_generation/generate_ltx.py + ltx_i2v_t2v_template.json, comfy_workflows/raw/ltx_i2v_t2v_template.json",
      "js/data/modelConstants/models.js (both LTX cards), modelDeps.js (int8 in, fp8/mxfp8 kept as deprecated)",
      "js/data/progressStages.js, js/services/commandExecutor.js, routes/comfy.js, routes/remotePodLifecycle.js",
      "comfy_workflows/input/*.latent (3 deleted)",
      "tests/optional-media-placeholder.test.cjs (rule inverted), output-prompt-capture.test.cjs, resolve-model-deps.test.cjs, lane-settle-on-bail.test.cjs",
      "docs/models/ltx/model-set.md, docs/models/ltx/tiers.md, docs/workflow-authoring/media-inputs.md",
      ".agents/mpi-kanban/tasks/MPI-466/ (task.json, brief.md, checklist.md, validation.md), MPI-456, MPI-457, MPI-467, MPI-468 (new), board.json"
    ],
    "files_to_read_first": [
      ".agents/mpi-kanban/tasks/MPI-466/validation.md",
      ".agents/mpi-kanban/tasks/MPI-466/checklist.md",
      ".agents/mpi-kanban/tasks/MPI-465/brief.md",
      "comfy_workflows/scripts/workflow_generation/generate_ltx.py"
    ]
  },
  "project_knowledge": {
    "profile": ".agents/mpi-kanban/project-profile.md",
    "knowledge_index": ".agents/mpi-kanban/project-knowledge-index.md",
    "mode": "file",
    "relevant_topics": ["models", "workflow authoring", "comfy engine", "generation lifecycle", "mpi nodes"]
  },
  "rules_active": [
    ".claude/rules/dos_and_donts.md",
    ".claude/rules/comfy_engine.md",
    ".claude/rules/comfy_injection.md",
    ".claude/rules/kanban.md",
    ".claude/rules/git.md",
    "docs/workflow-authoring/README.md",
    "docs/generation-lifecycle.md",
    "docs/playbooks/add-model/README.md"
  ],
  "recent_events": [
    {
      "at": "2026-08-07T03:40:00Z",
      "event": "handoff_created"
    }
  ],
  "resume_prompt": "You are resuming MPI-466, which re-wired LTX-2.3 in Cubric Vision. Read .agents/mpi-kanban/tasks/MPI-466/validation.md FIRST — it splits what was actually proved from what was only written, and its NOT-proved section is your work list. State: twelve LTX runtime files became TWO (ltx_i2v_t2v.json on bf16 for the High card, ltx_i2v_t2v_int8.json for Balanced), because the user's re-export merges t2v/i2v into one graph, carries both stages in it via MpiStageLatents, and derives routing from which frame slots are filled instead of op booleans; int8_convrot replaced the fp8/mxfp8 arch pair so variants.arch and the _fp8/_mxfp8 suffixes are deleted. npm test is 478/478 and everything is committed (92420fad, 5ab43e4e). NOTHING HAS RUN THROUGH THE APP. Your first job is to start the app: its engine still holds KJNodes 7f43f2c and the boot drift check repairs it to 35e5956, which is the prerequisite for LTX running at all (MPI-465 — all 12 old LTX graphs died at the stage-1 sampler because KJNodes called .to() on a ModelPatcher, and that regression is live in the CURRENT public release). Then run an LTX t2v on the BALANCED tier and read the DISPATCHED graph off /queue rather than trusting that the run finished — injection silently skips an Input_* title matching no node, and with media-derived routing the failure mode is a plausible WRONG video. Then prove all four routes by their output (t2v, start-only, end-only, start+end), then preview -> Continue, then re-measure PROGRESS_STAGES. Two things you must NOT do: do not delete a dep entry from modelDeps.js to retire a weight (the uninstall sweep iterates that map, so a deleted entry strands the file on every existing user's disk — retire it as a deprecated entry instead, which this session had to be corrected on), and do not start removing the now-dead variants axis without reading the constraint list, because a parallel session's scripts/smoke-workflows.mjs still passes variantTokens. That parallel session is live in this same tree on MPI-470 and MPI-467/468 — re-read shared files before writing and commit by explicit pathspec. Also outstanding: the int8 R2 upload was 28% done at the session boundary (rclone copy is idempotent, just re-run it, then HEAD the public URL for content-length 21505993424), MPI-465 still needs a hotfix-vs-1.4.0 decision and a Pod image rebuild, and MPI-452 (MiniMax H3) is still validating and untouched today."
}
