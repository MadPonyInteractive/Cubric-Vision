{
  "schema": "mpi-kanban/handoff/v1",
  "id": "46b37294-4fc0-42d0-b467-dfecc34cdf8c",
  "generated_at": "2026-08-02T16:30:00Z",
  "from_session": "MPI-427 round 2 — the node-install root cause, fixed and live-verified",
  "to_role": "integrator",
  "status": "complete",
  "session": {
    "name": "MPI-427 round 2 — custom nodes survive a blocked model host; 1.3.1 branch ready",
    "branch": "master (fixes committed) + branch 1.3.1 (the release lineage)"
  },
  "goal": {
    "original": "The user who started this (micha) could not generate because the MPI custom nodes were not installed or updated. Unblock him. Then ship 1.3.1 as a TRUE patch — the only difference from released 1.3.0 being the fix for this case, with none of master's in-flight masking or workflow work.",
    "status": "complete",
    "summary": "Root cause found, fixed, and verified LIVE in both directions. The universal-workflow dep set spans two hosts (custom nodes = github.com zips, engineAsset weights = models.cubric.studio) but installed all-or-nothing, and the reject sat ABOVE the custom-node install — so his blocked model host discarded a full set of nodes that downloaded perfectly, on every launch, forever. Fixed on master (0a1d2325, 81bfe1de) and cherry-picked onto branch 1.3.1 (5 commits). 305/305 suites green on master, 301/301 on the branch, eslint clean, release:check passes, notes re-approved. NOT built, NOT tagged, NOT pushed, NOT released."
  },
  "plan": {
    "file": "C:/Users/Fabio/.claude/plans/continue-agents-mpi-kanban-state-handoff-golden-horizon.md",
    "completed": [
      "Fix 1 — startUniversalWorkflowInstall resolves with the failure, installs the custom nodes that DID download, then throws; the error carries the modelJob",
      "Fix 1 sweep — both engine-provision callers recover the job (_provisionWindowsEngine archive, _provisionUvEngine uv-bootstrap). A one-platform fix here is a false done",
      "Fix 1c — /engine/repair-deps separates an outstanding NODE from an outstanding WEIGHT; nodes all present broadcasts engine:complete so the boot gate releases",
      "Fix 2 — the repair error phase carries a 'Continue without them' escape on the NEW engine:gate-release event, wired into the shell boot gate",
      "Fix 3 — the floating rejection on the non-resume download path (start() had no .catch, resumeFromFile did). This is what filled the reporter's log with [ERROR] [system] Unhandled promise rejection",
      "Corrected the shipped remedy copy — it led with 'set your DNS to 1.1.1.1', the exact advice this user tested and disproved. Now leads with a VPN",
      "tests/uw-partial-install.test.cjs — 6 lockstep assertions, negative-controlled against 3 doctored sources, each fails as required",
      "Fixed the test stub that hid Fix 3 (start() returned the push() index, not a promise, unlike the real NDH API)",
      "LIVE verification, both directions, against a throwaway CUBRIC_ENGINE_ROOT",
      "Branch 1.3.1 cut from the 1.3.0 maintenance branch in a git worktree; 5 commits; kanban files deliberately excluded; master's masking scratchpad reverted off it",
      "MPI-427 card updated with the round-2 root cause and the live evidence table"
    ],
    "pending": [
      "DONE 2026-08-02: built from branch 1.3.1 (mpi-ci run 30755518372), published v1.3.1 with full builds + delta bundles, baselines restamped to the 1.3.1 full manifests, mirror split to MPI-429, MPI-427 closed."
    ]
  },
  "task_card": {
    "id": "MPI-427",
    "title": "A blocked model host must be a readable error, not an error loop (1.3.1)",
    "column": "doing",
    "workspace": ".agents/mpi-kanban/tasks/MPI-427/",
    "links": {
      "plan": null,
      "checklist": ".agents/mpi-kanban/tasks/MPI-427/checklist.md",
      "validation": null,
      "handoffs": ".agents/mpi-kanban/tasks/MPI-427/handoffs/"
    }
  },
  "kanban_entry": null,
  "allowed_actions": [
    "read",
    "build",
    "release",
    "commit"
  ],
  "knowledge_preservation": {
    "completed": [
      "MPI-427 checklist.md carries a 'Round 2' section: the root cause, the fix at each of the three sites, the boot-gate trap, and the live-repro evidence table (pre-fix vs post-fix)",
      "checklist.md records that the LOCAL dev engine's custom_nodes/ComfyUI-MpiNodes is a SYMLINK to C:/AI/Mpi/ComfyUi-MpiNodes — deleting it to force a repair would destroy the node source repo",
      "docs/releases/2026-08-02-v1.3.1.md and js/data/releaseNotes.js both describe the new fixes; notes re-approved (.approved-1.3.1.json hash changed twice)",
      "DONE in bb13cedb — docs/download-manager.md gained a section on the TWO-HOST split of the universal dep set (custom_nodes = github.com via lockUrl/node_lock, engineAsset = models.cubric.studio via assetDeps), plus the resulting install contract. That routing gap is what let the round-2 bug hide.",
      "DONE in bb13cedb — the .download() marker guard is documented as PATH-equal (_isSameObjectUrl) instead of string-equal. NOTE the previously-proposed 'lines 188 and 202' was half wrong: 188's 'mismatch' is the SHA256 append-corruption note and was correct as written; only 202 was stale.",
      "DONE in bb13cedb — package.json gained a `test` script (node --test 'tests/**/*.test.cjs'). There was none, so every session rediscovered it. Deliberately NOT cherry-picked to the 1.3.1 branch: a dev-convenience script is not a patch fix.",
      "Memory added: tool_isolated_engine_root_harness — CUBRIC_ENGINE_ROOT + a require.cache DEPS swap runs the real install code with no app/port/admin, and the local custom_nodes/ComfyUI-MpiNodes is a SYMLINK that must never be deleted to force a repair."
    ],
    "pending": [
      "CLAUDE.md and .claude/rules/kanban.md point at <mpi-lib>/task-board-ops/_schema.md and mutate.md as if they were filesystem paths. Previous session called them unfindable; measured this session, they DO resolve at ~/.claude/skills/mpi-lib (a symlink). So the fix is to make the paths explicit rather than to relocate anything. Still not applied.",
      "PRE-EXISTING, not caused by this session: .agents/mpi-kanban/events.jsonl line 1959 is blank and fails JSON.parse. Present in HEAD. Left alone deliberately."
    ]
  },
  "next_action": {
    "description": "Build and publish v1.3.1 from branch 1.3.1 with BOTH the full portable builds and the delta update bundles — the standard mpi-release flow. Do NOT build from master: master is 45+ commits ahead and carries masking and workflow work that must not ship in a patch. Dispatch the mpi-ci build with ref: 1.3.1 (workflow_dispatch takes a branch, so no tag needs pushing first). CI reads release-baselines/<platform>-<arch>.json and passes --from-manifest automatically, and the branch already carries the 1.3.0 restamp (4d2cbe17), so the delta computes 1.3.0 -> 1.3.1 correctly. After publishing, restamp release-baselines/ from the published 1.3.1 full manifests — watch the win32-x64.json filename trap in context.constraints.",
    "command": "/mpi-continue MPI-427"
  },
  "context": {
    "key_decisions": [
      "1.3.1 is built from the maintenance branch 1.3.0, NOT master. This is what makes the patch scope structural rather than a matter of discipline: `git diff v1.3.0 1.3.1` is 18 files, all download/engine/boot/release, zero masking, zero workflows. Verified explicitly because the user asked for it.",
      "The kanban files in 6b27ab4b were deliberately NOT carried onto the branch (board.json, events.jsonl, tasks/MPI-427/). The branch's board is a v1.3.0 snapshot; the card lives on master. They were the only cherry-pick conflicts.",
      "The 71405415 pick dragged six lines into docs/releases/UNRELEASED.md that were never on this lineage — the mask-toolbar Detect entry, which belongs to master's in-flight masking work. Reverted on the branch (d5a01c6b); master keeps it. Scratchpad only, no shipped copy.",
      "Fix 2 uses a NEW event, engine:gate-release, rather than reusing engine:install-skipped. The latter means 'I will use RunPod instead' and MpiRunpodSettings.js:1640 syncs the skip-local-engine switch ON when it fires — reusing it would show the user a local-engine skip they never chose.",
      "Fix 3 fixed the test STUB rather than making the caller defensive. The stub returned the push() index from start() instead of a promise, unlike the real node-downloader-helper API. A stub that lies about the contract hides the whole class of bug.",
      "The shipped 1.3.1 message told users to set DNS to 1.1.1.1. brief.md records that this exact user tested a DNS switch and the download still died at ~20%, and that 1.1.1.1 was pulled from the draft advice before sending. Shipping disproven advice to the users the message exists for was wrong, so the copy now leads with a VPN. This changed a test assertion in transport-error-message.test.cjs.",
      "The mirror was NOT enabled and is NOT in this release. _MODEL_MIRRORS is still empty. The whole point of round 2 is that the app-side fix unblocks the user WITHOUT any Cloudflare change."
    ],
    "constraints": [
      "FULL BUILDS + DELTA BUNDLES — the standard release flow. The user first asked for delta-only, then chose on 2026-08-02 to publish the full builds alongside, specifically to avoid the baseline trap below. Do NOT re-litigate this to delta-only; the reasoning is recorded here.",
      "Still required either way: release-baselines/*.json must be restamped to the PUBLISHED 1.3.1 FULL portable-stage manifests after publishing (see release-baselines/README.md § Contract), or 1.3.2's delta computes against 1.3.0 and ships fat. Publishing the full builds is what makes this the ordinary mpi-release step rather than a manual artifact rescue — that is precisely why the user chose it.",
      "Filename trap in that restamp: the baseline is release-baselines/<matrix.platform>-<matrix.arch>.json, and the Windows matrix uses platform: win32, so it MUST be win32-x64.json — NOT windows-x64.json, which is the artifact label. It was wrong through 0.0.12 and every Windows build silently fell back to a FULL bundle (MPI-66).",
      "The tag is a BUILD TRIGGER, not a release marker (push: tags: v*). Prefer workflow_dispatch with ref: 1.3.1 so no tag is pushed prematurely. If a tag is pushed, verify `git rev-parse 'v1.3.1^{}'` equals the SHA CI actually built BEFORE publishing — v1.3.0's tag sat 63 commits behind across five rebuild rounds.",
      "Shared working tree with other live agents. Commit by explicit pathspec only, never git add -A. Other agents committed PromptBox work mid-session; MPI-365/validation.md, docs/masking-sam3.md, js/utils/maskTextPrompt.js, tests/mask-text-prompt.test.cjs and docs/releases/2026-07-30-v1.3.0.md were dirty and belong to them.",
      "The 1.3.1 branch lives in a git WORKTREE at C:/Users/Fabio/AppData/Local/Temp/claude/c--AI-Mpi-Cubric-Vision/db0be21f-6e4f-4866-b832-8d461a662334/scratchpad/wt-131 — deliberately, so switching branches never moved other agents' working tree. It is a scratchpad path and will not survive cleanup; the BRANCH itself is in the real repo and is what matters. Re-create with `git worktree add <path> 1.3.1` if needed.",
      "Branch 1.3.1 is LOCAL ONLY — never pushed. Push is a user-authorised live op.",
      "The local dev engine's custom_nodes/ComfyUI-MpiNodes is a SYMLINK to C:/AI/Mpi/ComfyUi-MpiNodes. Never delete that folder to force a repair — it destroys the node source repo. Use the isolated CUBRIC_ENGINE_ROOT harness pattern instead.",
      "Port 3000 was occupied by the user's running app all session, and 8188 by the ComfyUI bench. The live verification was done WITHOUT taking either, by pointing CUBRIC_ENGINE_ROOT and CUBRIC_MODELS_ROOT at scratch dirs."
    ],
    "files_modified": [
      "routes/downloadManager.js",
      "routes/engine.js",
      "js/shell.js",
      "js/events.js",
      "js/components/Compounds/MpiEngineInstall/MpiEngineInstall.js",
      "js/components/Compounds/MpiEngineInstall/MpiEngineInstall.css",
      "js/data/releaseNotes.js",
      "docs/releases/2026-08-02-v1.3.1.md",
      "docs/releases/.approved-1.3.1.json",
      "tests/uw-partial-install.test.cjs",
      "tests/transport-error-message.test.cjs",
      "tests/download-completion.test.cjs",
      ".agents/mpi-kanban/tasks/MPI-427/checklist.md",
      ".agents/mpi-kanban/tasks/MPI-427/task.json",
      ".agents/mpi-kanban/tasks/MPI-427/events.jsonl",
      ".agents/mpi-kanban/events.jsonl"
    ],
    "files_to_read_first": [
      ".agents/mpi-kanban/tasks/MPI-427/checklist.md",
      ".agents/mpi-kanban/tasks/MPI-427/brief.md",
      "release-baselines/README.md",
      "docs/releases/2026-08-02-v1.3.1.md",
      "docs/releases/portable-distribution-contract.md"
    ],
    "scratchpad": "The live repro harness is at C:/Users/Fabio/AppData/Local/Temp/claude/c--AI-Mpi-Cubric-Vision/db0be21f-6e4f-4866-b832-8d461a662334/scratchpad/live-repro.cjs (and live-master.cjs). It swaps DEPS in require.cache for one github.com node plus one weight on an unreachable .invalid host, points CUBRIC_ENGINE_ROOT at a throwaway dir, and runs the real startUniversalWorkflowInstall. Pre-fix: node folder NO, marker MISSING, leftover zip YES. Post-fix: 36 entries, marker at the pinned SHA, no zip. Recreate it from the checklist description if the scratchpad is gone."
  },
  "project_knowledge": {
    "profile": "Cubric Vision",
    "knowledge_index": "docs/README.md",
    "mode": "file",
    "relevant_topics": [
      "docs/download-manager.md",
      ".claude/rules/downloads.md",
      ".claude/rules/comfy_engine.md",
      ".claude/rules/versioning.md",
      "docs/releases/portable-distribution-contract.md",
      "release-baselines/README.md"
    ]
  },
  "rules_active": [
    ".claude/rules/dos_and_donts.md",
    ".claude/rules/downloads.md",
    ".claude/rules/versioning.md",
    ".claude/rules/git.md"
  ],
  "recent_events": [
    {
      "at": "2026-08-02T15:42:00Z",
      "event": "commit 0a1d2325 — custom nodes survive a blocked model host; 11 files"
    },
    {
      "at": "2026-08-02T15:55:00Z",
      "event": "commit d85fa1c6 — MPI-427 card round 2, live evidence recorded"
    },
    {
      "at": "2026-08-02T16:19:00Z",
      "event": "commit 81bfe1de — floating rejection on the non-resume download path"
    },
    {
      "at": "2026-08-02T16:25:00Z",
      "event": "branch 1.3.1 complete at 5328c033; 301/301 green; release:check passes"
    },
    {
      "at": "2026-08-02T16:30:00Z",
      "event": "handoff_created"
    }
  ],
  "resume_prompt": "You are finishing MPI-427 on Cubric Vision by building and publishing v1.3.1 as a patch release, with BOTH full builds and delta update bundles (the standard flow — the user considered delta-only and chose against it, so do not revisit that). Read .agents/mpi-kanban/tasks/MPI-427/checklist.md first — its 'Round 2' section carries the root cause and the live evidence, and brief.md carries a correction to an earlier wrong conclusion about Cloudflare that you must not re-derive. State of play: the code is DONE and verified live in both directions. The universal-workflow dep set spans two hosts (custom nodes are github.com zips, engineAsset weights are on models.cubric.studio) but installed all-or-nothing with the reject sitting above the custom-node install, so a user whose ISP blocks the model host had every downloaded node discarded on every launch and could never generate. Fixed on master as 0a1d2325 and 81bfe1de, cherry-picked onto branch 1.3.1 which is 5 commits, 301/301 suites green, release:check passing, notes approved. Nothing is built, tagged, pushed or released. Build from BRANCH 1.3.1, never from master — master is 45+ commits ahead with masking and workflow work that must not ship in a patch, and `git diff v1.3.0 1.3.1` is 18 files of download/engine/boot/release only. Dispatch the mpi-ci build with ref: 1.3.1 rather than pushing a tag, since the tag is a build trigger that goes stale. CI passes --from-manifest from release-baselines/ automatically and the branch already carries the 1.3.0 restamp, so the delta computes 1.3.0 -> 1.3.1 correctly; attach the full builds AND the -update-v1.3.1 bundles. After publishing, restamp release-baselines/ from the published 1.3.1 full manifests or 1.3.2's delta computes against 1.3.0 and ships fat — and note the filename must be win32-x64.json, matching the CI matrix platform, never windows-x64.json, which silently disabled Windows deltas through 0.0.12 (MPI-66). The branch is LOCAL ONLY and push stays a user-authorised op. The download mirror is still empty, still approval-gated, and deliberately NOT part of this release — the whole point of this fix is that it unblocks the user without it; consider splitting the mirror to its own card so MPI-427 can close on the release."
}
