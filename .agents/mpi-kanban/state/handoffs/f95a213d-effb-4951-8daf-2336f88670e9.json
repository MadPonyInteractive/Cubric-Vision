{
  "schema": "mpi-kanban/handoff/v1",
  "id": "f95a213d-effb-4951-8daf-2336f88670e9",
  "generated_at": "2026-08-07T01:10:00Z",
  "from_session": "MPI-452 continuation — H3 step 8, the stranded lane, and an LTX regression nobody had noticed",
  "to_role": "implementer",
  "status": "open",
  "session": {
    "name": "H3 step 8 SHIPPED; MPI-463 (stranded lane) and MPI-465 (LTX dead on 0.30.x) found, fixed and carded; MPI-466 filed to collapse LTX to one card",
    "branch": "master"
  },
  "goal": {
    "original": "Continue handoff 4340839f: finish MPI-452's app verification, then wire ref2va.",
    "status": "in_progress",
    "summary": "Picked up with LTX blocked on the user's authoring. Shipped checklist step 8 (H3 card preview reshot inside the trained frame range). Fixed the stranded-lane bug the previous session left to the user (MPI-463). Then the user reported their bench LTX graph failing while adding last-frame-only support — it was NOT their edit: ComfyUI 0.30.x broke KJNodes' LTX preview wrapper, killing the ENTIRE LTX fleet in the product too (MPI-465, fixed + proven live). Separately diagnosed and fixed their end-frame-only wiring in the bench template (one link), which the user confirmed works. The session ended on an INT8 finding that reshapes the LTX product (MPI-466). Steps 5 (i2v) and ref2va are still unreached."
  },
  "plan": {
    "file": null,
    "completed": [
      "MPI-452 CHECKLIST STEP 8 PASSES — H3 card preview reshot at 864x480, 5 s -> 124 frames (the n % 17 == 5 snap; the retired clip was 56), video + muxed AAC stereo, 371 s on the 48188 engine. minimax_h3_preview.webp regenerated at 480x266. Commits 9cbc3c6b + 1355d41f.",
      "NON-OBVIOUS CONSTRAINT FOUND: MpiTileSheet.js:101 derives the poster by filename convention and hands it to <video poster>, so it IS the card until hover playback starts — it must be frame 0 of the clip it ships with. A better-composed later frame jumps the moment the video plays. First take was rerolled for a wide opening shot rather than posterised from a later frame. Documented in docs/model-library.md (61c453ee).",
      "MPI-463 FIXED — the stranded lane the previous session left uncarded. generationStore.register() takes a LANE SLOT and _laneBusy is DERIVED from the store's running list, so ELEVEN pre-dispatch bails that called exec.onError and returned without settling wedged that lane until app restart. Not twelve: _abortedBail already settles to CANCELLED, and the post-dispatch catch settles once at its own top (which is why ITS nine branches are fine). Fixed structurally with _failBail (settle to PHASES.ERROR, then report); pinned by tests/lane-settle-on-bail.test.cjs with a negative control that names the exact line. 467/467, lint clean. Commits 9d83ea6e + ea2338b5.",
      "MPI-465 FOUND + FIXED — LTX is DEAD on ComfyUI 0.30.x, all 12 shipped graphs. ComfyUI f8a3fd9d (2026-07-25) made LatentUpscaleModelLoader return a ModelPatcher; KJNodes' LTX2SamplingPreviewOverride wrapper still called latent_upscale_model.to(device), and NEITHER patcher class defines .to (proven with hasattr against the engine's own python). Pin bumped 7f43f2c -> 35e5956 in dev_configs/node_lock.json. PROVEN LIVE: the exact failing prompt pulled from the bench /history and re-queued after the bump completed the whole graph in 124 s. Commit c077efa9.",
      "THE USER'S END-FRAME-ONLY WIRING FIXED (bench template, one link). Their `is lf` (a_false_b_true) correctly drove the two new packer selectors #606/#601 that skip the First guide — but the SIX gates that route the guided result back into the main path (#316 #318 #327 #328 #338 #349) were still on `is flf` = both_true, so with no start frame they all took the non-FLF branch and the end guide was computed then discarded. Repointed SetNode #559 (`is flf`) from #557 MpiBooleanCompare (both_true) to #507 MpiAnyChecker:1 (`has img2`). USER CONFIRMED END FRAME NOW WORKS.",
      "MPI-466 FILED — collapse LTX to ONE card (dev int8 + rank-111 distill LoRA as a turbo toggle). See its brief."
    ],
    "pending": [
      "MPI-452 CHECKLIST STEP 5 (i2v through the app) still never reached. Needs the user to drive one i2v run; then read the DISPATCHED graph off /queue and confirm Input_Start_Frame is populated — if it is not, the graph takes the t2v branch and returns a perfectly good video that ignored the image.",
      "MPI-463 NOT PROVEN LIVE. The wedge was seen live; the fix has not been. Renderer code — Ctrl+Shift+R. Cheapest proof: force bail #6 (Cue an op with a required media slot and no media), then Cue a normal generation on that same lane — before the fix the second sits on QUEUED forever.",
      "MPI-465 NOT PROVEN THROUGH THE APP, and the running server CANNOT see the new pin: getPinnedNodeCommit uses a CACHED createRequire, so /engine/deps-status reports driftedDeps: [] right now. The engine repairs itself only after an app RESTART. The Pod image also bakes nodes from this same lock and is still on the old KJNodes, so REMOTE LTX stays broken until it is rebuilt.",
      "LTX MIGRATION (MPI-456) still open — the user is mid-authoring. Its six _stage2 twins are live and correct until their export lands.",
      "MPI-466 — the LTX one-card plan. Do the LoRA-on-int8 check FIRST; it decides the whole thing.",
      "ref2va as a second ModelDef 'minimax-h3-ref2va' — its MODEL_LICENCES key ALREADY EXISTS, do not add it again."
    ]
  },
  "task_card": {
    "id": "MPI-452",
    "title": "Wire MiniMax H3 fl2va as a model",
    "column": "doing",
    "workspace": ".agents/mpi-kanban/tasks/MPI-452/",
    "links": {
      "plan": null,
      "checklist": ".agents/mpi-kanban/tasks/MPI-452/checklist.md",
      "validation": ".agents/mpi-kanban/tasks/MPI-452/validation.md",
      "handoffs": ".agents/mpi-kanban/tasks/MPI-452/handoffs/"
    }
  },
  "kanban_entry": null,
  "allowed_actions": ["read", "continue", "verify", "implement"],
  "knowledge_preservation": {
    "completed": [
      "docs/model-library.md — the poster must be frame 0 of the clip it ships with.",
      "docs/generation-lifecycle.md — register() takes a lane, so every exit owes a terminal (added to the per-gen identity doctrine, where the other lane hazards already live).",
      "tasks/MPI-452/checklist.md — step 8 PASS with its evidence table.",
      "tasks/MPI-463/brief.md, tasks/MPI-465/brief.md, tasks/MPI-466/brief.md — full root-cause records."
    ],
    "pending": [
      "docs/models/ltx/workflow-authoring.md does NOT yet carry the KJNodes/ModelPatcher trap. It is the natural home for 'the optional latent_upscale_model input on LTX2SamplingPreviewOverride is a version-coupled landmine'.",
      "No memory file written for this session's lesson (see context.key_decisions)."
    ]
  },
  "next_action": {
    "description": "Ask the user which they want first. (1) Restart the app — that alone lets the engine drift-repair onto the fixed KJNodes and closes MPI-465's second criterion, and it is the precondition for any LTX test through the app. (2) MPI-452 step 5: one i2v run, then read /queue. (3) MPI-463's live proof, which is a 30-second forced bail. (4) MPI-466: before any design, check whether ltx23-lora-merged / transition / talkvid were ACTIVE in the user's int8 runs — if they were, LoRA-on-int8 already works and most of the plan's risk is retired for free. Do NOT touch LTX workflow files until the user's authoring export lands.",
    "command": "mpi-continue MPI-452"
  },
  "context": {
    "key_decisions": [
      "THE LESSON OF THIS SESSION: a failure reported inside the user's own work-in-progress was NOT caused by it. The bench LTX crash landed while the user was adding last-frame-only, and it read as their bug — but the offending link had been in the shipped graphs since 2026-07-22 and the breakage came from a ComfyUI commit three days later. The tell was cheap and was almost skipped: check whether the failing wiring is OLDER than the edit being blamed. git had the answer in one command.",
      "The KJNodes fix is version-agnostic — `getattr(latent_upscale_model, 'model', latent_upscale_model)` handles both the patcher and the bare module. So the pin bump fixes the engine AS IT STANDS on 0.30.0; the separate engine bump to 0.30.2 the user wants neither fixes nor re-breaks this.",
      "The ENGINE is 0.30.0, the BENCH is v0.30.2 (a real tag on origin/release/v0.30, not a nightly). The version NUMBER is not the discriminator for this bug — 0.30.0 already contains the breaking commit. Read the file, not the version.",
      "MPI-463 was fixed structurally, not eleven times over. A helper every bail routes through makes a TWELFTH bail safe by construction — which matters, because unwired-one-at-a-time is exactly how eleven accumulated.",
      "The user's end-frame gate is now `has img2` (end frame present), NOT both-frames. That is correct because #606/#601 handle the start-present-or-not case inside the chain. The Set is still NAMED `is flf`, which is now a lie — renaming it to `use end guide` is 7 widget edits and was deliberately left to the user mid-test.",
      "MPI-466's shape came from the USER, and it is better than the framing offered to them: rather than choosing dev OR distilled, ship dev int8 and reconstruct distilled at runtime with the rank-111 LoRA — which is exactly what Krea 2 already does to avoid a second download."
    ],
    "constraints": [
      "NEVER hand-edit a repo workflow JSON. generate_*.py + node scripts/sync-raw-workflows.mjs own comfy_workflows/. (The BENCH authoring template was hand-edited this session at the user's explicit request — that file is theirs, not generated output.)",
      "js/services/*.js is RENDERER code — Ctrl+Shift+R (a plain Ctrl+R can serve a CACHED ES module). routes/*.js is SERVER code and needs a full app restart.",
      "routes/ reads dev_configs/node_lock.json through a CACHED createRequire — a pin change is invisible to a running server. Always restart before trusting /engine/deps-status.",
      "Bench = 8188 (G:\\ComfyUi, launched as `python_embeded\\python.exe ComfyUI\\main.py --preview-method taesd --output-directory D:\\WORK\\Images\\Outputs`, cwd G:\\ComfyUi). App engine = 48188. A node change needs a restart of whichever must see it.",
      "The bench's KJNodes is a git checkout now DETACHED at 35e5956 (was 7f43f2c). Backup of the user's template before the one-link edit: scratchpad/ltx_template_BACKUP.json.",
      "NOTHING H3 IS ON R2 AND NOTHING MAY BE — the licence grants rights only outside the EU/UK/USA/South Korea. LTX has no such bar: its deps serve FROM R2 with HF as mirrorUrl, so adopting an LTX weight means uploading it.",
      "Do NOT re-open the i2v aspect question (closed), and do NOT chase the KJNodes triton ImportError (benign)."
    ],
    "files_modified": [
      "comfy_workflows/display/minimax_h3_preview.{mp4,webp} (reshot)",
      "js/services/commandExecutor.js (_failBail + 11 bail sites)",
      "tests/lane-settle-on-bail.test.cjs (NEW)",
      "dev_configs/node_lock.json (comfyui-kjnodes 7f43f2c -> 35e5956)",
      "docs/generation-lifecycle.md, docs/model-library.md",
      ".agents/mpi-kanban/tasks/MPI-463/, MPI-465/, MPI-466/ + board.json",
      "OUTSIDE THE REPO: G:\\ComfyUi\\ComfyUI\\user\\default\\workflows\\ltx_i2v_t2v_template.json (one link), and the bench's comfyui-kjnodes checkout"
    ],
    "files_to_read_first": [
      ".agents/mpi-kanban/tasks/MPI-452/checklist.md",
      ".agents/mpi-kanban/tasks/MPI-465/brief.md",
      ".agents/mpi-kanban/tasks/MPI-466/brief.md",
      "js/services/commandExecutor.js"
    ]
  },
  "project_knowledge": {
    "profile": ".agents/mpi-kanban/project-profile.md",
    "knowledge_index": ".agents/mpi-kanban/project-knowledge-index.md",
    "mode": "file",
    "relevant_topics": ["models", "comfy engine", "workflow authoring", "mpi nodes", "generation lifecycle"]
  },
  "rules_active": [
    ".claude/rules/dos_and_donts.md",
    ".claude/rules/comfy_engine.md",
    ".claude/rules/kanban.md",
    ".claude/rules/git.md",
    "docs/generation-lifecycle.md",
    "docs/workflow-authoring/README.md"
  ],
  "recent_events": [
    { "at": "2026-08-07T01:10:00Z", "event": "handoff_created" }
  ],
  "resume_prompt": "You are resuming MPI-452 (MiniMax H3 in Cubric Vision) plus three cards this session opened. SHIPPED: checklist step 8 — the H3 card preview was reshot at 864x480, 5 s -> 124 frames, video + muxed AAC, and its .webp regenerated. Non-obvious constraint learned doing it: MpiTileSheet hands the derived .webp to <video poster>, so the poster IS the card until hover playback starts and must be frame 0 of the clip it ships with. FIXED + CARDED: MPI-463, the stranded lane — generationStore.register() takes a lane slot and _laneBusy derives from the store's running list, so eleven pre-dispatch bails that reported an error without settling wedged that lane until restart; _failBail now settles to PHASES.ERROR then reports, pinned with a negative control, 467/467. FIXED + CARDED: MPI-465 — LTX was DEAD on ComfyUI 0.30.x for all 12 shipped graphs, because ComfyUI f8a3fd9d made LatentUpscaleModelLoader return a ModelPatcher while KJNodes still called .to(device) on it; the KJNodes pin is bumped to 35e5956 and it is PROVEN on the bench (the exact failing prompt completed in 124 s). Two things about MPI-465 you must not misread: the engine is 0.30.0 and ALREADY contains the breaking commit (read the file, not the version), and the RUNNING server cannot see the new pin because node_lock is read through a cached require — /engine/deps-status says no drift until the app restarts. Also fixed the user's own bench wiring: their end-frame-only routing computed the end guide and then discarded it, because six gates were still on `is flf` (both_true) while only the two new packer selectors used `is lf`; repointing Set(is flf) to `has img2` fixed it and the user CONFIRMED end frame now works. FILED: MPI-466, the user's idea to collapse LTX to ONE card — dev int8 21.5GB plus the rank-111 distilled-1.1 LoRA as a runtime turbo toggle, exactly the Krea 2 turboToggle pattern, replacing ~66GB of card-pair with 24.25GB. Its gate is LoRA-on-int8, and there is a free partial proof available: check whether the three baked LTX LoRAs were active in the user's int8 runs. STILL OWED: MPI-452 step 5 (one i2v run through the app, then read /queue for Input_Start_Frame), live proof for MPI-463 and MPI-465, a Pod image rebuild for remote LTX, and ref2va. Do NOT touch LTX workflow files until the user's authoring export lands — their six _stage2 twins are still live and correct."
}
