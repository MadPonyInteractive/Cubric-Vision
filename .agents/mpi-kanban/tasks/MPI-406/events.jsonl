{"schema":"mpi-kanban/event/v1","id":"MPI-406","type":"task.created","at":"2026-07-30T22:15:00Z","actor":"opus","column":"todo","summary":"--cpu engine installs resolve generic PyPI torch and pull the whole CUDA 13 stack (torch 2.13.0 bare, triton, 14 nvidia-* wheels). Root cause: --fast-deps routes through comfy-cli DependencyCompiler which ignores the vendor flag - the same fall-through already exempted for --m-series at engine.js:370-372. Measured from requirements.compiled on a GPU-less Linux box during MPI-391 section D. Wasteful, not broken. --amd suspect for the same reason."}
{"schema":"mpi-kanban/event/v1","id":"MPI-406","type":"task.moved","at":"2026-07-30T22:35:00Z","actor":"opus","from":"todo","to":"doing","summary":"Fix landed: --fast-deps is now used only for --nvidia (routes/engine.js), so --cpu and --amd take comfy-cli standard vendor-aware install. Stale comment rewritten to name all three vendors. Needs live confirmation of a +cpu torch resolve on the Linux box."}
{"schema":"mpi-kanban/event/v1","id":"MPI-406","type":"validation.updated","at":"2026-07-31T01:20:00Z","actor":"opus","summary":"PARTIAL - stays in doing, do NOT close on the --fast-deps commit. Tested live on Linux: requirements.compiled is absent after a full install, which proves --fast-deps is genuinely off the --cpu branch, so this card diagnosis was correct for its stage. But the CUDA stack still installs - grep for \"downloading (triton|nvidia)\" returns 4 and the pip log shows torch, torchvision, triton and 14 nvidia-* wheels. Stage tags settle why: the original evidence was [comfy-install], the new lines are [system] [pip] = runPipCommand running ComfyUI-LTXVideo/requirements.txt with --upgrade. A --cpu engine has TWO paths to the CUDA stack; this card fixed one. Second path tracked as MPI-413. The 1.3.0 changelog entry claiming the processor-only build was REMOVED before publishing rather than shipped false."}
