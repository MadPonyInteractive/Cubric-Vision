# MPI-203 Checklist

- [x] Reconcile drives completion: `_reconcileFromHistory` success branch forwards synthetic terminal message(s) through the prompt's listener BEFORE resolving, so the normal onMessage path fires `_finishGeneration` → onComplete → card + status bar + lane free. Verify remote: decode-window stop on pod → reconcile line in log → card appears (also proves MPI-195 bridge).
- [x] Lane stomp on late settle (found during this card's verify, 2026-07-05): stopped job's exec settles AFTER the stop promoted the next queued job → unguarded `finishCueDispatch` cleared the successor's lane slot (queue 0 JOBS, status bar idle, successor unstoppable while still generating). Fix: identity guard in `_dispatchNextCue`'s wrapper — free lane only if `_lanes[lane].active === next`. Verify: stop gen with a queued follow-up → follow-up stays in queue panel with STOP button. (VERIFIED live: follow-up stayed in queue with STOP.)
- [x] Status-bar stomp on late settle (found after lane fix, 2026-07-05): follow-up stayed in queue BUT status bar showed idle — a Stopped gen's late `tool:cancelled` reset the global-singleton bar (no gen identity, keyed only on `tool==='groupHistory'`). VERIFIED live: stop-with-successor no longer stomps.
- [x] Two-lane bar (found after status-bar fix, 2026-07-05): with concurrent local + remote gens, the single status bar could show idle while a LOCAL gen ran — the two lanes fight one singleton, terminal from one lane cleared the other's progress. USER DECISION: last-started/last-active wins, with fallback to the other lane when the owner ends. Fix: stamp `id: exec.genId`/`_regId` on ALL driving emits (running/accepted/loading-model/sampling-start/progress/stage/indeterminate) + terminals; statusBar `_latch(id)` on every driving event (last-active wins), terminal clears only if it still owns `_activeGenId`; a surviving lane's next event re-latches (fallback). Logic verified via scratchpad latch-sim.mjs (5 sequences incl. stomp + two-lane fallback). Verify live: run local + remote concurrently → bar tracks the most-recently-active, and when one ends the other keeps/regains the bar; neither runs progress-blind.
