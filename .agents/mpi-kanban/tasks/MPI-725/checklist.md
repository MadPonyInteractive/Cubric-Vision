# MPI-725 — checklist

Fabio re-authored `comfy_workflows/raw/krea2_t2i_template.json` in the ComfyUI bench. Two
deliberate changes, in his words:

1. **Inpainting** was not working well. It is now much better, but **slower**.
2. **Image-to-image** was **cropping** the input image. A crop node was added so it now
   **resizes** instead.

- [ ] Convert `raw/krea2_t2i_template.json` against the **app engine on 48188**, not the
      8188 bench (`docs/workflow-authoring/converters.md` — bench schema runs ahead of what
      ships).
- [ ] Validate the converted template against the injection rules.
- [ ] Bake the two runtime files (`krea2_t2i_sfw.json` / `krea2_t2i_nsfw.json`) via
      `orchestrate.py` — hash-gated, so only the krea2 template rebuilds.
- [ ] Read the API-format diff of the runtime files to establish what ACTUALLY changed,
      rather than trusting the 5k-line LiteGraph churn.
- [ ] Write the two user-visible changes into `docs/releases/UNRELEASED.md`.
- [ ] Commit raw + generated by pathspec.

## Not ours

`scripts/sync-raw-workflows.mjs` refuses to run: it aborts on ANY uncommitted generated
workflow, and `comfy_workflows/qwen3vl_4b_prompt_enhancer.json` is dirty. That file and its
`raw/` twin are **claimed by MPI-664** (file claim `62da4764`, last heartbeat
2026-09-10T12:04:47Z — stale, but still that card's in-flight work). So the krea2 leg runs
as the sync script's own steps, by hand, touching nothing of MPI-664's.
