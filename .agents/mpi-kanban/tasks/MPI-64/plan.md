# RunPod Remote Engine

## Current State

Project mode: scalable-foundation.

**Status 2026-06-11 (end of session 3):** Phase 1 design + Phase 2 wrapper (live-proven on a real A4000 Pod) done. App-side Parallel Batch #1 (backend client + secret store), #2 (Comfy boundary adapter — local mode byte-identical, user-verified), and #3/#3.1 (Settings RunPod section + remote boot gate + volume-aware pickers, user-verified) are DONE and uncommitted on branch `RunPod`. Next: the small UI-polish item below (dropdown meta truncation + GPU $/hr), then Batch #4 (manifest validation UI) and Phase 3 (volume init + model download). Remote mode is wired end-to-end up to readiness polling; Pod auto-create/start at the comfy-start trigger is Phase 4.

The approved product direction is a Secure Cloud-only RunPod remote engine for Cubric Vision. Community Cloud is out of scope because it is unstable and limited for this use case.

Cubric currently owns generation through `generationService -> commandExecutor -> ComfyUIController.runWorkflow`, with single-dispatch Cue queue semantics, title-based workflow injection, app-owned result capture, local project persistence, and lifecycle events. A remote path must preserve those contracts rather than exposing arbitrary remote ComfyUI URLs.

The v1 architecture is a Cubric-owned hidden RunPod Pod template with a small Cubric HTTP wrapper service exposed through RunPod's HTTP proxy. The wrapper talks to ComfyUI inside the Pod, enforces token auth and compatibility checks, and presents Cubric-specific endpoints to the desktop app.

Persistent remote state should live on a RunPod network volume mounted at `/workspace`. The volume stores model/cache/custom-node/runtime manifest data and survives Pod deletion, but it is Secure Cloud-only and must be attached during Pod deployment.

### Local-coupling inventory (verified against source 2026-06-11)

These are the exact seams a remote engine must cross. Every Phase 1 design decision must account for all of them.

1. **Renderer talks to ComfyUI directly, not through Express.** `js/services/comfyController.js:40` hardcodes `serverAddress = "127.0.0.1:8188"`. Workflow submit is a renderer `fetch` to `http://127.0.0.1:8188/prompt` (`comfyController.js:501`); events arrive over a renderer-opened `new WebSocket('ws://127.0.0.1:8188/ws?clientId=...')` (`comfyController.js:291`). Express (`routes/comfy.js`) only manages the local subprocess (`/comfy/start|stop|status|unload`). The Settings field `mpi_comfy_url` exists in localStorage but is **not read** by the controller — it is dead config today.
2. **Latent previews are binary WebSocket frames.** 8-byte header + JPEG → blob URL → `onPreview` (`comfyController.js:270–297`, `commandExecutor.js:805`). TAESD is enabled via the `--preview-method taesd` launch arg (`routes/comfy.js:245`). Any remote transport must carry binary frames or re-encode them.
3. **Handled WS JSON event types:** synthetic `prompt_ack`, `preview`, `execution_cached`, `executing` (node and node=null completion), `executed`, `progress`, `progress_state` (`commandExecutor.js:779–881`). This list is the wrapper's minimum event contract.
4. **Model-init readiness comes from process stdout, not the ComfyUI API.** Express watches the local ComfyUI process stdout for `"Model Initializing"` / `"Model Initialization complete!"` and rebroadcasts as SSE on `GET /comfy/events/stream` (`routes/comfy.js:66–77, 115`); `commandExecutor.js:768` subscribes for terminal-phase sampler workflows. A remote Pod has no stdout visible to the app — the wrapper must synthesize an equivalent signal.
5. **Images/masks upload; videos/audio/latents do NOT.** Images and masks go through ComfyUI `POST /upload/image` with static cache-friendly filenames `mpi_<param>.png` (`comfyController.js:539–566, 377–415`). Videos/audio are injected as **absolute local filesystem paths** (`_resolveMediaPath`, `comfyController.js:574–596`) read by VHS nodes from disk. Trimmed video inputs are written as local temp files by `POST /api/video/trim-input` (`commandExecutor.js:117–172`). Multi-stage preview latents are copied into the local ComfyUI `input/` dir by `POST /comfy/stage-preview-latent` (`routes/comfy.js:170–195`). All path-injection flows break on a remote Pod and need wrapper upload/staging endpoints.
6. **Output capture streams from ComfyUI's `/view` endpoint.** `executed` messages → view URLs built against the hardcoded address (`commandExecutor.js:33–49`) → `POST /project/save-generation` → backend `streamDownload` from that URL into `<project>/Media/` + `.meta/` sidecars + ffmpeg thumbnails (`routes/projects.js:1324–1500`). Remote mode means the backend must stream from the wrapper with auth, and view URLs must not leak unauthenticated.
7. **Interrupt/queue ops are renderer-direct too:** `POST /interrupt`, `POST /queue {clear:true}`, `POST /queue {delete:[id]}` (`comfyController.js:150–221`).
8. **Boot gating assumes a local engine.** `shell.js:_bootApp` (≈140–224) blocks on `/engine/version-check` → install modal → `engine:ready` → `/engine/deps-status` → `comfy:ready` → model registry sync. Remote mode needs a parallel gate path that skips local install checks.
9. **Model/dependency status is local-filesystem-only.** `POST /comfy/models/check` resolves paths from `extra_model_paths.yaml` and calls `isCompleteOnDisk` (`routes/comfy.js:409–466`). Remote mode needs a wrapper-side equivalent against the volume.
10. **No secret-storage pattern exists in the app.** No keytar/safeStorage/electron-store anywhere; localStorage keys are enumerated in `js/core/storageKeys.js` and none are secret-bearing. The RunPod API key + wrapper token design starts from zero.

Known source files likely to matter:

- `js/services/generationService.js`
- `js/services/commandExecutor.js`
- `js/services/comfyController.js`
- `js/shell/shell.js`
- `routes/comfy.js`
- `routes/engine.js`
- `routes/platformEngine.js`
- `routes/projects.js` (save-generation streaming)
- `routes/shared.js`
- `routes/downloadManager.js` (SSE event shape for download progress)
- `js/components/Compounds/LandingPages/MpiSettings/MpiSettings.js`
- `js/state.js`
- `js/core/storageKeys.js`
- `dev_configs/system_dependencies.json`
- `docs/comfy.md`
- `.claude/rules/comfy_engine.md`
- `.claude/rules/comfy_injection.md`
- `.claude/rules/state.md`
- `.claude/rules/events.md`

## Completed

- [x] Investigated RunPod Pods, templates, exposed ports, network volumes, Pod lifecycle APIs, and GPU catalog.
- [x] Investigated OneTrainer's RunPod implementation pattern.
- [x] Chose Secure Cloud-only HTTP wrapper Pod architecture.
- [x] Captured the approved direction on the MPI board.
- [x] Mapped the exact local↔ComfyUI coupling inventory (transport, previews, input assets, outputs, boot gate, model checks, secrets) — see Current State.

## Remaining Work

## Phase 1: Contract and Manifest Design

> Phase 1 output is design documents, not code. Each decision to-do must produce a markdown file under `research/` in this task workspace (filenames given per item) so later phases and parallel workers can be briefed from it verbatim.

- [x] Decide the transport topology: does the desktop app reach the wrapper renderer-direct, or via an Express backend proxy that mirrors the local route shapes? Recommended default: **backend proxy** — Express forwards to the RunPod proxy URL with the wrapper token attached server-side, so (a) the token never lives in the renderer, (b) `save-generation` streaming keeps working against localhost URLs, (c) `comfyController.js` changes shrink to a base-URL/adapter swap instead of CORS + WSS + auth-header work in the renderer. Document the chosen topology, including how the WebSocket channel is bridged (Express WS proxy vs renderer WSS direct as the one exception). **Output:** `research/transport-topology.md`. **Verify:** the doc traces every item in the local-coupling inventory (1–7) through the chosen topology with no unauthenticated remote endpoint and no token in renderer-accessible storage.
- [x] Define the remote engine contract: wrapper endpoints, auth token flow, health/readiness (including a synthesized model-init signal replacing the stdout watcher — inventory item 4), workflow submit, the WS/streamed event channel carrying all event types from inventory item 3 **including binary preview frames**, output fetch (auth'd `/view` equivalent), interrupt, queue clear/delete, upload/staging endpoints for images, masks, video, audio, and `.latent` files (inventory item 5), model/dependency status, and lifecycle error mapping. **Output:** `research/wrapper-api-contract.md`. **Verify:** a written API sketch maps each existing local `ComfyUIController` + `commandExecutor` capability 1:1 without bypassing Cubric's Cue queue or title-based injection; every endpoint states its auth requirement.
- [x] Decide template ownership and image distribution. Constraint to resolve first: a user's RunPod API key can only deploy templates visible to that account — confirm against current RunPod docs whether a Cubric-account private/"hidden" template is deployable by other users' keys, or whether the app must programmatically create the template in the **user's own account** via `POST /templates` (referencing a Cubric-published Docker image), or use a public community template. Also decide where the Docker image is hosted (GHCR/Docker Hub; public pull required by RunPod) and how it is built/published (candidate: private `mpi-ci` repo workflow, consistent with the existing CI split). **Output:** `research/template-distribution.md`. **Verify:** doc names the template creation path that works with only a user API key, the image registry, the image tagging scheme tied to the manifest version, and the publish pipeline.
- [x] Define the remote volume manifest schema stored under `/workspace/cubric/`: Cubric template version, image digest/tag, ComfyUI version, Python, PyTorch, CUDA, custom-node bundle, workflow bundle, installed model state, volume ID, data center, and last-compatible GPU profile. **Output:** `research/volume-manifest-schema.md`. **Verify:** manifest fields cover all compatibility gates found in the RunPod investigation notes and every field has a defined comparison rule (exact match / minimum / informational).
- [x] Decide the Pod lifecycle policy for app launch/generation/app quit: create vs start existing Pod, stop vs delete Pod, abandoned Pod detection, and user-visible billing warnings. Must include a crash-safety net: Electron quit handlers cannot reliably await network calls, so design a Pod-side idle watchdog (wrapper self-stops the Pod after N minutes without authenticated traffic) as the backstop for app crash/kill. **Output:** `research/pod-lifecycle-policy.md`. **Verify:** lifecycle state table covers fresh install, existing stopped Pod, running Pod, failed start, app crash/relaunch, network loss mid-generation, and user quit — each row names who stops billing.
- [x] Decide local secret persistence for the user-provided RunPod API key and per-Pod wrapper token. Starting facts: the app has **no** existing secret store (inventory item 10); candidates are Electron `safeStorage` (preferred, OS-keychain-backed) with a documented fallback, vs an encrypted file under the app's user-data dir. Plaintext localStorage is not acceptable for the API key. **Output:** `research/secret-storage.md`. **Verify:** design keeps secrets out of project files, localStorage, logs, and bug reports, and does not introduce a Cubric auth backend.

## Phase 2: Remote Runtime Prototype

- [x] Build a minimal Cubric remote-wrapper prototype that runs inside a RunPod-compatible Linux container and forwards to local ComfyUI. Must demonstrate the hard parts of the contract, not just REST proxying: token-gated WS event channel relaying binary preview frames intact, an upload endpoint that lands a file where a ComfyUI loader node can read it, and a synthesized model-init/readiness signal (no stdout dependency). **Verify:** local container or dev host answers `/health`, rejects a missing/wrong token on both HTTP and WS upgrade, relays a `progress` event and one binary preview frame end-to-end, and accepts an image upload that a workflow can consume. — DONE 2026-06-11: prototype at `c:\AI\Mpi\cubric-remote-wrapper\` (Python/FastAPI, runtime decided = Python per OneTrainer precedent; permanent home = private `mpi-ci`). Now FULL contract (24/24 verify PASS): /health, /wrapper/manifest, /wrapper/prompt (+bundle-version gate), /ws relay (JSON+binary+synthesis), /wrapper/events/stream SSE, /wrapper/view (auth'd stream), /wrapper/interrupt, /wrapper/queue, /wrapper/upload/image|media|latent, /wrapper/models/status, plus the 15-min idle watchdog (self-stop via RunPod API) and error envelope. RunPod SDK lifecycle calls confirmed against OneTrainer clone.
- [x] Create the first Cubric RunPod template definition (per the Phase 1 template-distribution decision) using Secure Cloud-compatible ports and startup command. **Verify:** template exposes only the wrapper HTTP port and optional diagnostic SSH/TCP port, with no public raw ComfyUI endpoint in normal operation; a Pod deployed from a plain user API key reaches ready state. — DONE 2026-06-11: image built + pushed to public GHCR (`ghcr.io/madponyinteractive/cubric-vision-pod:v0.1.0`); template `ejrwtsej15` exposes only `8889/http` (wrapper); Pod `k8pr35yto3xy7n` (A4000, EUR-IS-1, volume `cfwqthmdrg`) reached ready — `/health` `{ready:true,comfy_ready:true}` through the RunPod proxy.
- [~] Add remote readiness checks for ComfyUI and Cubric wrapper compatibility. **Verify:** a Pod reports not-ready until both wrapper and internal ComfyUI are ready and manifest version matches the desktop app; readiness polling tolerates RunPod's known stale-runtime-payload window after start/resume (OneTrainer finding). — PARTIAL 2026-06-11: `/health` distinguishes `ready` vs `comfy_ready` and was polled live through the proxy (stale-payload tolerant: `runtime:null` until container up). REMAINING: `/wrapper/manifest` version-match gate vs app `APP_VERSION`/`COMFY_VERSION` (needs manifest written to volume — Phase 3), and the start/resume polling helper in app code (Parallel Batch).

## Parallel Batch: App Integration Surfaces

> Brief each worker with the Critical Rules Snapshot, the listed rule briefings, the local-coupling inventory from Current State, and the relevant Phase 1 `research/*.md` decision docs. Do not dispatch this batch until Phase 1 docs exist — every item below consumes at least one of them.

- [x] Add backend RunPod client, lifecycle service, and secret storage. Ownership: `routes/runpodRemote.js` (new), `routes/remoteEngine.js` (new), secret-storage module per `research/secret-storage.md`, `routes/logger.js` touchpoints, package dependency/config files if needed. Briefings: comfy_engine, state/events as needed. Consumes: `pod-lifecycle-policy.md`, `secret-storage.md`, `template-distribution.md`. **Verify:** mocked REST calls can create/start/stop/get Pod status; API key round-trips through the secret store; no code path logs the API key or wrapper token (grep test on logger calls). — DONE 2026-06-11 inline-sequential: `main/secretsStore.js` (safeStorage + AES-GCM fallback + fork bridge + `secrets:*` IPC), `routes/runpodRemote.js` (REST+GraphQL client + `redactSecret`), `routes/remoteEngine.js` (key resolver, wrapper-token gen, readiness poll); wired into `server.js`/`main.js`. Verified mocked + live GraphQL + secret-hygiene grep + user `npm start` smoke test (app boots clean, console clean).
- [x] Add remote engine abstraction at the Comfy execution boundary. Ownership: `js/services/comfyController.js`, `js/services/commandExecutor.js`, new `js/services/remoteEngineClient.js` if needed, plus the Express proxy routes if the backend-proxy topology was chosen. Key facts: the boundary today is renderer-direct with `serverAddress` hardcoded (`comfyController.js:40`) and view URLs built in `commandExecutor.js:33–49`; the unused `mpi_comfy_url` localStorage key should be retired or repurposed deliberately, not left half-wired. Briefings: comfy_injection, events. Consumes: `transport-topology.md`, `wrapper-api-contract.md`. **Verify:** local mode behavior is byte-identical (same URLs, same event flow); remote mode is selected through a narrow adapter; no component talks to Comfy directly. — DONE 2026-06-11 inline-sequential: NEW `js/services/remoteEngineClient.js` (narrow adapter; token in-memory only) + `routes/remoteProxy.js` (backend-owned mode state; `/remote/mode|ws-token|comfy/status`; `/proxy/prompt|view|interrupt|queue|upload/image` with Bearer+browser-UA; `/comfy/events/stream` SSE intercept with next() fall-through, mounted before comfy routes). EDITED `comfyController.js` (`httpBase()` seam, `_ensureRemoteReady`, WS URL fallback), `commandExecutor.js` (view URL via `httpBase()`), `server.js` (mount order). Verified: 19/19 mocked-router test + user-run local SDXL generation byte-identical (clean console). NOTE: `mpi_comfy_url` retirement deferred to the Settings batch (its UI owns the field); media/latent upload intercepts stay in Phase 4; `models/check` routing stays in Phase 3.
- [x] Add settings/state UX for Secure Cloud RunPod configuration plus the remote-mode boot gate. Ownership: `js/components/Compounds/LandingPages/MpiSettings/MpiSettings.js`, `js/state.js`, `js/shell.js` (`_bootApp` gate path — inventory item 8; NOTE: file is `js/shell.js`, not `js/shell/shell.js`), relevant settings services/routes. Briefings: components, state, events. Consumes: `pod-lifecycle-policy.md`, `secret-storage.md`. **Verify:** user can enter/test a RunPod API key, choose Secure Cloud GPU/data center/volume strategy, cannot select Community Cloud; in remote mode the app boots past the local-engine install gate without requiring a local engine. — DONE 2026-06-11 inline-sequential + USER-VERIFIED (key save+validate, EUR-IS-1 picker, advisory pop-up on boot, toggle-off boot unchanged). NEW `js/core/secretsClient.js` (sole renderer secrets:* IPC wrapper, write-only key); `storageKeys/storage/state` carry non-secret `runpodConfig` ({enabled,podId,datacenter,gpuType,volumeId}, localStorage-mirrored); MpiSettings RunPod section (toggle off-default, password key field + save-time weakEncryption warning + auto-validate, DC picker storage-DCs-only with "your volume" marker + mismatch warning, GPU picker Secure-Cloud `available:true` stock-sorted High→Med→Low→N/A with stock-first meta, volume dropdown from live `GET /runpod/volumes`, pod-id field); `js/shell.js` `_bootApp` remote gate (skips local engine install gate, pushes `/remote/mode`, advisory via showError; auto-start skipped in remote mode until Phase 4 Pod start). Reused existing primitives only.
- [x] RunPod Settings UI polish (follow-up from user review 2026-06-11): (1) `MpiDropdown` option meta is truncated/invisible at the Settings panel width — fix so stock + VRAM are readable (CSS in `MpiDropdown.css` or Settings-scoped width/`wrapLabels`); (2) show price per hour per GPU in the picker — RunPod GraphQL `gpuTypes` exposes price fields (e.g. `securePrice`; verify exact field names live), pipe through `client.gpuTypes` → availability payload → dropdown meta. **Verify:** stock + VRAM + $/hr all visibly rendered per GPU option at default panel width; prices match the RunPod console for 2–3 spot-checked cards. — DONE 2026-06-11 inline-sequential + USER-VERIFIED. `securePrice` confirmed live (scalar USD/hr on Secure-Cloud `gpuTypes`; e.g. A40 $0.44, RTX 4090 $0.69, H100 SXM $3.29). Added `securePrice` to `client.gpuTypes` query (`routes/runpodRemote.js`); `availability` returns gpuTypes verbatim so no other backend change. MpiSettings GPU meta now `STOCK · NN GB · $X.XX/hr` (null-guarded). Truncation fixed Settings-scoped (option A): `MpiDropdown` now forwards `extraClasses` onto the portalled list (root-only modifier can't reach body-portalled options); both RunPod pickers pass `mpi-dropdown--runpod`; `MpiSettings.css` `.mpi-dropdown--runpod.mpi-dropdown__list` stacks meta under the label (single-column grid, drops the 11ch ellipsis cap) so full text shows.
- [ ] Add compatibility and manifest validation UI/backend. Ownership: `routes/remoteEngine.js`, `routes/platformEngine.js`, settings/status components. Briefings: comfy_engine, state/events. Consumes: `volume-manifest-schema.md`. **Verify:** incompatible GPU/template/volume profile produces a clear gated state with repair/reinitialize action, not a failed generation.

## Phase 3: Storage, Models, and Workflow Bundle Sync

**Execution ordering (user decision 2026-06-11): Pod-start-first.** Model download is structurally dependent on a *running* Pod — the wrapper lives inside the Pod and the volume mounts at `/workspace`, so there is nowhere to download to until a Pod is up. We therefore build a minimal Pod-start lifecycle slice (normally Phase 4) *before* the download wiring, and verify each step on real hardware. Test against the existing stopped Pod `cubric-vision-test` (`k8pr35yto3xy7n`, A4000, EUR-IS-1) + volume `cfwqthmdrg` (150GB, EUR-IS-1). **Live billing starts at Step 2** — every Pod-start action is announced to the user first.

> **GPU↔PyTorch arch binding (user constraint 2026-06-11):** ComfyUI/PyTorch built on the volume for one GPU arch family will NOT run generations on a different arch (e.g. a volume initialized for RTX 5090/Blackwell fails on RTX 4090/Ada or A4000/Ampere). This is the `gpu_profile.arch_family` exact-match **Reinitialize** gate in `volume-manifest-schema.md`. Practical rule for the test: start the Pod on the **same arch family the volume was built for** (the existing volume is Ampere — A4000). Switching arch families requires a volume reinitialize. Verified live 2026-06-11: RTX A4000 ($0.25, Ampere) is still available in EUR-IS-1, so the existing Pod can start on its native card with no reinit. If A4000 stock is gone at start time: delete `cubric-vision-test` and create a new Pod on a cheap **available, same-or-noted-arch** card, reinitializing the volume if the arch differs.

- [x] **Step 1 — Volume UX (app-only, no Pod, no billing).** — DONE 2026-06-11 + USER-VERIFIED (created a real volume + deleted it, confirmed in the RunPod web console). In-dropdown create ("+ Create volume in {DC}…" → MpiOkCancel size prompt, default 150 GB → `POST /runpod/volumes`); existing volumes badged "✓ Ready · size · DC"; Delete button (MpiOkCancel confirm with re-download warning → `DELETE /runpod/volumes/:id`). Bug fixes during verify: (a) DC-dropdown change now re-renders the volume slot so the create label tracks the DC picker (was stale); (b) UI surfaces RunPod's real error body via `_runpodErrText` (RunPod returns HTTP 500 with a message). Confirmed RunPod refuses to delete a volume attached to a Pod even when EXITED — `cubric-vision-vol` (cfwqthmdrg) is attached to `cubric-vision-test`, so it cannot be deleted until the Pod is deleted (RunPod constraint, not a bug). Reused MpiDropdown/MpiButton/MpiOkCancel/MpiInput; `mpi-dropdown--runpod` scope avoids meta truncation. KNOWN UX (backlog, user-deferred): opening any pop-up closes the Settings slide-over (it subscribes to `ui:close-all-popups`, which `OverlayManager.request` fires on every modal open). Unified volume dropdown in Settings: existing volumes shown as "✓ Ready" items (size + DC); when the selected DC has no volume, surface a **"Create volume in {DC}…"** affordance that prompts for size (default 50 GB) and calls the existing `POST /runpod/volumes`, then re-lists + auto-selects; volume delete action with a storage-billing + "all models must be re-downloaded" warning (`DELETE /runpod/volumes/:id`). Multi-volume supported but single-volume is the common case. **Verify:** create, list, and delete a real network volume entirely from Settings; the new volume appears badged, auto-adopts its DC, and filters the GPU picker.
- [x] **Step 2 — Minimal Pod start/stop (lifecycle slice, real hardware, FIRST BILLING).** — DONE 2026-06-12 + USER-VERIFIED end-to-end on live L4: advisory gate ("Got it" → background start only on dismiss) ✓, background start with NO fullscreen modal + UI usable ✓, "Remote engine starting…"/"Remote engine ready" toasts ✓, Retry/Dismiss failure dialog ("pod not found" against a garbage podId; correct title, not the bug-reporter) ✓, quit → Pod stops (confirmed EXITED via REST) ✓. KNOWN BUG (found live, fix pending below): changing podId in Settings while a Pod is running orphans the running Pod at quit — stop-active targets the CURRENT configured podId, not the one that was started; the 15-min wrapper idle watchdog is the backstop. Route the existing `comfy:ready` boot path to `POST /pods/{id}/start` on the configured stopped Pod (create-if-missing deferred to Phase 4 except the fallback above); poll RunPod Pod status, then poll wrapper `GET /health` until `ready:true` (240 s timeout, matches `COMFY_READY_TIMEOUT_S`); mark the remote engine ready. Clean-quit calls `POST /pods/{id}/stop` via the Express backend. **Verify (announce before running):** app boot with RunPod active → real A4000 Pod starts → wrapper healthy → app reaches a ready state; clean quit stops the Pod; idle watchdog backstop noted.
  - Step 2 STATUS (2026-06-11): backend + renderer + quit-stop implemented and the **backend path is LIVE-VERIFIED** — A4000 Pod was unavailable so the existing Pod was swapped to **L4** (new podId `e4s5bxubw4qfy2`, EUR-IS-1, cu124 image, volume cfwqthmdrg attached); wrapper `/health` returned `{ready:true, comfy_ready:true}` on L4, proving Design A (same image, different arch card, same volume). App-side cold-start path was tested by the user: **Pod started after ComfyUI start + Pod stopped on app close — both confirmed.** Then two boot-UX bugs were fixed (see below) and are NOT yet re-verified by the user. Pod is STOPPED (EXITED), only volume storage billing remains.
  - Step 2 BOOT-UX FIX (2026-06-11, user-reported, code done / UNVERIFIED): (1) the long remote Pod cold-boot (~60-90s) was blocking the UI behind the fullscreen "Starting ComfyUI" modal — now in remote **background** mode that modal is suppressed and the UI stays usable, with toasts ("Remote engine starting…" / "Remote engine ready", 6000ms) instead. (2) The Pod was starting before the user dismissed the "RunPod Active" advisory — now the advisory is an acknowledge dialog (MpiOkCancel, single "Got it") and the **background Pod start only fires on dismiss** (gated on auto-start ON + podId set). Files: `js/shell.js` (`_initRemoteBoot` rewritten, boot-tail auto-start reverted to local-only), `js/services/comfyController.js` (`ensureServerRunning(opts)` + `_ensureRemoteReady({background})` skips `comfy:starting`, routes errors to `ui:error` in background), `js/shell/statusBar.js` (`notify` gained a `duration` param). Lazy start on first generation (auto-start OFF) still uses the foreground modal — correct.
  - Step 2 BOOT-UX FIX #3 (2026-06-11, live-test finding, code done / UNVERIFIED): first live run partially verified fixes #1/#2 (advisory gate ✓, background start ✓, no modal ✓, starting toast ✓, UI usable ✓) but the L4 Pod failed to resume — RunPod: "There are not enough free GPUs on the host machine" (a STOPPED Pod is pinned to its host; the dropdown's "LOW" badge is datacenter-wide stock, NOT host availability). Two problems fixed: (a) DOUBLE-ERROR BUG — `ensureServerRunning` had the remote dispatch inside its try/catch, so a remote failure re-emitted `ui:error` with the wrong title "ComfyUI failed to start" (the bug-reporter dialog) on top of the background path's own error; remote dispatch moved OUTSIDE the try. (b) RETRY DEAD-END — background failure now shows an MpiOkCancel "Remote engine failed to start" Retry/Dismiss dialog (`js/shell.js` catch re-invokes `_startRemoteInBackground`) instead of the bug-reporter; `_ensureRemoteReady` background mode now only throws (no `ui:error`), foreground keeps modal-bound `comfy:error`.
- [x] **Step 2.1 — Orphaned-Pod fix (small, found live 2026-06-12).** — DONE + USER-VERIFIED 2026-06-12 (started Pod, changed podId to garbage in Settings, quit → real Pod EXITED, confirmed in RunPod console). `_startedPodId` recorded on successful start BEFORE the wrapper-ready wait (timeout still = billing = must stop); `stop-active` prefers it over `_mode.podId` and works even with remote toggled off; cleared on successful stop in both stop routes. `/remote/pod/stop-active` must stop the podId that was actually STARTED this session, not whatever `_mode.podId` says at quit time (user changed podId mid-session → quit stopped the wrong/garbage id → real Pod kept billing until the watchdog). Fix in `routes/remoteProxy.js`: record `_startedPodId` on successful `/remote/pod/start`; `stop-active` prefers it (fall back to `_mode.podId`); clear after stop. **Verify:** start Pod → change podId in Settings → quit → the originally started Pod stops.
- [x] **Step 3 — Wrapper `/models/install` + manifest writes (Python, `cubric-remote-wrapper`).** — DONE 2026-06-12, LIVE-VERIFIED end-to-end autonomously (user-authorized Pod ops + billing; user away). Wrapper gained `POST /wrapper/models/install` (httpx streamed download → `<dest>.part` → sha256-as-it-streams → atomic rename; SSE progress `models:install-progress|complete|error` ~1Hz; `watchdog.touch()` per chunk so long downloads don't idle-stop; size/sha verify when caller supplies them; `force` re-install flag), `POST /wrapper/models/install/cancel`, `GET /wrapper/models/install/active`, atomic manifest upsert (`_manifest_record_model`, tmp+rename; creates minimal manifest when absent — full first-boot init still pending), traversal-guarded `_model_dest` (type regex + bare basename), and `.part`-aware `partialBytes` in models/status. Local suite extended 24→38 checks, 38/38 PASS. Image **v0.2.0** built + pushed to GHCR (digest `sha256:7b8149f5...`; Dockerfile reordered so version-ARG bumps keep the ComfyUI clone layer cached; GHCR 502 from last session did not recur). LIVE: old Pod e4s5bxubw4qfy2 DELETED; new L4 Pod **ru3g2fsz5u34ua** (EUR-IS-1, v0.2.0, volume cfwqthmdrg, fresh CUBRIC_TOKEN sha8 4bc82fb9, **RUNPOD_API_KEY now in Pod env → watchdog_enabled:true for the first time** — previous Pods could never self-stop!); SDXL_Realistic 6.94GB installed in ~4.5 min (~28MB/s DC bandwidth), manifest entry + sha256 recorded, models/status installed:true, **manifest + model + status all survive Pod stop→start**, Pod stopped after (EXITED, no GPU billing). GOTCHA: RunPod proxy can 404 wrapper routes for a few seconds right after Pod restart even when /health is already green — app-side calls need retry tolerance (hit live, first retry succeeded). NOTE for Step 4: app dep `filename` embeds the subdir (`checkpoints/SDXL_Realistic.safetensors`) — the Express interceptor must split it into wrapper `type` + bare `filename`. Test driver: `cubric-remote-wrapper/live_test_install.py` (re-runnable, `--pod-id` to reuse a Pod).
- [x] **Step 4 — App↔wrapper download wiring.** — DONE + USER-VERIFIED live 2026-06-12 on a recreated A4000 Pod (`s7l9gu900grdjw`, EUR-IS-1, v0.2.0, volume cfwqthmdrg). In remote mode the backend intercepts `POST /comfy/models/check` → wrapper `POST /wrapper/models/status` and routes the download trigger → wrapper `POST /wrapper/models/install`, surfaced through the existing `/comfy/downloads/stream` `download:*` SSE (renderer untouched). NEW `routes/remoteModels.js` (auth+browser-UA wrapper fetch with post-restart proxy-404 retry; `splitDepFilename`; `remoteModelsCheck`; `remoteInstallDep`; `remoteCancelInstall`; `openInstallEventStream` → maps wrapper `models:install-*` onto `download:*`). EDITED `routes/comfy.js` (models/check remote branch), `routes/downloadManager.js` (`_startRemoteDownload` + `_onRemoteInstallEvent` + shared install SSE; remote-aware cancel; pause/resume no-op remotely; custom_nodes marked complete = image-resident per Design A). App-side wrapper-token store: NEW `secrets:set-wrapper-token`/`clear-wrapper-token` IPC in `main/secretsStore.js` + write-only `secretsClient.setWrapperToken/clearWrapperToken`. **Bugs found+fixed live:** (1) `remoteModelsCheck` forwarded deps verbatim — must split the subdir-embedded `filename` ('checkpoints/X.safetensors') into `{type,filename}` or the wrapper misses the file; (2) the app's display `size` ("67MB") was passed as `size_bytes` and the wrapper rejected the byte-exact-correct file on a `done != expected_size` mismatch — stopped sending it (sha256 is the integrity check); (3) model-level `download:failed` carried no `error` → UI showed "undefined" — now surfaces the failed dep's error. **Verify (PASS):** SDXL Realistic read installed-from-volume (checkpoint present), `4x-NMKD-Siax` installed via wrapper with the card flipping partial→INSTALLED; local mode unaffected.
  - **Step 4.1 — Connect/Disconnect button + status row (Phase 4 item pulled forward).** — DONE + USER-VERIFIED 2026-06-12. `MpiSettings.js` RunPod section gained a "Remote engine: stopped/starting/ready" status row + a Connect/Disconnect `MpiButton` driving the existing `/remote/pod/start|stop` + 5s `/remote/comfy/status` poll, with a host-full hint surfacing the host-pinning lesson. GOTCHA fixed live: MpiButton's imperative `setLabel`/`setDisabled` live on `instance.el`, NOT the mount instance (rule: callers use `el.setX`) — calling them on the instance threw `setDisabled is not a function`. **SUPERSEDED in scope by Step 4.2 below** (user decided start/stop-existing-Pod is the wrong lifecycle — see Plan Drift 2026-06-12).
- [ ] **Step 5 — Manifest compatibility gate + repair/reinitialize (Batch #4 fold-in).** Read `GET /wrapper/manifest` at readiness; run the repair/reinitialize decision matrix from `volume-manifest-schema.md` (arch-family/CUDA/image-digest → Reinitialize; ComfyUI/PyTorch/bundle → Repair; VRAM/DC/model-missing → Warn); gate generation on an incompatible profile with a clear modal, not a failed generation. **Verify:** an incompatible GPU/template/volume profile produces a clear gated state with a repair/reinitialize action.
- [ ] Volume *initialization* of a fresh volume (Cubric directory layout + first manifest, written Pod-side by the wrapper init script) and the workflow/custom-node **bundle versioning** for the remote template. **Verify:** a fresh volume receives the layout + manifest; the app refuses to run against a stale workflow/custom-node bundle and can trigger an approved repair path. *(Fresh-volume init is exercised when the volume-create path of Step 1 meets a first Pod start; full bundle-versioning may extend into Phase 4.)*

## Phase 4: End-to-End Remote Generation

- [x] **Step 4.2 — Create-on-Connect / Delete-on-Disconnect lifecycle (supersedes start/stop; user decision 2026-06-12).** — DONE + USER-VERIFIED 2026-06-12: app boot shows the RunPod advisory with NO Pod created (no billing); Connect is disabled until a GPU is picked; picking a GPU + Connect creates a fresh Pod on that card and reaches ready with no app restart; the manual "Pod ID (existing)" field is gone; Disconnect + quit DELETE the Pod (volume persists). Replace the start/stop-existing-Pod model (Step 4.1) with create-per-session.
  - Implementation: `routes/remoteProxy.js` NEW `POST /remote/pod/create` (generateWrapperToken → client.createPod v0.2.0 spec [volume@/workspace, env CUBRIC_TOKEN+RUNPOD_API_KEY+CUBRIC_WRAPPER_VERSION=0.2.0, ports ['8889/http'], containerDiskInGb 50] → setWrapperToken keyed to new podId → record `_startedPodId` + setRemoteMode BEFORE the 300s ready-wait → waitForWrapperReady → {ready, podId}; GPU-unavailable returns RunPod's error verbatim) + `POST /remote/pod/delete-active` (deletePod(_startedPodId) → clearWrapperToken → clear ids); `/remote/mode` relaxed to allow active with NO podId (boot gate only needs the flag; /proxy/* still 503s via the token guard until a Pod exists); legacy start/stop routes retained as inert fallback. `routes/remoteEngine.js` NEW `clearWrapperToken()` bridge + export. `MpiSettings.js` Connect=create on selected GPU (disabled until GPU picked) / Disconnect=delete-active, status creating…/ready/stopped, removed the Pod-ID field + slot, `_pushRemoteMode` no longer needs podId, GPU-pick refreshes Connect. `main.js` quit teardown stopActiveRemotePod→deleteActiveRemotePod. `js/shell.js` `_initRemoteBoot` pushes `{active:true}` (no podId), advisory-only, host-full retry dialog removed. `comfyController._ensureRemoteReady` no longer lazy-starts a Pod — proceeds if Connected, else errors "press Connect" (DECISION: Connect-only creation, not lazy-at-generation, to avoid a silent billing surprise + mid-generation GPU-pick).
  - **Verify (PASS):** Connect with no live Pod creates a fresh Pod on the picked GPU and reaches ready without an app restart; out-of-stock GPU returns a clear message; Disconnect + quit DELETE the Pod (no GPU billing); the volume + its models survive a delete→create cycle.
- [x] **Step 4.2.1 — Custom-nodes false-PARTIAL fix (found live during 4.2 verify).** — DONE + USER-VERIFIED 2026-06-12: SDXL Realistic flipped PARTIALLY INSTALLED → INSTALLED on refresh with no Install press. ROOT CAUSE: `/comfy/models/check` remote branch forwarded ALL deps (incl. `custom_nodes`) to the wrapper, which only sees the `/workspace` volume; custom_nodes ship in the Pod IMAGE (`/opt/comfyui/custom_nodes`, Design A) so the wrapper always read them missing → pack stuck PARTIAL forever (Install only ever filled the model files). NOTE: the wrapper's `_is_complete_on_disk` checks file-presence + non-zero size only — sha256 is NOT consulted for status, so a null `sha256` was NOT the cause. FIX in `routes/remoteModels.remoteModelsCheck`: image-resident deps (`type==='custom_nodes'` only) marked `installed:true` inline, never sent to the wrapper; per-model `installed` recomputed as the AND of volume-checked + image-resident deps. SCOPE GUARD: `installOnEngine` is NOT image-resident — several are real model WEIGHTS that live on the volume and ARE wrapper-installed (4x-NMKD-Siax, yolo/sam .pth, etc.); only custom_nodes CODE ships in the image. (Separately ran `mpic-compute-dep-hashes` to fill the null `sha256` fields — install-integrity hardening, unrelated to this bug.)
- [x] **Step 4.3 — Stop-not-delete lifecycle + auto-reconnect on boot (with delete-fallback) (user decision 2026-06-12, REVISES 4.2 teardown).** — DONE + USER-VERIFIED 2026-06-12 (all sub-steps 4.3.0–4.3.3 complete + Step 4.4 feedback shipped). Quit/Disconnect now STOP (EXITED) the Pod instead of deleting it — a STOPPED Pod bills NO GPU (only volume + reserved container disk) and a warm resume is faster than a cold create. The Pod is only DELETED when the user switches GPU, when a resume fails (host-pinned), or when the saved GPU is no longer available. RUNNING bills continuously, so we never leave it running across quit. Self-healing fallback handles the host-pinning wall hit all session.
  - **Lifecycle:** Quit / Disconnect → `client.stopPod(_startedPodId)` (EXITED; clear in-memory ready state but KEEP the podId persisted so boot can resume it). Reconnect on the SAME saved GPU → `client.startPod`; on success warm-resume (~30-60s) + waitForWrapperReady; on failure ("not enough free GPUs on the host machine" / any non-already-running error) → DELETE the stuck Pod + `client.createPod` fresh on the same GPU (cold ~90-120s). Switch GPU (saved gpuType changed) → delete old + create new. Saved GPU out of stock at reconnect (checked via `/runpod/gpu-availability` BEFORE start/create) → delete the stuck Pod + pop-up "Selected GPU <name> is unavailable. Open Settings to pick a different GPU." (create nothing, no billing).
  - **Boot auto-reconnect:** persist a `wasConnected` intent + the `podId` (set on successful Connect/create, cleared on explicit Disconnect). `js/shell.js _initRemoteBoot`: if `enabled && wasConnected && podId && gpuType` → run the reconnect path above (start-or-recreate) in the BACKGROUND with toasts, no Settings trip; if `enabled` but never connected → advisory-only (current 4.2 behavior). Token note: a warm `startPod` keeps the SAME podId so the stored wrapper token still matches (getWrapperToken keys on podId); a delete+create path MUST regenerate + re-store the token keyed to the new podId.
  - **Backend (`routes/remoteProxy.js`):** add `POST /remote/pod/stop-active` semantics back for STOP-on-teardown (or repurpose delete-active → stop-active); add a `POST /remote/pod/reconnect` (or extend `/remote/pod/create`) that does start-then-recreate-on-failure keyed to a saved podId+gpuType, with the availability pre-check and delete-fallback. `main.js` quit teardown → STOP (not delete). Keep delete reachable for the GPU-switch / unavailable paths.
  - **Verify:** Connect → quit → relaunch → app warm-resumes the SAME Pod fast with no Settings trip; force the host-full case (or saved GPU mid-stock) → app auto-deletes + recreates fresh on the same GPU and still reaches ready; set the saved GPU to an out-of-stock card → relaunch → "pick another GPU" pop-up, stuck Pod deleted, nothing new created/billed; switch GPU in Settings → old Pod deleted, new created; Disconnect → STOP (confirm EXITED, no GPU bill, Pod still listed); generation after a warm resume works.
- [x] **Step 4.3.0 — NOT A BUG (resolved 2026-06-12): "ready but no Pod" was a RunPod console BROWSER-CACHE stale view.** Live log proved the lifecycle worked: stale saved `podId=jqbt0glhl8jzcb` → `/remote/pod/reconnect` → `startPod` failed host-pinned → recreate-fallback `createPod` HTTP 201 `podId=3ukammu6gnsmxh` → wrapper `ready=true`. The console just hadn't refreshed; after a hard reload the user saw the active `…mxh` Pod (plus a stopped `…vos2` orphan). The diagnostic logging added to `/remote/pod/create`, `/remote/pod/reconnect`, `_createPodInternal`, and `MpiSettings._connectEngine` is what pinned it — KEEP it (cheap, podId is non-secret, invaluable for the next lifecycle issue).
- [x] **Step 4.3.3 — Orphaned stopped-Pod accumulation (found live 2026-06-12, real).** — DONE + USER-VERIFIED 2026-06-12. `client.listPods` added (`GET /pods`, same endpoint validate() probes; Pod status field = `desiredStatus`, EXITED = stopped). `_sweepOrphanPods(key, keepPodId)` in `routes/remoteProxy.js`: lists account Pods, keeps `name==='cubric-vision'` AND `desiredStatus==='EXITED'` AND id NOT in `{keepPodId, _startedPodId, _mode.podId}`, deletes the rest (best-effort, logged). Runs server-side inside `_createPodInternal` (every create/recreate, keeps the fresh Pod) + a fire-and-forget `POST /remote/pod/cleanup-orphans` called from `_connectEngine` after a ready Connect (covers warm-resume, which does not create). NOTE: the "recreate-fallback should target the saved podId" half was ALREADY satisfied — `/remote/pod/reconnect` sets `_startedPodId = savedPodId` (line 303) before `_deleteTrackedPod`, so the fallback already deletes the saved Pod being replaced. **Verify (PASS):** stray EXITED `cubric-vision` Pod reaped on next Connect; recreate-fallback leaves no extra EXITED Pod; volume deletable without a manual console Pod-delete.
- [x] **Step 4.3.2 — Disconnect choice: Delete Pod vs Keep Pod (user decision 2026-06-12).** — DONE + USER-VERIFIED 2026-06-12. Disconnect opens a 3-button popup (inline `MpiModal` + 3 `MpiButton` — MpiOkCancel is 2-button only): **Terminate** (primary, keep warm = `/remote/pod/stop-active`, EXITED, keeps podId, clears wasConnected), **Delete Pod** (danger = `/remote/pod/delete-active`, clears podId + wasConnected), **Cancel** (no-op; Enter = Terminate, backdrop/Escape = cancel). Body copy explains warm-storage-fee vs cold-start. Buttons `sm`, one row (`flex-wrap:nowrap`). NEW `_deletePodAndDisconnect` for the Delete path. **Verify (PASS):** popup shows 3 buttons in a row; Terminate → EXITED + warm-resumable; Delete → Pod gone + fresh on next Connect; Cancel closes untouched.
- [x] **Step 4.3.1 — Delete attached Pod before deleting a volume (user decision 2026-06-12).** — DONE (volume-delete itself not re-tested live this round — user deferred to a follow-up test, but the code path is in). `MpiSettings._confirmDeleteVolume`: when a tracked `podId` exists, the confirm dialog warns the Pod will be deleted first; on OK it calls `POST /remote/pod/delete-active`, clears `podId`/`wasConnected`, refreshes Connect, THEN `DELETE /runpod/volumes/:id`. `_runpodErrText` "delete the Pod first" kept as the fallback for a non-tracked attached Pod. **Verify (DEFERRED to follow-up test):** stopped Pod attached → Delete Volume deletes the Pod first then the volume; both gone in console; no "attached" error on the happy path.
- [x] **Step 4.4 — Persistent connection feedback in the status bars (user decision 2026-06-12).** — DONE + USER-VERIFIED 2026-06-12. App-wide feed `_initRemoteConnectionFeed()` in `js/shell.js`: polls `/remote/comfy/status` every 5s (no-op when RunPod disabled), broadcasts `remote:connection { connected, gpuName, vramGb, ramGb }` ONLY on a connected-flip; on connect it fetches `GET /remote/pod/specs?gpuTypeId=` for the badge details. (1) **Landing badge** (`js/shell/heroStats.js`, `#heroStatEngine` span added to `index.html`): "local · offline" + local GPU/VRAM/RAM ↔ "remote · online" + the Pod card mirrored in the SAME format ("RTX A4000 · 16GB VRAM · NNGB RAM", VRAM accented); local line cached + restored on disconnect. (2) **Gallery status bar** (`js/shell/statusBar.js`): `_setIdle` → "IDLE · Local" / "IDLE · Remote", tracks `remote:connection`. Specs source (user decision): RunPod gpuTypes catalog (`memoryInGb` = VRAM, same field the picker shows) + `getPod` (container RAM, defensive field read) — NO wrapper/Docker rebuild. **Verify (PASS):** Connect flips both surfaces to Remote + the Pod card with VRAM/RAM; Disconnect/quit reverts to Local; labels track without a manual refresh.
- [ ] Implement remote input-asset transfer for all non-image inputs: video/audio upload replacing local path injection (`_resolveMediaPath`), the trimmed-video flow (trim locally via existing `/api/video/trim-input`, then upload the trimmed file), and remote `.latent` staging replacing `/comfy/stage-preview-latent` (inventory item 5). **Verify:** an I2V workflow with a trimmed local video input and a two-stage preview-latent workflow both run remotely with correct inputs; re-running with unchanged inputs preserves ComfyUI execution-cache behavior where the static-filename convention allows.
- [x] Run image generation through the remote wrapper and preserve local project save semantics. **Verify:** generated image is saved to the active project with prompt/model/seed/settings metadata exactly like local mode; `save-generation` streams the output through an authenticated path (inventory item 6). — DONE + USER-VERIFIED 2026-06-12 on a live v0.2.2 A4000 Pod. First real remote SDXL generation ran end-to-end: prompt → `/proxy/prompt` → wrapper → ComfyUI (MpiNodes + checkpoint resolved) → `/proxy/view` → saved into the project. NOTE: the app-side HTTP/WS/upload/view plumbing was already wired (Batch #2); the blockers were all Pod-image — see Plan Drift 2026-06-12 (image v0.2.1→v0.2.2 fix chain). The first-Connect-on-a-new-image-tag 504 (image pull > 300s timeout; reconnect warm-resumes) + GPU-availability-refresh-on-dropdown-open are tracked as UX follow-ups below.
- [ ] Run video generation, including multi-stage preview/final where applicable, through the remote wrapper. **Verify:** video outputs, preview latents/assets, latent preview frames in the queue panel, progress, cancel/interrupt, and project sidecars + ffmpeg thumbnails behave consistently with local mode.
- [ ] Verify lifecycle cleanup on app quit and crash. **Verify:** the app stops or deletes the active Pod according to policy on clean quit; on a simulated crash (kill the app process), the Pod-side idle watchdog stops the Pod within its window; user warnings about remaining network-volume/storage costs are shown.

## Phase 5: Hardening and Release Readiness

- [ ] Add integration tests/mocked tests for RunPod lifecycle and remote wrapper error states. **Verify:** tests cover bad API key, unavailable GPU, stale manifest, stopped Pod, wrapper not ready, mid-generation network loss, interrupt, and app quit cleanup.
- [ ] Audit secret hygiene end-to-end: logger output, `logs/app.log`, bug-reporter payloads, and any persisted state must never contain the RunPod API key or wrapper token; add redaction where a code path could carry them. **Verify:** a grep/runtime sweep over logs and bug-report payloads after a full remote session finds zero secret material.
- [ ] Add user-facing documentation or settings copy for Secure Cloud remote engine costs and responsibilities. **Verify:** user sees that RunPod billing/API key/storage are theirs, that stopped Pods may still bill storage, and that Community Cloud is unsupported.
- [ ] Preserve final architecture decisions in project memory or docs after validation. **Verify:** `mpi-end-session` can close the card with durable notes and no design knowledge stranded only in handoff files.

## Plan Drift

- 2026-06-11: Pre-execution plan revision. Added verified local-coupling inventory; added Phase 1 decisions for transport topology, template ownership/image distribution; expanded wrapper contract to cover binary preview frames, synthesized model-init signal, and upload/staging endpoints; added remote model download to Phase 3; added input-asset transfer and crash-watchdog items to Phase 4; added secret-hygiene audit to Phase 5. No scope change to the approved direction.
- 2026-06-11 (Batch #2): View-URL building lives in BOTH `comfyController.js` (`_buildComfyViewUrl`/`_collectComfyOutputUrls`) and `commandExecutor.js:33–49` — research docs cited only the executor. Boundary seam implemented as `ComfyUIController.httpBase()` + `js/services/remoteEngineClient.js`; Express side is `routes/remoteProxy.js` (mounted BEFORE `routes/comfy.js` for the `/comfy/events/stream` intercept fall-through). `js/shell/memoryOps.js:27` VRAM unload deliberately stays hardcoded to the LOCAL engine.
- 2026-06-11 (Batch #3, user decision): Settings "ComfyUI API URL" field REMOVED along with `mpi_comfy_url`/`getComfyUrl`/`setComfyUrl` — it was dead config the app never read. The frozen 1.0.0 tutorial mentions it, so the removal MUST be called out in the next release notes: pending entry lives in `docs/releases/UNRELEASED.md` (NEW convention — fold into `/mpi-version-bump` and clear). Pointing the app at an external ComfyUI instance is explicitly unsupported.
- 2026-06-11 (DESIGN LOCK — PyTorch location, resolves a doc contradiction): **Design A confirmed (user decision).** PyTorch + ComfyUI live in the **Docker image** (`/opt/comfyui`, per `template-distribution.md:204`), NOT the volume. The network **volume holds models only** (arch-agnostic safetensors). Consequence: the volume has **zero GPU-arch binding** — one volume is portable across every card whose arch the booted image's CUDA build supports. To cover a new arch, **Cubric publishes another Docker image** (e.g. cu124 image → Ampere/Ada/Hopper; future cu128 image → Blackwell/RTX 5090/B200). The user **never** reinitializes or recreates the volume to switch cards (this removes the recurring "delete volume + reinstall per card" pain). **This SUPERSEDES the `gpu_profile.arch_family` exact-match *Reinitialize* gate in `volume-manifest-schema.md`** — that gate assumed PyTorch-on-volume (Design B) and is now wrong. The real compatibility axis is **card-arch vs available-image CUDA build** ("is there an image for this card's arch?"), checked at GPU-pick / Pod-create, NOT a volume reinit. `volume-manifest-schema.md` needs an update to reflect this (the arch_family field becomes informational; image/CUDA selection moves to image choice). The compat **warning UI** (disable cards no current image supports, e.g. Blackwell while only a cu124 image exists) is deferred — for now we simply pick a card the existing cu124 image runs (Ada/Ampere/Hopper). Multi-image build (cu128 etc.) is a follow-up in mpi-ci.
- 2026-06-11 (Phase 3 ordering, user decision): Phase 3 reordered to **Pod-start-first**. Model download depends on a running Pod (wrapper lives in-Pod, volume mounts at `/workspace`), so the minimal Pod-start lifecycle slice (a Phase-4 concern) is pulled forward as Step 2, before download wiring. New Step 1 = app-only Volume UX (create/list/delete via existing `/runpod/volumes` routes, no Pod, no billing). Volume UX per user design: existing volumes badged "✓ Ready" in the dropdown; a DC with no volume offers a "Create volume in {DC}…" affordance (size prompt → create → auto-select); single-volume is the common case but multi stays supported. **GPU↔PyTorch arch binding** added as an explicit constraint + the manifest `arch_family` reinit gate is the enforcement: a volume built for one arch family won't generate on another. Live check 2026-06-11: existing Pod `cubric-vision-test` (k8pr35yto3xy7n) is EXITED (not deleted); volume cfwqthmdrg (150GB, EUR-IS-1) alive; RTX A4000 ($0.25, Ampere) still available in EUR-IS-1 so the existing Pod can start on its native card with no volume reinit. Fallback if A4000 stock gone: delete the test Pod, create on a cheap available card, reinit volume if arch differs.
- 2026-06-11 (UI polish): Shared primitive `MpiDropdown` now forwards `extraClasses` onto the **portalled list** (not just the root) — the list lives in `document.body`, so a root-only modifier (`.mpi-dropdown--runpod`) cannot reach its options. Additive; no existing usage styled list-options via `extraClasses` (the `--model-select`/`--compact` modifiers only target the trigger). Settings-scoped CSS stacks RunPod-picker meta under the label to fit `STOCK · NN GB · $X.XX/hr`. Price field verified live: `securePrice` (scalar USD/hr) on `gpuTypes` — `lowestPrice` is an object (subfield selection), `securePrice`/`communityPrice`/`oneMonthPrice` are scalars.
- 2026-06-11 (Batch #3.1, verified vs live console screenshots): RunPod GraphQL `gpuAvailability.available:true` does NOT match the console's availability column — the console badge is `stockStatus`, where `null` renders as "N/A" yet such cards may still deploy. UI now mirrors console labels (High/Medium/Low/N/A), stock leads the dropdown meta (it was truncated to invisibility — the original user-facing bug), N/A sorts last, cards stay selectable. Added `GET /runpod/volumes` + volume-aware DC picker (auto-adopt volume's DC, mismatch warning).
- 2026-06-12 (Step 4.2 + 4.2.1 DONE; Step 4.3 LIFECYCLE RE-REVISION, user decision): Step 4.2 (create-on-Connect / delete-on-Disconnect) shipped + user-verified. Step 4.2.1 fixed a false-PARTIAL (custom_nodes are image-resident, must not be sent to the wrapper status check) + dep-hashes filled the 9 null `sha256` fields. THEN the user revised the teardown again (Step 4.3): **STOP, not delete, on quit/Disconnect** — a STOPPED Pod bills no GPU and warm-resumes faster than a cold create; DELETE only on GPU-switch, on a failed resume (host-pinned), or when the saved GPU is unavailable. This brings back the "leave the Pod (stopped) between sessions" idea that Step 4.2 had removed, but with a **self-healing delete+recreate fallback** so the host-pinning wall ("not enough free GPUs on the host machine") that originally killed start/stop no longer dead-ends — a failed resume auto-deletes and recreates fresh on the same GPU. Boot auto-reconnects (start-or-recreate) when `wasConnected` + `podId` + `gpuType` are persisted, no Settings trip. Cost note corrected for the record: RUNNING bills GPU per-second (never left running across quit); STOPPED bills only volume + reserved container disk; DELETED bills only volume. Warm `startPod` keeps the same podId (token still matches); delete+create MUST regenerate the wrapper token keyed to the new podId. So `main.js` quit teardown + Disconnect change from delete-active → stop-active; a new reconnect path owns the start→(recreate-on-fail) logic + the availability pre-check + GPU-switch delete. NOTHING committed.
- 2026-06-12 (Step 4 done + LIFECYCLE PIVOT, user decision): Step 4 download wiring DONE + user-verified (see ticked item + 3 live-found bug fixes). Step 4.1 Connect/Disconnect button DONE but its **start/stop-existing-Pod lifecycle is being replaced** by **create-on-Connect / delete-on-Disconnect** (Step 4.2, the new NEXT item). Root cause: a STOPPED Pod is host-pinned and repeatedly failed to resume ("not enough free GPUs on the host machine") whenever its host's GPU was taken while idle — hit live multiple times this session (L4 then A4000). Creating fresh on Connect schedules against current capacity (no pinning); deleting on Disconnect/quit frees the card + kills the orphaned-billing/un-resumable classes. Volume persists separately (Design A → any supported card re-attaches, no reinit). User decisions: block Connect until a GPU is picked; HIDE the manual "Pod ID (existing)" field (created podId is app-managed); fresh wrapper token generated+stored server-side per create (removes the manual DevTools `setWrapperToken` step). Standing test infra: gitignored `cubric-remote-wrapper/.secrets/runpod.env` (CUBRIC_TOKEN sha8 4bc82fb9 — 64 hex; volume cfwqthmdrg 150GB EUR-IS-1; template ejrwtsej15); `cubric-remote-wrapper/recreate_pod.py` (NEW this session — queries availability, picks cheapest supported in-stock EUR-IS-1 card, delete-old+create-new, waits ready, leaves running). Last Pod `s7l9gu900grdjw` (A4000) is now **EXITED** (no GPU billing). NOTHING committed.

- 2026-06-12 (Step 4.3.1/4.3.2/4.3.3 DONE + volume-UX rework + lifecycle-feedback fixes, user-verified): Shipped the three Pod-cleanup steps (orphan sweep + delete-Pod-before-volume + Disconnect choice). Then a batch of user-driven UX fixes during live verify: (a) **Volume is now required + DC-derived** — a volumeless Pod has no persisted ComfyUI/models, so Connect is gated on BOTH a GPU and a volume; the volume dropdown was REPLACED by a badge ("✓ name · size · Ready") or "None" derived from the selected DC (one volume per DC), with a small Create↔Delete button that swaps on existence; switching DC re-derives the volume and clears gpuType; the cross-DC mismatch warning was removed (a mismatch is no longer reachable). `_volumeForDc(dcId)` helper added. (b) **Template reorder** — Network Volume now sits ABOVE GPU (DC→Volume→GPU→Connect): volume couples with DC, GPU couples with the Connect button below. (c) **"RunPod Active" boot advisory popup REMOVED** — boot no-ops when there is nothing to auto-reconnect; persistent status feedback (Step 4.4) will cover "remote enabled". (d) **Create-volume popup** now shows live cost math (`{size} GB → $X/mo · $Y/day` at $0.07/GB/mo, /30 daily) updating as the size field changes (scoped `white-space:pre-line`, shared MpiOkCancel untouched). (e) **GPU dropdown opens upward** (`direction:'up'`) — the downward list was clipped at the viewport bottom. (f) **Connect/Disconnect toasts re-added** via `Events.emit('ui:success|info|warning')` (StatusBar listens): "Creating a Pod…/Connecting…", "Remote engine ready" (the misleading "Pod created/recreated" affirmation was DROPPED — a warm resume is not a create), "Pod terminated (kept warm)", "Pod deleted", + failure warnings. (g) **Double-create bug fixed** — the renderer's `_engineBusy` is per-mount and reset on a Settings panel close/reopen, so a create in flight could show "stopped" + Connect enabled → a second Connect would fire a duplicate create; added a BACKEND `_connecting` flag (set in `/remote/pod/create` + `/remote/pod/reconnect` via try/finally, exposed on `/remote/comfy/status`) that survives a remount, and `_applyEngineStatus` now shows "connecting…" + disables Connect whenever the backend reports connecting. NOTHING committed.

- 2026-06-12 (Phase 4 image gen — ROOT CAUSE + DESIGN CHANGE B+, code done / image rebuild + live test pending): First remote image generation failed `503 engine_error` → ComfyUI `400 missing_node_type: Node 'Width' (class_type MpiInt) not found`. ROOT CAUSE: the v0.2.0 Pod image installs ZERO custom nodes — bare ComfyUI clone — so `ComfyUI-MpiNodes` (MpiInt/MpiText title-injection nodes) and all other packs were absent. Design A's "custom_nodes ship in the image" was never implemented in the Dockerfile. **DESIGN CHANGE (user, supersedes Design A custom_nodes decision + Step 4.2.1 image-resident assumption): split custom_nodes by lifecycle.** UNIVERSAL engine nodes (`installOnEngine: true` custom_nodes — 7 packs: MpiNodes, VideoHelperSuite, Impact-Pack, KJNodes, UltimateSDUpscale, Frame-Interpolation, Impact-Subpack) bake into the IMAGE (stable baseline, rare change). PER-MODEL nodes (PainterI2V + every future model's nodes) install onto the VOLUME at model-install time via the wrapper — so adding a new model NEVER forces an image rebuild (images only rebuild for ComfyUI/PyTorch/CUDA changes + the GPU-arch variants). ComfyUI loads BOTH via `--extra-model-paths-config` pointing at a volume `extra_model_paths.yaml` (`custom_nodes:` key — confirmed supported: ComfyUI issue #10646 + Magnopus). Mechanism: per-model node install reports `needs_comfy_restart` (ComfyUI scans custom_nodes only at startup) → app warm-cycles the Pod. Code shipped (NOT committed, image NOT rebuilt): (1) `mpi-ci/cubric-vision-pod/Dockerfile` clones the 7 universal packs into `/opt/ComfyUI/custom_nodes` + runs their requirements (impact/kj/subpack pip -r; frame-interp `python install.py`); (2) `start.sh` mkdir `/workspace/comfyui/custom_nodes` + writes `/workspace/cubric/extra_model_paths.yaml` + passes `--extra-model-paths-config`; (3) `cubric-remote-wrapper/wrapper.py` (+ synced to `mpi-ci/.../wrapper/wrapper.py`) NEW `_run_node_install` (zip→extract→rename `<repo>-<branch>`→folder→requirements) + `_node_dest` + custom_nodes branch in `models/status`+`models/install` + `needs_comfy_restart`; (4) `routes/remoteModels.js` `_isImageResident` now = `installOnEngine` custom_nodes ONLY (block-scoped regex over dependencies.js — verified extracts exactly the 7), per-model nodes route to wrapper with `type:'custom_nodes'`+bare folder+`install_command`; (5) `routes/downloadManager.js` drops the blanket custom_nodes=complete short-circuit (trusts `installed`), surfaces `comfy:needs-restart` on remote node complete; (6) `MpiSettings._connectEngine` fires `POST /comfy/stop` on a ready remote Connect (free local VRAM); (7) `routes/remoteProxy.js` POD_IMAGE/WRAPPER_VERSION → v0.2.1. **BUILD-TIME RISK (untested):** Frame-Interpolation `install.py` + impact-pack/subpack requirements run in the GPU-less buildx runner — may fail or pull heavy torch deps; watch the build log. **NEXT (user runs):** trigger mpi-ci `Cubric Vision Pod image` workflow_dispatch (manifest_version=0.2.1, wrapper_version=0.2.1, comfyui_ref=master) → make GHCR package public → recreate Pod (delete old, Connect fresh on A4000+volume) → retry SDXL generation. Also pending: GET `/proxy/queue` → wrapper 405 (no GET queue route; queue-snapshot broken remotely, non-blocking).

- 2026-06-12 (Image v0.2.1→v0.2.2 fix chain + first remote generation VERIFIED): The first remote generation surfaced a 3-stage Pod-image gap (app-side plumbing was already correct). (1) v0.2.0 image had ZERO custom nodes → `MpiInt` missing → fixed in v0.2.1 (bake the 7 universal `installOnEngine` nodes into the image). (2) v0.2.1 `start.sh` wired ComfyUI to the volume `custom_nodes` but NOT the volume MODEL dirs → a checkpoint the wrapper reported `installed:true` on `/workspace/mpi_models/checkpoints` was invisible to ComfyUI's loader (`ckpt_name 'SDXL_Realistic.safetensors' not in []`) → fixed in v0.2.2 (extra_model_paths.yaml now maps every model type checkpoints/loras/vae/... → `mpi_models/<type>`, mirroring the wrapper's MODEL_SUBDIRS). (3) The running app process was STALE — it had `POD_IMAGE=v0.2.1` in memory from before the edit, so every Connect kept creating v0.2.1 Pods even after v0.2.2 was on GHCR; an app restart fixed it (lesson: a POD_IMAGE bump needs an app restart, and the RunPod **Container** log `create container ...:vX` is the ground-truth image-version check). All three landed; v0.2.2 A4000 Pod generated + saved an SDXL image end-to-end, user-verified. mpi-ci committed/pushed (cubric-vision-pod build context + workflow + disk-free step + package linked to mpi-ci with Write); wrapper synced into mpi-ci build context. UX FOLLOW-UPS (not yet done): (a) first-Connect-on-a-fresh-image-tag returns 504 because `/remote/pod/create` long-polls `waitForWrapperReady` (300s) while RunPod cold-pulls the ~3.3GB image > timeout — reconnect warm-resumes every time; fix = stop long-polling create (return after createPod, renderer polls `/remote/comfy/status`) + first-time-image-pull messaging ("downloading engine ~3GB, one time") distinct from the ~90-120s normal cold-create + raise backstop to ~600s; (b) GPU dropdown should RE-FETCH availability on open (stale "LOW" hint → user keeps trying out-of-stock cards); (c) `/proxy/queue` GET fixed in v0.2.2 (wrapper GET /wrapper/queue) but unverified live. NOTHING in Cubric-Vision committed (POD_IMAGE→v0.2.2 + remoteModels/downloadManager/MpiSettings/start.sh changes all uncommitted).

## Verification

Final verification requires a real Secure Cloud RunPod account test with a Cubric-owned template and network volume:

- Create or select a network volume in a Secure Cloud data center.
- Create/start a Pod from the Cubric template using the user's RunPod API key.
- Confirm wrapper token auth, readiness, and manifest compatibility.
- Install at least one model onto the volume from the app and confirm it persists across Pod stop/start.
- Run at least one image workflow and one video workflow (including an I2V or multi-stage case with uploaded inputs) from Cubric through the remote engine.
- Confirm outputs save into the local project with existing metadata/sidecar behavior.
- Stop/delete the Pod through the app and confirm no active GPU billing remains except intentional persistent storage.
- Kill the app while a Pod is running and confirm the idle watchdog stops it.

## Preservation Notes

- Do not implement support for arbitrary remote ComfyUI URLs in v1.
- Do not expose raw ComfyUI publicly as the normal user path.
- Do not support RunPod Community Cloud for this feature.
- Keep the user's RunPod API key and wrapper token out of project JSON, localStorage, logs, and bug reports.
- The wrapper token must gate the WebSocket upgrade, not just HTTP routes.
- If the RunPod API or template docs change during implementation, re-check primary RunPod docs before coding lifecycle calls — especially whether private templates are deployable cross-account (this gates the whole distribution design).
- OneTrainer is a reference for lifecycle/SSH edge cases (notably stale runtime payloads after resume), not a direct architecture match for Cubric generation.
- Phase 1 `research/*.md` decision docs are the briefing source for the Parallel Batch; do not dispatch workers before they exist.
