{"schema": "mpi-kanban/event/v1", "id": "MPI-481", "type": "task.created", "at": "2026-08-08T05:45:00Z", "actor": "opus", "summary": "Created from a live catch during the MPI-467 smoke resume. The app held 13 frozen download jobs from the previous session's dead Pod; the next install POST would have attached to them and fired no wrapper install at all. Sibling of MPI-100, which fixed the identical staleness for 'complete' and left 'downloading' alone."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-481", "type": "task.moved", "at": "2026-08-08T05:52:00Z", "actor": "opus", "from": "todo", "to": "doing", "summary": "Picked up. Root cause confirmed by reading the wrapper: the ATTACH guard's two in-flight arms (_remoteDepIds and depJob.status=='downloading') are module-level cache scoped to a Pod instance that nothing invalidates when that Pod dies, is deleted, or warm-cycles. Fixing it with statusResults as the brief suggested would REGRESS MPI-97 (installed:false is also what a genuinely live download reports, so every shared-dep attach would fire a duplicate install and hit the wrapper's 409 install_in_progress). The real fresh truth is GET /wrapper/models/install/active, which the wrapper has carried since image v0.2.1 and the app has never called."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-481", "type": "validation.updated", "at": "2026-08-08T06:05:00Z", "actor": "opus", "summary": "Fix shipped + unit-proven; card held in doing/validating because nothing has touched a Pod. The guard now cross-checks BOTH in-flight arms against the wrapper's own install registry (GET /wrapper/models/install/active) instead of its module-level cache. Brief corrected on three points: fix (1) as written would have regressed MPI-97 (installed:false is also what a LIVE download reports, so it cannot separate a corpse from a real attach), fix (2) folds into fix (1) and must not be scoped to 'no Pod active' (a replacement Pod and a warm-stop resume are both stale WHILE isRemoteActive() is true), and there is no third instance - the local siblings cannot go stale because a local download dies with its own process. 4 new tests, mutation-checked; 503/503 suite green. Owed: one live interrupted Pod install, which the MPI-467 smoke run can carry."}
