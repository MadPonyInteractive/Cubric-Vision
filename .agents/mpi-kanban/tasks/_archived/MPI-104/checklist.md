# MPI-104 Checklist

- [ ] MpiSettings: add "Open in RunPod console" link in the RunPod section.
- [ ] Deep-link to the active Pod when connected (`?id=<podId>`); static list otherwise.
- [ ] Open in external browser (shell.openExternal), not in-app.
- [ ] Style per existing Settings link/hint pattern (BEM, CSS vars, icons.js — no raw SVG).
- [ ] Verify: connected → opens the Pod page; disconnected → opens the pods list.
