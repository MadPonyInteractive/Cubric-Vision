# MPI-619 Checklist

- [x] rename `klein-4b` to `FLUX.2 Klein 4B`
- [x] rename `klein-9b` to `FLUX.2 Klein 9B`
- [x] confirm `model.name` is display-only, not a lookup key (repo-wide sweep)
- [x] confirm the tier letters self-remove via the existing clash gates
- [x] confirm Boogu and LTX keep their letters — they are real tier variants
- [x] re-point the MPI-567 clash test at a discovered clash group, not a hardcoded one
- [x] `npm test` — 729/729
- [x] `npx eslint js/ --max-warnings=0` — clean
- [x] desktop suite green — 26/26
- [ ] Fabio: does this want a release-notes line at the next version stamp? (open question,
      not a blocker — the rename itself is verified)
