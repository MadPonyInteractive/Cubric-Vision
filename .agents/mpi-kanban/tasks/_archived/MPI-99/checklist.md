# MPI-99 Checklist

- [ ] Confirm failing link (binding vs .show() no-op) via DevTools probe
- [ ] Fix dialog .show() after install-driven re-renders / re-arm Overlays
- [ ] Verify: install then immediately uninstall → dialog opens every time
