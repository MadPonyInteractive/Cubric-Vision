{
  "schema": "mpi-kanban/task-card/v1",
  "id": "MPI-191",
  "title": "Pin the 40GB LTX transformer resident across the stage-1 -> stage-2 transition (kill the per-stage re-fault)",
  "description": "The single biggest remaining LTX perf lever, root-caused but never shipped. aimdo EVICTS the 40GB LTXAV transformer between ComfyUI subgraphs, so stage-2 (continue/upscale) re-faults the ENTIRE 40GB from scratch even though stage-1 had it resident seconds earlier. ~60-93s of every multi-stage gen is reloading an already-loaded model; the real stage-2 sampler is ~5s.\n\n=== LIVE EVIDENCE (2026-07-04, cu130 Pod, RTX 4090 24GB, bf16 after MPI-190 revert) ===\n- t2v: 93s between stage-1 and stage-2 (pure re-fault).\n- i2v warm full: 1m54s; ~60s of it is the stage transition, ~54s real work.\n- preview (stage-1): 30s first-latent fault + ~20s remaining 7 steps = ~1m02s.\n- preview -> continue (stage-2): ~60s to START inference, then inference ~5s. So ~92% of the continue is re-fault, ~8% is compute.\nThese are POST-cu130 numbers: cu130 already cut the per-stage re-fault from ~34-58s to ~12-26s baseline, but at these resolutions/tiers it's still 60-93s. bf16 and GGUF pay this IDENTICALLY (it's aimdo eviction, not the transformer format) so MPI-190's revert neither caused nor fixed it.\n\n=== THE FIX (from docs/builder/research/pod-perf-investigation.md, Fix #2 / line ~93-96, 374 — untested lever) ===\nMake aimdo/DynamicVRAM keep the 40GB LTXAV transformer resident across the stage-1 -> stage-2 boundary instead of evicting between subgraphs. On a card where the 40GB fits with room (32GB+), or where dynamic-VRAM can pin it, the second fault never fires -> continue drops from ~60-93s to ~5-11s.\nOPEN QUESTIONS to investigate (do NOT single-symptom patch — cf research-first-on-version-breaks):\n1. WHY does aimdo evict between ComfyUI subgraphs when the model was just resident? Is it a comfy-aimdo cache policy, a --cache-* flag, or a DynamicVRAM setting?\n2. Node 58 (MpiClearVram) is NOT the cause (removing it made stage-2 WORSE: 69 s/it vs 34 s/it — it de-fragments so the re-fault lands clean; see pod-perf doc). So the fix is NOT 'remove node 58'.\n3. Is there an aimdo 'keep-resident' / 'no-evict-between-prompts' knob? (--cache-none is the OPPOSITE — do not use.)\n4. Does the two-prompt (preview then continue) structure force the eviction because they're SEPARATE ComfyUI prompts? If so, would a single-prompt gate (Input_Is_Continue already baked in) that never leaves the graph avoid the re-fault?\n\n=== PRODUCT CONTEXT ===\nThe multi-stage preview->continue split is a deliberate FEATURE (iterate-cheap-commit-once, user's 2026-06-28 call) — the ~30s preview is fine. This card targets ONLY the wasteful 60-93s re-fault on the CONTINUE, not the staged structure itself.\n\n=== SCOPE ===\nLikely lives in: the ComfyUI workflow structure (comfy_workflows/scripts/workflow_generation/generate_ltx.py + the template's stage gating), aimdo config/flags on the Pod (mpi-ci/cubric-vision-pod/start-*.sh or the wrapper), or DynamicVRAM settings. All live Pod ops are USER-only. Read the pod-perf-investigation.md doc FIRST (RESOLVED banner + Fix #2 section) before touching anything.\n\nBRANCH: RunPod (+ possibly mpi-ci for aimdo config). Cross-ref: MPI-190 (bf16 revert that surfaced these clean numbers), MPI-187 (cu130 that cut the baseline), docs/builder/research/pod-perf-investigation.md (root cause + the untested Fix #2), MPI-157 (node 58 / MpiClearVram — keep it).",
  "column": "todo",
  "maturity": "planned",
  "status": "open",
  "created_at": "2026-07-04T21:45:00Z",
  "updated_at": "2026-07-04T21:45:00Z",
  "links": {
    "events": "events.jsonl"
  },
  "tags": [
    "runpod",
    "ltx",
    "perf",
    "aimdo",
    "stage-transition",
    "re-fault",
    "cu130-followup"
  ],
  "priority": "high"
}
