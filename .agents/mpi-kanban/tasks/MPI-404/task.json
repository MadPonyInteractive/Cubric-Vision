{
  "schema": "mpi-kanban/task-card/v1",
  "id": "MPI-404",
  "title": "First-run state lies - model count reads 0 / 18 with no engine, Stage-all-models live with no API key",
  "description": "Found live 2026-07-30 by the user on the real 1.3.0 portable extract, first run, MPI-390 RunPod escape hatch taken. At first run he pointed the models folder at G:\\CubricModels, which holds real weights across diffusion_models/, checkpoints/, loras/, text_encoders/, vae/ and more. The home hero still reads MODELS 0 / 18. His objection is the right one: the app knows where his models are because it just asked him, so an on-disk count should not require an engine.\n\nROOT, and it is structural rather than a bad render. The models root has TWO sources of truth and the install check can only see one of them. syncModelInstalled() is subscribed to the engine:ready event at js/shell.js:1465, and that subscription carries its own explanation - 'check models only after engine is set up - this ensures extra_model_paths.yaml exists before we try to read it'. On the server side getCustomRoot() at routes/shared.js:423 reads base_path out of extra_model_paths.yaml, and that yaml is resolved with getComfyPath(ENGINE_ROOT, ...) - it lives INSIDE the engine. routes/engine.js:519 is what writes it, during the post-install step of a local engine provision.\n\nSo when MPI-390's 'Skip the local engine install' is on: no engine is provisioned, no extra_model_paths.yaml is written, engine:ready never fires, syncModelInstalled never runs, and /comfy/models/check would have had no root to stat even if it had. The count is not wrong - it is the untouched initial 0 from heroStats.js:47 _renderModels(installedCount = 0). The folder the user chose at first run is stored and then never consulted by the install check.\n\nDO NOT FIX THIS ON THE COUNTER. Patching heroStats or firing syncModelInstalled unconditionally would just move the failure - the server resolver still has no root without the yaml. The real question is whether the first-run models-root choice should be persisted independently of the engine (app-level setting) with extra_model_paths.yaml written FROM it rather than being its only home. That also decides what the Model Library shows on a cloud-only machine before any Pod is connected.\n\nSCOPE NOTE: a cloud-only user is the exact population MPI-390's escape hatch was built for, so this is that user's FIRST impression of the app - it says zero models on a disk that has models. Also check the remote twin per the engine-split rule: on a connected Pod the check runs against the volume, so the local-disk count may be wrong there too.\n\nMERGED 2026-08-01: this card absorbs MPI-405 ('Stage all models on connect' is live in Settings with no RunPod API key saved).\n- MPI-405 - the ACCOUNT panel states the controls are locked until a key is saved, and the toggle below it is fully interactive with the key box empty.\nWHY ONE CARD: Both were found by the user in the same first-run pass on the real 1.3.0 portable extract, on the same path (MPI-390 RunPod escape hatch taken, no local engine), and both are the same defect class: a control or a count that asserts state the app has not got. They are read one after the other by a user in that state, so they are fixed and re-checked together.\nThe absorbed card(s) are closed as done/rejected - not built, kept as the record - and their full original text stays on disk at tasks/<id>/.",
  "column": "done",
  "maturity": "complete",
  "status": "done",
  "created_at": "2026-07-30T20:04:00Z",
  "updated_at": "2026-08-08T04:10:00Z",
  "labels": [
    "bug",
    "model-library",
    "engine",
    "first-run",
    "runpod"
  ],
  "acceptance": [
    "DECIDED 2026-08-05: the models root stays ENGINE-OWNED (see brief.md). The fix is that the hero must not claim a count it cannot have - not an app-level models root",
    "With 'Skip the local engine install' ON, the home hero does NOT read MODELS 0 / 18 - it shows an honest unknown / not-installed state instead of the untouched initial zero",
    "The Model Library is coherent on a cloud-only machine before any Pod connect - either it lists what is on disk or it states plainly why it cannot",
    "extra_model_paths.yaml is still written correctly when a local engine IS later provisioned, from the stored root rather than instead of it",
    "Both engine paths checked - local and remote-connected - per the engine-split sweep rule"
  ],
  "links": {
    "brief": "brief.md",
    "events": "events.jsonl",
    "checklist": "checklist.md",
    "validation": "validation.md"
  }
}
