{"schema": "mpi-kanban/event/v1", "id": "MPI-404", "type": "task.created", "at": "2026-07-30T20:04:00Z", "actor": "opus", "summary": "Found live by the user on the 1.3.0 portable extract during MPI-391 A2: models folder pointed at the G: CubricModels store which holds real weights, home hero still reads MODELS 0 / 18. Root traced to a gating chain rather than a render fault - syncModelInstalled is subscribed to engine:ready (js/shell.js:1465, comment says it waits for extra_model_paths.yaml), and getCustomRoot reads that yaml from inside ENGINE_ROOT (routes/shared.js:423), written only by a local engine provision (routes/engine.js:519). With MPI-390's skip-install on there is no engine, so the event never fires and the resolver has no root; 0 is the untouched initial from heroStats.js:47. Real question is whether the first-run models-root choice should outlive the engine as an app-level setting."}
