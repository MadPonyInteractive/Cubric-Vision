{"schema":"mpi-kanban/event/v1","type":"task.created","id":"MPI-457","at":"2026-08-06T06:15:00Z","actor":"opus","note":"Created from MPI-449 at the users request while bumping the app engine 0.29.2 -> 0.30.0 by hand. Two halves. USER-FACING: /engine/upgrade fs.removes the whole ~11 GB portable tree and re-downloads the ~1 GB 7z plus every node and python dep, for a delta that was ONE git checkout and THREE pip packages. The portable ships ComfyUI as a git checkout (proved: HEAD was 32212244 = the pinned v0.29.2 sha, remote Comfy-Org/ComfyUI) and bundles update/update.py, so in-place is supported upstream - ours must checkout the PINNED sha, not pull master. Reserve the wipe for a DETECTED signal: a node removed from node_lock.json or failing to import, a portable-level python/torch move, or a failed health check. The wipe also silently destroys the dev symlink custom_nodes/ComfyUI-MpiNodes -> c:/AI/Mpi/ComfyUi-MpiNodes; the in-place path left it intact. AGENT-FACING: no skill covers the APP engine bump - /mpi-bump-local-comfy is the G:/ComfyUi bench only and docs/versioning.md SS COMFY_VERSION is two lines. Encode the sequence proved today, including the two non-obvious steps: check a PORTABLE RELEASE ASSET exists (v0.30.1 and v0.30.2 were real tags with no Comfy-Org build, so 0.30.0 was the ceiling), and run an EMPIRICAL floor check - all 167 class_types used by comfy_workflows/*.json registered on 0.30.0, 0 missing, which is what replaced guessing at custom-node compatibility."}
{"schema":"mpi-kanban/event/v1","id":"MPI-457","type":"task.updated","at":"2026-08-07T01:47:30Z","actor":"opus","summary":"Scope narrowed to the mechanic (in-place upgrade + the bump skill). The playbook file and the smoke gate move to MPI-467 so two cold agents cannot write docs/playbooks/bump-engine/ at once. Dispatched under the new MPI-468 umbrella."}
{"schema":"mpi-kanban/event/v1","id":"MPI-457","type":"task.updated","at":"2026-08-07T03:47:36Z","actor":"opus","summary":"MPI-468 SPLIT CHANGED, recorded on both cards as the umbrella brief requires. The bump SKILL now EXISTS as /mpi-bump-engine, created under MPI-467 because routing was the gap the user hit: a cold agent told \"test all the models\" had no path to the playbook and would have grabbed /mpi-bump-local-comfy, which is BENCH-only. It follows the repo pattern (/mpi-add-model enforces add-model/, /mpi-add-flow enforces add-flow/). MPI-457 must EXTEND that skill with the in-place mechanic - git fetch --tags + checkout the pinned sha + pip only the changed core packages + restamp .mpi_engine_version - and the gate-5 empirical class_type check. It must NOT create a second bump skill: one skill, one playbook. That collision is the whole reason MPI-468 exists."}
{"schema":"mpi-kanban/event/v1","id":"MPI-457","type":"task.moved","at":"2026-08-07T04:09:00Z","actor":"opus","from":"todo","to":"doing","summary":"MPI-468 build order step 2 - the MECHANIC starts. Child MPI-467 (the GUARD) is in validating with only its live proving run outstanding, and that run is user-scheduled behind H3/LTX/Wan, so it does not block this. Scope confirmed against the umbrella: EXTEND /mpi-bump-engine, never create a second bump skill. node_lock.json stays READ-only here."}
{"schema":"mpi-kanban/event/v1","id":"MPI-457","type":"task.updated","at":"2026-08-07T04:27:25Z","actor":"opus","summary":"Mechanic shipped (fc731113). /engine/upgrade takes the in-place path by default - git fetch --tags + checkout the node_lock pinned sha + pip for ONLY the requirement lines that moved + restamp .mpi_engine_version + repair whatever nodes the same lock change drifted. The wipe survives as _fullEngineReinstall and is reached by a DETECTED signal (dead tree / a deprecated node carrying our .mpi_node_commit marker / a moved engine-owned package / any in-place throw). Gate 5 is now a command: scripts/engine-floor-check.mjs, proven in all three branches against the LIVE 0.30.0 engine (33 workflows, 171 class_types, 0 missing; a bogus-class_type probe and an unreachable engine both exit 1). Per MPI-468 the skill was EXTENDED, not duplicated: /mpi-bump-engine gate 5 + a new docs/playbooks/bump-engine/02-local-upgrade.md. Suite 482/482, eslint clean. OUTSTANDING: acceptance #5 only - the real end-to-end upgrade on this machine did not run because the engine was mid-LTX-generation all session and the in-place path stops ComfyUI first. The exact pass is written out in validation.md and the user scheduled it for when the engine is free."}
{"schema":"mpi-kanban/event/v1","id":"MPI-457","type":"validation.updated","at":"2026-08-07T04:27:25Z","actor":"opus","summary":"validation.md written: what ran (unit contract on the real 0.29.2->0.30.0 requirements diff, suite 482/482, eslint, the floor check in all three branches against a live engine) and what did NOT (the end-to-end upgrade on a real machine, plus the deprecated-node and engine-owned-package signals, which have no live case here). Also records that the floor check first exited 127 instead of 1 - undici keep-alive teardown aborts the process on Windows - and is now on stdlib http.get like workflow-to-api.mjs."}
{"schema":"mpi-kanban/event/v1","id":"MPI-457","type":"attention.required","at":"2026-08-07T04:27:25Z","actor":"opus","summary":"Acceptance #5 is the only thing left: run the in-place upgrade end to end on this machine when the engine is idle. Steps are in validation.md - checkout v0.29.2 + restamp, POST /engine/upgrade, assert the in-place path in logs/app.log, assert the stamp lands 0.30.0, assert custom_nodes/ComfyUI-MpiNodes is STILL a symlink, then the floor check. Minutes, no downloads, ends where it started."}
