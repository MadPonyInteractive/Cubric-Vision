{
  "schema": "mpi-kanban/task-card/v1",
  "id": "MPI-303",
  "title": "WAN i2v stage-2 runs on a hardcoded seed 0 (Input_Seed orphaned)",
  "description": "Found by the new injection-reachability check (MPI-300 follow-up), not by a user report.\n\nBUG: in comfy_workflows/wan22_i2v_stage2.json the `Input_Seed` node (717) has ZERO consumers, and sampler 733 (SamplerCustom) carries a BAKED `noise_seed: 0`. So every WAN i2v stage-2 finish runs on seed 0 instead of the run's seed. The app injects Input_Seed by title, the value lands in an orphan node, and nothing errors.\n\nPROOF (twin comparison, same node ids): wan22_t2v_stage2.json has 717 -> 733 with `noise_seed` LINKED to it. wan22_i2v_stage2.json does not. The t2v twin is the reference for the correct wiring.\n\nIMPACT: stage-2 is the finishing sampler, so i2v results are less varied than they should be across runs. Pre-existing; unclear how long.\n\nFIX (USER — graph edit, agents never edit workflow JSON): open the wan22_i2v raw template in ComfyUI, connect Input_Seed (717) to sampler 733's noise_seed exactly as the t2v twin does, re-export, then `node scripts/sync-raw-workflows.mjs`.\n\nVERIFY: `node scripts/validate-injection-rules.mjs comfy_workflows/wan22_i2v_stage2.json` must pass (it currently fails on this), and a repeat i2v run should differ.\n\nNOTE: the raw source for the stage-2 file needs checking — stage-2 files are baked by orchestrate.py from wan22_i2v_template.json, so the fix belongs in the TEMPLATE, not the generated file.",
  "column": "todo",
  "maturity": "planned",
  "status": "active",
  "created_at": "2026-07-18T08:45:00Z",
  "updated_at": "2026-07-18T08:45:00Z",
  "labels": [
    "bug",
    "workflow",
    "wan",
    "seed"
  ],
  "links": {
    "events": "events.jsonl"
  }
}
