# Validation

No implementation validation has been recorded yet.

Required validation before this task can move to done:

- Local static docs site loads all eight registered routes without console
  errors.
- Search is working or explicitly deferred in the publish readiness packet.
- Placeholder screenshots/videos are replaced or listed as approved remaining
  gaps.
- Content claims are source-grounded against Cubric Vision app files.
- `CNAME` contains `docs.cubric.studio`.
- User/dev confirms DNS and GitHub Pages settings.
- HTTPS is verified after DNS propagation.
- User explicitly approves any Docs repo push/publish step.
