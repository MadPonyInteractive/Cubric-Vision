{"schema": "mpi-kanban/event/v1", "id": "MPI-633", "type": "task.created", "at": "2026-08-27T18:18:23Z", "actor": "claude", "summary": "Raised from MPI-631. Two asymmetries found while fixing the gallery's VRAM retention. (1) Images got thumbnails and videos never did: MPI-319 gave images a 512px WebP because decoding full-res PNGs in cards was ~179x heavier, but a video card's 256px poster is THROWN AWAY on promotion and replaced with the full-res master - so a 3000x1280 clip in a ~300px card decodes ~100x the pixels the card shows. (2) One thumb size serves every card size: at slider level 4 the grid targets 2 cards per row, so on a wide window a card is ~950-1250px painted from a 512px source, and ffmpegThumb.js:9 still claims that size is 'sharp enough at the biggest gallery card' - true for whatever window it was measured in, false on Fabio's. Fabio proposed capping the max card size instead; rejected in the brief because the screen's pixel count is fixed (2 big cards or 6 small ones fill the same monitor), so size-matched renditions keep memory roughly flat AND fix quality, whereas a cap removes a feature to work around a fixable derivative and, by his own observation, forces MORE cards resident. MPI-631's trigger C is what makes a larger tier affordable at all: only the visible band is resident now."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-633", "type": "task.updated", "at": "2026-08-27T18:19:09Z", "actor": "claude", "summary": "ID CORRECTION: this card was written up as MPI-632 in the MPI-631 close-out event before it was allocated, and it is MPI-633. Self-inflicted and harmless: the brief was written with the Write tool to tasks/MPI-632/brief.md BEFORE createTask ran, and that write created the directory - which is exactly what the mkdir lock in the createTask recipe is for, so os.mkdir on 632 raised FileExistsError and the loop moved to 633. Nobody else's card was touched (tasks/MPI-632 was untracked and held only that one file), the stray directory is removed and the brief now sits in tasks/MPI-633/. The lesson for the next agent: allocate the id FIRST, then write the card's files - a Write to tasks/<id>/ is enough to consume the id you were about to claim. The MPI-631 task.moved event above still says 'Follow-up raised as MPI-632'; event logs are append-only, so it is corrected here rather than rewritten."}
