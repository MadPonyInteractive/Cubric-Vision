{"schema":"mpi-kanban/event/v1","id":"MPI-382","type":"task.created","at":"2026-07-29T15:00:00Z","actor":"claude","summary":"Grow / Shrink mask slider in MpiMaskStrip, so every mask tool gets it at once. Leading mechanism: blur plus an SVG feFuncA alpha threshold on Canvas2D = real-time dilate/erode at the 1536 working size; sharp is the fallback because it is a round trip per drag. Traps: compounding (derive every frame from a drag-start snapshot) and which layer to grow (maskCanvas is derived; a bake into manual must respect the MPI-371 layer order). Sequenced after MPI-380."}
{"schema":"mpi-kanban/event/v1","type":"task.updated","id":"MPI-382","at":"2026-08-01T21:40:00Z","actor":"claude","summary":"maturity idea -> planned. approach and fallback are both settled (canvas blur plus an alpha threshold, Sharp server-side as the fallback), and the card now carries the whole strip control pack. Board-wide re-stamp against the grown 10-value enum (todo gained research / needs-decision / blocked / deferred on 2026-07-31); every todo card had been sitting on idea or planned because those were the only values that existed when it was written."}
{"schema":"mpi-kanban/event/v1","type":"task.moved","id":"MPI-382","at":"2026-08-01T22:35:00Z","actor":"claude","from":"todo","to":"doing","summary":"Picked up by the user as step 1 of the MPI-424 umbrella, replacing the rejected MPI-379. Asked what to start with, he named both halves of this card unprompted: the slider that expands and shrinks the current mask, and turning a mask into just its edges with TWO sliders, one expanding outward and one inward - which is the MPI-400 half absorbed into this card earlier the same day. They are one primitive, not two: an edge band is dilate(outward) minus erode(inward), so the blur-plus-alpha-threshold mechanism gets written once and read three ways. Moved to doing for the DESIGN session; no implementation before the design is agreed. Open questions the card already carries: which layer grows (maskCanvas is derived by _recomposite so writing to it is pointless), whether the drag holds live at the 1536 working size, and the compounding trap - every frame must derive from the pristine drag-start snapshot, which UndoStack.pendingLayer already provides."}
{"schema":"mpi-kanban/event/v1","type":"task.moved","id":"MPI-382","at":"2026-08-01T23:30:00Z","actor":"claude","from":"doing","to":"todo","summary":"Back to todo: the DESIGN is complete but it cannot be built until MPI-425 declares the Adjust button. Re-scoped from a shared-strip control to its own tool in the Mask group - the strip could not host it, because Add/Subtract live in MpiMaskDetectRow and exist only on Detect and Points, so a strip slider would have had no commit control on the Brush tool, and Add/Subtract cannot express an edge band anyway since a ring REPLACES the mask. Agreed design: one Grow/Shrink slider, an Edge button that swaps that row for Outward + Inward, live pending-green preview, Apply + Reset, discard on leave. Bake-on-release was rejected by name - dilate-then-erode is a morphological close, so dragging back is not a restore. The alpha brush pack absorbed into this card earlier the same day moves to MPI-375, where the brush engine is extracted."}
