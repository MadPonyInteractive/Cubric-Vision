{"at":"2026-06-25T18:55:00Z","event":"task.created","card":"MPI-137","column":"todo","note":"Move R2 model weights cubric-builds/models -> dedicated cubric-models/vision/ bucket (per-product: builds in cubric-builds/<product>, models in cubric-models/<product>; scales for Cubric Audio). Placed in cubric-builds on 2026-06-25 as release-night expedient. Migration is SERVER-SIDE rclone copy (NO re-upload) once token re-scoped to both buckets. Prereqs (user, Cloudflare dash): create cubric-models, bind a public hostname (dl.cubric.studio is on cubric-builds; needs own host or Worker path-route), re-scope token. Then swap URLs in BOTH dependencies.js AND mpi-ci install_models_ltx23.sh (must stay in sync), verify, then double-gated GC of old copies. Internal tidiness, not user-facing (users see dl.cubric.studio only). Off-deadline."}
{"at":"2026-06-25T21:40:00Z","event":"task.updated","card":"MPI-137","note":"DO NOT migrate model hosting to HF Buckets (HF shipped S3-compatible buckets w/ Copy-to-bucket, noticed 2026-06-25). HF Buckets is still HuggingFace + Xet => same wave-throttle on long download streams that R2 was adopted to escape. Moving there = same throttle, different URL. R2 (Cloudflare, flat CDN, free egress, ~200MB/s observed live) is the download host, full stop. HF Buckets MIGHT be worth evaluating later ONLY as a faster UPLOAD/mirror path (its Xet dedup claims large repos finish in seconds) — that is about repo->bucket mirroring, NOT user download throughput, and does nothing for the throttle. Keep R2 as the user-facing download host."}
