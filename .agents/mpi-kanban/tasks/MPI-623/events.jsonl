{"schema": "mpi-kanban/event/v1", "id": "MPI-623", "type": "task.created", "at": "2026-08-26T00:58:36Z", "actor": "claude", "summary": "Captured from brainstorm: 3D scene generation"}
{"schema": "mpi-kanban/event/v1", "id": "MPI-623", "type": "task.updated", "at": "2026-08-26T02:20:00Z", "actor": "claude", "summary": "Large plan attached; 4 investigations preserved to research/. Maturity idea -> planned."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-623", "type": "task.moved", "at": "2026-08-29T09:45:00Z", "actor": "claude", "summary": "todo -> doing. Starting Phase 0 spike (bench only, no product code). Ownership declared."}
{"schema": "mpi-kanban/event/v1", "type": "checklist.updated", "id": "MPI-623", "at": "2026-09-01T15:12:00Z", "actor": "claude", "note": "A->B chain built in js/services/flowService.js: a `chain: { operation }` flow runs as two ordinary jobs - leg 1, then leg 2 from leg 1's onComplete, ONE completion reported to the caller (on leg 2), leg 2 with no media and reusing leg 1's tempId so Cancel and live previews keep working without touching MpiBaseFlow. tests/flow-chain.test.cjs, 7 tests, four executing the real chainCallbacks; the wrap was watched go red first. 860/860 npm test, lint clean. Q1 answered: a second OP, not a second `workflow` field. Q2 answered: job 1 owns the card, job 2 attaches splatPath. Registry half BLOCKED on live peers (MPI-664 holds the five registry files, MPI-591 holds generationService/commandExecutor), and comfy_workflows/flow_3d_scene_a|b.json still do not exist."}
{"schema": "mpi-kanban/event/v1", "type": "checklist.updated", "id": "MPI-623", "at": "2026-09-01T19:20:00Z", "actor": "claude", "note": "Output_Splat capture landed. splatViewFileInfo in js/utils/comfyOutputUrls.js turns the trainer's ABSOLUTE path into a /view file dict by splitting at the LAST `splats/` segment; commandExecutor.js gets the outputSplatNodeIds title set (exact `output_splat`, beside outputPromptNodeIds), the executed branch and a splatUrl side-output on onComplete. tests/flow-splat-capture.test.cjs, 4 tests; five mutations watched go red. 864/864 npm test, lint clean. AMENDMENT 52's BLOCKER WAS NEVER ONE - only `claimed` means an active writer, and MPI-664's claim is needs_verification while MPI-591's is complete. FINDING: Output_Splat reports a PATH, not a file dict, so the attach is a read PLUS a fetch - the ingest half (generationService -> projectService.saveGeneration -> /project/save-generation streamDownload) is unblocked but NOT started, and reaches routes/projects.js + projectService.js. Descriptor still gated on Fabio authoring comfy_workflows/flow_3d_scene_a.json and _b.json."}
{"schema": "mpi-kanban/event/v1", "type": "checklist.updated", "id": "MPI-623", "at": "2026-09-01T19:55:00Z", "actor": "claude", "note": "The ingest half is BUILT. splatViewUrl threaded generationService.onComplete -> projectService.saveGeneration -> POST /project/save-generation, where a streamDownload beside the audioViewUrl mux writes .meta/<id>.splat.ply and stamps meta.splatPath in the /project-file?path= shape add-from-cards re-points. Two things the shape had to get right: the URL goes to the FIRST item only (a bake is one scene; per-item, N cards would each re-fetch hundreds of MB), and the route RETURNS splatPath so the LIVE image item carries it - the reconciler hydrates a sidecar only on RELOAD, which is the miss flowId had in MPI-256. A failed fetch removes the stub and leaves splatPath ABSENT, never half-set. Image-only (createVideoItem/createAudioItem must not carry the key). tests/splat-companion.test.cjs +5 tests, 3 of them executing the real route against a stand-in /view server; SEVEN mutations watched go red, the silent three being the client body drop, the per-item thread and the live-item drop. 869/869 npm test, lint clean. NEXT is still Fabio's gate: comfy_workflows/flow_3d_scene_a.json and _b.json do not exist, so the descriptor and the two universal ops cannot land - inject-params-titles.test.cjs:918 reds on an absent graph and the UNIVERSAL_WORKFLOWS entries are covered by no existence assertion at all. Chain, capture and ingest are now all built and unconsumed. Phase 2's user-ux gate cannot be exercised until there is a Flow to run."}
