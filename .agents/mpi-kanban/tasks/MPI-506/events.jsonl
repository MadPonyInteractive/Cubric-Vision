{"schema": "mpi-kanban/event/v1", "id": "MPI-506", "at": "2026-08-09T13:38:00Z", "type": "task.created", "actor": "opus", "note": "SeedVR2 3B/7B as upscale-model choices in the History video Upscale tool. Research done: native in ComfyUI since PR 14424 (v0.28.0) so the pinned v0.30.0 engine already registers the nodes - verified on :48188. Recommended weights seedvr2_3b_fp8_e4m3fn 3.39GB + seedvr2_7b_fp8_e4m3fn 8.24GB + the shared mandatory VAE 0.50GB. Card is needs-decision because videoUpscale is a universal op and its deps install with the engine, so the plugin-registry opt-in shape has to be chosen before code."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-506", "at": "2026-08-09T13:52:00Z", "type": "task.updated", "actor": "opus", "note": "Fabio decided the shape: three plugins (SeedVR2 3B, 7B, 7B Sharp), not a ModelDef and not engineAsset. Card retitled, maturity needs-decision -> planned, and the plugin-contributes-a-dropdown-entry mechanism is now explicitly this card's reusable deliverable. The PiD half of the same conversation is MPI-507."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-506", "at": "2026-08-09T14:10:00Z", "type": "task.updated", "actor": "opus", "note": "Answered Fabio's 'I don't know what SeedVR needs' against /object_info on :48188 rather than guessing: SeedVR2Conditioning takes model + a VAE LATENT and emits both positive and negative conditioning, so there is no prompt and no denoise, and the weight set has no text encoder to give it one. chunking_mode 'auto' sizes chunks to free VRAM, so frames_per_chunk stays unexposed. A SeedVR2 entry therefore declares ZERO extra controls, which pairs with PiD's prompt+denoise to make the per-entry control list a real mechanism instead of a special case."}
