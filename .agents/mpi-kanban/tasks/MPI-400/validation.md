# MPI-400 - validation

**Not applicable.** This card was closed `done` / `rejected` on 2026-08-01: it was
never built, so there is nothing to validate.

Mask Edge - the two-slider edge band. Merged 2026-08-01 into MPI-382, which became the Adjust tool: grow/shrink and an edge band are one primitive, since an edge band is dilate(outward) minus erode(inward).

The original card text is preserved in `task.json` as the record. The work itself
is tracked on **MPI-382**.
