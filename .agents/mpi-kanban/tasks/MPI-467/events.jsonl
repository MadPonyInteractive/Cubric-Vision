{"schema":"mpi-kanban/event/v1","id":"MPI-467","type":"task.created","at":"2026-08-07T01:40:00Z","actor":"opus","summary":"User's call after MPI-465: we should test that all workflows still run every time ComfyUI is bumped, as part of a bump skill or playbook, creating one if it does not exist. Confirmed it does not - mpi-bump-local-comfy bumps only the BENCH at G:/ComfyUi, the engine core pin in node_lock.json has no skill and no playbook, and no test ever executes a workflow (tests/ has only workflow-input-staging-gate). The user flagged the obvious blocker himself: he cannot have every model installed at once. The answer is that he does not need to - smoke what IS installed locally (LTX is, which is exactly what would have caught MPI-465) and put the full matrix on the Builder Pod, whose volume already owns the weights. Note for whoever builds it: a validation-only pass is NOT sufficient - MPI-465 threw at sampling start after the loaders ran, so validation would have passed it green."}
{"schema":"mpi-kanban/event/v1","id":"MPI-467","type":"task.updated","at":"2026-08-07T01:47:30Z","actor":"opus","summary":"Scope stated explicitly (playbook + smoke runner + release gate; the mechanic is MPI-457) and dispatch notes added to brief.md: named home for the runner, the tier/step budget question, the one-GPU serialization constraint, and the fact that LTX cannot be smoked locally until MPI-466 ships - which is exactly the case the never-report-green-for-an-unrun-model rule exists for."}
{"schema":"mpi-kanban/event/v1","id":"MPI-467","type":"task.moved","at":"2026-08-07T03:15:25Z","actor":"opus","from":"todo","to":"doing","summary":"Implementation started. Plan approved by the user 2026-08-07. Design settled in-session: the smoke runs on a RunPod GPU Pod in EU-RO-1 off a SEPARATE 320GB network volume, not locally - one GPU here and it is busy, and a local run can never hold every weight, which is exactly the silent-skip failure MPI-465 shipped. Card order L4 -> RTX 3090 -> RTX 4090, first available. Runner drives the running app on :3000 because every route already exists (/runpod/gpu-availability, volume CRUD, /remote/*), so it writes no new API code and exercises the path users hit. Execution unit is the OP (~35), not the workflow file (12) - klein-4b alone runs 7 branches through one graph. Minimization is generic by node TITLE (Input_Width/Height/Steps/Frames), never a per-graph table. Smoke set is deduped by class_type SET (a bump breaks a node) at the lowest tier per family: 12 graphs / 312.2GB today, 285.1GB once Wan t2v is deprecated, ~281GB once LTX is int8 at 21GB - both in flight in other sessions, so the runner RECOMPUTES from the registry and hardcodes nothing."}
{"schema":"mpi-kanban/event/v1","id":"MPI-467","type":"checklist.updated","at":"2026-08-07T03:15:25Z","actor":"opus","summary":"Checklist derived from plan.md phases."}
{"schema":"mpi-kanban/event/v1","id":"MPI-467","type":"task.updated","at":"2026-08-07T03:47:36Z","actor":"opus","summary":"MPI-468 SPLIT CHANGED, recorded on both cards as the umbrella brief requires. The bump SKILL now EXISTS as /mpi-bump-engine, created under MPI-467 because routing was the gap the user hit: a cold agent told \"test all the models\" had no path to the playbook and would have grabbed /mpi-bump-local-comfy, which is BENCH-only. It follows the repo pattern (/mpi-add-model enforces add-model/, /mpi-add-flow enforces add-flow/). MPI-457 must EXTEND that skill with the in-place mechanic - git fetch --tags + checkout the pinned sha + pip only the changed core packages + restamp .mpi_engine_version - and the gate-5 empirical class_type check. It must NOT create a second bump skill: one skill, one playbook. That collision is the whole reason MPI-468 exists."}
{"schema":"mpi-kanban/event/v1","id":"MPI-467","type":"validation.updated","at":"2026-08-07T04:01:21Z","actor":"opus","summary":"validation.md written: what ran (self-check with negative controls, --plan at 11 models / 34 ops / 279.5GB, the release gate proven in all four branches, suite 478/478) and what did NOT (live GPU survey, and the entire live half - volume/CPU install/version assert/dispatch)."}
{"schema":"mpi-kanban/event/v1","id":"MPI-467","type":"attention.required","at":"2026-08-07T04:01:21Z","actor":"opus","summary":"Implementation complete and verified: playbook, runner, enforced release gate, /mpi-bump-engine skill. Outstanding is the LIVE proving run only - create the 320GB EU-RO-1 volume, fill ~281GB on a CPU Pod, rent L4/3090/4090, execute all 34 ops. The live GPU survey also never ran (app was not up on :3000). Schedule the proving run AFTER H3/LTX/Wan settle - the smoke set is resolved from the registry at run time, so smoking now measures a moving target, and the run doubles as the remote-half verification for those cards."}
