{"schema":"mpi-kanban/event/v1","id":"MPI-159","type":"task.created","at":"2026-06-29T15:00:00Z","actor":"claude","summary":"Spun out of MPI-135. Root cause of scarce-card connect failures = GraphQL↔REST GPU-id desync (picker lists GraphQL catalogue, REST createPod enum lags it). MPI-135 shipped the honest-fail half (parse schema-400, classify gpuUnsupported, stop the futile retry). THIS card = prevention: grey/hide unsupported cards in picker before Connect. Recommend probe-and-cache (option 2)."}
{"schema":"mpi-kanban/event/v1","id":"MPI-159","type":"task.updated","at":"2026-06-29T15:30:00Z","actor":"claude","summary":"LIVE-PROBED the REST createPod enum (via POST /runpod/pods raw passthrough, user's key, EU-RO-1). DEFINITIVE: NVIDIA RTX PRO 4500 Blackwell + NVIDIA RTX PRO 4000 Blackwell are ENUM-REJECTED under EVERY id variant tried (full id, 'RTX PRO 4500', 'NVIDIA RTX PRO 4500'). NO id-remap fixes it — they are genuinely absent from RunPod's REST gpuTypeIds enum. CONTROLS passed the enum (got 500 'no instances available', not enum-reject): NVIDIA RTX A4500, NVIDIA RTX 4000 Ada Generation — proving the probe is real and Ada/Ampere 4000-series ARE createable. CONCLUSION: user's 'connected before' memory was almost certainly the Ampere RTX A4500/A4000 or RTX 4000 Ada (no PRO/Blackwell), which DO work. The PRO Blackwell 4500/4000 were never REST-createable. So 159 = grey-out is correct (no remap to chase). Probes left ZERO pods (enum-reject creates nothing; 500s create nothing). Recommend probe-and-cache: on a gpuUnsupported reject remember the id (localStorage) + grey it in the picker thereafter; the live POST /runpod/pods route is a ready-made probe if a proactive enum fetch is wanted later."}
{"schema":"mpi-kanban/event/v1","id":"MPI-159","type":"task.updated","at":"2026-06-29T16:00:00Z","actor":"claude","summary":"RESTRUCTURED grey-out -> GraphQL create fallback (user goal: connect to ANY picker card). 4-agent investigation (external GraphQL capability + codebase split/blast-radius + history/rationale) + LIVE PROOF. KEY LIVE RESULT: raw GraphQL podFindAndDeployOnDemand for 'NVIDIA RTX PRO 4500 Blackwell' returned a RUNNING pod (id oo8el8ng4rngcb, our cu128 image) — the card REST enum-rejects — then DELETED + getPod-404-confirmed via REST routes (REST manages a GraphQL-created pod; shared id namespace). GraphQL gpuTypeId is a free String (no enum), same id the picker has; console uses this mutation; not deprecated. DESIGN: REST stays primary (keeps CPU pods + all MPI-135 handling + getPod/status/specs/stop/delete); on the gpuUnsupported flag MPI-135 already emits, fall back to GraphQL create with params translated (gpuTypeIds[]→gpuTypeId, dataCenterIds[]→dataCenterId, env obj→[{key,value}], ports[]→comma-string, cloudType:SECURE). CPU __cpu__ NEVER falls back (GraphQL lacks computeType:CPU). No prior GraphQL→REST migration to re-break. Full plan + param table + acceptance tests in task.json. Ready to implement next session."}
{"ts":"2026-06-29T17:30:00Z","type":"task.moved","from":"todo","to":"doing","note":"Picked up for build: GraphQL create fallback wired in remoteProxy._createPodInternal + client.createPodGraphql."}
{"schema":"mpi-kanban/event/v1","id":"MPI-159","type":"task.updated","at":"2026-06-29T17:45:00Z","actor":"claude","summary":"IMPLEMENTED + LIVE-VALIDATED. client.createPodGraphql (runpodRemote.js) translates the REST spec to podFindAndDeployOnDemand input (gpuTypeIds[]→gpuTypeId, dataCenterIds[]→dataCenterId, env obj→[{key,value}], ports[]→comma-string, cloudType:SECURE) and adapts the {data}|{errors} HTTP-200 GraphQL shape to the REST {ok,status,json:{id}} the caller expects. _createPodInternal: on the gpuEnumReject (MPI-135 gpuTypeIds/items/enum 400) and !noGpu, falls back to GraphQL create → routes the pod through the shared _afterPodCreated post-create flow; GraphQL failure surfaces honestly (gpuUnsupported:false so a real stock fail is retryable). CPU __cpu__ never falls back. LIVE PROOF (3 connects, app log L2419-2433 + ephemeral test): (1) RTX PRO 4500 Blackwell volume-backed EU-RO-1 → REST enum-reject → GraphQL fallback → pod ucvq0f0958xfbo created; (2) RTX PRO 4000 Blackwell volume-backed → GraphQL fallback → pod hhemfgj538ptwx created (and PRO 4000 DOES exist+deploy — earlier doubt resolved); (3) RTX PRO 4500 EPHEMERAL any-region (no volume) → also worked. Pod hhemfgj538ptwx then stopped/deleted via the REST routes → getPod 'pod not found' 404 = acceptance #4 proven (REST manages a GraphQL-created pod, shared id namespace). Headline goal MET: user connects to cards the REST enum rejects. User explicitly approved final completion → moving to done/complete."}
{"schema":"mpi-kanban/event/v1","id":"MPI-159","type":"task.moved","at":"2026-06-29T17:50:00Z","actor":"claude","summary":"doing -> done (complete, accepted). GraphQL create fallback SHIPPED (commit 358c42d) + LIVE-VALIDATED by user: RTX PRO 4500 (volume-backed EU-RO-1 AND ephemeral any-region) + RTX PRO 4000 (volume-backed) all created via the fallback; GraphQL-created pod deleted via REST routes (getPod 404). Headline goal MET — user connects to any card the picker shows. User explicitly approved final completion."}
