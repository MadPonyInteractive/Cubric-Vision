{"schema": "mpi-kanban/event/v1", "id": "MPI-464", "type": "task.created", "at": "2026-08-07T00:21:00Z", "actor": "claude", "summary": "Split from MPI-462. The orphan collector shipped local-only; the remote uninstall branch returns before it. Needs _remoteSharedDepIds + a volume inventory, and a live Pod to verify before any deletion code aims at a user's volume."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-464", "type": "task.updated", "at": "2026-08-07T00:44:00Z", "actor": "claude", "summary": "brief.md added so a cold agent can pick this up: what to mirror from the local sweep, the volume-inventory piece that has no local analogue, every refusal to copy, and the do-not-lower bar (a live Pod, read-only classification first - MPI-310 destroyed 5.24GB changing this guard)."}
{"schema":"mpi-kanban/event/v1","id":"MPI-464","type":"task.moved","at":"2026-08-07T01:25:00Z","actor":"opus","summary":"todo -> doing. Picked up to build the REMOTE orphan sweep. Shape settled from the code: protection map is _remoteSharedDepIds(null) (it already accepts a null exclusion - same MODELS.filter / _inFlightDepIds(null) / _pluginRequiredDepIds(null) path the local twin uses with null), orphan classification reuses _orphanedDepIds unchanged (a Set answers .has the same as a Map), and the volume inventory needs NO new wrapper endpoint - remoteModelsCheck already accepts a pseudo-model, which is exactly how the reconciler asks about a bare dep list at downloadManager.js:1904."}
{"schema":"mpi-kanban/event/v1","id":"MPI-464","type":"task.updated","at":"2026-08-07T01:33:00Z","actor":"opus","summary":"Code complete and unit-verified; maturity in-progress -> validating. _sweepOrphanedDepsRemote lands in routes/downloadManager.js and is wired into the remote uninstall branch (gated on deleteFiles, never fatal, swept N orphaned on the same log line, sweptOrphans on the same response and download:uninstalled payload as local). Three things the card asked for turned out to need NO new machinery: _remoteSharedDepIds already took a null exclusion, _orphanedDepIds was reusable unchanged because a Set answers .has like a Map, and the volume inventory needed no wrapper endpoint - remoteModelsCheck takes a pseudo-model, the same trick _reconcileOutstandingRemoteDeps uses. One refusal the local sweep does NOT need was found and added: bakedOnPod engineAssets (7 of them) live in the Pod image, so _isImageResident makes remoteModelsCheck report them installed while the wrapper cannot delete them. tests/orphan-sweep-remote.test.cjs 5/5 and npm test 477/477. NOT closable - the bar is a live Pod with a read-only classification read first; recipe in validation.md."}
{"schema":"mpi-kanban/event/v1","id":"MPI-464","type":"task.moved","at":"2026-08-07T02:01:00Z","actor":"opus","summary":"doing -> done. Pod-verified on a CPU download-mode Pod (cpu3c, wrapper 0.2.41) against the real 150GB EU-RO-1 volume - no GPU, because the sweep only needs /wrapper/models/status and /wrapper/models/delete. Read-only classification FIRST: 71 protected, 35 eligible, 35 echoed, 0 on volume, and the zero was checked dep-by-dep rather than trusted (boogu 4/7 is custom_nodes only; every ltx-23-balanced weight is genuinely defended by the installed ltx-23). depsEchoed == eligible is the load-bearing number - it proves the wrapper answers a 35-dep pseudo-model without dropping any, which is the one failure mode the unit test cannot see and would have read as \"no orphans\" while under-collecting. Then a seeded shared orphan (chroma-style-lenovo, one of the very LoRAs MPI-462 stranded locally) was collected for real while vae-qwen-image, needed by the installed krea2 and qwen-edit, survived beside it; and deleteFiles=false was proven to skip the sweep entirely. Net change to the volume: ZERO - every pre-existing file survived, everything deleted was seeded by the test, and the post-run read is identical to the pre-run one. Two findings recorded in the doc: a seeded orphan must be declared by >=2 models or it becomes that model exclusive evidence and defends itself (the first seed did exactly that, MPI-310 rule working), and the sweep classifies against POST-delete truth, which is what lets it collect an orphan the uninstall itself created. Pod deleted after; volume untouched."}
