{
  "schema": "mpi-kanban/task-card/v1",
  "id": "MPI-305",
  "title": "validate-injection-rules.mjs: Input_Seed on an add_noise:False sampler is a false-positive orphan",
  "description": "Fallout from MPI-303, which this check filed as a bug and which closed as NOT A BUG.\n\nPROBLEM: check 4 (injection reachability, scripts/validate-injection-rules.mjs) walks consumers of every Input_* node and demands a path to an Output_*. In comfy_workflows/wan22_i2v_stage2.json, Input_Seed (717) legitimately feeds nothing: sampler 733 runs `add_noise: False`, which makes its noise_seed a dead input, so the graph is correct with 717 unconnected. The check reports it as a silently-discarded injection and will keep re-filing MPI-303 on every run.\n\nWHY IT MATTERS: a validator that cries wolf on a correct graph trains us to ignore it — and check 4 is the thing that caught the real qwen_edit Input_Image_2 drop (MPI-300).\n\nFIX: exempt an Input_Seed whose only would-be consumer is a noise_seed on a sampler with `add_noise: False`. Narrow exemption — seed-specific, add_noise-specific. Do NOT weaken check 4 generally; every other Input_* orphan is still a real bug.\n\nSCOPE NOTE: check 2 (noise_seed present => an Input_Seed node must exist) is unaffected — wan22_i2v_stage2 has the node, so only check 4 fires. Leave check 2 alone.\n\nVERIFY: `node scripts/validate-injection-rules.mjs comfy_workflows/wan22_i2v_stage2.json` passes, the t2v twin still passes, and a deliberately-orphaned Input_Image in a scratch copy still FAILS (proves the exemption did not blanket-disable check 4).",
  "column": "doing",
  "maturity": "in-progress",
  "status": "active",
  "created_at": "2026-07-18T09:45:00Z",
  "updated_at": "2026-07-18T09:45:00Z",
  "labels": [
    "bug",
    "tooling",
    "validator",
    "wan"
  ],
  "links": {
    "events": "events.jsonl"
  }
}