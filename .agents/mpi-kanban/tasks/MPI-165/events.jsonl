{"schema": "mpi-kanban/event/v1", "id": "MPI-165", "type": "task.created", "at": "2026-06-30T11:00:00Z", "actor": "claude", "summary": "Consolidate engine-variant (deps+workflow+signal) into one engines: profile + resolve(model,op,engine) + resolve-engine-once. Root: workflow swap reads stale isRemote() before refresh -> bf16 workflow on Pod (live fail). Option A structure + phased delivery (A signal-fix fixes live bug, B migrate to profile, C delete old mechanisms + rule rewrite), both user-confirmed. 4-agent investigation backing. Plan written."}
