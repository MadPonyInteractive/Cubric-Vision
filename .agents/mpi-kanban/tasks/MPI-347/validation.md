# MPI-347 - validation

**Not applicable.** This card was closed `done` / `rejected` on 2026-08-01: it was
never built, so there is nothing to validate.

App for localized 4K/8K edits. Merged 2026-08-01 into MPI-355 - identical mechanism (InpaintCropImproved -> sample -> InpaintStitchImproved); this was the App-shaped outcome of that card's wiring.

The original card text is preserved in `task.json` as the record. The work itself
is tracked on **MPI-355**.
