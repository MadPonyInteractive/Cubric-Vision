# MPI-667 - checklist

- [ ] Offer every CPU flavor on the download-mode create spec instead of the single `cpu3c`.
- [ ] Correct the reconnect comment that asserts CPU capacity is always available.
- [ ] Prove the spec change lands: a runnable check that the CPU branch emits more than one flavor.
- [ ] Live: Connect with "No GPU - download only" in EU-RO-1 and get a podId.
