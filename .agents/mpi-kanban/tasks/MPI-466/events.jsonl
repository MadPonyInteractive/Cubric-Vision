{"schema":"mpi-kanban/event/v1","id":"MPI-466","type":"task.created","at":"2026-08-07T01:05:00Z","actor":"opus","summary":"Filed off a live bench A/B. The user tested the distilled-1.1 int8 (21.5GB) against our shipped fp8 (25.2GB) and reported it better in every way - sound, hands, eyes - and 10 s faster, measured on a 2 s clip at 320x640 (small-clip margin, re-check at a shipping tier). That alone justifies the Balanced swap. The bigger move is the user's: Kijai also publishes the distilled-1.1 delta as a rank-111 LoRA (2.74GB), so ONE dev int8 weight plus that LoRA covers both tiers at runtime - which is precisely what Krea 2 already does with capabilities.turboToggle, the krea2Turbo PromptBox control, Input_is_Turbo (MPI-365) and the Accelerator Lora node's MpiMath gate, and how its separate Turbo card was retired. Ships as ONE card instead of two, 24.25GB instead of ~66GB, and probably kills the variants.arch fp8/mxfp8 split too. The gate is LoRA-on-int8: turbo mode is a bf16 rank-111 LoRA patched onto int8 weights, which is unproven - though the user's runs already carry three baked LoRAs, so if any were live in those generations most of that risk is already retired for free."}
{"schema":"mpi-kanban/event/v1","id":"MPI-466","type":"task.updated","at":"2026-08-07T01:47:30Z","actor":"opus","summary":"Rewritten to the user decision: the distill-LoRA turbo toggle and the dev non-distilled model are DROPPED, so the gate that would have decided the old plan (does a bf16 rank-111 LoRA patch onto int8 weights) is dead - do not re-run it. HIGH stays bf16, BALANCED becomes distilled-1.1 int8_convrot. The card now also absorbs LTX half of MPI-456 because the re-export carries both stages in one 215-node graph, and it picks up media-derived routing (no op booleans), audio INPUT and the transition LoRA. Sequencing consequence recorded: fp8 weights are deleted and the bf16 was never on the drive, so LTX is unrunnable locally and MPI-465 app proof rides on this card rather than preceding it."}
