{"schema":"mpi-kanban/event/v1","id":"MPI-423","type":"task.created","at":"2026-08-01T08:30:00Z","actor":"opus","column":"todo","summary":"Raised from the docs-site side while writing the 1.3.0 pages: the copy tells users to \"hit Remove\" for object removal and there is no Remove. Verified absent, not merely unfound - no remove: key in commandRegistry.js, models.js itself lists remove under deprecated/never wired, and grep for a Remove label across js/ returns only Remove Background (a different BiRefNet tool, which is the trap: a user hunting for Remove finds it and gets the inverse). The real path is mask -> inpaint -> EMPTY prompt, which inpaint.help already states correctly, so the ? guide and the Model Library tile contradict each other. Three sites: models.js:529 is the urgent one because it is live UI a user reads while deciding to install Klein, not a changelog; releaseNotes.js:49 and the 2026-07-30-v1.3.0.md mirror are changelogs and their correction is a recorded judgement call. Docs site already ships the correct procedure (Cubric-Studio-Docs 04a400b), so the fix is entirely app-side."}
{"schema": "mpi-kanban/event/v1", "id": "MPI-423", "type": "task.moved", "at": "2026-08-01T08:45:00Z", "actor": "opus", "from": "todo", "to": "doing", "summary": "Picked up. The urgent site is fixed: models.js klein-4b description now reads 'mask an object, run Inpaint with the prompt left empty' instead of 'hit Remove'. DECISION RECORDED on the changelog judgement call - releaseNotes.js['1.3.0'] and the 2026-07-30-v1.3.0.md mirror were CORRECTED too, not left as historical record: the in-app changelog is live UI every user reads, and a rendered instruction to press a control that does not exist is a defect wherever it renders. What shipped is preserved in git and in an explicit correction note appended to the release doc's Platform Update Checklist. Grep across js/ confirms no other user-facing string instructs a Remove control; the only Remove labels left are Remove Background (BiRefNet, unrelated) and slot/point clear buttons. Remaining: the Patreon and Discord 1.3.0 posts are the one acceptance item an agent cannot check - the release doc now carries the corrected wording and an instruction to fix any post that already went out with the old sentence."}
