# MPI-377 Validation

NOT BUILT UNDER THIS CARD. Closed 2026-08-21 as absorbed into MPI-454.

The bug is real and is still being fixed - the drop routing is now acceptance lines 4, 5 and 6
on MPI-454, because the 2026-08-21 brainstorm made the drop one of three ways to fill the
Place tool's slot rather than a job of its own.

The FIX this card proposed was rejected by the user in that brainstorm: building a history
entry out of the dropped file would put an imported photo in the group history, which is not
a generation. The dropped file now fills a tool slot instead, and an entry is written only at
Apply - a composite, which is a legitimate history artifact.

Nothing to verify here. Verification lives on MPI-454.
