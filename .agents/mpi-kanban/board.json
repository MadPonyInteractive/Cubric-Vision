{
  "schema": "mpi-kanban/board/v1",
  "next_id": 544,
  "columns": {
    "todo": [
      "MPI-543",
      "MPI-541",
      "MPI-538",
      "MPI-536",
      "MPI-534",
      "MPI-533",
      "MPI-532",
      "MPI-531",
      "MPI-530",
      "MPI-529",
      "MPI-528",
      "MPI-527",
      "MPI-526",
      "MPI-525",
      "MPI-524",
      "MPI-523",
      "MPI-522",
      "MPI-520",
      "MPI-518",
      "MPI-516",
      "MPI-515",
      "MPI-511",
      "MPI-513",
      "MPI-512",
      "MPI-510",
      "MPI-509",
      "MPI-504",
      "MPI-502",
      "MPI-500",
      "MPI-497",
      "MPI-496",
      "MPI-489",
      "MPI-485",
      "MPI-367",
      "MPI-478",
      "MPI-477",
      "MPI-455",
      "MPI-416",
      "MPI-397",
      "MPI-377",
      "MPI-454",
      "MPI-358",
      "MPI-357",
      "MPI-355",
      "MPI-325",
      "MPI-332",
      "MPI-320",
      "MPI-289",
      "MPI-322",
      "MPI-302",
      "MPI-249",
      "MPI-183",
      "MPI-343",
      "MPI-348",
      "MPI-349",
      "MPI-403",
      "MPI-259"
    ],
    "doing": [
      "MPI-542",
      "MPI-540",
      "MPI-4",
      "MPI-506",
      "MPI-507"
    ],
    "done": [
      "MPI-539",
      "MPI-537",
      "MPI-535",
      "MPI-450",
      "MPI-519",
      "MPI-521",
      "MPI-517",
      "MPI-514",
      "MPI-433",
      "MPI-508",
      "MPI-483",
      "MPI-481",
      "MPI-501",
      "MPI-505",
      "MPI-503",
      "MPI-456",
      "MPI-492",
      "MPI-488",
      "MPI-468",
      "MPI-467",
      "MPI-465",
      "MPI-498",
      "MPI-495",
      "MPI-494",
      "MPI-480",
      "MPI-491",
      "MPI-493",
      "MPI-442",
      "MPI-490",
      "MPI-487",
      "MPI-482",
      "MPI-486",
      "MPI-461",
      "MPI-479",
      "MPI-484",
      "MPI-458",
      "MPI-432",
      "MPI-466",
      "MPI-463",
      "MPI-459",
      "MPI-449",
      "MPI-374",
      "MPI-453",
      "MPI-420",
      "MPI-404",
      "MPI-410",
      "MPI-451",
      "MPI-452",
      "MPI-462",
      "MPI-470",
      "MPI-474",
      "MPI-475",
      "MPI-476",
      "MPI-473",
      "MPI-472",
      "MPI-457",
      "MPI-471",
      "MPI-469",
      "MPI-464",
      "MPI-460",
      "MPI-440",
      "MPI-435",
      "MPI-439",
      "MPI-445",
      "MPI-448",
      "MPI-447",
      "MPI-446",
      "MPI-444",
      "MPI-436",
      "MPI-443",
      "MPI-441",
      "MPI-379",
      "MPI-370",
      "MPI-198",
      "MPI-499"
    ]
  },
  "updated_at": "2026-08-11T19:10:00Z",
  "events": [
    {
      "at": "2026-08-11T05:40:00Z",
      "type": "task.moved",
      "task": "MPI-537",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started. Bench authoring of the LTX 2.3 lipdub graph: the bench template is read (36 nodes / 49 links), every node type and the ic-lora-lipdub-0.9 LoRA are present on both 8188 and 48188, and plan.md is now written with five phases. It carries one correction to the brief: the audio latent is EMPTY (LTXVEmptyLatentAudio feeds LTXVConcatAVLatent), so the words come from the prompt text and the source audio enters only as speaker-identity ref tokens - swapping that socket swaps the VOICE, not the content. The unused frozen_audio output (noise_mask=0) is the one-link swap that makes the graph audio-driven, so both modes live in one file."
    },
    {
      "at": "2026-08-11T05:20:00Z",
      "type": "task.moved",
      "task": "MPI-535",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "note": "Closed on the user's own confirmation. He reran MiniMax H3 image-to-video on the fixed renderer and reported the latents playing continuously at clip speed instead of flashing the whole clip once per sampler step; the console error he then asked about was diagnosed as the known revoked-blob repaint residual (documented in docs/preview-bus.md, cosmetic, latched so it cannot repeat) and deliberately not carded. Agent evidence already stood: 6/6 headless lifecycle tests, a real-renderer probe measuring 24 painted frames/s with the marker never delivered, npm test 552/552, eslint clean."
    },
    {
      "at": "2026-08-11T04:45:00Z",
      "type": "validation.updated",
      "task": "MPI-535",
      "actor": "opus",
      "note": "Fixed and verified. Clip playback was a one-shot latch on the gallery card element, armed by the single VHS_latentpreview marker a burst previewer sends once per sampler run - so a missed marker stranded the whole run in still mode, which is a whole clip flashed per step at burst speed. It read as single-pass-only because a multi-stage H3 run is two prompts and gets a second arming. Clip state now lives on the generation (activeGenerations, dropped with the gen) and rides every frame to the card, which mirrors instead of latching, so a miss self-heals on the next frame. The marker's {length, rate} were also being discarded outright by commandExecutor: playback now runs at the announced rate (H3 24, LTX 16 - 8fps was a hardcode) and the ring is sized by the announced length (48 was clipping H3's 56-frame clip down to its tail). Evidence: 6/6 new headless lifecycle tests, a real-renderer probe that delivers frames with NO marker and measures 24 painted frames/s looping (the race, reproduced deterministically), 8fps when 8 is announced, and a no-clip burst still freezing as it should; npm test 552/552, eslint clean. Left open: Fabio's eyeball on the next H3 single-pass run."
    },
    {
      "at": "2026-08-11T04:20:00Z",
      "type": "task.created",
      "task": "MPI-535",
      "actor": "opus",
      "note": "Opened directly in doing from a live user bug report. MiniMax H3 latents played the whole clip once per sampler step at arrival speed instead of looping at the clip's own rate. Root cause is not H3: clip playback is a one-shot latch on the gallery card element, armed by a single VHS_latentpreview marker per sampler run, so any miss downgrades the rest of the run to still mode with no recovery. It read as a single-pass-only bug because a multi-stage run dispatches two prompts and gets a second marker; a later single-pass run worked, confirming a race, not a mode."
    },
    {
      "at": "2026-08-09T14:15:00Z",
      "type": "task.moved",
      "task": "MPI-505",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "note": "Turbo toggle shipped and verified: dep + capability + control + injection + single-pass + the EasyCache gate, all confirmed by tests, an offline wiring probe and the user’s own app runs. Measurements re-taken on the shipped graph and written to docs/models/h3/turbo.md. Remaining Pod smoke run belongs to release readiness, not to this card."
    },
    {
      "at": "2026-08-09T11:47:00Z",
      "type": "task.updated",
      "task": "MPI-504",
      "actor": "opus",
      "column": "todo",
      "note": "Two corrections from Fabio. (1) The LoRA-training sheet is OUT OF SCOPE, not a layout option - it is ~20 images and a whole different beast, addressed when that flow is addressed; MPI-504 owns the video-reference 3-panel sheet only. (2) The flow needs TWO MODELS, chosen per path at the bench - an EDIT model for the reference path, and for the attributes path whichever t2i model best obeys a prompt asking it to display the character in that layout. That second question is LAYOUT COMPLIANCE, not aesthetics, and has no answer on the board. Multiple models in one flow is the point of flows."
    },
    {
      "at": "2026-08-09T11:25:00Z",
      "type": "task.updated",
      "task": "MPI-504",
      "actor": "opus",
      "column": "todo",
      "note": "Re-scoped by Fabio. The MPI-348 s8.4 layout (side + front profile + full body) is a LORA-TRAINING sheet - a different consumer - so the two layouts are NOT a conflict to settle; layout becomes a flow option keyed to purpose, and this card owns the VIDEO-REFERENCE layout only. Input is now TWO paths: attributes-only (what the character is, wardrobe, scars, age, hair colour, eye colour) restructured into the sheet prompt, and a reference photo. The attributes path is the LOW-RISK one and ships first - it is exactly what Higgsfield did (prompt to sheet in a hosted model) and has no identity-projection problem; the photo path keeps the unproven back-view step. Added: with structured fields a slot template needs no LLM, so Cubric Prompt earns its place only on free prose or per-model shaping. Recorded that their 'never write an age' rule does NOT transfer - that is a hosted content filter and Vision runs local models."
    },
    {
      "at": "2026-08-09T10:05:00Z",
      "type": "task.moved",
      "task": "MPI-505",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "Backfilled: the two raw H3 templates were exported from the ComfyUI editor this session, so implementation work already exists. files.json records the twelve paths the card owns. Sync, dep entry and the app-side control are still pending."
    },
    {
      "at": "2026-08-09T10:00:34Z",
      "type": "task.created",
      "task": "MPI-505",
      "actor": "opus",
      "column": "todo",
      "note": "H3 Turbo LoRA carded after a full bench session. 204s -> 96s at 864x480 warm, measured across 12+ runs. H3 is the only non-distilled video model in the fleet, which is why SageAttention, SolAttn and EasyCache all collapse onto it and why LTX and WAN are explicitly out of scope. R2 upload done and verified; repo work pending."
    },
    {
      "at": "2026-08-09T07:30:00Z",
      "type": "task.created",
      "task": "MPI-504",
      "actor": "opus",
      "column": "todo",
      "note": "Character Sheet flow carded from Fabio's ask: one supplied image in, a 3-panel sheet out with the front full body HEADLESS. Was candidate workflow 4 inside MPI-348 s8, never its own card. Carries the Higgsfield HELL GRIND sheet spec and flags that it conflicts with Fabio's own past-tested layout - bench decides. Real risk named: back-view identity from a single photo is unproven locally."
    },
    {
      "at": "2026-08-08T19:20:00Z",
      "type": "task.moved",
      "task": "MPI-495",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "note": "Silent LoRA drop closed. ComfyUI validates per OUTPUT node and answers HTTP 200 {prompt_id, number, node_errors} whenever one output still validates (execution.py -> server.py); runWorkflow read only prompt_id, so an invalid style-LoRA branch was skipped and the run resolved success. comfyController now fails the generation on a non-empty node_errors, tagged with the EXISTING lora_missing_*/weights_missing_* codes so commandExecutor's toasts fire with no new UI, and tagged by ENGINE not carrier (a 200 is structured node_errors on remote too - wrapper JSONResponse(r.json()) and Express _passthrough both verbatim, all four hops read). Rejected input names collected by shape because MpiStyleLoras slots are lora_1..lora_5 (MPI-359), which a ['lora_name'] reader misses entirely. Guard tests/prompt-partial-validation.test.cjs drives the real runWorkflow, 7/7, fails 'Missing expected rejection' in 3.8ms with the guard removed; full suite 519 pass 0 fail. Durable fact landed in docs/comfy.md (the 400-body section, whose carrier-identifies-the-engine trap was scoped to 400 only). The identical blind spot in scripts/smoke-workflows.mjs was reported to MPI-467, who implemented and mutation-verified their own fix. NOT run on a real engine - the GPU matrix owned the Pod for the whole session, by instruction."
    },
    {
      "at": "2026-08-08T18:52:00Z",
      "type": "task.progressed",
      "task": "MPI-495",
      "actor": "opus",
      "column": "doing",
      "maturity": "validating",
      "note": "Fixed in js/services/comfyController.js: the 200 ack's node_errors is now read, logged, best-effort un-queued and thrown tagged with the EXISTING lora_missing_*/weights_missing_* codes, so commandExecutor's warning toasts fire with no new UI. Tags by ENGINE, not carrier - a 200 is structured node_errors on remote too (wrapper JSONResponse(r.json()) -> Express _passthrough, both verbatim, all hops read). Style slots are lora_1..lora_5 (MPI-359), so rejected input names are collected by shape; a ['lora_name'] reader matches none of the ten krea2 styles. Guard: tests/prompt-partial-validation.test.cjs drives the REAL runWorkflow, 7/7, and fails 'Missing expected rejection' in 3.8ms when the guard is removed. Full suite 519 pass 0 fail. NOT verified on a real engine (matrix owns the Pod, by instruction). Smoke runner has the identical blind spot at scripts/smoke-workflows.mjs:552 - not mine, filed to MPI-467 as a message."
    },
    {
      "at": "2026-08-08T18:40:00Z",
      "type": "task.moved",
      "task": "MPI-495",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "Root cause confirmed READ-ONLY (no Pod, no app restart): ComfyUI validates per OUTPUT node and returns HTTP 200 {prompt_id, number, node_errors} whenever at least one output still validates (execution.py:1247 -> server.py:1132). The Pod wrapper passes that body through verbatim (JSONResponse(r.json())), so BOTH engines carry it. runWorkflow reads only ack.prompt_id and drops ack.node_errors - the invalid style-LoRA branch is skipped and the run resolves success. Owns js/services/comfyController.js + tests/prompt-partial-validation.test.cjs."
    },
    {
      "at": "2026-08-07T14:20:43Z",
      "type": "task.moved",
      "task": "MPI-473",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "note": "SHIPPED and USER-VERIFIED live 2026-08-07: preview stops at preview, Continue resumes from the staged latent, and the warning that fired on EVERY multi-stage generation is gone. Preview_Only removed AND its twin Is_Continue, which the brief did not list - grep proved both had zero nodes in every shipped graph, on adjacent lines of the same block, so fixing one would have left the other claiming a node that does not exist. The comfyController guard was deleted whole: it was doubly unreachable, because the Input_ canonicalization pass already deleted the bare key before dispatch. Three files (models.js, resolveModelDeps.js, generate_h3.py) claimed H3 gates on Input_Preview_Only / Input_Is_Continue - FACTUALLY WRONG, not stale: minimax_h3_fl2va.json node 320 is an MpiStageLatents and generate_h3.py bakes is_preview/is_continue at its own line 83. Corrected, not deleted. 12 code sites across 9 files. Docs pass ran on explicit cardinal-rule-5 permission: 11 live sites over 6 rule files and 5 docs, the worst being comfy_injection_multistage.md:102, a troubleshooting instruction telling the reader to grep logs/app.log for a warning string that can no longer be emitted. docs/archive/ deliberately untouched. npm test 482/0. NOT COMMITTED - a concurrent session was writing this tree throughout (types.js, promptReuse.js, MPI-472 moved under this session), so commit by explicit pathspec."
    },
    {
      "at": "2026-08-07T13:55:00Z",
      "type": "task.moved",
      "task": "MPI-473",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started, plan approved by the user. Orientation confirmed the brief and WIDENED it: zero JSON workflow carries a Preview_Only / Input_Preview_Only node (grep, 0 hits), and minimax_h3_fl2va.json node 320 is MpiStageLatents titled Input_Video_Latent - so the models.js:1251 and resolveModelDeps.js:233 comments claiming H3 gates on Input_Preview_Only / Input_Is_Continue are FACTUALLY WRONG, not merely stale, and get corrected rather than deleted. The Input_ canonicalization pass at commandExecutor.js:844 deletes the bare key, so comfyController's params.Preview_Only half of the guard is doubly unreachable. Two sites the brief did not list: generationService.js:986 destructures Preview_Only out of injectionParams, but previewStage.getInjectionParams() returns {}; and generate_h3.py:14 contradicts its own BAKED_WIDGETS at line 83. 12 code sites total. Docs/rules NOT touched - .claude/rules/comfy_injection.md and comfy_injection_multistage.md still declare Input_Preview_Only REQUIRED on _ms base workflows, which is now false, but CLAUDE.md cardinal rule 5 needs explicit permission; reported at close instead."
    },
    {
      "at": "2026-08-07T04:01:21Z",
      "type": "task.updated",
      "task": "MPI-467",
      "actor": "opus",
      "note": "Implementation complete and verified: playbook, runner, enforced release gate, /mpi-bump-engine skill. Outstanding is the LIVE proving run only - create the 320GB EU-RO-1 volume, fill ~281GB on a CPU Pod, rent L4/3090/4090, execute all 34 ops. The live GPU survey also never ran (app was not up on :3000). Schedule the proving run AFTER H3/LTX/Wan settle - the smoke set is resolved from the registry at run time, so smoking now measures a moving target, and the run doubles as the remote-half verification for those cards."
    },
    {
      "at": "2026-08-07T03:47:36Z",
      "type": "task.updated",
      "task": "MPI-457",
      "actor": "opus",
      "note": "MPI-468 SPLIT CHANGED, recorded on both cards as the umbrella brief requires. The bump SKILL now EXISTS as /mpi-bump-engine, created under MPI-467 because routing was the gap the user hit: a cold agent told \"test all the models\" had no path to the playbook and would have grabbed /mpi-bump-local-comfy, which is BENCH-only. It follows the repo pattern (/mpi-add-model enforces add-model/, /mpi-add-flow enforces add-flow/). MPI-457 must EXTEND that skill with the in-place mechanic - git fetch --tags + checkout the pinned sha + pip only the changed core packages + restamp .mpi_engine_version - and the gate-5 empirical class_type check. It must NOT create a second bump skill: one skill, one playbook. That collision is the whole reason MPI-468 exists."
    },
    {
      "at": "2026-08-07T03:15:25Z",
      "type": "task.moved",
      "task": "MPI-468",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "Umbrella owned by this session. MPI-467 (the GUARD) starts now; MPI-457 (the MECHANIC - in-place /engine/upgrade + the repo-side bump skill) deliberately stays in todo, on the umbrella build order: the playbook is the file both cards write into, so its headings get fixed before the mechanic lands. Machine constraints in brief.md re-checked and still hold - node_lock.json stays READ-only here, and the LTX raw template is another session's dirty file."
    },
    {
      "at": "2026-08-07T03:15:25Z",
      "type": "task.moved",
      "task": "MPI-467",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started. Plan approved by the user 2026-08-07. Design settled in-session: the smoke runs on a RunPod GPU Pod in EU-RO-1 off a SEPARATE 320GB network volume, not locally - one GPU here and it is busy, and a local run can never hold every weight, which is exactly the silent-skip failure MPI-465 shipped. Card order L4 -> RTX 3090 -> RTX 4090, first available. Runner drives the running app on :3000 because every route already exists (/runpod/gpu-availability, volume CRUD, /remote/*), so it writes no new API code and exercises the path users hit. Execution unit is the OP (~35), not the workflow file (12) - klein-4b alone runs 7 branches through one graph. Minimization is generic by node TITLE (Input_Width/Height/Steps/Frames), never a per-graph table. Smoke set is deduped by class_type SET (a bump breaks a node) at the lowest tier per family: 12 graphs / 312.2GB today, 285.1GB once Wan t2v is deprecated, ~281GB once LTX is int8 at 21GB - both in flight in other sessions, so the runner RECOMPUTES from the registry and hardcodes nothing."
    },
    {
      "at": "2026-08-05T15:05:00Z",
      "type": "task.updated",
      "task": "MPI-433",
      "note": "Gate C date check: 1.4 ships BEFORE 2026-08-10, so nothing is uploaded and the card keeps its date. The release-note claim was scoped instead. The audit also found THREE more single-route deps (the MPI-420 preview decoders) that this card never covered - permanent noMirror by nature, recorded in brief.md and docs/download-manager.md. maturity=blocked (waiting on a date)."
    },
    {
      "at": "2026-08-05T15:05:00Z",
      "type": "task.updated",
      "task": "MPI-416",
      "note": "Gate C split. Symlink half SHIPPED (scope excluded from the staged tree + a whole-tree dangling-symlink assertion, proven on a real local stage). CLT half deferred out of 1.4 behind a known-issue line in the release notes. maturity=deferred."
    },
    {
      "at": "2026-08-05T14:10:00Z",
      "type": "task.updated",
      "task": "MPI-410",
      "note": "BUILT. Splash half REPRODUCED and root-confirmed: mainWindow ready-to-show fires on Chromium's error page, so the splash was closed 1.1s before the server bound (and destroyed mid-loadFile on a slow disk = the ERR_FAILED -2). Reveal now needs paint + a real HTTP response + a finished load, with two backstops so it can never regress into MPI-407's black window. Absorbed MPI-412 fixed at two roots: indeterminate is now a JOB-level flag on both engine twins (reusing the already-tested computeProgress rule), and the install screen's info line has one owner. 451 node + 17 desktop tests pass, negative control proven. maturity=validating - the strobe half has never been seen fire and needs a real engine install."
    },
    {
      "at": "2026-08-05T13:00:00Z",
      "type": "task.moved",
      "task": "MPI-410",
      "from": "todo",
      "to": "doing",
      "note": "Repro session. Reading main.js settles the trigger: not cold cache as such, but server-ready arriving AFTER the 5s fallback, which creates the main window while Express is still down."
    },
    {
      "at": "2026-08-05T12:45:00Z",
      "type": "task.updated",
      "task": "MPI-404",
      "note": "BUILT. Hero models slot renders an unknown dash instead of an unmeasured 0 / 18, gated on the existing hasNoEngine() predicate; absorbed MPI-405 hides the Stage-all-models plate behind the API key. Zero server changes - the models root stays engine-owned as decided. 451 node tests + 17 desktop tests pass, the extended runpod-settings spec has a proven negative control. maturity=validating pending the user's cloud-only first-run look."
    },
    {
      "at": "2026-08-05T12:20:00Z",
      "type": "task.moved",
      "task": "MPI-404",
      "from": "todo",
      "to": "doing",
      "note": "Implementing the decided fix: the hero must not claim a count it cannot have (engine-owned models root), plus the absorbed MPI-405 half (Stage-all-models toggle live with no RunPod key)."
    },
    {
      "at": "2026-08-05T07:20:00Z",
      "type": "task.created",
      "task": "MPI-454",
      "actor": "opus",
      "column": "todo",
      "note": "Third composite front end, requested by the user, placed directly after its blocker MPI-377. It INVERTS the shipped stack: maskComp/paintComp keep the selected entry on top and cut a hole to reveal the slot image underneath, while this puts the slot image ON TOP at its own size, positioned and scaled by a gizmo with the shape gizmo's handles, using its OWN alpha as the cut - no mask, no brush. Headline flow is grab an object from the internet, Remove Background, place it into an existing generation, then detail it to blend. MPI-377 blocks it because that flow starts with a drag-drop into the History workspace, which today silently becomes a prompt chip instead of an entry, so there is no project-file URL for the slot and nothing for Remove Background to run on. Likely NO new server code: POST /project/apply-paint plus compositeOverlay() already flattens an alpha-carrying RGBA layer onto a base, so rasterising the transform into a full-frame scratch layer client-side keeps the transform off the wire. Its ceiling is written into the brief rather than left to be discovered - PAINT_MAX_EDGE 4096 plus fit:fill resamples a small object on an 8K base - with the upgrade path being a placement rect composited by Sharp at source resolution. Placement gets its OWN scratch layer, never paintCanvas, for the reason docs/composite.md already records: the paint layer persists per entry."
    },
    {
      "at": "2026-08-05T03:20:00Z",
      "type": "task.created",
      "task": "MPI-449",
      "actor": "opus",
      "note": "Bench bumped 0.29.2 -> 0.30.2 and H3 nodes verified registered; the open question is feasibility, not wiring. Smallest usable weight set is 53.9 GB against 53.7 GB free on G: and 16 GB VRAM on the 4060 Ti. Engine stays pinned at 0.29.2 until this card returns a go."
    },
    {
      "at": "2026-08-04T21:55:00Z",
      "type": "task.created",
      "task": "MPI-445",
      "actor": "opus",
      "note": "User-reported while validating MPI-436: 8K sources lag the Adjust sliders. The measured ceiling MPI-436 shipped with a ponytail: comment - 1563 ms first move and 64 ms per frame at 4096, against 247/7 at 2048. Filed under MPI-440. The card's own trap is written into it: a fix that only defers the 1563 ms leaves the drag at 15fps."
    },
    {
      "at": "2026-08-04T21:55:00Z",
      "type": "task.moved",
      "task": "MPI-436",
      "from": "doing",
      "to": "done",
      "actor": "opus",
      "note": "User validated in the app 2026-08-04: the three fills read right and the Edge band replacing the scribble is the wanted behaviour, confirmed explicitly. Shipped in e3cab0f5. Answers MPI-440's alpha question for MPI-439. The 4K slider cost the user also reported is carded as MPI-445, not left in this card."
    },
    {
      "at": "2026-08-04T21:40:00Z",
      "type": "task.updated",
      "task": "MPI-444",
      "actor": "opus",
      "note": "Investigated the three options and found a fourth that beats all of them. dist/ is untracked in Cubric-Studio so a private checkout would also need a tsc build; vendoring would fork an interop contract against the documented ownership split. But all three consumers already dynamic-import the SDK inside try/catch and handle its absence by design (brokerBoot.js returns null with an SDK-not-installed comment), so it is an OPTIONAL dependency declared as a hard one. Moving it out of dependencies unblocks npm ci with no secret, no registry and no copy, and may also fix the shipped macOS dangling-symlink bug on MPI-416. Needs testing on a repo copy before committing - a missing file: target may still break lockfile resolution even under optionalDependencies."
    },
    {
      "at": "2026-08-04T21:05:00Z",
      "type": "task.moved",
      "task": "MPI-443",
      "from": "doing",
      "to": "done",
      "actor": "opus",
      "note": "Shipped in 7104a24a. Three desktop specs on a shared launch helper, and npm test + npm run test:desktop are now a mandatory step in mpi-version-bump step 6 with a do-not-approve-on-red note in mpi-release gate 1. Suites 417/0 unit and 17/17 desktop. Every assertion proven able to fail by sabotage-and-revert: the eager mount-time portal restored in MpiDropdown and MpiTreePicker each fail popup-contract independently, a throw in MpiGalleryBlock.setup fails only the gallery sweep test. The open()-count assertion had to be REWRITTEN to be non-vacuous - the MPI-356 subscription is gated on _isOpen, which open() sets last, so a first open can never re-enter and the assertion passed with the guard deleted; it now changes state.availableLoras under an already-open overlay, where removing _rescanning gives 3 and also removing assetService _same() gives 7792. Testing docs split out of DEVELOPMENT.md (over the 200-line rule) into docs/testing.md. CI deliberately deferred to MPI-444 - npm ci cannot resolve the @cubric/connector file: dependency on a lone checkout."
    },
    {
      "at": "2026-08-04T20:45:00Z",
      "type": "task.created",
      "task": "MPI-444",
      "actor": "opus",
      "column": "todo",
      "note": "MPI-443 made npm test + npm run test:desktop a release gate, but the gate is a human step in mpi-version-bump - nothing runs the suites on push. A windows-latest runner can run BOTH today: Electron needs no xvfb on Windows, and Playwright drives it through the electron npm binary, so no npx playwright install step is needed. The one blocker is real and must be decided first: package.json line 28 pins \"@cubric/connector\": \"file:../Cubric-Studio/packages/connector\", a sibling path that does not exist on a lone checkout, so npm ci fails before any test runs. Three ways out, all needing a call from the user: (a) a second actions/checkout of the private MadPonyInteractive/Cubric-Studio repo with a PAT secret, (b) publish @cubric/connector to a registry, (c) vendor it into this repo. Note the existing .github/workflows/build-portable.yml runs NO tests - it only dispatches the private mpi-ci build on a v* tag, so a red suite currently cannot stop a build."
    },
    {
      "at": "2026-08-04T19:57:00Z",
      "type": "task.moved",
      "task": "MPI-443",
      "from": "todo",
      "to": "doing",
      "actor": "opus",
      "note": "Implementation started. Plan approved by the user: launch.js helper, popup-contract.spec.js (primitive contract, reproduces the 8184709b probe), model-settings-popup.spec.js (real surface, also guards the MPI-356 storm), workspace-sweep.spec.js (breadth), then the SKILL.md release gate. CI workflow deliberately deferred - npm ci cannot resolve @cubric/connector (file:../Cubric-Studio/packages/connector) on a lone checkout."
    },
    {
      "at": "2026-08-04T19:57:00Z",
      "type": "task.created",
      "task": "MPI-443",
      "actor": "opus",
      "column": "todo",
      "note": "Shipped 1.3.0 with the LoRA and upscale pickers dead in Model Settings (8184709b: MpiDropdown/MpiTreePicker portalled to body at MOUNT, so a picker built inside a body-mounted MpiOverlay before show() was swept into the overlay stash). Same surface broke once before (MPI-356: overlay in the #tool-container stacking context painting under the picker, plus an exponential open() reopen loop). Nothing runs at release today - release:check is static file consistency, CI dispatches a build, and the 80 green unit tests plus 8 desktop specs are never gated. This card adds desktop smoke specs for that bug class and makes both suites a release gate."
    },
    {
      "at": "2026-08-04T02:55:00Z",
      "type": "task.moved",
      "task": "MPI-368",
      "from": "doing",
      "to": "done",
      "actor": "opus",
      "note": "Shape gizmo SHIPPED and USER-VERIFIED across three rounds: both mounts off one ShapeManager, Shift proportion lock, 7.5 degree rotation snap, and a redrawn rail icon. Docs split rather than trimmed (masking-shapes.md new, masking-adjust.md carved out of masking-tools.md, now 147/200). Suite 374/0, 14 negative controls, a 22-assertion real-pixel probe. Commit 6478a71d."
    },
    {
      "at": "2026-08-04T02:55:00Z",
      "type": "task.moved",
      "task": "MPI-437",
      "from": "doing",
      "to": "done",
      "actor": "opus",
      "note": "Composite fillHoles default flipped to opt-IN and USER-VERIFIED on the original edge-band repro. The MPI-431 twin its sweep missed; a shipped test that asserted the old behaviour was inverted. Commit 55f5a048."
    },
    {
      "at": "2026-08-04T02:35:00Z",
      "type": "task.moved",
      "task": "MPI-437",
      "from": "todo",
      "to": "doing",
      "actor": "opus",
      "note": "Fixed the same session it was carded - the user asked for it if it was quick, and it was a one-line default flip. compositeThroughMask now requires an explicit fillHoles: true. The shipped test that asserted the OLD behaviour was inverted: its stated justification (MaskDetailerPipe contour_fill: true) had already been removed by MPI-431."
    },
    {
      "at": "2026-08-04T02:15:00Z",
      "type": "task.created",
      "task": "MPI-437",
      "actor": "opus",
      "column": "todo",
      "note": "User-reported edge-band composite bug, root cause found the same session: compositeThroughMask defaults fillHoles ON and no caller ever passes it, so a ring mask is filled to a disc. The MPI-431 twin the graph-side sweep missed. Placed BEFORE MPI-373, which keeps this route under both its front ends."
    },
    {
      "at": "2026-08-04T00:50:00Z",
      "type": "task.moved",
      "task": "MPI-368",
      "from": "todo",
      "to": "doing",
      "actor": "opus",
      "note": "Implementation started. Card 4 of the MPI-424 umbrella, unblocked by MPI-375. Compact plan attached; three user decisions front-loaded (paint mount commits Fill/Erase, gizmo stays after a commit, filled only). Verify mode user-ux."
    },
    {
      "at": "2026-08-04T00:40:00Z",
      "type": "task.created",
      "task": "MPI-436",
      "actor": "opus",
      "column": "todo",
      "note": "Adjust for the RGBA paint layer, split out of the MPI-368 planning session. Point MaskManager._morph at the paint layer: shrink is destination-in against the layer, grow fills the new ring in the current colour with the original drawn back on top, and the edge band IS the outline tool. This is WHY MPI-368 ships filled-only - an outline mode on the gizmo would be a second, worse implementation that only outlines shapes."
    },
    {
      "at": "2026-08-01T23:30:00Z",
      "type": "task.created",
      "task": "MPI-425",
      "column": "todo",
      "note": "Created from the MPI-424 brainstorm, and it is the card everything else waits on. Toolbar becomes three groups - Mask (brush, detect, adjust, shapes), Paint (brush, shapes), Composite (mask comp, paint comp) - and Detect becomes a COLLAPSE BUTTON that opens a small vertical strip outside the tool panel with points / text / auto, dismissing itself on an unhovered timer. No corner triangle and no long-press: the user rejected both by name. Deliberately NOT in scope, and this is the simplification that keeps the card small: the three detect modes are NOT collapsed into one mode - they stay maskPoints / maskText / maskDetect in TOOL_OPTIONS_REGISTRY and _MASK_TOOLS, and only their presentation moves. The toolbar already renders grouped stacks, so the only new component is the floating strip. This card reverses a decision recorded in MpiHistoryTools.js ('Shapes joins this group; it does not become a switcher') and must update that comment rather than leave it contradicting the code."
    },
    {
      "at": "2026-08-01T23:30:00Z",
      "type": "task.moved",
      "task": "MPI-382",
      "from": "doing",
      "to": "todo",
      "note": "Back to todo: the DESIGN is complete but it cannot be built until MPI-425 declares the Adjust button. Re-scoped from a shared-strip control to its own tool in the Mask group - the strip could not host it, because Add/Subtract live in MpiMaskDetectRow and exist only on Detect and Points, so a strip slider would have had no commit control on the Brush tool, and Add/Subtract cannot express an edge band anyway since a ring REPLACES the mask. Agreed design: one Grow/Shrink slider, an Edge button that swaps that row for Outward + Inward, live pending-green preview, Apply + Reset, discard on leave. Bake-on-release was rejected by name - dilate-then-erode is a morphological close, so dragging back is not a restore. The alpha brush pack absorbed into this card earlier the same day moves to MPI-375, where the brush engine is extracted."
    },
    {
      "at": "2026-08-01T22:35:00Z",
      "type": "task.moved",
      "task": "MPI-379",
      "from": "doing",
      "to": "done",
      "note": "REJECTED by the user in the MPI-424 brainstorm this card was moved to doing for. The WHOLE card is dropped, not trimmed - including Part 1, the hover-to-select half that needed no download. User's words: what we have right now is fine as it is, and moving the selection onto the canvas is another layer of complexity that does not necessarily need to exist; he had been leaning this way before the session. The thumb strip STAYS, the Detect button STAYS, and Part 2 (COCO YOLO, its R2 upload, the bbox-vs-segm folder trap, its own coarse-mask quality problem) dies with it. Kept as the record rather than deleted: the live measurements on this card and in MPI-380's brief are still true and should be read before anyone re-proposes canvas-side selection - yolov8n-seg enumerates from the segm folder in 0.5s flat regardless of object count, hover CAN name a region via ImpactDecomposeSEGS, class filtering is the labels string only because preset does nothing server-side, and native segm masks are blobby enough that the hover preview would have read as poor output quality. Umbrella consequence: MPI-421 is no longer downstream of this card and gets SIMPLER - with the chip strip and Detect button staying, it is exactly what it says."
    },
    {
      "at": "2026-08-01T22:35:00Z",
      "type": "task.moved",
      "task": "MPI-382",
      "from": "todo",
      "to": "doing",
      "note": "Picked up by the user as step 1 of the MPI-424 umbrella, replacing the rejected MPI-379. Asked what to start with, he named both halves of this card unprompted: the slider that expands and shrinks the current mask, and turning a mask into just its edges with TWO sliders, one expanding outward and one inward - which is the MPI-400 half absorbed into this card earlier the same day. They are one primitive, not two: an edge band is dilate(outward) minus erode(inward), so the blur-plus-alpha-threshold mechanism gets written once and read three ways. Moved to doing for the DESIGN session; no implementation before the design is agreed. Open questions the card already carries: which layer grows (maskCanvas is derived by _recomposite so writing to it is pointless), whether the drag holds live at the 1536 working size, and the compounding trap - every frame must derive from the pristine drag-start snapshot, which UndoStack.pendingLayer already provides."
    },
    {
      "at": "2026-08-01T21:55:00Z",
      "type": "task.created",
      "task": "MPI-424",
      "column": "doing",
      "note": "Canvas-tool umbrella over MPI-379 / 421 / 382 / 368 / 375 / 373 - six cards on one surface (MpiCanvasViewer plus the MPI-381 MpiMaskStrip rail), each one changing what the next is built on. Opened straight in doing because its first step starts now. That step is a BRAINSTORM WITH THE USER on MPI-379, not implementation: the user's position 2026-08-01 is that the real feature is A LOT SIMPLER than the card, which carries three rounds of parked options - a second detection engine (COCO YOLO plus an R2 upload and the segm-vs-bbox folder trap), a per-pixel index map, an unsettled box-vs-blob hover affordance, and a late note that Part 2 may not be needed at all. Whatever the session rules out gets DELETED from MPI-379, not parked. Order and reasoning in tasks/MPI-424/brief.md: 379 first because it may drop the thumb strip and the Detect button and auto-fire detection, which every card below inherits and which directly sets MPI-421's scope; then 421 (cache the dispatches, queue what survives); then 382 (finish the shared strip BEFORE new tools inherit it); then 368 (cheapest tool, pure geometry); then 375 (paint, introduces the RGBA layer); then 373 last, because its erase-to-reveal layer IS that paint layer. This card holds no implementation of its own."
    },
    {
      "at": "2026-08-01T21:55:00Z",
      "type": "task.moved",
      "task": "MPI-379",
      "from": "todo",
      "to": "doing",
      "note": "Moved to doing for a BRAINSTORM SESSION WITH THE USER - step 1 of the MPI-424 umbrella - NOT for implementation. The user's position 2026-08-01: what they want is a lot simpler than what this card proposes. Agenda in tasks/MPI-424/brief.md - the user states their model first, then box-vs-blob on hover, then whether Part 2 (COCO YOLO) survives at all, then whether the thumb strip and Detect button are really dropped on both tools, then auto-fire plus debounce decided with MPI-421 in view, then where the instant green on click comes from. Rule for the session: what the user rules out is deleted from the card, not demoted to another candidate - three rounds of parked options is the defect being fixed."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-405",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-404 and closed as rejected - not built, kept as the record. Both were found by the user in the same first-run pass on the real 1.3.0 portable extract, on the same path (MPI-390 RunPod escape hatch taken, no local engine), and both are the same defect class: a control or a count that asserts state the app has not got. They are read one after the other by a user in that state, so they are fixed and re-checked together."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-366",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-348 and closed as rejected - not built, kept as the record. MPI-366 is an input to this track, not a separate one: it is a Krea2 identity-edit graph that gets read at the same bench, in the same session, and its useful parts feed the swap family this card is authoring. Both also need the user at the node graph and one of them needs the VPN, so they are the same sitting."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-339",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-343 and closed as rejected - not built, kept as the record. Both are research-only 'is this newer or alternative weight worth shipping' questions with the same method - read the source, check the licence flags, weigh size and quality against what we already ship, then decide. They queue behind each other anyway; one card keeps the shortlist in one place instead of leaking a card per candidate weight."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-344",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-349 and closed as rejected - not built, kept as the record. Both are watch/research on the same question - how the app gets a GPU when RunPod cannot give it one. Deploy-when-available, the network-volume availability crunch and a second provider are three answers to that question and get decided together, not one at a time."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-347",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-355 and closed as rejected - not built, kept as the record. Identical mechanism: InpaintCropImproved -> sample -> InpaintStitchImproved. MPI-347 is the App-shaped outcome of exactly the wiring this card does to the edit ops, and the 4K/8K case is the one that proves the wiring is right. Ship the op wiring first, then the App descriptor on top of it - two deliverables, one piece of engineering."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-294",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-420 and closed as rejected - not built, kept as the record. Same subsystem and the same missing table: we switch TAESD previews on in two places and no taesd / vae_approx entry exists anywhere in js/data/modelConstants/. Adding the vae_approx dep family is the work, and the LTX decoder is another file in that same family - one dep-table pass, one download-manager check, both engines."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-412",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-410 and closed as rejected - not built, kept as the record. Both are first-run-only visual defects on the boot into install path, both are cosmetic (nothing fails), and both are reproduced by the same act: a cold first launch that goes on to install an engine. One investigation session covers the pair."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-417",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-416 and closed as rejected - not built, kept as the record. Both are hard stops on a clean Mac between download and first generation, both are found and fixed in the same sitting, and both are verified on the same trip to a Mac. Verification rides MPI-249 (blocked on hardware), but the fixes do not - the symlink half is a build-script change and the CLT half is a detect-and-guide change."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-402",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-421 and closed as rejected - not built, kept as the record. Both cards live in the same call path - _runAutoMaskWorkflow in MpiCanvasViewer.js and runAutoMask in commandExecutor.js. MPI-402 removes most of the dispatches; this card gives the ones that survive a queue lane, a progress signal and a Stop. Do them together or the progress work gets written for runs that should not happen at all. NOT merged and still separate: MPI-403 (engineAssets never staged to the Pod fast disk) is the download/staging subsystem, even though it also makes the first Detect slow."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-401",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-382 and closed as rejected - not built, kept as the record. All three add controls to the SAME shared MpiMaskStrip and all three are pixel operations over the same mask layers - grow/shrink and edge are the same dilate/erode primitive read two ways, and the brush pack is the strip's other half. Shipping them separately means rebuilding the strip three times."
    },
    {
      "at": "2026-08-01T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-400",
      "from": "todo",
      "to": "done",
      "note": "MERGED into MPI-382 and closed as rejected - not built, kept as the record. All three add controls to the SAME shared MpiMaskStrip and all three are pixel operations over the same mask layers - grow/shrink and edge are the same dilate/erode primitive read two ways, and the brush pack is the strip's other half. Shipping them separately means rebuilding the strip three times."
    },
    {
      "at": "2026-08-01T08:55:00Z",
      "type": "task.moved",
      "task": "MPI-423",
      "from": "doing",
      "to": "done",
      "note": "DONE. All three sites now name the real path - mask an object, run Inpaint with the prompt left empty - in models.js:529 (Model Library tile, the live-UI one), releaseNotes.js['1.3.0'] (in-app changelog) and the 2026-07-30-v1.3.0.md mirror. Shipped in ca0eb45e. The changelog judgement call was DECIDED, not assumed: both changelogs were corrected rather than preserved as historical record, because a changelog that renders in the app is live UI and an instruction to press a control that does not exist is a defect wherever it renders; what shipped is kept in git plus an explicit correction note appended to the release doc's Platform Update Checklist. Grep across js/ confirms no user-facing string instructs a Remove control - the only Remove labels left are Remove Background (BiRefNet, unrelated) and slot/point clear buttons. Acceptance item 4 resolved by the user 2026-08-01: the Patreon and Discord 1.3.0 posts have NOT gone out, so nothing downstream repeats the claim; the announcements are carded in the MadPony-Identity repo as its MPI-67 - social is that repo's domain, not this one. Docs site needed nothing (Cubric-Studio-Docs 04a400b already documents the real three-step procedure)."
    },
    {
      "at": "2026-08-01T08:45:00Z",
      "type": "task.moved",
      "task": "MPI-423",
      "from": "todo",
      "to": "doing",
      "note": "Picked up. The urgent site is fixed: models.js klein-4b description now reads 'mask an object, run Inpaint with the prompt left empty' instead of 'hit Remove'. DECISION RECORDED on the changelog judgement call - releaseNotes.js['1.3.0'] and the 2026-07-30-v1.3.0.md mirror were CORRECTED too, not left as historical record: the in-app changelog is live UI every user reads, and a rendered instruction to press a control that does not exist is a defect wherever it renders. What shipped is preserved in git and in an explicit correction note appended to the release doc's Platform Update Checklist. Grep across js/ confirms no other user-facing string instructs a Remove control; the only Remove labels left are Remove Background (BiRefNet, unrelated) and slot/point clear buttons. Remaining: the Patreon and Discord 1.3.0 posts are the one acceptance item an agent cannot check - the release doc now carries the corrected wording and an instruction to fix any post that already went out with the old sentence."
    },
    {
      "at": "2026-08-01T08:40:00Z",
      "type": "task.moved",
      "task": "MPI-391",
      "from": "doing",
      "to": "done",
      "note": "CLOSED on the user's decision. The 1.3.0 sweep ran across all three platforms and shipped; the umbrella holds no work of its own and every unticked line belongs to a card that already owns it - MPI-249 (one model per family, both platforms), MPI-291 (opportunistic, do not force), MPI-420 (TAESD weights), MPI-422 (Windows update path at 1.4.0). Three stale lines healed before closing: the TWO DEFECTS line still read 'neither is carded yet' when (a) is MPI-419 (done) and (b) is MPI-420 (todo); MPI-387 C is now formally accepted as unit-tested-only rather than pending; and the Linux generation line records that it is blocked on hardware the project does not have (the only Linux box is a 2011 i3 that thermally shuts down), confirmed with the user 2026-08-01. macOS generation IS done and was re-validated on build #5 - an earlier summary of mine that implied otherwise was wrong. Standing lesson kept in the checklist: a green checklist is not a validated release; build #4 passed every automated signal on an image that was uniform grey noise, and only opening the file caught it."
    },
    {
      "at": "2026-08-01T06:50:00Z",
      "type": "task.created",
      "task": "MPI-422",
      "column": "todo",
      "note": "Spun out of MPI-387. The NEW Windows in-app updater (win-update.cjs, fix D) is immune to the bug that killed the old one, but is silent by construction - Electron spawns it detached with stdio:'ignore', there is no console any more, and it writes no log file, so a failure quits the app with no window, no message and no evidence (the MPI-369 blindness class on a new path). Second gap: the prompt promises the app will reopen and nothing relaunches. Also carries the two 1.4.0-gated validation items inherited from MPI-387 - a SECOND update from the new layout (the only thing exercising loadExtractZip's resources/app branch + evictBusyFile) and in-app fetch+spawn on the new path."
    },
    {
      "at": "2026-08-01T06:50:00Z",
      "type": "task.moved",
      "task": "MPI-387",
      "from": "doing",
      "to": "done",
      "note": "CLOSED. Every defect this card was raised for is settled on the machine that reported it: A (MAX_PATH) end to end - 6419 files in zip = 6419 on disk with 84 chars of headroom on the dev PC, no Long-Path hint from the LTXVideo pip at a real deep path on the laptop; B (Impact-Pack git+sam2) SEEN FIRING on genuinely git-less hardware - 'requirements filtered for ComfyUI-Impact-Pack on win32: dropped git+.../sam2', zero 'Cannot find command git'; D (SAC) double-click launched straight into the app with VerifiedAndReputablePolicyState=0x1 ENFORCED on an unsigned exe still carrying a 99-byte MOTW, both preconditions established BEFORE the launch so the pass is not vacuous; D transition, a real v1.2.0 install crossed to 1.3.0 via its OWN shipped applier; E release note shipped; F1 absent, F2 present (presence is the pass), F3 confirmed as documented. THE LAST ITEM RAN 2026-08-01 AND FAILED: the in-app fetch+spawn on the 1.2.0-era path. The fetch half passed live for the first time ever (portable check current=1.2.0 latest=1.3.0 -> prompting -> 451,116,582 bytes downloaded byte-exact), then the apply half did nothing. Root cause, proven with three runs: main.js spawns detached with stdio:'ignore', which hands the child a NUL stdout, so update.bat's 'powershell ... > latest-update-path.txt' capture is ALWAYS 0 bytes and update-from-zip.bat is ALWAYS called with an empty argument. Decisive control: the same spawn with a 6 KB download also captured 0 bytes (size innocent), while the identical command under a normal cmd /c console captured 170 bytes and exited 0 (command innocent). Not fixable by shipping - the batch is on the user's disk - and master already replaced the whole path in fix D with win-update.cjs, which is immune by construction (spawnSync stdio ['ignore','pipe','inherit'] + a hard !bundle guard). Mitigation shipped the same day on the user's decision: the 451 MB Windows update bundle was DELETED from the v1.3.0 release (macOS 3.3 MB and Linux 2.8 MB kept - different, curl-based updaters), release notes corrected on GitHub and in docs/releases/2026-07-30-v1.3.0.md, and the trap recorded in docs/releases/portable-distribution-contract.md with a 'never re-introduce a shell-redirect capture into an updater' rule. Windows deltas resume at 1.4.0. NOT closed and deliberately accepted: fix C (failure attribution) was never exercised - three clean installs across two machines produced no fatal failure to describe - and stays unit-tested only. Residuals to MPI-422."
    },
    {
      "at": "2026-08-01T06:30:00Z",
      "type": "task.moved",
      "task": "MPI-369",
      "from": "doing",
      "to": "done",
      "note": "CLOSED - every item settled against the real published 1.3.0 artifacts. Fix 1 (a fatal boot writes no evidence) was user-witnessed live on 2026-07-30: a deliberate throw after the two process.on registrations produced BOTH the 'Cubric Vision failed to start' dialog and a synchronous FATAL line on disk, then was reverted with node --check clean. Fix 2 (the update-only bundle looks launchable) verified at the cut: all THREE update bundles unzip to CubricVision-v1.3.0-update-only, and the ASSET name is still exactly CubricVision-windows-x64-update-v1.3.0.zip, which shipped updaters glob and which must never drift. Fix 3 (stale delta baselines) took three commits and the ORDER is the lesson: 36f972cf restamped to 1.3.0 while 1.3.0 was still being rebuilt, addc03a2 restored the 1.2.0 baselines so the bundles carried real deltas, and eaa6de3c did the final restamp AFTER the GitHub release published. Verified on disk 2026-08-01: all three read fromVersion null / kind portable-stage / toVersion 1.3.0 at 6418 / 6563 / 6383 files. Restamp AFTER publish, never at the cut - a rebuild between the two is the failure mode. Deltas proved healthy in the same build: Linux 209 files / 2.8 MB and macOS 208 / 3.3 MB against the 1226-file 90 MB bundle a stale 1.0.0 baseline used to produce; Windows is 6408 files / 451 MB and that is CORRECT because MPI-387 fix D moved every Windows path to resources/app. Not done, recorded only: renaming the update ASSET to say 'update-only' still needs a dual-upload transition (same bytes under both names for two or three releases) so shipped updaters keep matching."
    },
    {
      "at": "2026-07-30T11:10:00Z",
      "type": "task.moved",
      "task": "MPI-398",
      "from": "doing",
      "to": "done",
      "note": "CLOSED on the user's explicit approval. Fix shipped on the POD, not in the app, and was user-verified live on a fresh Pod - user's words 'it was instant' against the reported 10-15s blank grid. Root cause was found by MEASUREMENT and both of the card's own hypotheses were WRONG: POST /wrapper/models/status fired ZERO times in 62s of idle, so MPI-326's storm has NOT regressed and must not be reopened on this evidence, and EU-RO-1 per-call RTT is fine at ~100ms. The real cause: the Pod wrapper runs a SINGLE asyncio loop that also carries the WS pump and the ComfyUI stdout drain, and TWO handlers did multi-second synchronous filesystem work on it, so each one froze every other request. /wrapper/disk ran `du -sb` over a 150GB network volume as a bare subprocess.run with every concurrent caller spawning a RIVAL walk (1.8s idle -> 32s p50 -> one 125s call that Cloudflare 524'd, /health failing 3 of 4); /wrapper/models/status looped _is_complete_on_disk over 100+ deps in one POST (~10s, dragging /wrapper/stats from 94ms to 8693ms). Both are MPI-191's /wrapper/stats fix applied to the endpoints it missed - asyncio.to_thread, plus single-flight behind a lock and a 60s TTL cache for disk, invalidated by _manifest_record_model and models_delete. models/status is deliberately NOT cached: install-state feeds the whole download UI and must stay truthful. Wrapper 0.2.38 -> 0.2.40, mpi-ci e760604, Vision 77d34ea0. Cold-open numbers: /health p50 5013ms with 3 of 4 ERR -> 141ms 30/30 OK; /wrapper/manifest 13714ms -> 282ms; /wrapper/disk p50 32294ms max 125072ms -> 371ms; models/status 8679ms -> 2259ms. LIVE GATE THAT OUTLIVES THIS CARD, not tracked as a card: dev is 0.2.40 and stable is still 0.2.38, so a 1.3 release without publish-runtime.sh promote ships released users the OLD runtime and loses this fix - mpi-release's pre-flight owns that check. /wrapper/ls has the IDENTICAL blocking pattern and is worse than either fixed handler, but it has ZERO consumers in routes/ or js/ - manual diagnostic only, left alone deliberately. Full record in tasks/MPI-398/validation.md."
    },
    {
      "at": "2026-07-30T01:40:00Z",
      "type": "task.moved",
      "task": "MPI-398",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started as a MEASUREMENT, not a code read. No renderer file will be opened and the MpiTileSheet bisect stays cancelled - the user's local-vs-remote discriminator already refuted the tile hypothesis. Instrument once at the ONE boundary that answers both questions: a temporary probe (routes/_probe398.js, mounted with one line in server.js, reverted after) that counts every LOCAL route hit and wraps globalThis.fetch to record every OUTBOUND wrapper call with its round-trip ms. That single hook catches /wrapper/models/status (via remoteModels.wrapperFetch) AND the telemetry GETs /wrapper/stats + /wrapper/disk (which use raw fetch in remotePodLifecycle.js, so a wrapperFetch-only counter would have missed them). Yields requests-per-minute per path and per-call latency from the same sample, which is exactly the volume-vs-latency split the card needs. Pre-state measured before the move: GET /remote/mode reads active:false, so nothing is connected yet and the probe goes in before the connect - routes/ is main-process, so it needs a FULL app restart, and doing it now costs the user one connect instead of two."
    },
    {
      "at": "2026-07-30T00:55:00Z",
      "type": "task.moved",
      "task": "MPI-396",
      "from": "doing",
      "to": "done",
      "note": "LIVE-PROVEN on the local engine, which is the last thing the card was holding for. Two rounds. Round 1 hit the precondition the vacuous first attempt missed - scripted poll of /comfy/downloads/status fired the uninstall sub-millisecond after the job read complete WHILE STILL LISTED - and passed, but was still not attributable: the job's dep was on disk, so a 15s reconciler tick landing in that 1.3s window could have pruned it via confirmedInstalled (reconciler.js:139 builds that set from the JOB's deps, not the model universe). Round 2 closed it by deleting the dep out of band once the job settled, which makes confirmedInstalled impossible to form: the immortal done job then sat in the store for 25 seconds with version FROZEN at 21093 - defect 3, the self-idling reconciler poll, observed live instead of inferred - and vanished inside the uninstall POST with exactly one version bump. With the file absent no prune path existed, so store.dropModel is the only possible cause. Target was nvidia-pid / vae-sd3 (168MB, single-owner so the shared-dep guard could not spare it, absent beforehand so the run restored the disk to its starting state - no user weights touched). Backend log confirms the LOCAL leg ran (moved to trash G:\\CubricModels\\vae\\sd3_vae.safetensors). Remote leg rides the engine-split guard test plus the round-2 store evidence; its one live confirmation is folded into the MPI-385 Pod-session brief per the standing rule rather than holding this card open."
    },
    {
      "at": "2026-07-29T20:05:00Z",
      "type": "task.moved",
      "task": "MPI-394",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED LIVE and approved for done, all five acceptance items, on a remote CPU Pod - the harshest path, since a remote connect also fires renderList({force:true}) + awaitReSync(). Install of SDXL Realistic 9.0GB to completion: 4/14 -> 5/13, volume 139.4 -> 149GB, model moved Available -> Installed, every other tile kept its picture; that is the exact download:complete -> awaitReSync -> renderList path that used to blank for ~20s. Uninstall did the same and the card returned to Available. LTX 2.3 video tile painted throughout both rebuilds, having previously been last every time. Toast read 'SDXL Realistic uninstalled.', not 'updated'. User words: no flickering and stopping with empty cards anymore. Root cause was TWO mechanisms: renderList destroys every loaded preview, AND the lazy <img> replacements have no pixels and defer their load behind a main thread pegged by _listSignature (O(n^2) _sharedOwnedDepIds per model) plus reSyncInstalledModels. Fix keeps the ELEMENTS alive via a consumer-owned previewCache Map on MpiTileSheet, re-parenting instead of rebuilding - covers every rebuild path, not just the two reported. A keyed whole-grid diff was rejected: section moves cross sheet INSTANCES. Card also corrected two claims: FIVE models are video not one, and LTX is worst because its mp4 is 39.8MB so preload=metadata must pull a moov atom - fixed with 5 ffmpeg .webp posters by filename convention. Shipped d76b60c8. Suite 289/289 at commit time (293/293 after MPI-395), negative control 0/3 unfixed -> 3/3 fixed. Noted, pre-existing, NOT from this work: the uninstall toast fires while the count and tile still read installed because reSyncInstalledModels re-stats over the network after ~9GB of deletion; the card does then move correctly, so it is latency, not a stuck state - it was simply hidden inside the 20s of blank tiles before. Uncarded by the user's call."
    },
    {
      "at": "2026-07-29T19:40:00Z",
      "type": "task.updated",
      "task": "MPI-394",
      "note": "USER-VERIFIED LIVE - all five acceptance items, on a remote CPU Pod (the harshest case: a remote connect also fires renderList({force:true}) + awaitReSync()). Install of SDXL Realistic 9.0GB ran to completion: 4 installed/14 available -> 5/13, volume 139.4 -> 149GB, model moved Available -> Installed, every other tile kept its picture - that is the exact download:complete -> awaitReSync -> renderList path that used to blank for ~20s. Uninstall did the same and the card then moved back to Available. LTX 2.3 video tile painted its frame throughout both rebuilds, having previously been last to repaint every time. Toast read 'SDXL Realistic uninstalled.', not 'updated'. User words: no flickering and stopping with empty cards anymore. Sibling tiles showing a bar during the install were checked and are the MPI-258 shared-dep partial clearing on re-sync, not a regression - the user identified it as expected before it was raised. Noted and NOT from this work: the uninstall toast fires on download:uninstalled while the count and tile still read installed, because reSyncInstalledModels re-stats the dep universe over the network after ~9GB of deletion; the card did move correctly afterwards, so it is latency, not a stuck state - it was simply hidden inside the 20s of blank tiles before. Uncarded pending the user's call."
    },
    {
      "at": "2026-07-29T19:40:00Z",
      "type": "task.updated",
      "task": "MPI-395",
      "note": "USER-VERIFIED LIVE on the two headline items, after a renderer reload (both fixes are renderer files; the reload also reset shell.js _didFirstConnectDriftCheck, so the reconnect genuinely re-ran the engine-asset heal - the wedge condition). (1) No engine:assets toast on the reconnect, and the heal definitely ran, so that is the suppression working rather than the heal being skipped. (2) The install started immediately - SDXL Realistic went straight to a live bar at 243.5 MB/s, 0.9/9.0GB, footer on Cancel, and ran to completion; pre-fix the same click parked on Queued with no server-side job at all. THREE RESIDUALS still unexercised live: the legitimate queue (second install must read Queued then start - now cheap, the uninstall freed ~9GB), cancel-while-queued (the new done.cancel() path, the one piece of the fix with no live coverage), and the OS notification when unfocused. Card stays doing/validating on those."
    },
    {
      "at": "2026-07-29T19:05:00Z",
      "type": "task.created",
      "task": "MPI-395",
      "to": "doing",
      "note": "Found live 2026-07-29 while validating MPI-394 on a fresh CPU Pod: the user pressed Install on SDXL NSFW and the tile sat on 'Queued...' with nothing else installing. Proven from the live app: /comfy/downloads/status held NO sdxl-nsfw job at all (only engine:assets and klein-4b, both complete) and /comfy/downloads/active returned {models:[],engine:false} - so the POST never fired. Installs are serialised through a client-side promise chain gated on _inFlight (downloadService.js:76, MPI-184); 'queued' is only set when _inFlight > 0, so something held the chain. The only other start() in the session was engine:assets (drift heal logged nothing, no generation ran, App Library never opened) - so its chain link had not settled DESPITE download:complete having fired, which the completion toast proves. That is only possible if the listener was armed after the event. Root: run() armed _awaitDownloadDone INSIDE the post-POST .then(), but routes/downloadManager.js calls _startPendingDeps() BEFORE res.json() (register-before-respond, G8) and a fully-satisfied install goes terminal right there - 0 queued deps, nothing to download. The terminal broadcast therefore beats a listener armed after await res.json(). _awaitDownloadDone then burns its full 30-MINUTE ceiling with _inFlight pinned at 1, and every install clicked in that window sits at 'queued' forever - with no revert timer, because downloadService.js:90 arms one only for 'pending'. SHIPS TO USERS: _installRemoteEngineAssets re-runs on the first connect of EVERY session and is a pure no-op once the volume already holds the weights, so the common case (any session after the first) is the one that wedges, and the victim is the first model install after connecting to a Pod. NOT caused by the earlier volume-full session; a fresh volume would HIDE it, because a real 1.76GB download always lands after the listener is armed. Second, cosmetic half of the same trigger: notificationService announced 'engine:assets installed.' - a raw internal job id - as a toast and, unfocused, an OS notification, on every connect, even though shell.js states the heal must run silently."
    },
    {
      "at": "2026-07-29T18:20:00Z",
      "type": "task.updated",
      "task": "MPI-394",
      "note": "Code complete, awaiting the user in-app check (verify mode user-ux). Root cause was TWO mechanisms, not one: renderList() destroys every loaded preview, AND MpiTileSheet rebuilt each thumb as loading=\"lazy\", so the replacement had no pixels and its load was deferred until after layout - behind a main thread busy with _listSignature (O(n^2) _sharedOwnedDepIds per model) plus the reSyncInstalledModels that triggered the render. That is the ~20s. Fix keeps the preview ELEMENTS alive: MpiTileSheet takes a consumer-owned previewCache Map, builds each thumb once per id and re-parents it, so an already-decoded element paints in the same frame; MpiModelManager owns the Map because the sheets are re-created every render. Covers every rebuild path (install, uninstall, filter, search, draft toggle, engine switch), not just the two reported. Rejected the literal per-tile diff: section moves cross sheet INSTANCES, so an honest diff means persisting 4 sheets plus headers plus per-tile reconcile inside a Primitive shared by 3 surfaces - much larger blast radius, same visible result. Card was also wrong on one detail: FIVE models are video, not one, and LTX high is worst because its mp4 is 39.8MB, so preload=metadata must pull a moov atom before showing any frame - fixed with 5 ffmpeg-generated .webp posters wired by filename convention. Toast now says uninstalled for a whole model/plugin and keeps updated for the op/arch-removal half of an Update, via a _wholeUninstalls marker the handler consumes. Suite 289/289 (baseline 286), eslint clean, and the new guard is negative-controlled: 0/3 with both sources stashed, 3/3 restored. NOT verified: the paint itself - no jsdom in this suite, so the tests pin the wiring, not the pixels."
    },
    {
      "at": "2026-07-29T18:06:00Z",
      "type": "task.moved",
      "task": "MPI-394",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started. Root cause traced before any edit and it is NOT the rebuild alone: renderList() wipes bodySlot, which DESTROYS every loaded preview element, and MpiTileSheet rebuilds them as loading=\"lazy\" <img>, so the replacements have no pixels and their load is deferred until after layout - behind a main thread pegged by _listSignature (O(n^2) _sharedOwnedDepIds per model) plus reSyncInstalledModels. That is the ~20s of blank tiles. LTX is worst for a second reason the card did not have: ltx23_high_preview.mp4 is 39.8MB, so preload=metadata must pull a moov atom out of it (5 models are video, not 1). Fix keeps the preview ELEMENTS alive across rebuilds: MpiTileSheet takes an optional previewCache Map, builds thumb media once per id and re-parents it, so an already-decoded element paints in the same frame and the grid never blanks. Covers every rebuild path (install, uninstall, filters, search, draft toggle, engine switch), not just the two reported. Rejected a keyed whole-grid diff: section moves cross sheet INSTANCES, so an honest diff means persisting 4 sheets + headers + per-tile reconcile inside a Primitive shared by 3 surfaces - far more blast radius for the same visible result. Also in scope: generated .webp posters for the 5 video previews, and the uninstall toast wording."
    },
    {
      "at": "2026-07-30T00:45:00Z",
      "type": "task.moved",
      "task": "MPI-388",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED live, all four acceptance cases: empty box on Gallery re-entry reads t2i, media present leaves the op alone, the remembered pick returns when media comes back, and delete-last-image inside the Gallery still drops to t2i with no MPI-337 detail->upscale return. Shipped as ONE shared decision rather than a second fork: pickTextOnlyOp + isTextOnlyOp lifted into commandRegistry, MPI-356's branch body extracted to _dropToTextOp(), and el.dropToTextOpIfEmpty() exposed for a workspace to fire on ENTRY. Gallery calls it after _wirePromptBox and after the count sync - both deliberate, the first so operation-change reaches the block's activeOperation, the second because the box's live counts are the only truthful read once slot-id mismatch and capacity eviction have had their say. Reuse path deliberately excluded, a reused op is authoritative. Tests: new gallery-entry-text-op.test.cjs 4/4, full suite 269/278 with the SAME 9 known pre-existing failures and no new ones, eslint clean. NO CHANGELOG ENTRY, user's call and correct: MPI-356 shipped after 1.2.0, so this regression never reached a user. docs/generation-lifecycle.md gained the no-force-DOWN rule and its two named exceptions."
    },
    {
      "at": "2026-07-30T00:20:00Z",
      "type": "task.moved",
      "task": "MPI-388",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started, user scheduled it INTO 1.3. Root cause found before any edit and it is NOT History-specific: MpiGalleryBlock seeds the op from getSelectedOp(activeModelId) - the MPI-247 per-model memory - with no regard for media, so any user pick of a media-hungry op (in History OR in the Gallery before deleting the image) is restored onto an empty box on the next Gallery mount. MPI-356's drop-to-text-op is gated on hadMedia, a TRANSITION, which is exactly why mount never fires it. Fix runs POST-mount, not pre-mount: the box's live imageCount/videoCount are ground truth, because a pre-mount read of state.promptMedia.gallery can disagree once slot-id mismatch and capacity/role eviction have had their say. Firing after _wirePromptBox also means operation-change is heard, so the block's activeOperation and the op strip stay in sync instead of drifting. programmatic: true keeps s_selectedOpByModel intact so the user's real pick returns when media comes back. Scoped to the Gallery block, so History and its filterNoInputOps mode are untouched by construction. Zero overlap with MPI-387 (main.js / install / scripts)."
    },
    {
      "at": "2026-07-29T18:00:00Z",
      "type": "task.created",
      "task": "MPI-384",
      "to": "todo",
      "note": "Text masking - the 4th rail tool, opened at the TOP of todo. Raised by the user while verifying MPI-380 and proven by them live in the node graph before carding: text 'bikini:2' with individual_masks true returned two clean per-object masks, including the thin side-straps the Points tool fights with. Points is precise but needs MORE dots than expected, especially NEGATIVE ones, or SAM3 over-selects; thin strappy subjects are its weak spot and a face can take six dots. Naming the object fixes exactly that. This does NOT revive the cancelled MPI-361 Phase B - that was killed as a HOVER ENUMERATOR and that verdict stands (4 categories cost 10.5s, so YOLO keeps hover in MPI-379); a deliberate type-it-and-pick tool at ~3.6s on an explicit press is a different job. Cheap because MPI-380 already landed the groundwork: the SAM3 CheckpointLoaderSimple is in the graph and its CLIP output is unwired and free, the weight is in R2, and the MPI-381 split makes a rail entry a one-line registry addition. Four traps recorded in brief.md, the sharpest being that the count MUST reach the node as name:N - a bare category silently returns ONE detection - and that the text param must target an MpiText node, never MpiString, which is in PATH_MEDIA_CLASSES and would be media-staged and break the remote engine."
    },
    {
      "at": "2026-07-29T18:00:00Z",
      "type": "task.updated",
      "task": "MPI-380",
      "note": "LOCAL RUN USER-VERIFIED - user words: everything here seems to be working fine, and this point system is more precise. Scope dial gone, dots place and remove, right-click negatives work, Add accumulates. Card set to doing/validating; the remote Pod leg is DEFERRED by the user until the mask feature set lands. Characterised but NOT a defect: SAM3 needs more points than expected, especially negative ones, or it over-selects - thin strappy subjects are the weak spot. That weakness motivated MPI-384. Pod research done ahead of the deferred leg: NO image rebuild is needed to test, because SAM3_Detect is core ComfyUI 0.28 baked at image build and the dev image v0.17.0-dev-cu130 is already on 0.28 (MPI-341 validation digest-verified comfyui_version.py). The weight is a normal R2 dep. Released users are separate - stable is its own tag and MPI-342 deliberately left stable pins untouched, so the 0.28 image must be promoted before this reaches them. Still unverified for the Pod: that the REMOTE install path picks up a newly added engineAsset, which is the likeliest failure and should be checked first. Mac and Linux CLEAR: nodes_sam3.py imports only torch, comfy, folder_paths, comfy_api and av - no onnxruntime, so this is NOT the MPI-370 class of breakage."
    },
    {
      "at": "2026-07-29T16:00:00Z",
      "type": "task.moved",
      "task": "MPI-380",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started. SCOPE CUT BY THE USER TO THE POINTS PATH after a blocker found in orientation: SAM3 cannot replace the SAM 1 refiner by rewiring. SAM 1 sits in ImpactSimpleDetectorSEGS.sam_model_opt (node 1588, the segment branch) and that slot wants an Impact SAM_MODEL, while SAM3 box prompts want BOUNDING_BOX dicts - and nothing in the graph emits those from YOLO SEGS, since MpiBboxToMask takes Impact BBOX, a different type. A refiner swap therefore needs a NEW MpiNodes node plus an mpi-ci release plus a node_lock bump before it could even be tested remotely. So: points path only, SAM 1 is KEPT for segment refine, and both weights ship as engineAsset (user chose keep-both over retire). The refiner swap moves to MPI-379, which rebuilds YOLO enumeration and will produce boxes natively. The acceptance item about proving SAM3 at 4K before retiring SAM 1 relaxes to a sanity check, because nothing is being retired. Confirmed against nodes_sam3.py in our own 0.28 engine: SAM3_Detect positive_coords and negative_coords are force_input STRING so they must be WIRED, and MpiString is the injection target - bare-title injection writes its string field, precedent Input_Mask in klein_t2i.json. Point path ignores threshold entirely, only refine_iterations applies, so the Scope dial is DELETED not remapped. Compact plan attached."
    },
    {
      "at": "2026-07-28T10:00:00Z",
      "type": "task.created",
      "task": "MPI-383",
      "to": "doing",
      "note": "Crop past the image edge - the Photoshop behaviour, aimed at outpainting and reusable by Apps. The box may leave the image on every side; the new pixels take a colour from an MpiColorPicker (same primitive as the Resize pad and Remove Background) and that colour is what the user asks an edit model to fill. Snapping to the image edges kills the accidental 1-2px border, and in ratio mode it adjusts the SCALE so the ratio survives. Third resolution type RESOLUTION takes an exact WxH, seeds the box at exactly those pixels, and is the ONLY type that resamples. User decided against auto-masking the new area. Opened straight in doing."
    },
    {
      "at": "2026-07-29T15:00:00Z",
      "type": "task.updated",
      "task": "MPI-376",
      "note": "USER RESTEER - masks are now a FIRST-CLASS driver of undo, not the conditional half, and the card moved up to sit between MPI-382 and MPI-379. The original reasoning (a mask is cheap to redo, so paint drives) has expired: the family is three tools with a fourth coming, Add and Subtract BAKE irreversibly into manualCanvas and subtractCanvas, and MPI-382 will rewrite the whole layer in one gesture. Card rewritten - maturity idea to planned, acceptance regrown to 9 items. New requirements: enumerate the undoable units from the code first (stroke, Clear, Add/Subtract bake, points commit, grow release) because a mutation that bypasses the stack is a silent hole; undo must restore the MPI-371 layer ORDER not just pixels; history must survive a tool swap since the mask does; paint (MPI-375) plugs into the SAME stack. The stop-and-brief rule is kept and sharpened."
    },
    {
      "at": "2026-07-29T15:00:00Z",
      "type": "task.created",
      "task": "MPI-382",
      "to": "todo",
      "note": "Grow / Shrink mask slider, user request, sequenced right after MPI-380. Feasibility carded but NOT proven live: blur plus an alpha threshold is a morphological dilate or erode, so ctx.filter = blur(Npx) followed by an SVG feFuncA discrete threshold should hold a live drag at the 1536 working size - contrast() cannot do the threshold because it acts on colour and our layers are white-on-transparent. Sharp exists server-side (the MPI-362 feather) but is a round trip per drag, so it is the fallback, not the default. Two traps recorded: COMPOUNDING (every frame must derive from a drag-start snapshot, or grow 3 applies three times - the MPI-351 double-scale class of bug), and WHICH LAYER grows, since maskCanvas is derived by _recomposite() and a grow baked into manual must not resurrect an erased region."
    },
    {
      "at": "2026-07-29T14:30:00Z",
      "type": "task.moved",
      "task": "MPI-381",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED live. Mask family is now three tools with one job each - Brush (new organism, no CSS, it IS the strip with its brush pair), Points unchanged, Detect stripped of its brush - plus the black-and-white mask view between invert and trash. The split is real, not cosmetic: brush:false now forwards to a new setMaskPaintEnabled(), so a brushless tool pans on drag and zooms on wheel instead of painting with no visible brush control, and the flag is held on the VIEWER because a canvas rebuild would restore the manager default true and silently re-arm it. B/W pins alpha to 1 over a flat backdrop, composes with the display-only invert (backdrop and mask swap together), keeps the green auto layer on top and makes the opacity slider inert. Guard shipped: tests/mask-tool-registry.test.cjs, negative-control proven in both directions - a rail mask tool missing from _MASK_TOOLS or from TOOL_OPTIONS_REGISTRY now fails a test instead of failing silently. docs/masking.md re-trimmed to exactly 200 lines: cancelled MPI-361 Phase B roadmap entry deleted, stale Hair detect model corrected to the shipped Face / Hand / Person. Shapes joins as the 4th rail entry when MPI-368 ships - that card still owns the whole gizmo."
    },
    {
      "at": "2026-07-29T13:10:00Z",
      "type": "task.moved",
      "task": "MPI-381",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started, first card of the agreed 381 -> 380 -> 379 masking order. Scope resolved from the board: MPI-368 still owns the whole shapes gizmo (handles, rotation, rasterisers), so this card ships THREE tools - Brush / Points / Detect - plus the black-and-white mask view, and Shapes slots in as the 4th when 368 ships. Compact plan attached."
    },
    {
      "at": "2026-07-28T13:38:00Z",
      "type": "task.created",
      "task": "MPI-377",
      "to": "todo",
      "note": "Media dropped into the History workspace produces no entry and no canvas load - it silently becomes a prompt chip, discovered only when the dropped image comes out of the latents on the next run. Root cause located while carding: the drop handler in MpiGroupHistoryBlock.js only calls injectMedia and never appends to history. Chip staging is load-bearing in video mode, so the entry must be added without stripping the chip globally."
    },
    {
      "at": "2026-07-28T13:36:00Z",
      "type": "task.created",
      "task": "MPI-376",
      "to": "todo",
      "note": "No undo exists anywhere on the canvas - a grep returns comments only. Tolerable for masks, fatal for paint, so MPI-375 drives it. Command stack on MpiCanvas, one stroke per command, Ctrl+Z via the hotkey registry, dirty-rect patches rather than 9MB full-layer snapshots. Masking undo is CONDITIONAL: only if it is not a MaskManager refactor, otherwise split it rather than half-wire it."
    },
    {
      "at": "2026-07-28T13:34:00Z",
      "type": "task.created",
      "task": "MPI-375",
      "to": "todo",
      "note": "New paint tool for History image mode: brush, eraser with the existing opacity slider, colour picker, Clear paint, Apply paint. A scribble is a shape reference for the models - paint a rough shape, mask it, detail it. Reuses the MpiMaskStrip from MPI-371; new part is an RGBA paint layer independent of the binary mask layers, since masking the paint afterwards is the whole point."
    },
    {
      "at": "2026-07-28T13:32:00Z",
      "type": "task.created",
      "task": "MPI-374",
      "to": "todo",
      "note": "UI size set with Ctrl+wheel or Ctrl+plus / Ctrl+minus is thrown away on every restart - applyUiZoom reads the factor live from webFrame and never stores it. Persist through js/core/storage.js with a key in storageKeys.js, restore early enough to avoid a resize flash, clamp on read, no-op in Browser Mode."
    },
    {
      "at": "2026-07-28T13:30:00Z",
      "type": "task.created",
      "task": "MPI-373",
      "to": "todo",
      "note": "User redesign of the MPI-362 composite after living with it: instead of a blind Add/Subtract modal gated on one entry already carrying a mask, selecting two entries should overlay the first over the second on the canvas with a brush/eraser pair - the eraser removes the top image to reveal the one under it, the brush paints it back. Delivered as a NEW composite tool in the rail, sibling of the MPI-371 mask family. If the erase layer IS a mask, Apply reuses the existing full-res server route untouched and the only new code is the live two-image preview."
    },
    {
      "at": "2026-07-28T11:30:00Z",
      "type": "task.created",
      "task": "MPI-372",
      "to": "todo",
      "note": "PromptBox must stay reachable while a mask tool is active. Today prompt is a RAIL TOOL, so masking and adjusting generation settings are mutually exclusive modes - mask, switch to prompt, tweak a setting, switch back. With the mask family now at two tools and Shapes + Text coming, that round trip is the dominant annoyance of the whole masking flow. User words: extremely annoying."
    },
    {
      "at": "2026-07-28T11:20:00Z",
      "type": "task.moved",
      "task": "MPI-371",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED LIVE. Mask surface is now a tool family: MpiToolOptionsMaskDetect + MpiToolOptionsMaskPoints, each mounting the shared MpiMaskDetectRow and MpiMaskStrip (brush pair optional, hidden on Points). MpiToolOptionsMask deleted. One mask toolKey keeps settings across swaps. Both traps proven headlessly: Points destroy() restores right-click, and no destroy() clears the paint layers. A defect the user found while verifying turned out to be an MPI-361 layer-ORDER bug - auto picks now union LAST so a detection beats an older erase - fixed in 3 sites, 12/12 checks with a negative control, and also user-verified."
    },
    {
      "at": "2026-07-28T11:00:00Z",
      "type": "task.moved",
      "task": "MPI-367",
      "from": "todo",
      "to": "doing",
      "note": "Content pass starts with the detail op. User correction: detail is LOCALISED img2img, not just a refiner - the denoise slider is the whole story. Low denoise refines what is under the mask; above ~0.50 it behaves like inpaint, so a user can paint an empty area and ask for something that is not there yet, or remove something by naming what should be there instead. The shipped copy claims detail only improves what is already there, which undersells the op."
    },
    {
      "at": "2026-07-28T08:30:00Z",
      "type": "task.created",
      "task": "MPI-371",
      "to": "todo",
      "note": "Mask tool family - split Points / Detect / Shapes / Text into real sibling tools and extract the shared bottom strip (brush, eraser, invert, clear, opacity) into ONE component. User direction after MPI-361 Phase A verified. Traps carried in the brief: destroy() must restore the image context menu via setMaskPointsMode(false), and a tool swap must not wipe the manual + subtract layers. Two open questions for the user first - rail shape and whether per-tool settings split off the single 'mask' toolKey."
    },
    {
      "at": "2026-07-28T08:30:00Z",
      "type": "task.updated",
      "task": "MPI-361",
      "note": "Detection colour FIXED and USER-VERIFIED. Root was structural: _recomposite() unioned manual + auto picks into ONE maskCanvas so a detected region was indistinguishable from a painted one. Split the DISPLAY, never the export - MaskManager gained autoCanvas (auto picks AND NOT subtract, skipped entirely when nothing is selected so the brush hot path is unaffected) and MpiCanvas draws it as a second pass tinted --accent-ok green through a new shared _recolorMaskLayer() that also absorbed the old displayInverted scratch buffer. getURL() / getMaskDataURL() still emit the single unioned B/W mask. Add / Subtract un-gated so a YOLO detection gets the same green-then-commit flow. Attention cleared; card stays doing/validating for the docs home + Phase B."
    },
    {
      "at": "2026-07-28T00:15:00Z",
      "type": "task.created",
      "task": "MPI-366",
      "to": "todo",
      "note": "Investigate a shared community Krea2 ID-edit character-dataset workflow (tagger + optional RTX 50xx attention patch) as an App candidate. Source needs the user's VPN; nothing fetched or tested yet."
    },
    {
      "at": "2026-07-28T00:05:00Z",
      "type": "task.moved",
      "task": "MPI-364",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED. Picker opens with the active model's tile centred vertically."
    },
    {
      "at": "2026-07-27T23:45:00Z",
      "type": "task.moved",
      "task": "MPI-364",
      "from": "todo",
      "to": "doing",
      "note": "Implementing: centre the active model tile in the picker body on open."
    },
    {
      "at": "2026-07-27T23:35:00Z",
      "type": "task.created",
      "task": "MPI-364",
      "to": "todo",
      "note": "Model picker should scroll the currently-selected model into view and centre it when opened."
    },
    {
      "at": "2026-07-27T23:20:00Z",
      "type": "task.moved",
      "task": "MPI-356",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED live. Handoff 5d9ca59a queue closed: MpiModelSettings reopen loop fixed at both causes (loadAll fake state:changed feeding open()s own listener; tool-container mount under a body-mounted picker), cogwheel moved back after the model selector and restyled secondary, empty-box auto-t2i with an op-memory negative control, docs/op-model-selection.md + component contracts + 1.2.0 release notes. LoRA & Upscale now reads as a button. Remaining inpaint workflow belongs to the Klein card."
    },
    {
      "at": "2026-07-27T23:05:00Z",
      "type": "task.moved",
      "task": "MPI-362",
      "from": "doing",
      "to": "done",
      "note": "Mask composite USER-VERIFIED live both directions, hole fill matching MaskDetailerPipe contour_fill, feathered edge. Tests 3/3, route smoke-verified, lint clean."
    },
    {
      "at": "2026-07-27T23:00:00Z",
      "type": "task.moved",
      "task": "MPI-363",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED LIVE. Alt+drag a gallery card now drops a real file into any app (tested: Discord upload). main.js gained ipcMain.on(drag-out-files) -> webContents.startDrag; MpiGalleryGrid gained _tryNativeDragOut() on both card dragstart handlers. Plain drag unchanged. Docs: docs/gallery.md Card drag section + UNRELEASED.md whatIsNew entry."
    },
    {
      "at": "2026-07-27T22:10:00Z",
      "type": "task.created",
      "task": "MPI-363",
      "to": "doing",
      "note": "Alt+drag a gallery card = real OS file drag via Electron startDrag. Card drag-out only works into Explorer/Finder because MPI-318 uses Chromium DownloadURL, a VIRTUAL-FILE PROMISE; Discord/Photoshop/upload zones read CF_HDROP and see nothing. startDrag REPLACES the HTML5 drag so it cannot share the plain-drag gesture with the in-app application/mpi-media drop - hence a second gesture, Alt. Multi-select Alt+drag exports every selected file, which DownloadURL could not do."
    },
    {
      "at": "2026-07-26T00:45:00Z",
      "type": "task.validated",
      "task": "MPI-350",
      "note": "TIER-1 BRANCH NOW VERIFIED LIVE - both sides of the MpiIfElse gate confirmed. Non-turbo upscale of t2i_038 (768x1344) produced upscale_018 at 1152x2016 (correct 1.5x) with stages 1/3 -> 2/3 -> 3/3 (2 tiles + refiner). This is the first run to take the 'is high tier' TRUE path: Input_Tier 1 -> MpiMath 'a == 1' true -> node 1685 FromBasicPipe base model, no accelerator LoRA on the UltimateSDUpscale tiles, refiner still distilled. Every earlier upscale sidecar recorded Input_Tier 2, so only the false path had been exercised until now. Tile stages were visibly slower than the turbo runs while the refiner stage stayed quick - the expected asymmetry of the gate. User also ran a side-by-side compare of upscale_017 (turbo) vs upscale_018 (non-turbo) and accepted both. Card was already done/complete; this closes the last unexercised path in the template rewire."
    },
    {
      "at": "2026-07-26T00:30:00Z",
      "type": "task.moved",
      "task": "MPI-350",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED LIVE, both halves. Progress: grid off read 1/2 during the tile then 2/2 on the refiner; grid on with 2 tiles read 2/3 then 3/3 (load bar does not register as a stage, so _tileOffset is 0 - 2 tiles + 1 refiner = 3). The earlier '9/9 twice' report was NOT a defect: that run had total_tiles=8 per logs/app.log, so 8 tiles + refiner = 9, and the two 9/9 sightings were the refiner's two steps (0/2 at 0:59, 1/2 at 1:17, matching log timestamps 21:51:23 and 21:51:38). It reads like a stall because the refiner is 2 steps at ~15s each on the full frame. That exact logged sequence is now replayed as a test case. Upscale quality also confirmed by the user: refiner makes raw usable and turbo much better. Suite tests/tile-post-pass-stage.test.cjs 7/7; negative controls 5/6 fail vs unfixed HEAD and 5/7 fail vs the first cut. Touched: js/services/phaseProgress.js, js/data/progressStages.js, js/services/commandExecutor.js, plus the synced workflow cards. Generated workflow files remain STAGED for mpi-end to commit."
    },
    {
      "at": "2026-07-26T00:30:00Z",
      "type": "task.progress",
      "task": "MPI-351",
      "note": "NEGATIVE CONTROL - did NOT reproduce on a clean run, which kills the pre-scale theory outright. Fresh source t2i_038 (768x1344), factor 1.5, Use Grid on: the staged asset handed to the graph measured 768x1344 (its TRUE size, hash 9d52e898...) and the output was 1152x2016 - exactly 1.5x. So media staging does NOT resize; a normal run stages the source at native size. That leaves the stale-reference path as the only remaining explanation for the four bad runs, which all pointed at hash 1ed8902c... measuring 1152x2016. Bug is TRIGGER-DEPENDENT, not always-on: a clean session upscales correctly, so repro needs the trigger (entry deletion in the lineage, possibly with a concurrent browser generation). Diagnostic that worked: compare generationSettings.mediaItems in the sidecar against the selected item's own file, and measure the staged asset's PNG header - the size of the staged file is the tell."
    },
    {
      "at": "2026-07-26T00:05:00Z",
      "type": "task.progress",
      "task": "MPI-351",
      "note": "User-reported context added, and it shifts the leading hypothesis. At the time of these runs the user (1) DELETED several upscale entries to make room to keep testing, and (2) at one point had a generation finishing in the ComfyUI browser while starting another in the app. (1) lines up with the MPI-225 comment above _opScopedMediaItems in generationService.js: a stale PromptBox media chip gets snapshotted into generationSettings.mediaItems and injects the wrong image, and the reference orphans once a card in that lineage is deleted. _opScopedMediaItems only filters chips whose mediaType the op does not declare - upscale DOES declare an image slot, so a stale image chip passes straight through and becomes Input_Image. That explains all four runs recording the same asset hash with a correct factor and a correct graph. (2) is unlikely alone: a one-off race would not produce the SAME hash at dispatch on four consecutive runs, so treat it as a possible aggravator. The earlier deliberate-pre-scale guess is SUPERSEDED - no code was found doing it and the stale-chip path explains the data without it; do not start from it. Repro plan in brief.md: check generationSettings.mediaItems in the new sidecar first, clean state vs after deleting a lineage entry."
    },
    {
      "at": "2026-07-25T23:55:00Z",
      "type": "task.created",
      "task": "MPI-351",
      "to": "todo",
      "note": "Upscale op applies the factor TWICE - 1.5x on a 768x1344 source yields 1728x3024 (2.25x). Root is NOT the factor and NOT the workflow: Input_Upscale_Factor has exactly one writer (PromptBoxControls.upscaleFactor, injects the raw 1.5), the sidecars for upscale_013/014/015/016 all record 1.5, node 1657 feeds only MpiGridDimensions.upscale_factor and UltimateSDUpscale.upscale_by, and the user ran the same graph in the ComfyUI browser and got the correct size. The graph is fed the WRONG IMAGE: generationSettings.mediaItems points at Media/.preview-assets/1ed8902c...png which measures 1152x2016, i.e. the 768x1344 source already enlarged 1.5x. 1152x2016 * 1.5 = 1728x3024 exactly. t2i_039's own sidecar reports pixelDimensions 768x1344 and has no previewAssets, so that file is not a full-res twin - it exists only in the content-addressed staging store. All four runs used the same hash, consistent with a deterministic pre-scale of the same source at the same factor. UNPROVEN: which code writes the pre-scaled staged copy. Start there."
    },
    {
      "at": "2026-07-25T23:15:00Z",
      "type": "task.progress",
      "task": "MPI-350",
      "note": "Live app test caught a hole in the first progress fix. routes/comfy.js forwards the RAW tqdm value from 'USDU: t/T', so T tiles emit T+1 events (0/T .. T/T) - the last fires AFTER the final tile's inner bar. The first cut re-armed the post-tile detector on EVERY tile bar, so that trailing tick re-armed it after the last tile and the refiner never tripped it; app still read 1/1 on both stages. Guard is now 'if (tileIndex < tiles)' - arm only for a tile that will actually run. The original test called tile() once per tile and never modelled the trailing tick, so it passed against broken code: a WRONG test, not a thin one. It now models the tick explicitly. Also added the user's requested honest total: progressStages.js records 'krea2_upscaler.json': {postTile: 1} (no 'single' - the tile count is a runtime value that tile mode overrides), new postTileBarsFor() shares _baseKey with stagesFor, commandExecutor passes it into createStageProgress, and tile() uses _total = _tileOffset + tiles + _postTileBars. A blanket +1 in tile() was rejected because 7 of the 9 USDU cards have no refiner and would advertise a stage that never runs. Stage clamp in tile() also had to move from min(_total, ...) to min(_tileOffset + tiles, ...) or the trailing tick advances into the refiner slot early. Now: grid off 1/2 -> 2/2; grid on 4 tiles 1/5..4/5 -> 5/5; other upscalers unchanged. Test at 6 cases, all pass; TWO negative controls - vs unfixed HEAD 5 of 6 fail, vs the first cut 4 of 6 fail including both trailing-tick cases. eslint clean on all three touched modules. Verified: postTileBarsFor returns 1 for both krea2 upscaler variants and 0 for chroma/sdxl/pony/ill, stagesFor unregressed (krea2_t2i 2, ltx_t2v 3). Awaiting user re-test in the app."
    },
    {
      "at": "2026-07-25T22:40:00Z",
      "type": "task.progress",
      "task": "MPI-350",
      "note": "Refiner exposed a progress defect - FIXED in js/services/phaseProgress.js. progressStages.js was NOT the mechanism: phaseProgress.tile() overrides the total with the live tile count when UltimateSDUpscale's outer bar reports, so a static entry would be discarded on the first tile. Real bug was that tile mode was one-way - once _tileMode was set, step() only moved _percent and never advanced _stage, so the refiner's bar was swallowed and the fill ran 0-100 percent a second time while the readout sat on the last tile (reads as a stall at the end of every upscale). Rejected the simpler '_total = tiles + 1': it fixes the printed total but not the stall, and tile() is shared by all 9 USDU cards - the 7 without a refiner would advertise a stage that never runs. Fix counts inner bars since the last tile bar; USDU emits its tile bar before every tile's inner bar, so during tiling the counter resets each tile and only a post-tile pass reaches 2. On the second bar it leaves tile mode and falls through to the normal per-bar logic, which stages it and self-corrects the total. 1 tile -> 2/2, 4 tiles -> 5/5. tile() untouched, so tile counting is unchanged. Ceiling named in the comment: assumes one inner bar per tile, true while all 9 USDU cards run seam_fix_mode None (swept). Guarded by tests/tile-post-pass-stage.test.cjs, 5 cases, negative-control proven (3 of 5 fail against the unfixed module, 5/5 pass restored). eslint clean. Still NOT verified in the app."
    },
    {
      "at": "2026-07-25T22:10:00Z",
      "type": "task.created",
      "task": "MPI-350",
      "to": "doing",
      "note": "Card opened directly in doing (in-progress). FIX for the Krea2 upscale op, which produced heavy noise - unusable on raw (turbo OFF) and only passable on turbo, because the accelerator (turbo distill) LoRA was applied in BOTH tiers. User reauthored the raw template (34 -> 38 nodes): new MpiIfElse 'is high tier' driven off the existing Input_Tier via MpiMath 'a == 1' now picks the UltimateSDUpscale model, so tier 1 runs the base model with no accelerator LoRA; and a refiner chain (VAEEncode -> ClownsharKSampler_Beta -> VAEDecode) was appended between the upscale and Output_image. The refiner deliberately runs on the accelerator-LoRA model in both tiers as a cheap 2-step polish - user-confirmed intentional. Blast radius verified zero: the moved node ids are unreferenced in js/ and routes/, injection is title-keyed, Output_image is unchanged, krea2Turbo already ships Input_Tier on the upscale command, upscalers are deliberately excluded from progressStages, and no new node types were introduced. Detail in brief.md."
    },
    {
      "at": "2026-07-25T09:15:00Z",
      "type": "task.created",
      "task": "MPI-349",
      "to": "todo",
      "note": "Card opened in todo (idea). WATCH card for two RunPod announcements. Deploy-When-Available went GA 18 Jun 2026 but is CONSOLE-ONLY - verified against the live REST schema (POST /pods has no queue/schedule/subscription field) and our create path is REST plus the GraphQL podFindAndDeployOnDemand fallback, so nothing to hook; autoRetry (MPI-110) is our app-side equivalent and is safer on billing (never runs with the app closed) but loses the race to a real server-side reservation. Re-open when the field appears in a schema, and gate it behind explicit consent - not folded into autoRetry. Second half is the real signal: RunPod publicly steered users off network volumes and ruled out credits, and our remote engine is volume-pinned; the escape hatch already exists (R2 weights plus ephemeral Any-region pods, MPI-78) so the work is to MEASURE ephemeral-first, not build it. Also narrows the Vast.ai no-volumes red flag in MPI-344. Detail in brief.md."
    },
    {
      "at": "2026-07-25T08:10:00Z",
      "type": "attention.required",
      "task": "MPI-346",
      "note": "Session close-out. Node pinned at v1.2.2 and live-schema-gated; template rewiring converted, validated and baked (both cards 104 nodes, 0 dangling, injection surface 34->34 unchanged); describer scaffold intact. User exercised every Krea2 op in the node graph EXCEPT text-to-image. NOT verified: any app-level generation, fit_mode/ref_boost output effect, describer first-word behaviour end-to-end, remote engine drift reinstall. Card stays in doing at maturity validating."
    },
    {
      "at": "2026-07-25T04:10:00Z",
      "type": "task.created",
      "task": "MPI-348",
      "to": "todo",
      "note": "Collaborative workflow-authoring track for the Krea2 swap family (face / head / character). User authors in the node graph, agent supplies krea2edit v1.2.2 semantics, graph topology and the measured regimes. Workflow proven in the graph BEFORE any App is built; app ideas that surface mid-authoring get their own cards. brief.md is a full working reference - node semantics, ref_boost two-regime rule, ref_boost_mask exact behaviour, the pose+edit topology finding, the untested two-LoRA gate, the div-16 rule, composite-vs-crop-stitch, and node ids. Target release v1.3 = the Apps release."
    },
    {
      "at": "2026-07-25T03:50:00Z",
      "type": "task.created",
      "task": "MPI-347",
      "to": "todo",
      "note": "New App idea: localized edits on 4K/8K images via InpaintCrop/Stitch - mask a small region on a huge scene, sample only the crop, so a small character keeps its definition. Mechanism proven by the user on Qwen. Card carries the crop-stitch tone-patch trap and its root cause (context_from_mask_extend_factor 1.2 starves the model of scene context; feathering at 32px was already on when b3f9a018 gave up on the krea2 masked edit). Also records a dep-declaration defect: app_head_swap.json uses the inpaint classes but neither the app nor the qwen-edit card declares comfyui-inpaint-cropandstitch - only the Krea2 cards do. Static finding, not reproduced live."
    },
    {
      "at": "2026-07-25T03:20:00Z",
      "type": "task.created",
      "task": "MPI-346",
      "to": "doing",
      "note": "Bump comfyui-krea2edit 17af8833 (v1.1) -> 223a9383 (v1.2.2). We already ship the v1.2 identity-edit weights but pinned the v1.1 nodes, so the v1.2 weights ran on legacy reference geometry. Unlocks vae+source_image pixel path, fit_mode, ref_boost / ref_boost_a / ref_boost_mask, GroundedEncode system_prompt, and the ref_latents wrapper tolerance ComfyUI 0.29 will require. Node is installRequirements:false so this is a lock-file edit only - no Pod image rebuild, the .mpi_node_commit drift ladder reinstalls on both engines. Workflow rewiring + live A/B owned by the user."
    },
    {
      "at": "2026-07-25T00:30:00Z",
      "type": "task.moved",
      "task": "MPI-345",
      "from": "doing",
      "to": "done",
      "note": "FIXED. Root cause: MpiBaseApp binds the global generation.run hotkey at setup, but closing an app only HID the overlay - the shell destroyed the instance only when a DIFFERENT app opened. HotkeyManager fires ALL handlers bound to an id, so the next Ctrl+Enter ran the PromptBox run AND the closed app _run, re-submitting the persisted s_appInputs. Explains every observation (same params, real prompt id, injector logged 8.5 min later = the phantom dispatched when the 1m41s Qwen edit drained the single remote lane). Fix: the app emits close outward and the shell DESTROYS the instance on it (every open remounts fresh), killing all stale listeners. Proven live on the real Electron shell by tests/desktop/app-close-destroys-instance.spec.js - passes with the fix, FAILS (Expected 0, Received 1) with both source files stashed. Blast radius swept: all 30 Hotkeys.bind sites; MpiBaseApp was the only hidden-but-alive holder of a global hotkey. Not re-staged end-to-end (needs a Pod + Head Swap session) - the mechanism is proven in both directions instead. Detail in validation.md."
    },
    {
      "at": "2026-07-25T00:00:00Z",
      "type": "task.moved",
      "task": "MPI-345",
      "from": "todo",
      "to": "doing",
      "note": "Root cause found by code read (no repro needed): MpiBaseApp binds Hotkeys generation.run at SETUP, but closing an app only HIDES its overlay - the shell destroys the instance only when a DIFFERENT app opens. So the closed Head Swap app kept a live Ctrl+Enter handler; the next Ctrl+Enter in the main workspace fired BOTH the PromptBox run and the stale app _run, enqueuing a second job from the still-persisted s_appInputs. Fixing by destroying the app instance on close."
    },
    {
      "at": "2026-07-24T01:20:00Z",
      "type": "task.moved",
      "task": "MPI-318",
      "from": "doing",
      "to": "done",
      "note": "User-verified live 2026-07-24: single card dragged to an OS folder lands with its real Media-folder filename; drag-to-prompt still works (the thing the prior attempt broke). Shipped via Chromium DownloadURL alongside application/mpi-media on the same HTML5 drag â€” no preventDefault/startDrag/IPC, one file touched (MpiGalleryGrid.js). Multi-select export dropped by user (DownloadURL is single-file). Changelog fix added to UNRELEASED.md; gallery.md documents the two-consumer dragstart contract. validation.md represents PASS."
    },
    {
      "at": "2026-07-24T01:11:59Z",
      "type": "task.moved",
      "task": "MPI-318",
      "from": "todo",
      "to": "doing",
      "note": "Drag-out to file system. Routed the docs first (gallery.md, events.md, components.md) per the brief. Root of the prior revert confirmed: preventDefault on the shared card dragstart kills the HTML5 drag that in-app drag-to-prompt rides (consumers: MpiPromptBox getData, MpiGalleryBlock/MpiGroupHistoryBlock types-sniff). User scoped it to SINGLE-card only (dropped multi-select to avoid the startDrag+IPC surface). Fix = Chromium DownloadURL type set ALONGSIDE application/mpi-media on the same drag, NO preventDefault, NO startDrag, NO IPC, NO main.js change. Real media-folder filename via extractFilenameFromPath. In-app drag physically cannot break. Awaiting live drag-to-Explorer verification (DownloadURL drop-to-target behavior in this Electron build is the one unverified bit)."
    },
    {
      "at": "2026-07-23T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-342",
      "from": "doing",
      "to": "done",
      "note": "ComfyUI v0.27->v0.28 wave COMPLETE + user-approved for done. All 5 phases verified: pins (4 sites), dev image build both legs green, MPI-341's 4 checks, MPI-340 build-gated dev-tag leg (both legs pulled live), workflow sweep on 0.28. Stable pins untouched; released users unaffected."
    },
    {
      "at": "2026-07-23T21:40:00Z",
      "type": "task.moved",
      "task": "MPI-341",
      "from": "doing",
      "to": "done",
      "note": "Pod build hardening COMPLETE + user-approved for done. All 4 checks: smoke layer green on clean dev build, unpin-kornia proof RED at smoke layer (LTXVideo IMPORT FAILED, run 30028617382), +cu130 trio + constraints.txt in final image, one LTX gen on dev Pod. The load-bearing grep gate is proven to bite."
    },
    {
      "at": "2026-07-23T06:34:00Z",
      "type": "task.updated",
      "task": "MPI-340",
      "note": "Scope extended (user): the dev/stable split must land in docs, rules and skills, not just code. Grep-verified blast radius added to brief: CLAUDE.md:74 (Context Router row - highest-traffic instruction, currently teaches 'publish-runtime.sh stable' as THE way to ship a runtime edit), docs/runpod-remote-engine.md s5+s6, docs/builder/02-image-and-rebuild.md:100, .claude/commands/build-pod-image.md:123+94, .claude/rules/comfy_engine.md:291, docs/runpod-troubleshooting.md (4 mentions), mpi-ci/cubric-vision-pod/README.md 'Runtime externalize', plus a dos_and_donts.md one-liner. KEY FINDING: mpi-release/SKILL.md and mpi-version-bump/SKILL.md have ZERO Pod awareness today (grep-verified) - harmless with one channel, but this card creates the hazard, so mpi-release gains a Preconditions drift check (curl dev vs stable manifest.json, compare the sha256 fields they already carry) that STOPS and asks the user to promote or confirm deliberate non-shipping. Never auto-promote in the release flow - live op affecting released users, same class as git push. mpi-version-bump deliberately unchanged: app version and Pod runtime are separate artifacts on separate cadences."
    },
    {
      "at": "2026-07-23T06:22:00Z",
      "type": "task.updated",
      "task": "MPI-340",
      "note": "Moved to TOP of todo (user) - MPI-340 unblocks MPI-333/MPI-329 (both likely land fixes in wrapper.py, which currently publishes straight to the 'stable' channel released users boot). Brief extended: the dev/user split for wrapper.py + start.sh is the CHANNEL, not duplicate wrapper.dev.py files (git versions the file, the channel versions the deploy). Added a 'publish-runtime.sh promote' verb that rclone-copies vision/dev/* -> vision/stable/* server-side so promotion ships the EXACT bytes tested on dev, refusing on a working-tree/dev sha mismatch (the script already emits per-file sha256 in manifest.json). Runtime promote and image bump are independent paths."
    },
    {
      "at": "2026-07-23T06:09:34Z",
      "type": "task.created",
      "task": "MPI-340",
      "note": "Dev Pod images (GPU + CPU) + dev R2 runtime channel, both gated on the EXISTING server-side _devMode (remotePodLifecycle.js:37). 1.1.0 is released and every released app pins the same image tag AND the same 'stable' runtime channel, so publish-runtime.sh stable reaches real users on their next Pod boot. No new Dockerfile, no CI change (manifest_version=0.17.0-dev already yields v0.17.0-dev-cu130). Safety substrate for MPI-333 / MPI-329 / MPI-341."
    },
    {
      "at": "2026-07-23T06:09:34Z",
      "type": "task.created",
      "task": "MPI-341",
      "note": "Adopt two build practices from YanWenKun/ComfyUI-Docker: (1) RUN --quick-test-for-ci --cpu after the node bake so an import failure fails the BUILD (the MPI-149 kornia/LTXVideo class that shipped and died live as Node Stage1_Bypass not found; also automates the hand-run '8 nodes register' gate from MPI-139); (2) pip constraints file pinning the cu130 trio so torch drift is unresolvable, with the MPI-244 assert kept as the belt. Blocked on MPI-340 - build to the dev tag, never rebuild released v0.16.0."
    },
    {
      "at": "2026-07-22T06:15:59.000Z",
      "type": "cleanup",
      "note": "mpi-cleanup: archived 3 orphaned deleted-status task workspaces (MPI-251, MPI-288, MPI-290) to tasks/_archived/ (not in any board column). Swept all handoff records to state/archive/handoffs/ and emptied index.json active_handoffs (no active sessions; all done). MPI-242 acknowledged message kept."
    },
    {
      "at": "2026-07-22T06:15:59.000Z",
      "type": "task.moved",
      "task": "MPI-337",
      "from": "doing",
      "to": "done",
      "note": "User-verified live: LTX (t2v_ms/i2v_ms) passed â€” validates wan-22 (same ops); wan22-5b covered by automated test. Image/KREA/History/Gallery behavior confirmed working earlier this session. 6 code files shipped; docs+rules future-proofed (data-driven gating documented in playbooks + generation-lifecycle + component-events)."
    },
    {
      "at": "2026-07-22T05:17:08.000Z",
      "type": "task.moved",
      "task": "MPI-337",
      "from": "todo",
      "to": "doing",
      "note": "Starting implementation of approved Option A change set (kill force-DOWN, add requiresMask guard, radial show-all-dimmed)."
    },
    {
      "at": "2026-07-22T05:09:33.000Z",
      "type": "task.updated",
      "task": "MPI-338",
      "note": "Compact plan attached (idea -> planned). Reuse the single radial instance: add a 'dev' CONTEXTS context + Ctrl+Tab hold in MpiRadialMenu, move Apps/Restart-Engine off the main radial's extraItems, gate on APP_CONFIG.dev_mode. Verify mode user-ux."
    },
    {
      "at": "2026-07-22T05:06:43.000Z",
      "type": "task.updated",
      "task": "MPI-337",
      "note": "Compact plan attached (idea -> planned). Single change set: kill force-DOWN in _refreshOpOptions + _emitMediaChange, add requiresMask generate-time guard+toast, radial show-all-dimmed. Verify mode user-ux. Say 'continue this plan' to start."
    },
    {
      "at": "2026-07-22T04:59:57.000Z",
      "type": "task.created",
      "task": "MPI-338",
      "note": "Split off from the MPI-337 brainstorm: dev-only Ctrl+Tab radial holding the dev extraItems (Apps, Restart Engine) so main-radial Tab shows operations only (clean tutorials + tidiness). Gated behind APP_CONFIG.dev_mode. Dev items already sit after ops, so this is not about op-position memory (that lands in MPI-337)."
    },
    {
      "at": "2026-07-22T04:59:57.000Z",
      "type": "task.updated",
      "task": "MPI-337",
      "note": "Brainstorm complete â€” approved Option A. Op changes only on media-add force-UP + Reuse; a lost requirement (mask/media removed, maskless card) never switches, op stays selected, Run toasts the missing input. Change set: kill _refreshOpOptions fallback (detail->upscale bug) + _emitMediaChange force-DOWN branch; add requiresMask generate-time guard+toast; radial show-all-dimmed (stop filtering disabled, new dimmed non-selectable MpiRadialMenu item state). Run = toast-on-click (reuse existing). Dev Ctrl+Tab radial split to MPI-338."
    },
    {
      "at": "2026-07-22T04:51:11.717Z",
      "type": "task.moved",
      "task": "MPI-336",
      "from": "doing",
      "to": "done",
      "note": "User-verified live: changed every perModel+shared control, all restored on Reuse. Scope-driven persistence fix + scope-contract playbook doc shipped."
    },
    {
      "at": "2026-07-22T04:39:51.031Z",
      "type": "task.updated",
      "task": "MPI-336",
      "note": "Structural root-cause fix: perModel control persistence/snapshot/reuse is now scope-driven (no hand-maintained key-lists). Dispatch-time wholesale controlState snapshot + modelWide-flag write guard + new scope-contract playbook doc. Round-trip test PASS; live reuse test pending."
    },
    {
      "at": "2026-07-22T03:42:06.912Z",
      "type": "task.moved",
      "task": "MPI-336",
      "note": "todo/idea -> doing/validating: krea2Turbo now snapshotted into sidecar controlState.model (generationService) + restored on legacy reuse path (promptReuse). Root cause = perModel key missing from both snapshot loops. Live reuse test pending."
    },
    {
      "at": "2026-07-22T01:30:00Z",
      "type": "task.created",
      "task": "MPI-335",
      "note": "Publish the 1.1.0 GitHub release - prep DONE + delta update proven live; only tag-move + Gate 2 + publish remain. Deferred to a later session (user). Run mpi-release."
    },
    {
      "at": "2026-07-22T00:56:00Z",
      "type": "task.created",
      "task": "MPI-333",
      "note": "RunPod post-install model VERIFY takes longer than the download (portable 1.1.0 install test). Verify tile sits on 'Verifying...' longer than the download bar took. Suspect a full SHA256 re-read off the network volume post-land vs hash-while-streaming (local does this, MPI-296). Moved to TOP of todo (user priority). Distinct from MPI-329 cold model-LOAD."
    },
    {
      "at": "2026-07-21T20:25:00Z",
      "type": "task.created",
      "task": "MPI-332",
      "note": "Rip 3 deprecated test apps (image-regen/sdxl-4k/video-stitch), keep Head Swap. 8-touchpoint blast-radius map grep-verified in card. Carded now, code deferred (user)."
    },
    {
      "at": "2026-07-21T20:20:00Z",
      "type": "task.moved",
      "task": "MPI-306",
      "from": "doing",
      "to": "done",
      "note": "App-UI foundation complete: step-carousel frame + STEP_KINDS shipped/verified; Head Swap shipped, user-verified local + remote-proven (MPI-324). Phase 3 hold-until-Apply was built (bcbe161f) then reverted on the UX pass (results save themselves) - card was stale describing a removed phase, nothing left to verify."
    },
    {
      "at": "2026-07-21T20:00:00Z",
      "type": "task.moved",
      "task": "MPI-315",
      "from": "doing",
      "to": "done",
      "note": "Board cleanup: log-architecture shipped + user-verified; only untracked leftover (MPI-147 progress-tick) lives elsewhere."
    },
    {
      "at": "2026-07-21T20:00:00Z",
      "type": "task.moved",
      "task": "MPI-299",
      "from": "doing",
      "to": "done",
      "note": "Board cleanup: Head Swap umbrella closed - wiring + node-lock shipped, remote-proven in MPI-324 sweep; residuals split to MPI-306 (UI) + MPI-325 (box pad)."
    },
    {
      "at": "2026-07-21T20:00:00Z",
      "type": "task.moved",
      "task": "MPI-308",
      "from": "doing",
      "to": "done",
      "note": "Board cleanup: image_descriptor dev harness + Describe-Image radial shipped; abliterated encoder (MPI-311, 1.2.0) cleared the refusal blocker."
    },
    {
      "at": "2026-07-21T20:00:00Z",
      "type": "task.moved",
      "task": "MPI-312",
      "from": "todo",
      "to": "archived",
      "note": "Archived to tasks/_archived/MPI-312; migrated to Cubric Studio (Docs) MPI-10. Krea2 reference-order is user documentation, not an app change - the Vision wiring is correct."
    },
    {
      "at": "2026-07-21T20:00:00Z",
      "type": "task.moved",
      "task": "MPI-313",
      "from": "todo",
      "to": "archived",
      "note": "Archived to tasks/_archived/MPI-313; migrated to Cubric Studio (Docs) MPI-11. Two-subjects-in-one-pass is a documented Krea2 model limitation with a chained-pass workaround - docs content, nothing to build."
    },
    {
      "at": "2026-07-21T20:00:00Z",
      "type": "task.updated",
      "task": "MPI-330",
      "note": "Escalated to release-blocker (user). release-blocker + bug added to labels (maturity stays idea - not yet started). Traces to the recent chip-repositioning feature stranding the required Input_Image slot; must fix before release. Moved to top of todo."
    },
    {
      "at": "2026-07-21T19:45:00Z",
      "type": "task.moved",
      "task": "MPI-331",
      "from": "doing",
      "to": "done",
      "note": "Collapsed 3 release skills into one GitHub-only mpi-release (+ build-dispatch/copy-review refs). Deleted mpi-merge-branches/apply-patch/release-public/release-shared. Updated mpi-version-bump, mpi-end step 3, docs/releases/* (README, portable-contract, github-checklist), docs/README.md map, kanban index; deleted patch-distribution.md; bannered app-update-strategy.md. macos-x64 dropped (arm64-only), R2/Cloudflare stripped from release docs. Auto-verified; user-confirmed on close."
    },
    {
      "at": "2026-07-21T19:20:00Z",
      "type": "task.moved",
      "task": "MPI-331",
      "from": "todo",
      "to": "doing",
      "note": "Implementation started: collapse 3 release skills -> one GitHub-only mpi-release. User approved forks: fold shared lib into new skill, GitHub release carries full + update bundles, delete patch-distribution.md + banner app-update-strategy.md."
    },
    {
      "at": "2026-07-21T18:50:00Z",
      "type": "task.created",
      "task": "MPI-331",
      "note": "Next-session: collapse the 3 release skills into one GitHub-only release skill (2nd digit = features, 3rd = fixes, same op) + update CLAUDE.md/versioning docs. Handoff c85c2cfe-43bf-4023-b725-fc7ab3cbcddb."
    },
    {
      "at": "2026-07-21T18:45:00Z",
      "type": "task.moved",
      "task": "MPI-324",
      "from": "doing",
      "to": "done",
      "note": "RunPod validation sweep COMPLETE (1.1.0 release gate). MPI-282/300/310 remote-proven, MPI-326 fix verified; Head Swap residual tracked on MPI-299/306/325. Closed at 1.1.0 release close-out."
    },
    {
      "at": "2026-07-21T18:45:00Z",
      "type": "task.moved",
      "task": "MPI-327",
      "from": "doing",
      "to": "done",
      "note": "Public 1.1.0 changelog aggregation shipped; version-rename landed on master; drag-drop-import wording corrected to LoRA/upscale only. release:check green."
    },
    {
      "at": "2026-07-21T18:25:00Z",
      "type": "task.created",
      "task": "MPI-330",
      "note": "Removing a non-last image chip may strand the required Input_Image -> Qwen-Edit silently returns no output. Local engine healthy, NOT the MPI-326 fix. Primary hypothesis: chip-slot repack (project_media_slots_addressable_never_packed); secondary: stale Pod path. Deterministic repro + code entry points in card. Non-blocking for 1.1."
    },
    {
      "at": "2026-07-21T18:15:00Z",
      "type": "task.moved",
      "task": "MPI-310",
      "from": "doing",
      "to": "done",
      "note": "Remote PASS (RTX PRO 4000, MPI-324 sweep). Full shared-dep guard lifecycle: inverse-GC kept encoder on Krea uninstall (2->1 holder), describe from gallery+history landed caption, negative-flip correct, uninstall freed 5.24GB (104.2->99GB) + row flipped to Install after refresh. Both engine twins proven. Core deliverable + plugin entity + GC guard complete; stale destructive-bug attention cleared. Deferred step5 video-describe=MPI-287, plugin-row download-mode race=MPI-320."
    },
    {
      "at": "2026-07-21T18:05:00Z",
      "type": "task.moved",
      "task": "MPI-300",
      "from": "doing",
      "to": "done",
      "note": "Remote PASS (RTX PRO 4000 Pod, MPI-324 sweep). Tier radio (all 3), 2-image combine (Head Swap), 3rd chip Input_Image_3 (injects local + remote - chip-3-unique blue-hair girl landed in qwenEdit_005 remote AND qwenEdit_003 local; graph wiring traced correct not crossed), style rack (3 styles remote). Caveat: scene + 2 distinct subjects in one pass is model-unstable (keeps one, morphs other) - same family as Krea2 2-subject limit, mitigation = chain per MPI-313. Injection correct; instability is the transformer."
    },
    {
      "at": "2026-07-21T17:55:00Z",
      "type": "task.created",
      "task": "MPI-329",
      "note": "Perf: remote cold model-load ~2min per swap vs ~14s warm (RTX PRO 4000, MPI-324 sweep). Per-model cold cost, consistent Krea2/Chroma. User caveat: may just be a weak inference card - retest on mainstream GPU. Not a 1.1 blocker."
    },
    {
      "at": "2026-07-21T17:50:00Z",
      "type": "task.moved",
      "task": "MPI-326",
      "from": "doing",
      "to": "done",
      "note": "Remote runtime PASS (RTX PRO 4000 Pod, MPI-324 sweep). Idle-connected: Network models filter 0/32 over 20s (no 5s models/status poll - Fix A), op dropdown stayed open, Denoise slider held 0.71 with no snap-back. Root killed at source. Fix A+B committed js/shell.js + js/data/modelRegistry.js."
    },
    {
      "at": "2026-07-21T16:55:00Z",
      "type": "task.created",
      "task": "MPI-327",
      "note": "Aggregate 1.1.0 + 1.1.1 + UNRELEASED.md into the public 1.2.0 changelog (GitHub skips 1.1). NOT simple concat - dedup bug-fix overlap between 1.1 and 1.2 (download queue). Curate for first-time users. Handoff: state/handoffs/f7677602-e881-4c74-b412-666604cf366b.json."
    },
    {
      "at": "2026-07-21T16:55:00Z",
      "type": "task.created",
      "task": "MPI-328",
      "note": "Remote models/check fails OPEN: remoteModels.js:325 defaults wrapper-omitted models to installed:true + :339 vacuous [].every(). Observed 1.1.1 CPU download Pod (booting wrapper -> 5 models falsely installed, no download). Lines present in 1.2.0. Adjacent to MPI-326. Fix: default false + guard every."
    },
    {
      "at": "2026-07-21T16:50:00Z",
      "type": "task.moved",
      "task": "MPI-326",
      "from": "todo",
      "to": "doing",
      "note": "Root VERIFIED + fix A+B IMPLEMENTED for 1.2 (user go). A: edge-guard the sync at shell.js:1370. B: diff-gate the models:checked emit at modelRegistry.js:218. node --check clean; all reSyncInstalledModels callers swept safe. maturity=validating pending remote runtime proof on a Pod. Files uncommitted."
    },
    {
      "at": "2026-07-21T15:55:00Z",
      "type": "task.moved",
      "task": "MPI-282",
      "from": "doing",
      "to": "done",
      "note": "RunPod remote pass (MPI-324 sweep, RTX 2000). Krea2 t2i/i2i/depth/upscale/masked edit + 2-character edit all clean; accel LoRA resolved (no silent degrade); backslash LoRA path worked on Linux. Step count result-verified not console-counted."
    },
    {
      "at": "2026-07-21T15:35:00Z",
      "type": "task.created",
      "task": "MPI-326",
      "note": "Remote op-dropdown flashes + auto-closes on a ~1s models:checked rebuild storm. Defect B confirmed (PromptBox _refreshOpDropdown rebuilds unconditionally; Model Library already guards the same). Defect A hypothesis (remote:connection connected-edge flapping during Pod init re-fires syncModelInstalled). Surfaced in MPI-324 sweep. Scope 1.2-blocker vs 1.3 pending user."
    },
    {
      "at": "2026-07-21T15:15:00Z",
      "type": "task.moved",
      "task": "MPI-309",
      "from": "doing",
      "to": "done",
      "note": "Remote-proven on RTX 2000 Pod (MPI-324 sweep). Wrapper reinstalled MpiNodes aaa1d2d on connect; app_head_swap.json ran with no unknown-node rejection; box crop followed a user resize; correct swap saved. Hyper = 4 steps. node_lock.json ready to commit at close."
    },
    {
      "at": "2026-07-21T15:15:00Z",
      "type": "task.created",
      "task": "MPI-325",
      "note": "Head Swap: allow the target/reference box to extend OUTSIDE the frame and auto-pad that edge, keeping the box square. Surfaced during MPI-324 Head Swap validation - an edge-adjacent head cannot capture full hair with an in-frame square without grabbing the neighbour (caused the first-run fail). Pad, do not shrink. Workflow/UX improvement, not a v1.2 blocker."
    },
    {
      "at": "2026-07-21T13:00:00Z",
      "type": "task.created",
      "task": "MPI-324",
      "note": "RunPod validation sweep umbrella (1.2.0 release gate): MPI-282, MPI-300, MPI-310, Head Swap. MPI-198 deferred."
    },
    {
      "at": "2026-07-21T13:00:00Z",
      "type": "task.moved",
      "task": "MPI-309",
      "from": "todo",
      "to": "doing",
      "note": "node_lock MpiNodes pin bumped ba9e156 -> aaa1d2d (1.2.6 adds MpiBox/MpiBoxCrop/MpiBoxMask/MpiFromBox, pure superset). Code done; maturity validating pending Pod-connect proof the wrapper reinstalled the new commit. Folded into MPI-324 Phase 0."
    },
    {
      "at": "2026-07-21T12:00:00Z",
      "type": "task.created",
      "task": "MPI-323",
      "note": "Evaluate FLUX.2 Klein 4B (Apache 2.0) for masked object/text removal. Fill dev + Klein 9B ruled out non-commercial. Outpainting already solved via black-border + edit models. Cheaper pre-test: does Krea2 masked-edit (MPI-282) already remove on empty prompt?"
    },
    {
      "at": "2026-07-20T20:10:00Z",
      "type": "task.created",
      "task": "MPI-318",
      "note": "Carded after a failed attempt on 2026-07-20 was fully reverted. Native Electron drag-out (startDrag) worked - real filenames, multi-file drop - but the e.preventDefault() it required killed the shared HTML5 dragstart that in-app drops ride on, so dragging cards inside the library stopped reaching the prompt and landed as imported cards. Reverted all three files to HEAD; tree clean, nothing shipped. Root process failure: skipped the Context Router (components.md / gallery.md / events.md) and never asked what else consumed that dragstart. Card instructs the next agent to read the docs FIRST. Open and unexplained: pressing Cancel on the Windows file-conflict dialog exited the app, with no trace in app.log because main.js has no render-process-gone or uncaughtException handler."
    },
    {
      "at": "2026-07-19T23:55:00Z",
      "type": "task.created",
      "task": "MPI-316",
      "note": "Test-first card: does RAW + a speed-up LoRA match the Turbo transformer? If yes, drop ~24.5GB of Turbo weights and move Krea2 to the Qwen-Edit runtime tier-radio shape. Negative-prompt gating is currently structural (cfg 1 discards it) and would be LOST in a single-card collapse - flagged in the card as a design decision."
    },
    {
      "at": "2026-07-19T22:30:00Z",
      "type": "task.moved",
      "task": "MPI-315",
      "from": "todo",
      "to": "doing",
      "note": "Picked up. Decision: aggressive source-filter of ComfyUI stdout (user has terminal access for INFO detail), delete destructive startup line-trim, gitignore logs/. Reporter 2000-char slice UNCHANGED (user: expanding it breaks GitHub issue creation, already empirically trimmed). R2-upload idea explicitly parked."
    },
    {
      "at": "2026-07-19T18:45:00Z",
      "type": "task.created",
      "task": "MPI-314",
      "note": "Orphaned-dep reclaim â€” 19GB B1-era LTX shared deps, no owner, no UI delete path."
    },
    {
      "at": "2026-07-19T21:00:00Z",
      "type": "task.created",
      "task": "MPI-315",
      "note": "Log architecture â€” split ComfyUI output out of app.log; reconcile line-trim vs byte-rotation."
    },
    {
      "at": "2026-07-19T21:30:00Z",
      "type": "task.moved",
      "task": "MPI-310",
      "note": "doing -> todo (validating). Shared-dep bug fixed + user-verified locally; RunPod validation deferred."
    },
    {
      "at": "2026-07-20T17:20:00Z",
      "type": "task.moved",
      "task": "MPI-317",
      "note": "Created in doing/investigating - local download reliability: failed-download resume, single-stream throughput ceiling, speed/ETA feedback."
    },
    {
      "at": "2026-07-23T07:10:00Z",
      "type": "task.moved",
      "task": "MPI-340",
      "note": "todo -> doing: implementing dev Pod image tags + dev R2 runtime channel (dev_mode-gated) + publish-runtime.sh promote verb + the docs/rules/skills sweep."
    },
    {
      "at": "2026-07-23T08:05:00Z",
      "type": "task.updated",
      "task": "MPI-340",
      "note": "Code + docs shipped; moved to validating (needs live Pod verify + dev-channel seed)."
    },
    {
      "at": "2026-07-23T08:40:00Z",
      "type": "task.updated",
      "task": "MPI-340",
      "note": "build-pod-image dev/release flow wired in; MPI-119 version-guard contradiction fixed; section-6 CUDA drift corrected."
    },
    {
      "at": "2026-07-23T08:50:00Z",
      "type": "task.updated",
      "task": "MPI-340",
      "note": "Live-verified: GPU + CPU Pods both boot the dev runtime channel. Remaining: released-build stable assertion + one real promote."
    },
    {
      "at": "2026-07-23T09:05:00Z",
      "type": "task.moved",
      "task": "MPI-342",
      "note": "Created in todo, directly after MPI-341 - both are baked-layer changes that ship in ONE dev image build."
    },
    {
      "at": "2026-07-23T09:35:00Z",
      "type": "task.moved",
      "task": "MPI-343",
      "note": "Created at the BOTTOM of todo (user: not a priority). Research-only idea surfaced by the 0.28 notes."
    },
    {
      "at": "2026-07-23T10:20:00Z",
      "type": "task.moved",
      "task": "MPI-341",
      "note": "todo -> doing: Dockerfile guards implemented in mpi-ci/cubric-vision-pod/Dockerfile - (1) node-import smoke test AFTER the MPI-244 assert, with a grep for IMPORT FAILED because --quick-test-for-ci exits 0 even when a custom node fails to import (verified in main.py at v0.27.0 AND v0.28.0 - the brief's bare RUN would have been vacuously green); (2) constraints file emitted from pip list after the cu130 trio install plus ENV PIP_CONSTRAINT so every later pip call (node bake, kornia pin, runtime wrapper installs) cannot resolve a non-cu130 torch. Docs recorded in docs/builder/02-image-and-rebuild.md. Left: the dev-tag CI build (rides with MPI-342) plus the unpin-kornia proof that the smoke test bites."
    },
    {
      "at": "2026-07-23T11:05:00Z",
      "type": "task.deleted",
      "task": "(bulk)",
      "note": "mpi-archive: archived 84 done tasks to tasks/_archived/ (workspaces preserved, per-task task.deleted events logged). Held back MPI-263/308/315 (attention required) and MPI-243 (open message ff4e8854)."
    },
    {
      "at": "2026-07-23T11:30:00Z",
      "type": "task.updated",
      "task": "MPI-342",
      "note": "FINAL STAGE added (user): 342 carries the only Pod rebuild, so it must close the verification of MPI-341 (4 checks incl. the unpin-kornia proof that the smoke test bites) and MPI-340's build-gated dev-image-tag leg. MPI-340's other two open items are explicitly NOT build-gated."
    },
    {
      "at": "2026-07-23T11:55:00Z",
      "type": "attention.required",
      "task": "MPI-341",
      "note": "Session close-out: implementation committed (mpi-ci 41a5517) but unbuilt; all verification is gated on MPI-342's user-authorized dev-tag build."
    },
    {
      "at": "2026-07-23T21:41:33.041Z",
      "type": "task.moved",
      "task": "MPI-329",
      "from": "doing",
      "to": "done",
      "note": "shipped f7e42376: switch-tax fix (HOT_STORE_MIN_GB 0.1) + dynamic disk (volume-mirror) + VRAM-relative stage skip + stage-on-connect toggle; live-verified on a 4090 (switch 80s->9s, LTX streams)"
    },
    {
      "at": "2026-07-24T02:53:48Z",
      "type": "task.moved",
      "task": "MPI-340",
      "from": "doing",
      "to": "done",
      "note": "Last item (dev->stable runtime promote) done this session: ran publish-runtime.sh promote, stable manifest now wrapper 0.2.38 (de748061). Dev/stable split shipped in v1.2.0. mpi-release Preconditions drift-check exercised end-to-end (caught this promote + the pod-image 0.28 pin)."
    },
    {
      "at": "2026-07-26T20:30:00Z",
      "type": "task.created",
      "task": "MPI-358",
      "to": "todo",
      "note": "Attribution sweep for shipped LoRAs - spun out of MPI-354 licence check."
    },
    {
      "at": "2026-07-26T21:00:00Z",
      "type": "task.created",
      "task": "MPI-359",
      "to": "todo",
      "note": "Migrate Krea2 + Qwen-Edit style racks to MpiStyleSelector/MpiStyleLoras. Injection support already shipped with MPI-354."
    },
    {
      "at": "2026-07-26T22:00:00Z",
      "type": "task.moved",
      "task": "MPI-359",
      "from": "todo",
      "to": "doing",
      "note": "Starting the migration. Raw LiteGraph templates for krea2_t2i + qwen_edit already carry the new rack (uncommitted); API templates, runtime files and generate_krea2.py still on the old shape."
    },
    {
      "at": "2026-07-26T23:10:00Z",
      "type": "task.progress",
      "task": "MPI-359",
      "note": "Graphs + app + docs + guards SHIPPED (891d4415, d61e84f5, 86460c82). krea2_t2i_sfw/nsfw + qwen_edit run the new rack; every lora and trigger is byte-identical to the old rack slot-for-slot, so styleLoraLabels stay aligned. Legacy Input_Style/Input_Stylization keys removed. Found + fixed on the way: comfyController step 3b healed only a fixed key list, so the banks' lora_1..lora_5 slots would have shipped Windows backslashes to every Linux engine. BLOCKED on 2 things: node_lock still pins MpiNodes aaa1d2d9 and the commit carrying the new nodes (0dd056c) is NOT pushed; and live per-model verification."
    },
    {
      "at": "2026-07-26T22:20:00Z",
      "type": "task.created",
      "task": "MPI-360",
      "to": "todo",
      "note": "Per-op prompting help popup. Extends the existing commandRegistry info: one-liners."
    },
    {
      "at": "2026-07-26T23:30:00Z",
      "type": "task.created",
      "task": "MPI-361",
      "to": "todo",
      "note": "Auto Detect Mask v2 with SAM3 open-vocabulary text prompts + point coords."
    },
    {
      "at": "2026-07-27T00:10:00Z",
      "type": "task.moved",
      "task": "MPI-359",
      "from": "todo",
      "to": "doing",
      "note": "Migration shipped by a parallel session and verified on disk; held in validating because the MpiNodes pin still lacks the new nodes."
    },
    {
      "at": "2026-07-27T00:00:00Z",
      "type": "task.moved",
      "task": "MPI-359",
      "from": "doing",
      "to": "done",
      "note": "USER-VERIFIED live. node_lock bumped aaa1d2d9 -> 69a43336 (pushed origin/main tip, carries MpiStyleSelector + MpiStyleLoras); node-drift 23/23. Both blockers closed - card complete."
    },
    {
      "at": "2026-07-27T21:30:49Z",
      "type": "task.created",
      "task": "MPI-362",
      "to": "doing",
      "note": "Mask composite (Add/Subtract) in image history - Sharp server-side, feathered mask, result appended as a new history entry."
    },
    {
      "at": "2026-07-28T00:50:00Z",
      "type": "task.moved",
      "task": "MPI-361",
      "from": "todo",
      "to": "doing",
      "note": "Masking-methods research done against the live bench + web. brief.md written: Impact Mask Pointer gives click-points + confidence on the sam_vit_b we already ship (zero download); SAM3 adds text/box for 1.75GB."
    },
    {
      "at": "2026-07-28T01:05:00Z",
      "type": "task.created",
      "task": "MPI-368",
      "to": "todo",
      "note": "Shape masking - rectangle / triangle / ellipse gizmo with rotate-about-handle, committed to the mask as Add or Subtract. Follow-up split out of the MPI-361 session."
    },
    {
      "at": "2026-07-28T02:30:00Z",
      "type": "task.moved",
      "task": "MPI-354",
      "from": "doing",
      "to": "done",
      "note": "FLUX.2 Klein 4B shipped and user-validated in-app: all seven ops, bar counts, styles, enhancer, depth with two images, no ratio on the image-sized ops. Verified with only ~6GB VRAM free (spills to RAM and completes). RunPod re-test deferred by the user - no card, they will open one if it bites."
    },
    {
      "at": "2026-07-29T21:30:00Z",
      "type": "task.moved",
      "task": "MPI-376",
      "from": "todo",
      "to": "doing",
      "note": "User resequenced undo AHEAD of MPI-382: grow/shrink needs a pristine drag-start snapshot, which is the same primitive undo builds, and both 382 and 368 bake irreversibly. New order 376 > 382 > 368 > 379."
    },
    {
      "at": "2026-07-29T22:25:00Z",
      "type": "task.created",
      "task": "MPI-386",
      "to": "todo",
      "note": "Age gate leaves an orphan childless full-viewport .mpi-modal-backdrop (z 10009, pointer-events auto) that intercepts every click on the landing page. Found incidentally during the MPI-376 live smoke; unrelated to masking. Electron first-run impact UNKNOWN - check that first."
    },
    {
      "at": "2026-07-29T22:35:00Z",
      "type": "board.reordered",
      "task": "MPI-382",
      "note": "To do reordered so the masking chain reads in the decided order: MPI-382 (grow/shrink) > MPI-368 (shapes) > MPI-379 (hover/COCO), all after MPI-376 undo which is now in doing. MPI-368 was sitting below unrelated cards."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-387",
      "type": "task.created",
      "at": "2026-07-29T23:10:00Z",
      "actor": "opus",
      "to": "doing",
      "summary": "UMBRELLA: clean Windows 11 install broken end-to-end. Six defects reproduced live on a clean Win11 Home laptop - MAX_PATH kills LTXVideo pip (266 vs 260 chars, doubled root folder costs 32), Impact-Pack needs git for a sam2 requirement (deterministic, Retry cannot fix), the error blames extraction when every extraction passed, and Smart App Control blocks the whole vbs/bat/cmd launch chain. Existing Win11 SAC users are unreachable by any update path."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-323",
      "type": "task.moved",
      "at": "2026-07-29T08:05:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "done",
      "summary": "Superseded by MPI-354, which shipped FLUX.2 Klein 4B end to end. The evaluate-then-decide question is answered: klein-4b (Apache 2.0) is wired in js/data/modelConstants/models.js with 'inpaint' in supportedOps and opInject Input_wf_type 5, the shared 'inpaint' op in js/data/commandRegistry.js routes empty-prompt to erase and any text to fill, and comfyui-inpaint-cropandstitch ships as a node dep specifically for removal. User confirms masked removal was tested in the app."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-386",
      "type": "task.moved",
      "at": "2026-07-29T08:15:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "User recalls fixing this with an agent, so it moves to doing / needs verification rather than sitting in To do. No fix is on disk: MPI-386 has no plan, no brief, no handoff and one task.created event, no commit references it, and both files that create .mpi-modal-backdrop (MpiModal.js, MpiStartingComfy.js) are unchanged since before the card. The two age-gate fixes that DO exist predate it - f30a6ef4 2026-07-22 (MPI-333, gate chains the changelog so the backdrops stop stacking) and 3845756c 2026-06-16 (MPI-99, re-arm show after a backdrop torn down without hide). Likeliest reading: the memory is of MPI-333, and this leak survived it. Next step is the fresh-profile repro, not code reading - every teardown path reads correct statically."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-386",
      "type": "task.moved",
      "at": "2026-07-29T08:25:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's recollection of fixing it in a live session (the agent handed over a console one-liner to clear mpi_maturity_acknowledged and reload, so the gate could be re-triggered for testing). RECORD THE CAVEAT: no fix for this card is on disk. No commit references MPI-386, the card carries no plan/brief/handoff, and MpiModal.js + MpiStartingComfy.js - the only two files that create .mpi-modal-backdrop - are unchanged since before the card was filed. The nearest real fix is f30a6ef4 2026-07-22 (MPI-333, the 18+ gate chains the changelog so two backdrops stop stacking), which predates the report by 7 days and whose testing needs the same console one-liner. If a dead landing page after CONTINUE is ever seen again, reopen and go straight to the fresh-profile repro: localStorage.removeItem('mpi_maturity_acknowledged'); location.reload() then count document.querySelectorAll('.mpi-modal-backdrop') after clicking CONTINUE."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-395",
      "type": "task.moved",
      "at": "2026-07-29T22:55:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "All three residuals verified live on CPU Pod omi9588i0gymlu; user approved close. Legitimate queue read Queued... and cancel-while-queued released the chain - proven server-side: after the cancel, the next Install POST reached the server 20s after the click (job table 20:36:04) instead of parking forever, which is the wedge the fix targets. The 400 it then got is the correct volume-full pre-flight, not a defect. engine:assets stayed suppressed on the unfocused reconnect (the OS toast that did fire was the legitimate Pod connected one). Note the client-side queue is INVISIBLE to /comfy/downloads/status by design - a Queued... tile has no server job - so residuals 1 and 2 rest on user observation plus that timing proof."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-396",
      "type": "task.created",
      "at": "2026-07-29T22:55:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Found while closing MPI-395: uninstalling a model leaves its DONE job in the install store for the full 120s DONE_TTL, so the tile renders a 100% progress bar instead of Install - and it survives Ctrl+R because the store is main-process."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-397",
      "type": "task.created",
      "at": "2026-07-29T22:55:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Uninstall reads as laggy: the grid only flips after a remote /comfy/models/check round trip to the Pod wrapper. Fixing it means an optimistic install-state flip, which contradicts the install-state-IS-files-on-disk invariant - product decision, not a bug fix."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-396",
      "type": "task.moved",
      "at": "2026-07-30T00:05:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "Blast-radius sweep done; implementing the store-side fix."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-398",
      "type": "task.created",
      "at": "2026-07-30T00:40:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Model Library grid paints blank on a COLD renderer (app start, Ctrl+R) for 10-15s. MPI-394 fixed the REBUILD case with a consumer-owned preview cache; a cold renderer's cache is empty, which is the one condition that mechanism cannot help. Observed twice on 2026-07-29/30."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-198",
      "type": "task.moved",
      "at": "2026-07-30T21:55:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "MPI-391 section D started on the Linux desktop (ThinkPad X121e, Ubuntu 22.04). 1.3.0 tar.gz extracted and integrity-checked; launching the local engine next. Card owns the live loader-path check."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-406",
      "type": "task.created",
      "at": "2026-07-30T22:15:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "--cpu engine installs resolve generic PyPI torch and pull the whole CUDA 13 stack (torch 2.13.0 bare, triton, 14 nvidia-* wheels). Root cause: --fast-deps routes through comfy-cli DependencyCompiler which ignores the vendor flag - the same fall-through already exempted for --m-series at engine.js:370-372. Measured from requirements.compiled on a GPU-less Linux box during MPI-391 section D. Wasteful, not broken. --amd suspect for the same reason."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-407",
      "type": "task.created",
      "at": "2026-07-30T22:25:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Slow server start leaves a permanently black window. main.js:789-795 gives up after a hardcoded 5000ms and creates the window anyway; loadURL is refused with ERR_CONNECTION_REFUSED and no did-fail-load handler exists, so it never retries. Server bound ~11.6s after launch and was healthy the whole time. No user-accessible recovery - Ctrl+R is not bound in the production build. Found on the Linux desktop during MPI-391 section D; never seen before because every prior test machine bound the port in under 5s."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-406",
      "type": "task.moved",
      "at": "2026-07-30T22:35:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "Fix landed: --fast-deps is now used only for --nvidia (routes/engine.js), so --cpu and --amd take comfy-cli standard vendor-aware install. Stale comment rewritten to name all three vendors. Needs live confirmation of a +cpu torch resolve on the Linux box."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-407",
      "type": "task.moved",
      "at": "2026-07-30T22:35:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "Fix landed: did-fail-load handler on mainWindow.webContents retries the main-frame loadURL every 500ms (cap 60, skips ERR_ABORTED) until the server answers. The 5000ms fallback is deliberately unchanged - raising it is the symptom patch. Race is INTERMITTENT: cold start reproduced it, warm restart painted normally."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-408",
      "type": "task.created",
      "at": "2026-07-30T22:45:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "Retry after a failed engine install can never succeed on Linux/macOS. uv venv exits 2 with \"A virtual environment already exists at: comfy-venv\" - it refuses ANY existing venv, complete or not - so the first attempt past step 1 poisons every later one and the Installation Failed dialog offers a Retry that is guaranteed to fail. Root cause is an asymmetry in _provisionUvEngine: step 0b clears a stale ComfyUI workspace, its sibling comfy-venv had no guard. Fixed by passing --clear. Found live after a mid-install thermal shutdown."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-409",
      "type": "task.created",
      "at": "2026-07-30T23:55:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "RELEASE BLOCKER. Commit 36f972cf restamped release-baselines to the 1.3.0 manifests BEFORE 1.3.0 shipped, so every rebuild emits a 1.3.0-to-1.3.0 update delta (windows 970KB vs the correct 451MB). v1.2.0 does attach update zips to its GitHub release, so these are real assets. Fix: restore baselines to 36f972cf~1, rebuild, verify fromVersion 1.2.0, publish, restamp afterwards. New FULL artifacts are valid and in place; stale update zips quarantined."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-409",
      "type": "validation.updated",
      "at": "2026-07-31T00:25:00Z",
      "actor": "opus",
      "summary": "Fixed and verified. Baselines restored byte-exact from 36f972cf~1 (blob hashes match) in commit addc03a2; README timing rule corrected to restamp AFTER publish. mpi-ci run 30589473208 from addc03a2 rebuilt all three platforms: update bundles back to 451106855 / 2787524 / 3313302 bytes and every update-manifest.json reads fromVersion 1.2.0 -> toVersion 1.3.0, kind update-bundle, 6408 / 209 / 208 files - counts identical to the last known-good run 30559394491. The MPI-406/407/408 fixes are present in both the deltas and the Linux full tar.gz (engine.js uv venv --clear, --fast-deps gated to --nvidia, main.js did-fail-load retry). Artifacts promoted to D:/CubricStudio/Vision/Builds/v1.3.0/; pre-fix zips moved to SUPERSEDED-pre-fix-bundles-MPI-409/ (kept, not deleted - CI retention is 1 day). Publish is unblocked; restamp to the 1.3.0 baselines only after the release is live."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-409",
      "type": "task.updated",
      "at": "2026-07-31T00:25:00Z",
      "actor": "opus",
      "summary": "maturity in-progress -> validating; validation.md linked."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-407",
      "type": "validation.updated",
      "at": "2026-07-31T00:40:00Z",
      "actor": "opus",
      "summary": "LIVE-VERIFIED on Linux. Cold page cache (sync + drop_caches) reproduced the race: server bound 13.5s after launch, the 5000ms fallback fired, 8 ERR_CONNECTION_REFUSED retries were logged and then stopped - retry 9 succeeded. User confirmed the window painted and the app started normally; gpu-detect and the update check ran after, both renderer-driven. Pre-fix this exact sequence ended in a permanently black window. NOTE the first launch after extracting did NOT trip it (warm page cache, server bound in 0.8s) - a warm-cache launch cannot test this card."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-407",
      "type": "task.updated",
      "at": "2026-07-31T00:40:00Z",
      "actor": "opus",
      "summary": "maturity in-progress -> validating; validation.md linked."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-410",
      "type": "task.created",
      "at": "2026-07-31T00:40:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Startup splash never shows on a first/cold run. main.js:751 logs ERR_FAILED (-2) loading app/splash/splash.html, but the file IS in the artifact - ERR_FAILED is what Electron raises when the WebContents is destroyed mid-navigation, not a missing file (that is ERR_FILE_NOT_FOUND -6). Cross-platform and pre-existing per the user. Inverted behaviour: fails on the slow start it exists to cover, works warm. Candidates: main window ready-to-show firing on the error page and closing the splash at main.js:419-421, or the splash load being starved. Not fixed in 1.3.0 on purpose."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-408",
      "type": "validation.updated",
      "at": "2026-07-31T00:55:00Z",
      "actor": "opus",
      "summary": "LIVE-VERIFIED on Linux. Deliberate repro - Install, Ctrl+C once comfy-install started, relaunch, Install again. Step 1 now logs \"Creating virtual environment with seed packages at: comfy-venv\" and \"+ pip==26.2\" instead of exit 2 / \"A virtual environment already exists\", and step 2 installed comfy-cli 48 packages. uv venv --clear closes this card. The SAME retry then failed at step 3 on a different root cause - comfy install refuses an existing valid clone - filed as MPI-411; that does not reopen this one."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-408",
      "type": "task.updated",
      "at": "2026-07-31T00:55:00Z",
      "actor": "opus",
      "summary": "maturity in-progress -> validating; validation.md linked."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-411",
      "type": "task.created",
      "at": "2026-07-31T00:55:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "Retry still impossible after an interrupted install on Linux/macOS - the workspace half of MPI-408. engine.js:344 only clears a workspace with NO .git, but the clone finishes in seconds while deps take minutes, so an interrupted setup leaves a COMPLETE clone that comfy install refuses (exit 1, names --restore). Fixed by pushing --restore when the workspace is already a clone. Found while verifying MPI-408, which is itself confirmed fixed."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-412",
      "type": "task.created",
      "at": "2026-07-31T01:05:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Engine install screen strobes between a \"preparing dependencies\" label and the download byte counter during the dependency phase. Reported live on Linux; cosmetic, install completes. Candidate cause (unverified): engine:extracting and engine:downloading events interleaving so the install screen flips view on every event."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-406",
      "type": "validation.updated",
      "at": "2026-07-31T01:20:00Z",
      "actor": "opus",
      "summary": "PARTIAL - stays in doing, do NOT close on the --fast-deps commit. Tested live on Linux: requirements.compiled is absent after a full install, which proves --fast-deps is genuinely off the --cpu branch, so this card diagnosis was correct for its stage. But the CUDA stack still installs - grep for \"downloading (triton|nvidia)\" returns 4 and the pip log shows torch, torchvision, triton and 14 nvidia-* wheels. Stage tags settle why: the original evidence was [comfy-install], the new lines are [system] [pip] = runPipCommand running ComfyUI-LTXVideo/requirements.txt with --upgrade. A --cpu engine has TWO paths to the CUDA stack; this card fixed one. Second path tracked as MPI-413. The 1.3.0 changelog entry claiming the processor-only build was REMOVED before publishing rather than shipped false."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-413",
      "type": "task.created",
      "at": "2026-07-31T01:20:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Custom-node requirements reinstall the CUDA torch stack over the CPU one - the second half of MPI-406. downloadManager.js:2181 runs pip -r <node reqs> --upgrade with no index constraint, so torch comes from default PyPI (CUDA) and --upgrade replaces the correct CPU torch. Measured via ComfyUI-LTXVideo on a driverless box: torch, torchvision, triton, cuda-toolkit, 14 nvidia-* wheels. The comment at downloadManager.js:2033 asserting --upgrade is a no-op when satisfied is wrong and is why this stayed invisible. requirementsDrop cannot fix it - timm/diffusers pull torch transitively."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-414",
      "type": "task.created",
      "at": "2026-07-31T02:05:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "A failed comfy install leaves an engine that passes /engine/status and /engine/deps-status, has a real clone and a 359-package venv, and still cannot boot (No module named sqlalchemy) because ComfyUI core requirements never ran and no version stamp was written. Retry routes to deps-only because engineReady is judged by the venv python, which exists from step 1 on the uv path. No in-app recovery exists. Downstream of MPI-411."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-423",
      "type": "task.created",
      "at": "2026-08-01T08:30:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "1.3.0 tells users to \"hit Remove\" for object removal, but there is no Remove command or button - commandRegistry.js has no remove key, models.js lists remove as deprecated/never wired, and the only Remove labels in js/ are Remove Background (a different BiRefNet tool a user will find and misuse). The real path is inpaint with an EMPTY prompt, which inpaint.help already states correctly - so the ? guide and the model tile contradict each other. Three sites; models.js:529 is the urgent one because it is the Model Library detail panel, not a changelog."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-426",
      "type": "task.created",
      "at": "2026-08-02T08:15:43Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Found during MPI-365 live verification: a SAM3 detection is auto-selected and unioned into maskCanvas, so an un-Added pick is injected as Input_Mask. The green overlay must stay a preview - the user may want to Subtract it, not Add it. Reverses the MPI-372/MPI-384 contract, so the op strip has to be re-gated on baked content instead."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.created",
      "id": "MPI-427",
      "at": "2026-08-02T13:30:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Blocked model host must be a readable error, not an error loop. User micha: 44/44 models.cubric.studio downloads failed with TLS WRONG_VERSION_NUMBER in <200ms while 45/45 github.com downloads succeeded - network-side interception, install and D: layout both fine. Fix three defects for 1.3.1: raw BoringSSL error surfaced with no host or remedy plus an unhandled rejection; installStore FAILED is terminal so the failed->queued retry requeue is rejected and the UI read path sticks on failed for every user; app.getPath('documents') throwing hard-exits at boot despite an existing shared.js fallback. Mirror-host failover is approval-gated, see brief."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.moved",
      "id": "MPI-427",
      "at": "2026-08-02T13:40:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "Todo -> Doing. User approved all three fixes for a 1.3.1 release: actionable transport-failure error, failed->queued retry transition, and the app.getPath('documents') boot guard. Mirror-host failover stays out of scope pending a separate go-ahead."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.progress",
      "id": "MPI-427",
      "at": "2026-08-02T15:05:00Z",
      "actor": "opus",
      "summary": "Code complete for 1.3.1, staying in DOING - not closed. Landed: transport-error classification with host + remedy routed to a warning toast instead of the Report-on-GitHub dialog; UW failures now carry their reason instead of bare dep ids; store.requeueDep as an explicit retry door that does NOT widen the transition table (reconciler still cannot resurrect); retry now credits the resumable partial so the bar stops running backwards; app.getPath('documents') boot guard; and a mirror-failover mechanism whose marker comparison is path-equal so a host swap resumes the partial rather than scrapping it. 71/71 suites green (transport-error-message 8 new, install-store 29). Version stamped 1.3.1 and notes approved. OPEN: _MODEL_MIRRORS ships EMPTY so failover cannot fire for anyone - enabling it is an approval-gated Cloudflare change; a same-provider mirror is unproven against this user's failure mode (he verified DNS alone is insufficient and only a tunnel works, stream died at 20 percent = deep-packet, not hostname blocking); 1.3.1 not released; nothing verified in a running app."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.created",
      "id": "MPI-428",
      "at": "2026-08-02T15:10:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Boogu Image Edit gains localised (masked) edit. Carved out of MPI-365, which recorded Boogu as out of scope for the one-master-template migration (already one file) but named its localised-edit work as a separate card. The user re-authored boogu_edit_template.json in the bench: InpaintCropImproved/InpaintStitchImproved, MpiMaskSquareBbox, an MpiString titled Input_Mask feeding an MpiLoadImageFromPath titled mask, and MpiIfElse branches off has_mask - the same mask shape Krea2, Klein and Qwen already ship."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.moved",
      "id": "MPI-428",
      "at": "2026-08-02T15:10:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "Todo -> Doing. Work starts immediately: the raw export is already on disk and the only app-side change the graph implies is the comfyui-inpaint-cropandstitch dep on both Boogu cards."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.progress",
      "id": "MPI-428",
      "at": "2026-08-02T15:17:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "validating",
      "summary": "Code complete, awaiting the live run. Both baked Boogu files went 33 -> 43 nodes carrying Input_Mask, the InpaintCrop/Stitch pair, MpiMaskSquareBbox, MpiAnyChecker and the 2 MpiIfElse branches, with Input_Tier still 1 (high) / 2 (balanced); validate-injection-rules passes on both. comfyui-inpaint-cropandstitch declared on both Boogu ModelDefs - the key already existed in nodesDeps.js (Klein and Head Swap were the prior holders), so no new registry entry and no new download for a Klein user. progressStages stays {single: 1}: the graph already carried 2 SamplerCustom nodes at HEAD (one per tier chain) and crop/stitch adds no sampler. Node suite 305 pass / 0 fail. UNRELEASED and docs/masking.md were extended in earlier commits. Left: the live verify - masked edit on a source over 1536px plus an unmasked edit - and G:\\ComfyUi is holding port 8188, which must be closed first."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.moved",
      "id": "MPI-428",
      "at": "2026-08-02T15:26:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "column": "done",
      "summary": "Doing -> Done. Localised (masked) edit LIVE-VERIFIED by the user on Boogu Image Edit B: a mask painted over edit_014 (832x1248) regenerated only the masked area (a chihuahua) and returned the rest of the frame untouched at the same dimensions - the full new branch, Input_Mask -> MpiAnyChecker -> mask loader -> MpiMaskSquareBbox -> InpaintCropImproved -> sampler -> InpaintStitchImproved. Shipped: the re-authored raw template synced through to both tier files (43 nodes each, injection gate green), comfyui-inpaint-cropandstitch on both Boogu ModelDefs, and the UNRELEASED localised-edit bullet EXTENDED rather than duplicated. 305/305 node tests, lint 0 errors. Two legs recorded as unproven in validation.md and NOT hidden: no source over 1536px was tested (so the InpaintCropImproved dims assert that broke Klein in 1.3.0 is inherited by construction, not measured), and the unmasked whole-image path was not re-dispatched after the rewiring. User approved final completion."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.updated",
      "id": "MPI-382",
      "at": "2026-08-02T15:40:00Z",
      "actor": "opus",
      "column": "todo",
      "summary": "Planned. plan.md written. Grew a STEP 1 the card did not have: the PREVIEW CONTRACT. Verified in code that an unapplied Detect preview never dies - _exitAutoMaskMode(false) in MpiCanvasViewer.js:713 drops the picks, preserves manual+subtract and re-emits mask-ready/mask-clear, and it has NO CALLER; commitAutoMask (line 1352) has none either; mountOptions() destroys the outgoing options compound on every rail switch and never touches the picks. So a detection survives a tool switch and reaches the graph without Add - which is exactly MPI-365 open item. User settled it 2026-08-02 and generalised it to the whole family: every tool is visited, previewed, then applied, or the preview goes away, because surviving previews stack and the user ends up judging a composite he never committed to. Doing it first also dissolves the layer-order problem - with picks gone, Adjust only ever sees manual minus subtract, so the MPI-371 union-last rule cannot interact with a shrink. Accepted cost: Add becomes mandatory. Also healed acceptance 1 and 5, which still said in MpiMaskStrip and baked once on release - both overridden by the 2026-08-01 re-scope on the same card. Mechanism stays blur-plus-alpha-threshold first (needs calibrating against a known circle), with circular-offset stamping as the exact fallback; sharp server-side is ruled out, a round trip per drag frame is not live. Verify mode user-ux. Card stays todo/planned - implementation runs through mpi-continue.",
      "maturity": "planned"
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.updated",
      "id": "MPI-424",
      "at": "2026-08-02T15:40:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "Umbrella progress. MPI-425 shipped 2026-08-02 and is ticked; MPI-382 is planned and is next. brief.md gains a new FIRST standing constraint - THE PREVIEW CONTRACT, stated by the user 2026-08-02: every tool is visited, previewed, then applied, or the preview goes away, and an unapplied preview must never outlive its tool. Rationale is not tidiness - surviving previews stack on each other, so the user judges a composite he never committed to while the graph receives something else again. Detect is the only producer today and it VIOLATES the rule. MPI-382 fixes it and builds ONE discard seam in mountOptions(); MPI-368 and MPI-373 hang their own previews on that seam rather than re-deciding it."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.moved",
      "id": "MPI-382",
      "at": "2026-08-02T15:41:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Implementation started. Step 1 is the preview contract - wire the existing but uncalled _exitAutoMaskMode(false) to a single discard seam in mountOptions(), so no unapplied preview outlives its tool; step 2 is the Adjust tool itself. Checklist derived from plan.md, validation.md seeded. Verify mode user-ux."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "checklist.updated",
      "id": "MPI-382",
      "at": "2026-08-02T15:41:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "Checklist derived from plan.md: the preview contract, then the Adjust tool."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "checklist.updated",
      "id": "MPI-382",
      "at": "2026-08-02T17:24:00Z",
      "actor": "opus",
      "column": "doing",
      "summary": "Step 1 (the preview contract) checked off; step 2 (the Adjust tool) still open."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "validation.updated",
      "id": "MPI-382",
      "at": "2026-08-02T17:24:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Step 1 of 2 SHIPPED and user-verified in the app: THE PREVIEW CONTRACT. el.discardPreview() on MpiCanvasViewer is the one seam, called by mountOptions() on every rail switch and guarded so a preview-less switch does nothing. Found while wiring it that _exitAutoMaskMode(false) was only three quarters written - it cleared the canvas auto layers and the persisted entry but NOT _autoMaskPicks, _lastDetectThumbUrls or the thumb strip, so calling it verbatim would have left the strip advertising picks for pixels that no longer existed; all four now drop together. tests/preview-contract.test.cjs adds 5 tests, three of them negative-controlled against the real source. Suite 310 pass / 0 fail, lint 0 errors on both configs. docs/masking.md was already 211 lines - OVER its own 200 cap before this session - and the remaining prose is load-bearing, so the tool-family half was SPLIT into docs/masking-tools.md (128 + 124) rather than trimmed, the third such split after masking-sam3 and masking-undo, with routing updated in docs/README.md and the CLAUDE.md context router. User verified live: no flicker, undo still correct, brush strokes survive the switch. Step 2 (the Adjust tool itself) NOT started - card stays in doing. Side effect: this closes MPI-365 open detected-but-not-applied mask is still injected item."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.created",
      "id": "MPI-430",
      "at": "2026-08-02T20:58:00Z",
      "actor": "opus",
      "column": "todo",
      "maturity": "needs-decision",
      "summary": "CivitAI licence method is blind to the page badge (verified on 5 Chroma LoRAs); sweep every CivitAI weight and settle whether the ToS grant covers our R2 mirror. DM-the-creator is an accepted remediation."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "type": "task.updated",
      "id": "MPI-365",
      "at": "2026-08-02T21:05:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Licence gate CLOSED. The documented CivitAI method was incomplete: the v1 API carries no licence field, so the Apache-2.0 badge on two weights was invisible and they read as RentCivit-only. Four style LoRAs ship; chroma-style-cinema was DROPPED for withholding Image and the master template was re-exported to renumber the rack to four. Three credit blocks added. New doc docs/models/chroma/licences.md; klein/licences.md gained the trap; sweep carded as MPI-430. R2 upload of 962 MiB running."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-365",
      "type": "task.updated",
      "at": "2026-08-02T21:40:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Brief healed. The 4 dangling-Set BLOCKER is struck through: all four were settled by design during the migration (krea2 stops at two images, Qwen bypassed Width/Height and has no i2i denoise), proven by zero unconsumed Input_ nodes in either runtime graph; only cosmetic GC of nine dead raw-template nodes remains. Status section rewritten: Klein/Krea2/Qwen/Chroma migrated and Chroma fully closed out, but the card CANNOT close - SDXL (5 models, 3 files each) and the video models are unmigrated, so the GC acceptance items (shared-op injectParams, the REPLACE-not-merge branch, DEFAULT_STYLE_OPS) all still have live consumers, verified present today."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-365",
      "type": "validation.updated",
      "at": "2026-08-02T21:55:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Close-out. Chroma slice VERIFIED IN THE APP by the user: depth ratio picker + padded card (one cause - imageSizedOps omitted depth), dead batch control on VAE-encoded-latent ops (new per-op ModelDef.batchOps, swept Chroma AND SDXL), and style strength now defaulting to 0.6 via new ModelDef.controlDefaults. Licence gate closed and weights on R2. Card STAYS in doing: SDXL and the video models are unmigrated, so every GC acceptance item still has live consumers."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-422",
      "type": "task.moved",
      "at": "2026-08-03T04:00:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Validated live and moved to doing. The user ran the shipped 1.3.0 portable at D:\\cubric-install-test, took the prompt, pressed Update, and reported nothing happened - but the update FULLY SUCCEEDED: fetch-release pulled CubricVision-windows-x64-update-v1.3.1.zip (1166674 bytes), apply-update backed up to update/rollback/2026-08-03T03-44-16-461Z and wrote resources/app at APP_VERSION 1.3.1, confirmed by a relaunch showing V1.3.1 in the app header. So validation item 2 (in-app fetch+spawn on the new win-update.cjs path) PASSES, and loadExtractZip took its resources/app branch. evictBusyFile is still unexercised - the delta shipped no CubricVision.exe. What the user experienced is exactly the two carded gaps: no console, no log file, and no relaunch. Fixing both now."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-422",
      "type": "task.updated",
      "at": "2026-08-03T04:15:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "validating",
      "summary": "Both code gaps FIXED and verified live against the 1.3.1 install. (1) win-update.cjs now tees itself plus BOTH helpers stdout and stderr to update/update.log truncated per run - runHelper piped stderr instead of inheriting it from a NUL parent, so the real fetch-release reason appeared for the first time. (2) Relaunch on BOTH outcomes, targeting root CubricVision.exe not process.execPath (evictBusyFile can have renamed the running image aside) with ELECTRON_RUN_AS_NODE deleted from the child env. (3) Failure writes update/update-result.json carrying the last helper stderr line, new main.js update-last-result IPC reads-and-deletes it, updateChecker raises showError and skips that boots update prompt so two dialogs never stack. (4) Same marker + relaunch on update.sh and update.command, logging to a file only when stdout is not a tty so a double-clicked run keeps its terminal output AND its real exit status. Evidence: bogus-repo run wrote log + valid marker + relaunched; real-repo run re-applied the 1.3.1 delta from the resources/app layout and cold-relaunched the app (0 processes to 6, fresh boot in app.log); a planted marker made the app log previous update run failed and raise the dialog. Test install restored to clean 1.3.1, all three files hash-matched. 314/314 node tests pass, eslint clean. NOT closed: the fix cannot help 1.3.1 to 1.4.0 because the updater that runs is the one already on disk - it first takes effect 1.4.0 to 1.5.0, and 1.4.0 release notes must tell users to reopen manually. evictBusyFile still unexercised."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-422",
      "type": "task.moved",
      "at": "2026-08-03T04:25:00Z",
      "actor": "opus",
      "column": "done",
      "maturity": "complete",
      "summary": "CLOSED - every gap fixed and every inherited validation item settled, none of it deferred to 1.4.0. Two fakes replaced the two things that looked like they needed a real newer GitHub release, and neither needed an upload. (a) evictBusyFile, unexercised since MPI-387: apply-update accepts an already-extracted DIRECTORY as its bundle (the MPI-62 Safari path), so a folder holding a copy of CubricVision.exe plus a manifest listing that one file is a complete update bundle - run through CubricVision.exe as node the target is genuinely busy. The Aug 1 image moved to CubricVision.exe.old and the new one landed at the original path. (b) The in-app button end-to-end: check-for-update reads require(./package.json).version, so stamping the install back to 1.3.0 makes the LIVE 1.3.1 release genuinely newer - real compare, real prompt, real user click, real fetch, real apply, real relaunch, button to running-again in about 3 seconds, confirmed on screen. CORRECTION to this brief: its claim that process.execPath can be the retired binary is WRONG. Measured - with the running image renamed aside mid-flight a live electron-as-node process still reported the original CubricVision.exe path, because Node captures execPath once at bootstrap and eviction leaves that path holding the NEW binary. Root path kept for being explicit, not because execPath is broken; doc corrected. Install restored to pristine 1.3.1, three files hash-matched. Residual, NOT blocking: the Linux and macOS updaters were changed but never run (sh -n only, no box), and 1.4.0 users still get 1.3.1 silent updater - the release-note requirement is now in github-release-checklist.md where it will actually be read."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-438",
      "type": "task.created",
      "at": "2026-08-04T06:15:00Z",
      "actor": "opus",
      "column": "todo",
      "maturity": "planned",
      "summary": "Carded out of MPI-413 Pod testing. UNIVERSAL ops break on a Pod volume because code-only node packs reach the volume only via a MODEL dep list. universal_workflows.js lines 5-7 promise universal deps install WITH the engine, and shared.js checkUniversalWorkflowDepsStatus delivers that for the LOCAL engine only - there is no remote twin, so baked packs ride the image while the 7 code-only packs are model-gated. Live proof on Pod jtoo2fl2xgtt5y volume 9t3awufudk: Resize Video died with missing_node_type VHS_LoadVideoPath, Pod object_info had 1822 node types and zero VHS_*, remote/model-present said comfyui-videohelpersuite present:false while control comfyui_ultimatesdupscale said present:true. NOT an MPI-413 regression - the deleted wrapper code only ran requirements for an already-extracted folder and never downloaded a node. Measured blast radius: VideoHelperSuite needed by resizeVideo, declared by 2 of 18 models (wan-22, wan22-5b); comfyui-inpaint-cropandstitch needed by appHeadSwap, declared by 3 of 18 (klein-4b, boogu-edit-high, boogu-edit-balanced) - a SECOND live-broken op nobody reported, kept alive by MPI-332; MpiNodes needed by all 12 ops but declared by all 18, so broken only on a zero-model volume. Sweep trap recorded: local custom_nodes/ComfyUI-MpiNodes is a SYMLINK so readdir withFileTypes isDirectory() is false and every Mpi* class vanishes from the index."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-438",
      "type": "task.moved",
      "at": "2026-08-04T06:25:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Implementation started. Option A chosen by the user: ensure the code-only UNIVERSAL node packs exist on the Pod volume on EVERY connect, matching what the local engine already does at boot. Rejected: lazy per-graph install (needs a graph-to-pack map at dispatch and still stalls mid-generation). Shape: shared.js exports the universal dep OBJECTS; remoteModels.ensureUniversalNodesOnVolume() keeps custom_nodes deps that are NOT _isImageResident, one batched remoteModelsCheck to find the absent ones, remoteInstallDep each (wrapper stamps .mpi_node_commit from getPinnedNodeCommit), poll until they land, then ONE ComfyUI restart; remotePodLifecycle fires it once per Pod, non-blocking, when /remote/comfy/status first sees the wrapper ready. A complete volume costs one batched status call and nothing else."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-438",
      "type": "task.progress",
      "at": "2026-08-04T06:45:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Code landed, suite green, live verification NOT claimed. shared.js getUniversalWorkflowDeps() returns the universal DEP objects; remoteModels._universalVolumeNodeDeps() selects the code-only packs (exactly 7, with 16 baked/weight deps excluded) and ensureUniversalNodesOnVolume() does one batched remoteModelsCheck, installs each missing pack via remoteInstallDep so the wrapper stamps .mpi_node_commit from getPinnedNodeCommit, polls the same check to a 60s deadline, and restarts ComfyUI ONCE if anything landed; remotePodLifecycle._ensureUniversalNodes() fires it once per podId, non-blocking, the first time the wrapper reports comfy_ready. A complete volume costs one batched status call and nothing else. New guard tests/universal-nodes-remote.test.cjs asserts the SHIPPED selection, not a copy of it. Four sabotages run: dropping the _isImageResident filter, dropping VideoHelperSuite, and an id-less dep were all caught; the fourth (removing the id stamp in getUniversalWorkflowDeps) PASSED and that was the useful one - all 112 DEPS entries already self-identify, so the spread was dead code carrying a comment that misstated the data, and both were deleted. Suite 390/0, eslint 0 errors and 0 new warnings (routes/remoteModels.js:695 has a pre-existing unused eslint-disable, confirmed on the unmodified file, left alone). NOT VERIFIED LIVE: no Pod was running, so ensureUniversalNodesOnVolume has never executed against a wrapper - the install/poll/restart sequence is code-verified only and folds into the MPI-413 image test."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-413",
      "type": "attention.required",
      "at": "2026-08-04T07:05:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Session ended with the card in doing. Wrapper 0.2.41 is LIVE on the R2 dev channel and proved loaded on a real Pod; the start.sh guard is committed (mpi-ci 4854496) but NOT published. Next steps are all USER-AUTHORIZED live ops: ./publish-runtime.sh dev, then the image build, then one Pod boot. promote is NOT part of this card - it is a release gate."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-438",
      "type": "attention.required",
      "at": "2026-08-04T07:05:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "validating",
      "summary": "Session ended with the card in doing, maturity validating. Code complete and suite-green at 390/0, with validation.md holding the diagnosis, the measured blast radius and the four-sabotage table. ZERO live verification - ensureUniversalNodesOnVolume has never executed against a wrapper. Needs the same Pod boot as MPI-413: on a volume with no Wan 2.2 and no Klein/Boogu, Resize Video AND Head Swap must both succeed."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-439",
      "type": "task.created",
      "at": "2026-08-04T12:50:00Z",
      "actor": "opus",
      "column": "todo",
      "maturity": "planned",
      "summary": "Convert mask to paint / paint to mask on the image-mode canvas context menu. Gated on viewer.el.hasMask() / hasPaint(); both directions are layer-wide one-shot ops needing _recordUndo() after the no-op guard, or they are silent Ctrl+Z holes. Filed under the MPI-424 canvas tool family; three open product questions carry assumed defaults in brief.md."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-424",
      "type": "task.note",
      "at": "2026-08-04T12:50:00Z",
      "actor": "opus",
      "column": "done",
      "maturity": "complete",
      "summary": "Follow-up MPI-439 filed against this family (mask <-> paint conversion on the canvas context menu). This card stays done - its five members all shipped; 439 builds on the taxonomy rather than joining the set."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-440",
      "type": "task.created",
      "at": "2026-08-04T12:55:00Z",
      "actor": "opus",
      "column": "todo",
      "maturity": "planned",
      "summary": "Second canvas-family umbrella, opened after the user spotted MPI-424 in done with family work still outstanding. Verified first: 424 is correctly closed - all five of its members shipped. The five open cards (426, 435, 436, 439, 421) were never members of it, so the fix is a new umbrella, not a reopen. Order and the one shared alpha-vs-luminance decision are in brief.md."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-424",
      "type": "task.note",
      "at": "2026-08-04T12:55:00Z",
      "actor": "opus",
      "column": "done",
      "maturity": "complete",
      "summary": "Confirmed correctly closed on 2026-08-04: MPI-425, 382, 368, 375 and 373 are all in done, which is exactly what this card acceptance required. The canvas work still outstanding (426, 435, 436, 439, 421) was never part of the five - it is now carried by MPI-440, umbrella #2. Do not reopen this card."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-440",
      "type": "task.moved",
      "at": "2026-08-04T13:01:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "Umbrella #2 picked up. Order CORRECTED after reading all four member cards end to end: MPI-421 is not independent (it shares MpiCanvasViewer exec.onMasks with MPI-426, so they are a pair), and MPI-435 drops from second to last (pure polish, brushDab.js already shipped, blocks nothing). New order 426 -> 421 -> 436 -> 439 -> 435."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-426",
      "type": "task.moved",
      "at": "2026-08-04T13:01:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "First member of MPI-440. Reverses the MPI-372/384 decision that a detection puts real pixels in maskCanvas: exec.onMasks calls setSelectedAutoPicks + evaluateMask, MaskManager composites picks into the mask, so an un-Added preview ships into Input_Mask. The op strip must be re-gated on BAKED content in the same pass or MPI-372 returns."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-426",
      "type": "task.progress",
      "at": "2026-08-04T13:20:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "validating",
      "summary": "Code complete, probe-verified, docs updated. Root cause: maskCanvas answered two questions at once - what is on screen and what gets sent - because _recomposite() unioned selectedAutoPicks into it. Fix is a DELETION: drop that union, and all 12 hasMaskContent(maskCanvas) sites plus getURL() became baked-only for free. Twin _buildCompositeFromTemp() fixed in the same pass (it fed _previewMaskCache -> getCurrentMaskDataURL, the second door). Display verified pixel-identical - autoCanvas already drew green on top and that recolour is opaque. USER DECISION: op strip gates on baked content, so a bare detection leaves masked ops locked until Add; this needed NO new code since _opOptions() already reads hasMask(). Rejected the alternative (unlock on detection) because it would offer a masked op while the mask sent was empty. Proof: 12/12 real-pixel probe in Chromium against the live MaskManager, with a sabotage control that failed exactly the two bug assertions; 392/392 node suite; eslint 0. NOT verified: no live click-through of detect-then-Add in the app."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-426",
      "type": "task.moved",
      "at": "2026-08-04T13:51:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "USER-VALIDATED live in the app 2026-08-04: a detection no longer enters the mask, and the masked ops stay locked until Add. Fix was a deletion in MaskManager._recomposite() plus its twin _buildCompositeFromTemp(); op-strip re-gating came free because _opOptions() already read hasMask(). Shipped in f64fc75e. First member of the MPI-440 umbrella; next is MPI-421 on the same exec.onMasks path."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-440",
      "type": "task.progress",
      "at": "2026-08-04T13:51:00Z",
      "actor": "opus",
      "column": "doing",
      "maturity": "in-progress",
      "summary": "Member 1 of 5 shipped: MPI-426 done and user-validated (f64fc75e). Session ends here by choice, umbrella stays in doing. NEXT IS MPI-421 - it shares MpiCanvasViewer exec.onMasks with the card just closed, which is why the order was corrected to put it second. Standing decision now inherited by every remaining member: hasMask() means BAKED content only, and the op strip gates on it, so a bare detection leaves masked ops locked until Add. Still open and still needing ONE shared answer before 436/439 diverge: does an op read paint alpha or luminance, and do soft edges survive."
    },
    {
      "at": "2026-08-04T16:20:00Z",
      "type": "task.deleted",
      "task": "(bulk)",
      "note": "mpi-archive: archived 88 done tasks to tasks/_archived/ (workspaces preserved, per-task task.deleted events logged). Held back MPI-198/MPI-370/MPI-379/MPI-406 - attention required. 19 of the archived cards had no validation.md; user approved on the grounds that their evidence is the commit and the subsystem doc."
    },
    {
      "at": "2026-08-04T16:45:00Z",
      "type": "task.deleted",
      "task": "MPI-406",
      "note": "Closed and archived: hold reason named MPI-413, which shipped and absorbed the residual."
    },
    {
      "at": "2026-08-04T16:56:00Z",
      "type": "task.moved",
      "task": "MPI-441",
      "note": "todo -> doing. Implementation started under the MPI-440 umbrella; plan.md attached."
    },
    {
      "at": "2026-08-04T19:12:00Z",
      "type": "task.moved",
      "task": "MPI-441",
      "note": "doing -> done. User-validated in the app: Grow +50 and a 35px Edge band both track the real silhouette."
    },
    {
      "at": "2026-08-04T19:35:00Z",
      "type": "task.created",
      "task": "MPI-442",
      "note": "The pre-commit hook told everyone to flip lint to strict \"in a separate tracker once clean\" - there was no tracker. Measured: 19 warnings, 9 files."
    },
    {
      "at": "2026-08-04T22:30:00Z",
      "type": "task.moved",
      "task": "MPI-444",
      "from": "todo",
      "to": "doing",
      "summary": "Picked up. Consumers re-verified: server.js:146, services/brokerBoot.js:61 and services/connectorResponder.js:80 all dynamic-import @cubric/connector inside try/catch and return null on failure, so option (d) is sound in principle. Next step is the npm ci test on a repo COPY outside c:/AI/Mpi so the sibling is genuinely absent."
    },
    {
      "at": "2026-08-04T23:50:00Z",
      "type": "task.created",
      "task": "MPI-446",
      "note": "Split from MPI-444: two desktop specs are test.fixme'd on CI because shell.js parks boot behind the first-run engine-install modal on any engine-less profile. Fixture gap, not an app bug."
    },
    {
      "at": "2026-08-05T00:05:00Z",
      "type": "task.moved",
      "task": "MPI-444",
      "from": "doing",
      "to": "done",
      "summary": "Green master run 30957212665 and a deliberate red on PR #1. Three real defects found by the gate before it was finished; two desktop specs fixme'd on CI under MPI-446."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-449",
      "type": "task.moved",
      "at": "2026-08-05T05:10:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "Picked up for bench research. Storage moves off G: to C:/AI (205.8 GB free), so the 53.7 GB ceiling in the brief no longer binds. Weight matrix, variant meanings and the turbo-LoRA question researched; a UK licence exclusion surfaced as the real blocker."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-451",
      "type": "task.created",
      "at": "2026-08-05T06:30:00Z",
      "actor": "opus",
      "summary": "Licence flow-down obligation from the MiniMax H3 authorization needs a user consent gate in front of a licence-gated model download. Blocks shipping H3; does not block MPI-449 bench research."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-451",
      "type": "task.updated",
      "at": "2026-08-05T07:00:00Z",
      "actor": "opus",
      "summary": "Generalised from an H3-only consent gate to a reusable licence-gate system driven by a ModelDef descriptor; Flux named as the next consumer. Design settled: territory-restricted licences route the user to the licensor's own authorization form rather than disclaiming responsibility onto them. Marked as blocking MPI-452."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-452",
      "type": "task.created",
      "at": "2026-08-05T07:00:00Z",
      "actor": "opus",
      "summary": "H3 model wiring split out from the research card. Created blocked: the licence gate (MPI-451) must land first, and whether the authorization reaches end users in excluded territories is still unconfirmed."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-458",
      "type": "task.created",
      "at": "2026-08-06T13:52:00Z",
      "actor": "opus",
      "summary": "Filed from the Flow-rename session: npm start died immediately while npm run test:desktop was running. Verified at the time that the suite takes its own free port (60525, 61661) and a per-test CUBRIC_E2E_USER_DATA, and that nothing was listening on 3000 - so neither port nor profile collided. main.js sets userData at 258 BEFORE requestSingleInstanceLock at 291, which should already isolate the lock, so the mechanism is NOT understood and the card opens at research. Second suspect with an identical symptom: server.js exits non-zero on EADDRINUSE and main.js makes that fatal. Repro must name which fires (silent quit = lock, app.log fatal = port) before any guard ships."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-462",
      "type": "task.moved",
      "at": "2026-08-06T23:33:00Z",
      "actor": "claude",
      "from": "todo",
      "to": "doing",
      "summary": "Moved to doing. Defect 1 fix applied: _sharedOwnedDepIds now gates on _installedOpsOf (>=1 op, the predicate every other install decision and the backend uninstall guard use) instead of the whole-universe m.installed flag, and intersects each owner's universe with its on-disk dep status - a >=1-op model is not universe-complete, so an absent dep must stay in the target model's denominator. Defect 2 (16GB owned by nothing) still open."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-464",
      "type": "task.created",
      "at": "2026-08-07T00:21:00Z",
      "actor": "claude",
      "summary": "Split from MPI-462. The orphan collector shipped local-only; the remote uninstall branch returns before it. Needs _remoteSharedDepIds + a volume inventory, and a live Pod to verify before any deletion code aims at a user's volume."
    },
    {
      "at": "2026-08-07T04:09:00Z",
      "type": "task.moved",
      "task": "MPI-457",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "MPI-468 build order step 2 - the MECHANIC starts. Child MPI-467 (the GUARD) is in validating with only its live proving run outstanding, and that run is user-scheduled behind H3/LTX/Wan, so it does not block this. Scope confirmed against the umbrella: EXTEND /mpi-bump-engine, never create a second bump skill. node_lock.json stays READ-only here."
    },
    {
      "at": "2026-08-07T04:27:25Z",
      "type": "task.updated",
      "task": "MPI-457",
      "actor": "opus",
      "note": "Mechanic shipped (fc731113). /engine/upgrade takes the in-place path by default - git fetch --tags + checkout the node_lock pinned sha + pip for ONLY the requirement lines that moved + restamp .mpi_engine_version + repair whatever nodes the same lock change drifted. The wipe survives as _fullEngineReinstall and is reached by a DETECTED signal (dead tree / a deprecated node carrying our .mpi_node_commit marker / a moved engine-owned package / any in-place throw). Gate 5 is now a command: scripts/engine-floor-check.mjs, proven in all three branches against the LIVE 0.30.0 engine (33 workflows, 171 class_types, 0 missing; a bogus-class_type probe and an unreachable engine both exit 1). Per MPI-468 the skill was EXTENDED, not duplicated: /mpi-bump-engine gate 5 + a new docs/playbooks/bump-engine/02-local-upgrade.md. Suite 482/482, eslint clean. OUTSTANDING: acceptance #5 only - the real end-to-end upgrade on this machine did not run because the engine was mid-LTX-generation all session and the in-place path stops ComfyUI first. The exact pass is written out in validation.md and the user scheduled it for when the engine is free."
    },
    {
      "at": "2026-08-07T04:27:25Z",
      "type": "task.updated",
      "task": "MPI-468",
      "actor": "opus",
      "note": "Umbrella progress: MPI-457 (the MECHANIC) implemented and in validating alongside MPI-467 (the GUARD). The split held - one skill, one playbook: 457 extended /mpi-bump-engine gate 5 and added 02-local-upgrade.md rather than creating a second bump skill. node_lock.json was never touched. Both children now sit on a LIVE run each: 457 needs one in-place upgrade on this machine (minutes, free, user-scheduled for when the engine is idle); 467 needs the Pod smoke matrix (~281GB + a GPU hour, scheduled behind H3/LTX/Wan)."
    },
    {
      "at": "2026-08-07T04:47:20Z",
      "type": "task.updated",
      "task": "MPI-467",
      "actor": "opus",
      "note": "Two defects in --models, fixed in 51dc080e, found while the user asked whether a release can skip models. (1) The flag NEVER RAN: resolveSmokeSet returned early on the scoped branch and skipped the tail that computes totalGb/depIds/skippedWeights, so printPlan died on set.totalGb.toFixed(1). Both branches now share one tail. (2) A scoped run wrote evidence indistinguishable from the full matrix - 7 pass / 0 fail - because checkSmokeEvidence reads only engine version, fail count and staleness. The runner refuses to fold an in-run SKIP into the pass count; a model excluded by --models never became a result row at all, the same lie one level up. evidence.scope now carries requested/modelsRun/covers/unproven/modelsInRegistry; --plan prints the unproven list BEFORE anything is rented; release:check REPORTS coverage and does not gate on it (the user explicitly chose to leave the gate engine-pin-only). A scoped set is deliberately not deduped, so covers is empty and --models sdxl-realistic proves nothing about its four siblings. Verified: --plan --models klein-4b prints 18 of 19 unproven, the full --plan prints no scope warning, all three release:check branches exercised by probe evidence files (written, run, removed). Suite 482/482."
    },
    {
      "at": "2026-08-07T04:47:20Z",
      "type": "task.updated",
      "task": "MPI-467",
      "actor": "opus",
      "note": "Release-gate scope decided by the user and NOT widened: checkSmokeEvidence stays engine-pin-only (release-health-check.mjs:448). Known hole, deliberately accepted - a release that bumps only a node pin does NOT owe smoke evidence, which is the exact shape of MPI-465s own fix commit c077efa9 (it moved a nodes[].commit and left comfyui.core.tag alone). Same for adding a model. Widening the trigger would cost a ~281GB volume fill plus a GPU hour on every node bump; the user will drive the smoke by hand per version instead. Revisit only if a node-bump release ships broken. The floor check (scripts/engine-floor-check.mjs) is also NOT in release:check - it needs a live engine on 48188 and CI has none; wiring it would need its own evidence file."
    },
    {
      "at": "2026-08-07T06:33:50Z",
      "type": "task.created",
      "task": "MPI-471",
      "actor": "opus",
      "note": "Fixed in 22a91d12 in the same session it was found. Root cause was WHERE the marker lived, not a missing re-check: it is a claim about site-packages, so it belongs beside the interpreter that owns them. Anchored on getPythonBin(). Verified live on this machine - after the reinstall the marker was ABSENT at the new path (old code would have skipped again), the curated install ran, 80 packages landed, and scripts/engine-floor-check.mjs went from 17 missing to 0 (1877 class_types, 171/171). Invariant pinned in tests/curated-python-deps.test.cjs: beside the interpreter, never ENGINE_ROOT, and on win32 inside the tree the wipe removes."
    },
    {
      "at": "2026-08-07T06:35:30Z",
      "type": "task.moved",
      "task": "MPI-457",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "note": "ACCEPTANCE 5 RAN AND PASSED on the real machine. Engine put back to v0.29.2 + stamp, POST /engine/upgrade, in place 0.29.2 -> 0.30.0 in ~3 seconds with NO download. Verified: stamp 0.30.0, comfyui_version.py 0.30.0, HEAD exactly the node_lock pinned commit, the ComfyUI-MpiNodes junction SURVIVED (acceptance 4), update/ and the .bat launchers untouched, and scripts/engine-floor-check.mjs green at 1877 class_types / 171 of 171 / 0 missing. Driven through a throwaway :3999 harness because the app server on :3000 predated the fix and Express does not hot-reload - POSTing there would have run the OLD wipe. The run found THREE defects no unit test could reach: (1) _findDeprecatedNode false-positived on a leftover ComfyUI-MpiNodes.stale-aaa1d2d9.disabled and sent an 11GB engine to the wipe - ComfyUI SKIPS .disabled folders so it was never imported; (2) the wipe never called stopComfyUI, and because node deletes children CONCURRENTLY it destroyed update/ and every .bat before EBUSY aborted it, leaving a half-deleted engine; (3) MPI-471, a SHIPPED bug - the curated-deps marker at ENGINE_ROOT outlived the site-packages inside the portable that the wipe removed, so /comfy/start skipped the install and five node packs IMPORT FAILED with 17 class_types gone, silently. All three fixed and committed (6856d841, 22a91d12). Card closes. NOT proven: a bump that genuinely MOVES a pip pin (this one was Requirement already satisfied x3) and the engine-owned-package signal, both unit-covered only."
    },
    {
      "at": "2026-08-07T06:35:30Z",
      "type": "task.updated",
      "task": "MPI-468",
      "actor": "opus",
      "note": "Child MPI-457 (the MECHANIC) CLOSED - in-place upgrade proven end to end on the real machine, floor check 171/171. Umbrella stays in doing for MPI-467, whose Pod smoke matrix has still never executed. The user surfaced the next blocker for it: the Pod image is a full engine behind (mpi-ci/cubric-vision-pod/node_lock.json is v0.29.2 vs Vision v0.30.0, with comfyui-kjnodes and ComfyUI-MpiNodes drifted). Nothing tested today is invalidated by that - today was the LOCAL half, gate 5 - and the smoke would have hard-failed at gate 7 (assertPodEngineVersion dies on a mismatch) before spending anything. The Pod is stale, not broken: it runs the coherent OLD pairing 0.29.2 + old KJNodes, while MPI-465s break is 0.30.x + old KJNodes. Playbook gate 6 and the skill now spell out the cross-repo lock SYNC that was missing - the Pod lock is a different file in a different repo, so bumping Vision does nothing to it, and an un-synced lock rebuilds the image at the OLD engine. Drift command added."
    },
    {
      "at": "2026-08-07T19:58:00Z",
      "type": "task.created",
      "task": "MPI-476",
      "actor": "opus",
      "note": "Four user-reported drifts on the model surfaces, all shipped in d2800e4c + 01014c1e and left in validating for the users own eye test in Electron. Wan 2.2 Smooths card still offered an Operations toggle after MPI-470 deprecated t2v_ms - one entry, nothing to choose - so wan-22, the last commonDeps+operations{} model, is FLAT and the whole per-op install UI is gone (toggles, drafts, s_modelOpDraftByModel, the per-op uninstall subtraction). That forced a guard ADDITION, not just removals: isOperationInstalled ignored the op argument on a flat model, so flattening wan would have answered YES for the deprecated t2v_ms and re-opened MPI-453 for a legacy history item; it now requires the op to be in supportedOps. ModelDefs may now declare minVramGb - H3 states 12 where the fitted floor rounded to 16, which read as do-not-bother to a 12GB card. The H/B/L letter gated on modelFamily, so cards that disambiguated nothing wore one; it now needs an INSTALLED model with the SAME name in another tier, which today is only LTX 2.3. MiniMax H3 re-tiered high to balanced. And staged chips kept the labels of whichever model was active when the media was staged."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-476",
      "type": "task.moved",
      "at": "2026-08-08T00:30:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "User confirmed items 2b and 2c in the UI 2026-08-08 - all four fixes now eye-tested in Electron. Verified independently offline first: footprint.js imports into bare Node, and across all 20 models every floor lands on CARD_SIZES, RAM is monotonic in VRAM and every figure sits on the 4GB grid; wan-22, ltx-23-balanced and qwen-edit read 12GB min, sdxl 8 and ltx-23 16 unchanged. The changelog entry drafted for this card was CUT on the user's call - the fix is real but not worth a line."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-478",
      "type": "task.created",
      "at": "2026-08-08T00:30:00Z",
      "actor": "opus",
      "summary": "The local engine launches ComfyUI with --lowvram for EVERY NVIDIA GPU, unconditionally (routes/comfy.js). The authoring bench runs with no VRAM flag at all. That is the one unconditional difference between where the fast H3 runs happened and where the slow ones do, and it matches the reported symptom - dedicated VRAM sits at 11.5 of 16GB while 23.7GB spills to shared. NOT yet proven to be the cause: the app's own log shows 6.4 to 143 s/it tonight, so settings dominate. One isolation test decides it."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-479",
      "type": "task.created",
      "at": "2026-08-08T01:40:00Z",
      "actor": "opus",
      "summary": "Found while chasing an unrelated H3 speed report. Evidence is the user's own .meta sidecars: 7 ref2v_ms runs, and every one that ran at the DEFAULT refImageSize recorded op: null while both runs where he touched the control recorded the key. Carded rather than fixed inline because the snapshot feeds every Reuse Prompt path."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-475",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "H3 ref2va CLOSED on the user's own call 2026-08-08 after a full live session: the judgement run had already passed, and the evening exercised the chips, the Reference detail control, the settings popup and a dozen real generations. He said H3 is ready to ship."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-474",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-470",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08. Wan 2.2 runs as a single i2v_ms op and its Model Library tile was seen in the same session that eye-tested MPI-476's four fixes."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-462",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08. The remaining item was one uninstall after a server restart, where 'swept 0' was always a valid pass. Remote twin stays deferred to MPI-464."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-452",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08 - H3 fl2va has been generating throughout the H3 sessions."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-451",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08. The licence gate was awaiting validation in the H3 session; that session ran and both H3 cards install and generate."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-410",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-404",
      "type": "task.moved",
      "at": "2026-08-08T04:10:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-420",
      "type": "task.moved",
      "at": "2026-08-08T04:20:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-453",
      "type": "task.moved",
      "at": "2026-08-08T04:20:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08 ('maybe, not sure'), so the gap is recorded rather than dropped: the code is backed by 9 passing tests in tests/uninstalled-op-gate.test.cjs built from a REAL 400 body transcribed out of app.log, including a test that asserts the OLD reader fails on it - but the user has never SEEN the toast fire live, because reaching it needs an op whose weights are genuinely absent. If an uninstalled op ever opens the error dialog instead of toasting, reopen this."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-374",
      "type": "task.moved",
      "at": "2026-08-08T04:20:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08 - UI size survives a restart."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-449",
      "type": "task.moved",
      "at": "2026-08-08T04:20:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed on the user's call 2026-08-08. This was the feasibility research card and it produced its answer in full: H3 ships as TWO models (minimax-h3 fl2va and minimax-h3-ref2va), both generating, with the findings written up across docs/models/h3/ (README, performance.md, ref2va.md). Nothing about it is still open - the follow-on work has its own cards."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-459",
      "type": "task.moved",
      "at": "2026-08-08T04:45:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed at close-out 2026-08-08 under the new rule: agent evidence is sufficient evidence. npm test 459/459 including a new ordering assertion, eslint clean on all three route files, a require() smoke proving no cycle - AND a live run on the user's own machine 2026-08-06 with the marker deliberately corrupted to force the pass, captured in app.log: the pip pass ran at 14:35:42 and ComfyUI spawned at 14:35:57, i.e. before the process existed, which is the whole WinError-5 mechanism. Self-heal confirmed by the restamp. Nothing here needed a human eye."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-463",
      "type": "task.moved",
      "at": "2026-08-08T04:45:00Z",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "summary": "Closed at close-out 2026-08-08 under the new rule. Evidence is tests/lane-settle-on-bail.test.cjs - 4 tests that bound the pre-dispatch region by its two ends and BITE BY CONSTRUCTION: the pre-fix shape fails with the exact line (1468: exec.onError?.(new Error('model_missing'))). Test-only, which the rule accepts; if a lane is ever seen stranded in the wild again, reopen."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-467",
      "type": "task.updated",
      "at": "2026-08-08T05:00:00Z",
      "actor": "opus",
      "summary": "Handoff c3a71f04 written for a fresh session. MPI-467 is the 1.4 blocker and the only one: release:check fails solely on the missing smoke-evidence.json. The handoff leads with the prerequisite the --plan run reports (the Pod lock is behind on python_deps.txt, so smoking now measures the old engine), then the DEV-tag-only image build, then the smoke run itself."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-461",
      "type": "task.moved",
      "at": "2026-08-08T05:40:00Z",
      "actor": "opus",
      "summary": "todo -> doing. Rescoped: the twelve-bail lane strand was already fixed structurally by MPI-463 (_failBail, commit 9d83ea6e). MPI-461 keeps the one acceptance MPI-463 never carried - the user-visible half. Four pre-dispatch bails still settle SILENTLY (workflow-name resolve, trimmed-video prep, workflow JSON fetch, injector throw), and the fetch one is exactly the live 404 that was reported."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-479",
      "type": "task.moved",
      "at": "2026-08-08T05:40:00Z",
      "actor": "opus",
      "summary": "todo -> doing. Implementing fix (1) from the brief: backfill the reuse snapshot with each control the op actually OFFERS, resolved through op/model/global defaults. Needs the PromptBox mount gate extracted first - backfilling ungated would write a hidden control default into the cross-model shared bucket."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-461",
      "type": "task.moved",
      "at": "2026-08-08T06:05:00Z",
      "actor": "opus",
      "summary": "Shipped the residual half. MPI-463 had already closed the lane strand structurally (_failBail, 9d83ea6e); what MPI-461 still owed was the user-visible acceptance. _failBail now clientLogger.errors EVERY pre-dispatch bail and, unless the site passes { reported: true }, emits ui:error through the same _formatWorkflowError the post-dispatch catch uses. Four bails were silent before this - workflow-name resolve, trimmed-video prep, workflow JSON fetch (the live 404 that was reported) and the injector throw. Six sites opt out because they already emit their own actionable copy. Tests: two new ones "
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-479",
      "type": "task.moved",
      "at": "2026-08-08T06:05:00Z",
      "actor": "opus",
      "summary": "Fix (1) from the brief, shipped, plus the extraction it needed. visibleControlIds (PromptBoxControls) now owns the control-visibility gate that used to be 13 inline continues in MpiPromptBox._refreshOpSlot, and resolveControlDefaults resolves each visible control default through the op/model/global layers. _snapshotControlState merges those UNDER the stored values in all three buckets, and the ratio/batch reconciles no longer require the key to pre-exist. ratio, batch and qualityTier opt out via snapshotDefault:false (their stored shape is not their defaultValue). PROVEN LIVE in the renderer, "
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-481",
      "type": "task.moved",
      "at": "2026-08-08T05:52:00Z",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "summary": "Picked up. Root cause confirmed by reading the wrapper: the ATTACH guard's two in-flight arms (_remoteDepIds and depJob.status=='downloading') are module-level cache scoped to a Pod instance that nothing invalidates when that Pod dies, is deleted, or warm-cycles. Fixing it with statusResults as the brief suggested would REGRESS MPI-97 (installed:false is also what a genuinely live download reports, so every shared-dep attach would fire a duplicate install and hit the wrapper's 409 install_in_progress). The real fresh truth is GET /wrapper/models/install/active, which the wrapper has carried since image v0.2.1 and the app has never called."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-481",
      "type": "validation.updated",
      "at": "2026-08-08T06:05:00Z",
      "actor": "opus",
      "summary": "Fix shipped + unit-proven; card held in doing/validating because nothing has touched a Pod. The guard now cross-checks BOTH in-flight arms against the wrapper's own install registry (GET /wrapper/models/install/active) instead of its module-level cache. Brief corrected on three points: fix (1) as written would have regressed MPI-97 (installed:false is also what a LIVE download reports, so it cannot separate a corpse from a real attach), fix (2) folds into fix (1) and must not be scoped to 'no Pod active' (a replacement Pod and a warm-stop resume are both stale WHILE isRemoteActive() is true), and there is no third instance - the local siblings cannot go stale because a local download dies with its own process. 4 new tests, mutation-checked; 503/503 suite green. Owed: one live interrupted Pod install, which the MPI-467 smoke run can carry."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-479",
      "type": "task.moved",
      "at": "2026-08-08T07:20:00Z",
      "actor": "opus",
      "summary": "CLOSED on the user's own live test, 2026-08-08. Krea2-NSFW upscale, local engine, isolated instance: generated once with Upscale factor UNTOUCHED at its 1.5 default, moved the control to 2.0 without generating, then Reuse Prompt on that card - the factor dropped back to 1.5. That is exactly the recall that was impossible before, because an untouched control recorded nothing and an absent key was a no-op on restore rather than a reset. Shipped in 0c34ec2d. Existing sidecars stay unfixable by design - the value was never recorded; only runs from here on improve."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-461",
      "type": "task.moved",
      "at": "2026-08-08T07:30:00Z",
      "actor": "opus",
      "summary": "CLOSED on code + test evidence at the user's call, 2026-08-08. The structural half was MPI-463 (_failBail, 9d83ea6e); this card carried the user-visible acceptance and shipped it in f006dc4f - _failBail now logs every pre-dispatch bail and emits ui:error by default, with { reported: true } as the explicit opt-out for the six sites that already say something actionable. Four bails were silent before: workflow-name resolve, trimmed-video prep, workflow JSON fetch (the 404 that was reported live) and the injector throw. Evidence: two negative-controlled tests in lane-settle-on-bail (the default must be loud; an opt-out must have a real emit near it - the negative control names the exact line), plus T21 in generation-store proving a queued->error settle frees the lane and the NEXT job dispatches. Suite 499/499, eslint 0 errors. NOT live-watched: nobody has seen the dialog paint. Accepted because the emit is two lines through ui:error, the same path the post-dispatch catch has used for years and which the user did see paint during this session (the port-48188 refusal, commandExecutor.js:2090)."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-442",
      "type": "task.moved",
      "at": "2026-08-08T06:45:00Z",
      "actor": "opus",
      "summary": "todo -> doing. Re-measured: 20 warnings / 0 errors, not 19 - MpiAppLibrary.js is now MpiFlowLibrary.js and heroStats.js has 5 require-destroy-on-events hits, not 4. The 2 null-ruleId messages are NOT violations: they are unused eslint-disable directives in footprint.js (reportUnusedDisableDirectives), so the fix is deleting stale comments."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-482",
      "type": "task.moved",
      "at": "2026-08-08T06:50:00Z",
      "actor": "opus",
      "summary": "todo -> doing. Measuring true bytes for all 111 file deps via HEAD, as a sibling pass in scripts/computeDepHashes.py. Scoping note before any edit: nodesDeps (14 entries) CANNOT be measured this way - their url is a lockUrl() call to a git repo, not a file, so the existing scanner's url regex never matched them and a HEAD has no Content-Length to give. They stay hand-written and that will be stated, not hidden. Also confirmed no dep url points at civitai.com (13 civitai hits are credit/licence metadata only), so this needs no VPN. Consumers are NOT being changed: routes/downloadManager.js, routes/comfy.js and routes/install/computeProgress.js all read dep.size through _parseSizeToBytes, and a routes/ edit needs a server restart that would kill the live MPI-467 fill. So `size` is regenerated from the measured bytes and every consumer inherits the correction with no code change."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-482",
      "type": "task.moved",
      "at": "2026-08-08T07:20:00Z",
      "actor": "opus",
      "summary": "doing -> done. 107 file-backed deps now carry `bytes:` from a HEAD and a `size:` regenerated from it (0ac94728 the pass, af829e0f the data + playbook). Closed on local evidence: the re-run is BYTE-IDENTICAL (0 rewritten, cmp clean), npm test 508/508, eslint clean, and an offline recompute of the full remote/cuda weight union agrees at 401.7 GB from both the strings and the integers. THE PREMISE INVERTED: the typed strings totalled 498.9 GB against a true 478.2 GB - 4.1% OVER, not under - so the registry cannot explain the MPI-467 volume overrun and MPI-483's du -sb apparent-bytes bug is the only live candidate. Both proof entries also failed: krea2-lora-filterbypass really IS 160 bytes (a valid safetensors with one F32 [1,12] tensor) and h3-qwen3vl-32b-clip is declared once at 26.36GB with no 28.3GB anywhere in the tree. Two silent pre-existing script bugs were fixed on the way: the entry-body regex truncated at the first NESTED object's closing brace, so nine deps (the seven CivitAI merges plus chroma1-hd-flash/-hyper) had been invisible to the sha256 pass for as long as it existed; and Path.write_text would have rewritten all 604 lines of the LF-only loraDeps.js as CRLF. THIRD ACCEPTANCE CRITERION NOT MET LOCALLY - whether the corrected total predicts a real fill needs a RunPod fill, which belongs to MPI-467/483, not here. NO consumer was changed: they all read `size`, and touching routes/ needs a restart that would kill the live fill."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-496",
      "type": "task.created",
      "at": "2026-08-08T18:14:50Z",
      "actor": "opus",
      "summary": "todo. Stage-all-on-connect copies onto a disk that cannot hold the set, and does it on Pods with no ComfyUI Surfaced by the user from RunPod Pod logs during the live MPI-467 smoke run; evidence is quoted verbatim on the card."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-497",
      "type": "task.created",
      "at": "2026-08-08T18:14:50Z",
      "actor": "opus",
      "summary": "todo. An already-present dep still fires an 'installed' toast on re-verify Surfaced by the user from RunPod Pod logs during the live MPI-467 smoke run; evidence is quoted verbatim on the card."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-496",
      "type": "task.updated",
      "at": "2026-08-08T18:16:39Z",
      "actor": "opus",
      "summary": "Scope CORRECTED before any work started. The card claimed two defects; the second (stage-all thrashes whenever the set exceeds container disk) does not exist - MPI-329 already mirrors the GPU Pod's container disk to the network volume (455 GB for aghcuvg7nl), so eviction never fires there. The 20 GB seen in the evidence is CONTAINER_DISK_CPU_GB, a RunPod hard cap on CPU Pods. One real defect remains: staging runs at all on a download-mode Pod that has no ComfyUI to serve it."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-498",
      "type": "task.created",
      "at": "2026-08-08T19:42:35Z",
      "actor": "opus",
      "summary": "Carded from a live smoke FAIL. Affects RELEASED users (v1.3.0/v1.3.1 pin 69a43336 also has the required input). Not caused by the MPI-467 pin bump - that misreading is recorded on the card. Id claimed with exclusive mkdir per MPI-488."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-498",
      "type": "task.moved",
      "at": "2026-08-08T20:18:00Z",
      "actor": "opus",
      "summary": "todo -> doing. Working the offline half only; the live GPU smoke matrix keeps the Pod. Owns the four workflow files (API + raw twins); scripts/smoke-workflows.mjs stays MPI-467's."
    },
    {
      "schema": "mpi-kanban/event/v1",
      "id": "MPI-498",
      "type": "task.updated",
      "at": "2026-08-08T20:26:00Z",
      "actor": "opus",
      "summary": "Severity SETTLED offline, no Pod - and it is worse than the card guessed, not milder. MpiLoadImageFromPath subclasses PreviewImage with OUTPUT_NODE=True, so the INPUT LOADER is a valid output node; it survives while every real PreviewImage is dropped. ComfyUI therefore answers 200 + node_errors and runs only the loader branch, echoing the input image back. PiD never sampled in v1.3.0/1.3.1. Root cause is a node-pack change (ba9e156) adding a required widget to graphs already frozen on disk - not the converter, the app, or the pin bump. A default does not rescue a missing key: validate_inputs tests membership before reading any default. Fix 1 applied to all 6 API nodes + their raw twins; AST sweep 6 holes at HEAD, 0 now. Fix 2 (gate 8) messaged to MPI-467."
    },
    {
      "at": "2026-08-09T01:57:49Z",
      "type": "task.created",
      "task": "MPI-499",
      "actor": "opus",
      "note": "npm test leaks ~10 GB of real disk per run - ftruncate is NOT sparse on NTFS, and the code under test trashes the file"
    },
    {
      "at": "2026-08-09T01:57:49Z",
      "type": "task.created",
      "task": "MPI-500",
      "actor": "opus",
      "note": "Stop trashing model weights - delete them permanently"
    },
    {
      "at": "2026-08-09T01:57:49Z",
      "type": "task.created",
      "task": "MPI-501",
      "actor": "opus",
      "note": "restart-comfy destroys any in-flight generation - the needs-restart gate never checks the queue"
    },
    {
      "at": "2026-08-09T01:57:49Z",
      "type": "task.created",
      "task": "MPI-502",
      "actor": "opus",
      "note": "Gate 9 needs a Pod-side twin - read /object_info from the wrapper to cover third-party and programmatic-INPUT_TYPES nodes"
    },
    {
      "at": "2026-08-09T03:55:00Z",
      "type": "task.moved",
      "task": "MPI-465",
      "actor": "opus",
      "from": "doing",
      "to": "done",
      "note": "LTX is PROVEN ALIVE on the shipped engine. The card's own bar was 'close this on smoke-evidence.json, not another local check', and the third matrix reached the GPU stage for the first time: ltx-23-balanced/t2v_ms PASS 78s 1 media, i2v_ms PASS 42s 2 media, on L4 orxp87067tddah with gate 7 clear (Pod image built from 0.30.0, matches node_lock). Real wall-clock and real media, not the 4s no-op shape that let a dead graph read green. The LOCAL half was user-confirmed 2026-08-08. The matrix's one remaining failure is minimax-h3/t2v_ms = MPI-501, unrelated to LTX and tracked on MPI-467."
    },
    {
      "at": "2026-08-09T13:38:00Z",
      "type": "task.created",
      "task": "MPI-506",
      "actor": "opus",
      "note": "SeedVR2 3B/7B as upscale-model choices in the History video Upscale tool. Research done: native in ComfyUI since PR 14424 (v0.28.0) so the pinned v0.30.0 engine already registers the nodes - verified on :48188. Recommended weights seedvr2_3b_fp8_e4m3fn 3.39GB + seedvr2_7b_fp8_e4m3fn 8.24GB + the shared mandatory VAE 0.50GB. Card is needs-decision because videoUpscale is a universal op and its deps install with the engine, so the plugin-registry opt-in shape has to be chosen before code."
    },
    {
      "at": "2026-08-09T13:52:00Z",
      "type": "task.updated",
      "task": "MPI-506",
      "actor": "opus",
      "note": "Fabio decided the shape: three plugins (SeedVR2 3B, 7B, 7B Sharp), not a ModelDef and not engineAsset. Card retitled, maturity needs-decision -> planned, and the plugin-contributes-a-dropdown-entry mechanism is now explicitly this card's reusable deliverable. The PiD half of the same conversation is MPI-507."
    },
    {
      "at": "2026-08-09T13:52:00Z",
      "type": "task.created",
      "task": "MPI-507",
      "actor": "opus",
      "note": "Move NVIDIA PiD out of the model picker into the image upscale dropdown as four plugins, on the mechanism MPI-506 builds. Split from MPI-506 because it is a migration, not an addition: it touches a shipped ModelDef, the flowSdxl4k Flow that declares requiredModels ['sdxl-nsfw','nvidia-pid'] to exercise the multi-model install path, and dev_configs/smoke-evidence.json which gates release:check. Today PiD is all-or-nothing at 16.48GB; as four plugins one path costs ~8.1GB (5.23GB shared gemma encoder + ~2.8GB)."
    },
    {
      "at": "2026-08-09T14:10:00Z",
      "type": "task.updated",
      "task": "MPI-506",
      "actor": "opus",
      "note": "Answered Fabio's 'I don't know what SeedVR needs' against /object_info on :48188 rather than guessing: SeedVR2Conditioning takes model + a VAE LATENT and emits both positive and negative conditioning, so there is no prompt and no denoise, and the weight set has no text encoder to give it one. chunking_mode 'auto' sizes chunks to free VRAM, so frames_per_chunk stays unexposed. A SeedVR2 entry therefore declares ZERO extra controls, which pairs with PiD's prompt+denoise to make the per-entry control list a real mechanism instead of a special case."
    },
    {
      "at": "2026-08-09T14:10:00Z",
      "type": "task.updated",
      "task": "MPI-507",
      "actor": "opus",
      "note": "Fabio closed 3a and 3b: a text input and denoise go in the toolbar conditionally when PiD is selected, and he adds the missing pidResolution entries so the existing 1.5/2K/3K/4K radio resolves for PiD. pidVariant disappears with the plugin split. Only 3c (flowSdxl4k requiredModels) stays open, plus one new question - the radio's labels are multipliers but PiD's resolutions are absolute targets."
    },
    {
      "at": "2026-08-09T14:40:00Z",
      "type": "task.updated",
      "task": "MPI-506",
      "actor": "opus",
      "note": "Answered Fabio's three questions off the shipped ComfyUI templates and the live engine rather than guessing. Precision corrected to int8_convrot (his call, and what every official template uses - fp8 is Ada+ only, int8 also runs on Ampere and Vision still ships to 30-series). SeedVR2 confirmed to do IMAGES as well as video via two official image templates, so its entries go in both dropdowns; there is no 7B video template, so bench that. Reference graph captured from the templates. And the 1K/2K/3K/4K relabel turns out to be native - core ResizeImageMaskNode has a 'scale longer dimension' mode taking an absolute longer_size, which retires the 'four buttons, two meanings' warning."
    },
    {
      "at": "2026-08-09T14:40:00Z",
      "type": "task.updated",
      "task": "MPI-507",
      "actor": "opus",
      "note": "PiD scoped image-only per Fabio, which turns MPI-506's entry declaration into a tool-scoped one. Answered his 'do I need four workflows' with no: the four branches differ in exactly three literals (unet_name, vae_name, latent_format) and everything else is verbatim-identical, so one workflow with three injected literals drops 43 nodes to ~25. More importantly the four-branch shape BREAKS once weights are optional - unet_name is a folder-listing COMBO and validate_prompt rejects unknown values before execution, with no lazy exemption, so a user holding only one plugin would be rejected at prompt time. It only works today because nvidia-pid installs all four weights."
    },
    {
      "at": "2026-08-09T15:05:00Z",
      "type": "task.updated",
      "task": "MPI-506",
      "actor": "opus",
      "note": "Fabio corrected the resolution framing: SeedVR2 is natively a multiplier (the templates ship 'scale by multiplier'), only PiD is absolute, and an earlier note here implied otherwise. Kept one hard finding on the absolute side: the multiplier input caps at 8.0, so a sub-512px source cannot reach a 4K label - 'scale longer dimension' has no such ceiling. Choice, not a requirement."
    },
    {
      "at": "2026-08-09T15:05:00Z",
      "type": "task.updated",
      "task": "MPI-507",
      "actor": "opus",
      "note": "Recorded why PiD cannot just adopt SeedVR2's multiplier: every checkpoint is 1024_to_4096 and the graph enforces it (input normalised to 1024 via MpiScaledDimensions + MpiCrop divisible_by 16, output latent sized at 4096), so it is a fixed 4x trained at one input size. Named the two candidate techniques with their different failure modes - native-then-downscale (safe, no time saving) vs scale-the-input-keep-4x (fast, off-regime input, needs the bench) - plus a cheap A/B decider. Fabio owns the test."
    },
    {
      "at": "2026-08-09T15:25:00Z",
      "type": "task.updated",
      "task": "MPI-507",
      "actor": "opus",
      "note": "Fabio decided four separate PiD workflow files rather than one file with three injected literals. Accepted - it removes the hardcoded-name validation break just as well, since each file names only its own weight; the costs are four copies of every shared fix and a workflow-file field on the plugin entry. Also resolved the resolution question by observation rather than bench: the live graph implements technique A, with the 1024/2048/3072 scalers hanging off VAEDecode and any_4 passing 4096 through, so correctness is not in question and the only thing to surface is that 1K costs the same as 4K."
    },
    {
      "at": "2026-08-09T15:25:00Z",
      "type": "task.updated",
      "task": "MPI-506",
      "actor": "opus",
      "note": "Recorded the asymmetry so the four-file PiD decision does not get copied here: SeedVR2's three variants differ in one injectable literal and share a VAE, so two files (one per tool) cover it rather than six."
    },
    {
      "at": "2026-08-09T22:05:00Z",
      "type": "task.moved",
      "task": "MPI-507",
      "actor": "opus",
      "from": "todo",
      "to": "doing",
      "note": "Starting the workflow-generation half: Fabio exported comfy_workflows/raw/nvidia_pid_template.json (26 nodes, single branch, arch VAE titled PiD_VAE so the generator cannot grab the pixel_space loader by mistake). Writing generate_pid.py + the registry prefix rule so ONE template bakes the four runtime files by setting UNETLoader.unet_name, PiD_VAE.vae_name and PiDConditioning.latent_format. The app-side half stays blocked on the flowSdxl4k decision (brief 3c) and is not claimed yet."
    },
    {
      "at": "2026-08-09T22:15:00Z",
      "type": "validation.updated",
      "task": "MPI-507",
      "actor": "opus",
      "note": "Workflow-generation half PASSES. generate_pid.py + the nvidia_pid_ registry rule turn one template into the four runtime files, baking UNETLoader.unet_name, PiD_VAE.vae_name and PiDConditioning.latent_format. Evidence in validation.md: per-file bake check passes and the shared pixel_space VAELoader is untouched in all four; a structural diff of sd3/qwen/sdxl against flux differs in EXACTLY 1595.latent_format, 1597.vae_name, 1598.unet_name and nothing else; validate-injection-rules.mjs returns 'All 4 file(s) conform' rc=0. Converted against the ENGINE on :48188, not the :8188 bench. No prune pass written - all 25 API nodes are upstream of Output_Image, so there is nothing to drop. Old nvidia_pid.json left in place deliberately: models.js still resolves it and deleting it would break the shipped model. Nothing has EXECUTED these graphs yet. Card stays in doing - the app-side half is still blocked on 3c."
    },
    {
      "at": "2026-08-09T22:40:00Z",
      "type": "task.updated",
      "task": "MPI-506",
      "actor": "opus",
      "note": "First real SeedVR2 run came out degraded and the log named the cause: auto chose frames_per_chunk=5 for 81 frames at 3.31Mpx on a 16GB card, and the template ships temporal_overlap=0 = plain concatenation. Reproduced the sizing formula exactly against the log. The fix needs no MpiNode and no loop pack - temporal_overlap is clamped to what the chunk allows, so a constant 8 self-adapts everywhere. Two structural facts recorded: chunking cannot be avoided at any useful resolution on this card, and the result is dominated by the user's GPU via a flat 10.7GiB reserve. Corrects this card's earlier 'auto solves VRAM with no UI'."
    }
  ]
}
