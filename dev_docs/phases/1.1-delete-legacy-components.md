# Phase 1.1 — Legacy Component Deletion

> **Session type:** Cleanup / Destruction  
> **Estimated time:** 10 min  
> **Files touched:** DELETE 6 files. No new code. Minor search to confirm deletion safety.  
> **Depends on:** All of Group 0 complete. Phases 1.2 and 1.3 will follow this.

---

## ⚠️ WARNING — INTENTIONAL BREAKAGE

After this phase, the app WILL have import errors for any tool that imports the deleted files.
This is expected and intentional. Tool rebuilds in Group 2 fix those imports.
Do NOT run the full app between this phase and completing Group 2.

The Component Gallery and the app shell should still load — they do not import these files.

---

## Context

These 6 files are legacy components that predate the factory system.
Their factory replacements are already built under `js/components/Compounds/` and `Blocks/`.
We delete the originals now.

---

## Files to Delete

```
js/components/PromptBox.js         → replaced by Blocks/MpiPromptBox/
js/components/Slider.js            → replaced by Compounds/MpiSlider/
js/components/MuteIcon.js          → replaced by Compounds/MpiMuteIcon/ (Phase 1.2)
js/components/VolumeControl.js     → replaced by Compounds/MpiVolumeControl/ (Phase 1.2)
js/components/customDropdown.js    → replaced by Blocks/MpiDropdown/
js/components/videoPlayerCore.js   → replaced by Compounds/MpiVideoPlayer/ (Phase 1.3)
```

---

## Tasks

### 1. Confirm which tools import each file (search only — do not edit)

Run these searches and note the results. Do NOT fix them here — just document.

```powershell
# Who imports PromptBox.js?
Select-String -Path "js/**/*.js","templates/**/*.html" -Pattern "PromptBox" -Recurse

# Who imports Slider.js?
Select-String -Path "js/**/*.js" -Pattern "from.*Slider\.js|Slider\.js" -Recurse

# Who imports MuteIcon.js?
Select-String -Path "js/**/*.js" -Pattern "MuteIcon" -Recurse

# Who imports VolumeControl.js?
Select-String -Path "js/**/*.js" -Pattern "VolumeControl" -Recurse

# Who imports customDropdown.js?
Select-String -Path "js/**/*.js" -Pattern "customDropdown" -Recurse

# Who imports videoPlayerCore.js?
Select-String -Path "js/**/*.js" -Pattern "videoPlayerCore" -Recurse
```

Document each result as a comment in your commit message: "Removes X — was used by Y, Z (fixed in Phase 2.N)".

---

### 2. Delete the files

```powershell
Remove-Item "js/components/PromptBox.js"
Remove-Item "js/components/Slider.js"
Remove-Item "js/components/MuteIcon.js"
Remove-Item "js/components/VolumeControl.js"
Remove-Item "js/components/customDropdown.js"
Remove-Item "js/components/videoPlayerCore.js"
```

---

### 3. Commit

```powershell
git add -A
git commit -m "[R8-1.1] Delete 6 legacy components (replaced by factory Compounds/Blocks)"
```

---

## Verification

- [x] 6 files no longer exist
- [x] `js/components/` root now only contains: `factory.js`, `types.js`, `README.md`, `interactiveCanvas.js`, `interactiveCanvas/`, `assetBrowserModal.js`, `mediaDetailModal.js`, `mediaContextMenu.js`, `modelManagerModal.js`, `shaderBackground.js`, `Primitives/`, `Compounds/`, `Blocks/`
- [x] Component Gallery (`tpl-components.html`) still loads (it doesn't import the deleted files)

## What NOT to do

- Do NOT fix broken imports in tools — wait for their specific Group 2 phase
- Do NOT create shims or re-exports
- Do NOT delete `interactiveCanvas.js` — it stays (not a factory component)
- Do NOT delete the modal files (`assetBrowserModal.js`, etc.) — they are not being replaced in this refactor
