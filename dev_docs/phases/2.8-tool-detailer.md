# Phase 2.8 — Tool Rebuild: detailer.js

> **Session type:** Tool migration
> **Estimated time:** 90 min
> **Files touched:** js/tools/detailer.js, templates/tpl-detailer.html
> **Depends on:** Phase 2.6 (pattern reference)

---

## Context

Complex ComfyUI tool — masking, auto-detection, comparison slider. Most complex tool.

---

## MANDATORY: Read Before Writing

`
1. dev_docs/06_refactor_master_plan.md  — confirm current phase status
2. js/components/README.md              — factory patterns
3. js/components/types.js               — component props API
4. The current tool file (understand what it does before deleting anything)
`

Also read any components you will use (see "Component Quick Reference" in the master plan).

---

## Migration Checklist

Replace all instances of these patterns:

| Old pattern | New pattern |
|---|---|
| `import { PromptBox } from '../components/PromptBox.js'` | `import { MpiPromptBox } from '../components/Blocks/MpiPromptBox/MpiPromptBox.js'` |
| `import { Slider } from '../components/Slider.js'` | `import { MpiSlider } from '../components/Compounds/MpiSlider/MpiSlider.js'` |
| `import { VolumeControl } from '../components/VolumeControl.js'` | `import { MpiVolumeControl } from '../components/Compounds/MpiVolumeControl/MpiVolumeControl.js'` |
| `import { MuteIcon } from '../components/MuteIcon.js'` | `import { MpiMuteIcon } from '../components/Compounds/MpiMuteIcon/MpiMuteIcon.js'` |
| `import { renderCustomDropdown } from '../components/customDropdown.js'` | `import { MpiDropdown } from '../components/Blocks/MpiDropdown/MpiDropdown.js'` |
| `import { videoPlayerCore } from '../components/videoPlayerCore.js'` | `import { MpiVideoPlayer } from '../components/Compounds/MpiVideoPlayer/MpiVideoPlayer.js'` |
| Manual DOM: `el.innerHTML = '...'` in init | `Component.mount(container, props)` |
| `document.dispatchEvent(new CustomEvent('media:updated'...))` | `Events.emit('media:updated', { projectId })` |
| Inline debounce/throttle | `import { debounce, throttle } from '../utils/async.js'` |

---

## Tasks

### 1. Read the current tool file completely before touching it

### 2. Rebuild the tool following the factory pattern

- Mount components using Component.mount(container, props)
- Listen for events using instance.on('event', handler) 
- Use Events.emit() for cross-module communication
- Keep ALL existing business logic (LLM calls, ComfyUI integration, state persistence)
- Import from ../utils/ for DOM helpers and seed generation

### 3. Update the HTML template only if mount points changed

Keep all id attributes that shell.js references.

---

## Commit

`powershell
git add js/tools/ templates/
git commit -m "[R8-2.8] Rebuild detailer using factory components"
`

---

## Verification

- [ ] Tool opens and renders without console errors
- [ ] All interactions work (test each button, input, drag/drop if applicable)
- [ ] No imports of deleted files remain
- [ ] `Events.emit('tool:running', ...)` fires when a run starts
- [ ] State persists across navigation (navigate away and back)
- [ ] Component Gallery still loads

## What NOT to do

- Do NOT touch shell.js, init.js, or comfyController.js
- Do NOT add keyboard handlers (init.js owns them)
- Do NOT change other tool files
- Do NOT remove state persistence logic
