# Phase 3.3 — CSS Audit: Dead Rules & Global Cleanup

> **Session type:** CSS cleanup  
> **Estimated time:** 45 min  
> **Files touched:** `styles/03_forms.css`, `styles/04_comfy.css`, `styles/05_tools.css`  
> **Depends on:** Phases 3.1 and 3.2

---

## Context

After Phases 3.1 and 3.2 extracted component-specific CSS, the global partials should now only contain:
- App shell, sidebar, and layout rules
- Tool layout skeletons (`.tool-panel`, `.tool-header`, `.source-preview`)
- Global utility classes
- CSS design tokens and animations

This phase audits what remains and removes dead rules.

Progress: 100%
Status: Completed

---

## Tasks

### 1. Search for orphaned component classes

- [x] Search for orphaned `.mpi-` component classes in `03_forms.css`, `04_comfy.css`, `05_tools.css`.
- [x] Cross-reference identified classes against `js/` and `templates/`.
- [x] Delete verified dead rules.
- [x] Mark active overrides as "Layout Context" in documentation.
- [x] Apply documentation headers to all remaining global stylesheets.
- [x] Verify `preloadComponentStyles()` in `shell.js` for cold-start FOUC prevention.

### 2. Verify each flagged rule is truly dead

For each flagged rule:
```powershell
# Check if selector is used anywhere
Select-String -Path "js/**/*.js","templates/**/*.html","styles/**/*.css" -Pattern "class-name-here" -Recurse
```

If zero hits — it's dead. Delete it.

### 3. Document what remains in each file

After cleanup, add a comment block at the top of each partial documenting what it contains:

```css
/**
 * styles/03_forms.css — Global form and modal styles.
 *
 * Contains:
 * - Modal overlays (.modal-overlay, .modal-content)
 * - Generic form fields (.form-group, .form-label)
 * - Provisioning wizard (.wizard-*)
 * - Model card grid (.model-card-*)
 * - Hover bridge utility (.popup-hover-bridge)
 *
 * Does NOT contain:
 * - Component-specific styles (those are co-located in js/components/)
 */
```

---

## Commit

```powershell
git add styles/
git commit -m "[R8-3.3] CSS audit — remove dead rules, document global partials"
```

---

## Verification

- [x] Zero `.mpi-` component rules remaining in global partials (unless they are layout-context overrides)
- [x] App renders correctly — full navigation pass, no visual regressions
- [x] Each `styles/` file has a header comment documenting its scope

## What NOT to do

- Do NOT aggressively delete rules you're unsure about — mark with `/* DEAD? */` and verify first
- Do NOT touch `styles/01_base.css` or `styles/02_shell.css` (those are already clean)

---

## ✅ CSS Preloader Verification (from Phase 3.2)

Phase 3.2 added `preloadComponentStyles()` to `shell.js`. Verify it here as part of final audit:

- [x] `preloadComponentStyles()` is called inside `initShell()` in `shell.js`
- [x] All CSS paths listed in the preloader function actually exist on disk
- [x] Navigate to any tool on cold app start (no Gallery visit) — components render fully styled
- [x] No duplicate `<link>` tags in `<head>` for the same CSS file (browser deduplicates, but confirm)
