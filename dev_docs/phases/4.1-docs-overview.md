# Phase 4.1 — Docs Update: `dev_docs/01_overview.md`

> **Session type:** Documentation  
> **Estimated time:** 20 min  
> **Files touched:** `dev_docs/01_overview.md` only  
> **Depends on:** All other groups complete (or near-complete)

---

## Context

`dev_docs/01_overview.md` is the first file an agent reads. It must reflect the final R8 architecture.

---

## Tasks

### 1. Update "Key Files" table

Add new entries:
- `js/events.js` — Central Event Bus (pub/sub, mediator channels)
- `js/utils/dom.js` — DOM helpers (qs, qsa, on, off)
- `js/utils/async.js` — Timing helpers (debounce, throttle, sleep)
- `js/utils/seed.js` — Seed generation (generateSeed)
- `js/utils/string.js` — String utilities
- `js/utils/file.js` — File type utilities
- `CHANGELOG.md` — Version history

Update existing entries:
- `js/toolState.js` — note Project Templates API added
- `js/components/factory.js` — already correct
- Component paths — update to reflect new Compounds (MpiMuteIcon, MpiVolumeControl, MpiVideoPlayer)

Remove references to deleted files:
- `js/components/PromptBox.js` — deleted
- `js/components/Slider.js` — deleted

### 2. Update "Folder Map"

Update `js/` section:
```
js/
  ├── events.js           # Central Event Bus
  ├── utils/              # Shared atomic utilities
  │   ├── dom.js          # qs, qsa, on, off
  │   ├── async.js        # debounce, throttle, sleep
  │   ├── seed.js         # generateSeed()
  │   ├── string.js       # truncate, slugify
  │   └── file.js         # getExtension, formatBytes, isImage/Video
  ├── components/
  │   ├── factory.js
  │   ├── types.js
  │   ├── Primitives/     # (list all 8)
  │   ├── Compounds/      # (list all — including new MpiMuteIcon, MpiVolumeControl, MpiVideoPlayer)
  │   └── Blocks/         # (list all 3)
  └── tools/              # All tools rebuilt using factory components
```

### 3. Update component tier lists to be accurate

Make sure every component folder under Primitives/, Compounds/, Blocks/ is listed.

---

## Commit

```powershell
git add dev_docs/01_overview.md
git commit -m "[R8-4.1] Update 01_overview.md for post-R8 architecture"
```

---

## Verification

- [ ] All new files/folders present in folder map
- [ ] No references to deleted files remain
- [ ] A fresh agent reading this file gets accurate architecture picture
