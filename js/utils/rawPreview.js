/**
 * rawPreview.js — DEPRECATED. Replaced by RawGpuPipeline (GPU preview).
 * Kept to avoid hard-crash on any stale import. Remove when Phase 6 lands.
 */
export function buildCSSFilter() { return ''; }
