/**
 * js/core/buildInfo.js - build metadata stamped by portable staging.
 *
 * Source/dev runs stay at "dev". `scripts/build-portable.mjs` overwrites the
 * staged copy with a short Git commit hash for release artifacts.
 */

export const BUILD_HASH = 'dev';
