/**
 * js/core/appName.cjs — CommonJS twin of appName.js for Node-side consumers (main.js).
 *
 * Keep `APP_NAME` in sync with `appName.js` (ESM). When changing the display
 * name, update both files.
 */

module.exports = { APP_NAME: 'Cubric Studio' };
