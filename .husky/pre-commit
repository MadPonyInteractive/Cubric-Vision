#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

npx lint-staged || true
echo ""
echo "⚠️  Lint ran in warn-only mode. Violations above do not block commit."
echo "   Flip to strict mode in a separate tracker once clean."
